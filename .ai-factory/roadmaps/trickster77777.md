> Owner: trickster77777@gmail.com

# Skills Roadmap

> Generic AI Factory skills — reusable slash-command packages for Claude Code.

## Skills generate to today's norm, not yesterday's

### Phase 19 — the aif family stops generating what Phase 2 just cleaned out

Governing spec: `docs/sakshi-harness/sakshi-harness.md`

Phase 2's five alignment passes found the same two defects landing in every project family, always from the same source: `aif` (and its `config-template.yaml`) still generate the rules file at the orchestrator-invisible `.ai-factory/rules/base.md` instead of the canonical `.ai-factory/RULES.md`, and a `.ai-factory/config.yaml` the orchestrator never reads. Every future `/aif` run regenerates the junk this whole day was spent removing. This phase conforms the generators to the harness contract at the source.

- [x] **19.1 — aif family: stop generating retired artifacts, align to the canonical harness contract** — `aif` writes the rules file at the orchestrator-invisible `.ai-factory/rules/base.md`, not canonical `.ai-factory/RULES.md`, plus a `.ai-factory/config.yaml` (language persistence) the orchestrator never reads — rename the rules target to `RULES.md`, retire the three config-machinery files outright (verified unreferenced), and drop the config.yaml-first read in `aif-architecture`/`aif-docs` for their existing default-fallback. Guard: fixed-English invariant restated per-generator (`aif` and `aif-architecture` don't share a body) as `## Language Resolution` is removed; config-residue sweep grep-defined, verified by content. Spec: `.ai-factory/specs/trickster77777/78-aif-generates-to-norm.md`. [56m 49s]
- [x] **19.2 — aif: grant `Bash(ln *)` for the AGENTS.md symlink step** — `aif/SKILL.md` L5 `allowed-tools` grants `Bash(mkdir *)` and (until 19.1) `Bash(node *update-config.mjs*)`, but the `## AGENTS.md Generation` section runs `ln -sfn CLAUDE.md AGENTS.md` with no `Bash(ln *)` grant — so the required AGENTS.md→CLAUDE.md symlink step raises a permission prompt interactively and can block a headless orchestrator run. Add `Bash(ln *)` to the grant list (not a broader `Bash(*)`); touch nothing in the AGENTS.md section itself. Guard: 19.1's first task also edits this same L5 (dropping the retired `update-config.mjs` grant) — independent concerns on one line, each must preserve the other. Spec: `.ai-factory/specs/trickster77777/80-aif-ln-grant-for-agents-symlink.md`. [3m 47s]
- [x] **19.3 — rules-generation.md: drop the dangling spec citation** — `src/skills/aif/references/rules-generation.md` cites `.ai-factory/specs/41-aif-rules-counter-default-filter.md` for "the composition-model reasoning", but no such file exists under `specs/` or `notes/` (leftover from an earlier numbering scheme; 19.1 correctly left it byte-identical as out-of-scope). The surrounding sentence is self-contained, so drop the parenthetical — no repoint, since no live artifact carries that reasoning (the RULES.md contract lives in `docs/sakshi-harness/sakshi-harness.md` but is the contract, not the promised deep-dive). Touch only the parenthetical; rules-generation semantics unchanged. Spec: `.ai-factory/specs/trickster77777/81-rules-generation-dangling-spec-cite.md`.

## Test-coverage: Layer 1 should read the stack it already has, not guess it again

Phase 8's 8.1 review (all three rounds) flagged Layer 1 as having "no Python detector" — but Layer 1 already reads `.ai-factory/ARCHITECTURE.md` for module boundaries one bullet earlier, and every `ARCHITECTURE.md` `aif-architecture` generates already declares `- **Tech stack:** [language, framework]`. Growing the hard-coded manifest-sniff list per stack (Python today, the next language tomorrow) repeats a generic-skill regression instead of closing it; the fix is to consult the project's own declared stack first.

### Phase 20 — Layer 1 reads the declared stack before guessing

- [x] **20.1 — roadmap-test-coverage: Layer 1 reads `$STACK` from ARCHITECTURE.md, manifest-sniff becomes fallback** — Layer 1 already reads `ARCHITECTURE.md` for module boundaries but never consults its own `- **Tech stack:** ...` line for `$STACK`, instead re-deriving it from a closed four-manifest list (`package.json` / `pubspec.yaml` / `go.mod` / `Cargo.toml`) with no Python entry (flagged across all three 8.1 review rounds) and no room for the next missing stack either. Reorder: `$STACK` = ARCHITECTURE.md's Tech-stack value when filled in (not the unfilled `[language, framework]` placeholder); the four-manifest sniff becomes fallback only, unextended — no new per-language detector ever again. Guard: Layers 2–8 byte-identical. Behavior-baseline: a scratch project with a filled Tech-stack line and no manifest resolves `$STACK` from it; one with no ARCHITECTURE.md but a `package.json` still falls through unchanged. Spec: `.ai-factory/specs/trickster77777/79-test-coverage-layer1-python-stack-detection.md`. [11m 18s]
- [x] **20.2 — roadmap-test-coverage: give `$TEST_CMD` a first-class derivation rule and define Layer 7's no-command behavior** — 20.1 fixed `$STACK` but left its sibling `$TEST_CMD` with no rule of its own: it is still derived solely from the unconditional four-manifest read (`roadmap-test-coverage/SKILL.md` L49–53), with no Python manifest, no declared-source path, and no rule for "none of the four exists". A Python/pytest project with a filled ARCHITECTURE.md Tech-stack line and no sniffable manifest resolves `$STACK` but leaves `$TEST_CMD` empty, and Layer 7 (L277/L333) runs the empty command against a hard "all tests green before Layer 8" gate — undefined behavior; the safety gate silently no-ops for exactly the stack 20.1 serves. Give `$TEST_CMD` a first-class rule (primary: the project's declared command in CLAUDE.md `## Commands`; fallback: the four-manifest sniff, unextended) and define Layer 7 to explicitly skip the gate — logged, recorded in the handoff — when no command resolves, never execute empty. Guard: `$STACK` and Layers 2–6/8 byte-identical; no per-language command detectors. Spec: `.ai-factory/specs/trickster77777/82-test-cmd-derivation-rule.md`. [13m 36s]

## The skill's sidecar grammar mirrors the orchestrator — keep the mirror honest

Source handoff: `.ai-factory/handoffs/01-task-rescue-implemented-index-sync.md`. `task-rescue` is the only skill-side writer of the sidecar "artifact produced" markers, and its documented closed set explicitly claims to mirror `_validate_sidecar_step()` / `_detect_task_step()` in `orchestrator/resume.py`. Orchestrator task 18.2 (governing spec `orchestrator/.ai-factory/specs/trickster77777/32-resume-carries-verify-iteration.md`, decomposed into `orchestrator/.ai-factory/roadmaps/trickster77777.md` Phase 18, not yet implemented) indexes both markers and retires the bare forms — so the mirror will lie until the skill re-syncs. Land paired with 18.2.

### Phase 21 — task-rescue's rollback markers carry the iteration index

Orchestrator 18.2 replaces the bare always-valid markers `"planned"` / `"implemented"` with indexed `"planned:N"` / `"implemented:N"` and empties the bare always-valid tuple in `_validate_sidecar_step` (clean break, no legacy branch). After it lands, a sidecar carrying a bare marker passes validation, matches no dispatch branch, and degrades to the disk heuristic — non-fatal (the heuristic still reaches round 1 at these rollback depths, since all prior-round artifacts are deleted), but the skill's explicit-marker contract is broken and its closed-set table becomes factually wrong. This phase is the paired skills-side re-sync; it is a doc/skill edit, no code.

- [x] **21.1 — task-rescue: sidecar rollback markers gain an iteration index (`planned:1` / `implemented:1`)** — `src/skills/task-rescue/SKILL.md` writes bare `"planned"` (spec+plan depth) and `"implemented"` (spec+plan+code depth) at the Step 4 depth summary, both rollback procedures, both Emit lines, the closed-set table, the always-valid guard, the Note paragraph, and the "Do not write" guard — but orchestrator 18.2 retires the bare forms for indexed `"planned:N"` / `"implemented:N"`. Change every written value to `"planned:1"` / `"implemented:1"` (the rescue always writes N = 1: both depths delete all prior-round plan-reviews/reviews, so the next round is 1); generalise the two table rows to `"planned:N"` / `"implemented:N"` with a note that the rescue writes N = 1 and the bare forms are retired. Guard: `plan_reviewed` and the reference-only `plan_review_failed:N` / `review_failed:N` rows byte-identical; guard *conditions* unchanged, only the written value gains the index; repair-depth deletion/JSON mechanics untouched. Land paired with orchestrator 18.2. Spec: `.ai-factory/specs/trickster77777/83-task-rescue-indexed-sidecar-markers.md`. [8m 39s]

## The channel protocol must travel with the pair, not live in this repo's docs

Handoffs 02 and 03 caught the paired loop's channel protocol drifting from how the loop is actually run, and the pair gets instantiated in other projects where this repo's `docs/`, root CLAUDE.md, and `reserved-words.md` are all absent — so the shared contract cannot live in any of them. It moves into a thin global engine both entities load at birth, exactly as `roadmap-decompose` loads `roadmap-engine` at the start of its run.

### Phase 22 — the channel-message formats become a resident engine both entities load

The architect↔editor channel is under-specified on three fronts (handoffs 02/03): `agent-architect/SKILL.md` mandates a trailing-only `::` marker and treats a mid-message `::` as literal payload text — rejected by the user, whose model is a mid-message split where the before-mark payload is worked by both entities in parallel, not relayed only; its relay-expansion reads "apply", which misreads a research relay as an edit order; and `editor.md` has no report-only default, so a relayed skill with its own write-capable default can promote a round to edit-mode (hit once live, handoff 03 §3). The fix extracts the shared output contract into a new thin engine holding only the two channel-message formats and how a receiver keys its mode off which arrived; `::` parsing stays entirely in the architect, since the editor only ever receives one of the two formats.

- [x] **22.1 — paired-loop channel protocol becomes a resident engine both entities load** — the architect↔editor channel is under-specified (handoffs 02/03: trailing-only `::`, relay-expansion reads "apply", editor has no report-only default). Extract the shared output contract into a new thin engine `src/skills/architect-editor-engine/SKILL.md` holding only the two channel-message formats — REPORT-ONLY and APPLY-EDIT — and the rule that a receiver keys mode off the token literally opening each message; no `::`, no policy. `agent-architect/SKILL.md` loads it and rewrites its `::` grammar in place; `editor.md` loads it first on spawn and keys its discriminator off the two tokens; add the `active/skills/architect-editor-engine` symlink. Spec: `.ai-factory/specs/trickster77777/84-paired-loop-channel-protocol.md`. [18m 28s]

- [x] **22.2 — paired-loop protocol post-review cleanup: thin the engine, fix `agent-architect` before-mark** — a handoff-focused review of 22.1 found residues in two files. `architect-editor-engine/SKILL.md`: slim the 48-line body to a ~20-line `.h` shell — it restates the frontmatter and re-homes `APPLY-EDIT` mechanics that already live in the callers — and purge every `::`, frontmatter included. `agent-architect/SKILL.md`'s `## Relay on the marker` section: gate enrich on an explicit after-mark instruction (it reads "you may enrich" — architect discretion), move the reconcile mechanic from `## Review in parallel` into the general before-mark rule it claims to specialize, and show the literal `REPORT-ONLY` token in the skill-expansion example. `editor.md` untouched. Spec: `.ai-factory/specs/trickster77777/85-paired-loop-post-review-cleanup.md`. [9m 33s]

## The deferred-observation marker vocabulary has one home; callers cite it, never restate it

A session investigation of `task-rescue`'s Step 5.6 found it hardcodes the deferred-observation status markers `orchestrator-artifacts` §6 retired (`[promoted → <path>]`, `[audit-dismissed]`) as live write-instructions, instead of citing §6's current vocabulary (`[routed → <path>]` / `[dismissed]`) the way `roadmap-prune`'s own gate logic already does — the same "restate instead of cite" drift the paired-loop cleanup (Phase 22) just closed for the channel-message engine. `task-rescue` also attributes two retired markers to `task-rescue-audit` as its responsibility to write, but `task-rescue-audit` is chat-only and writes no files at all — dead attribution from an earlier design. `roadmap-prune` carries one further, low-severity echo of the same pattern. `orchestrator-artifacts` §6 stays the one home unchanged, including its deliberate legacy-marker list (mixed-vocabulary roadmaps still need it recognized as pinned).

### Phase 23 — task-rescue and roadmap-prune stop restating the marker vocabulary, cite the engine instead

- [x] **23.1 — task-rescue & roadmap-prune: cite the marker vocabulary, don't restate it** — `task-rescue/SKILL.md`'s Step 5.6 + "What NOT to do" hardcode markers `orchestrator-artifacts` §6 retired (`[promoted → <path>]`, `[audit-dismissed]`) instead of citing the current `[routed → <path>]` / `[dismissed]`, and falsely attribute two retired markers to `task-rescue-audit`, which writes no files. Rewrite: cite §6 for the two disposal branches; describe undisposed entries as left unmarked for the resolution session, dropping the audit attribution. `roadmap-prune/SKILL.md:65–66`'s parenthetical similarly restates the three literals — cite §6 instead. Guard: §6 untouched (keeps both vocabularies); `task-rescue-audit` untouched; sidecar `step` markers out of scope. Spec: `.ai-factory/specs/trickster77777/86-marker-vocabulary-one-home.md`. [8m 15s]

## The plan-citation scan nets the shapes first imagined, not the shape that actually leaks

Source handoff: `.ai-factory/handoffs/04-plan-citation-rule-is-narrower-than-the-leak.md`. Step 7.5's citation scan and the home rule at `src/global/CLAUDE.md:18` enumerate the same shapes — `Phase N`, note number, `ROADMAP`/`Plan`, `.ai-factory/` path — but the leak that dominates in practice is neither: `# Task N:` section comments (45 across `orchestrator/tests/`) and inline `task N.M` coordinate citations (`test_agents.py:260`, already dangling since phase 2 was pruned from the orchestrator roadmap). Re-reading the scan against handoff §4's four asks narrows them to one real change: the scan is already whole-repo (§4's whole-repo ask is moot), already construct-blind as a raw grep (§4's "beyond comments" ask is a category error against a grep), and already directional via `--exclude-dir=.ai-factory` (plan-cites-plan stays exempt) — only its shape list is short. This phase widens only the scan; `CLAUDE.md:18`'s mirrored wording is deliberately left for the separate one-home decision (handoff §5), so the scan ships as a known, temporary report-only superset of the home rule — permitted by Step 7.5's own "false positives are acceptable" clause.

### Phase 24 — Step 7.5's citation grep gains the two shapes the leak actually takes

- [x] **24.1 — roadmap-prune Step 7.5: widen the citation grep to the `Task N` / `task N.M` forms** — the scan (`roadmap-prune/SKILL.md` Step 7.5, item-3 prose list + the `grep -rInE` invocation) nets `Phase [0-9]`, `note [0-9]{2}`, `\.ai-factory/(specs|notes)`, `ROADMAP`, `Plan [0-9]`, but the dominant real leak (handoff 04: `# Task N:` section comments, `task N.M` coordinate cites like `test_agents.py:260`) matches none. Add two alternations — `Task [0-9]` and `task [0-9]+\.[0-9]+` — to both the prose list and the command. Guard: `--exclude-dir` flags preserved; still report-only; whole-repo scope and Steps 0–8 + Commit byte-identical; `CLAUDE.md:18` untouched (mirror deliberately deferred, §5). Spec: `.ai-factory/specs/trickster77777/87-prune-scan-task-citation-shapes.md`. [7m 58s]

## A third run outcome lands on the orchestrator side; the skills mirror catches up

Source handoff: `.ai-factory/handoffs/05-escalation-outcome-and-a-skill-that-ran-from-inside-the-pipeline.md`. The orchestrator grew a third run outcome — escalation (an agent stops on a decision outside its authority), beside success/failure/halt. Our skills-side surfaces predate it; this direction mirrors its on-disk protocol into them, plus one unrelated audit-wording fix.

### Phase 25 — task-rescue and orchestrator-artifacts learn escalation is a third outcome, not a failure

`orchestrator-artifacts`, `task-rescue`, and `reserved-words.md` § Orchestrator predate escalation: none recognizes an escalated task, and `task-rescue` would misread one as a Plan-phase failure with no way to clear its wedged state. This phase adds recognition, a short-form report, and a clear-and-reset path reusing the existing spec-depth reset, plus the vocabulary.

- [x] **25.1 — task-rescue gains an escalation branch; orchestrator-artifacts, reserved-words.md, and task-rescue-audit absorb the rest** — `task-rescue`'s four diagnosis classifications, its Diagnosis Report, and its closed-set sidecar table have no branch for an escalated task — one whose artifact ends with the `ESCALATION` marker and whose sidecar `step` is `escalated`. Today it misreads as a Plan-phase failure and narrates a defect that isn't there, and nothing clears the escalated state (the run re-halts on every resume). Add a Step 2 classification checked ahead of the failure conditions, a Step 3 short-form report of the missing decision, a Step 4 `AskUserQuestion` whose clear-and-reset reuses the existing spec-depth full reset, and one closed-set row. `orchestrator-artifacts` §2/§3/§7 and `reserved-words.md` § Orchestrator absorb the tokens and vocabulary; `task-rescue-audit:32` `cold rescue`→`cold audit` rides along. Guard: no new repair-depth flow; no escalation logic added to the audit. Spec: `.ai-factory/specs/trickster77777/88-task-rescue-escalation-outcome.md`. [15m 24s]

- [x] **25.2 — task-rescue: close three non-blocking edges left by 25.1's escalation branch** — Step 4's escalation options 1/2 both point at the whole "Depth: spec" procedure ("reuse ... verbatim"), but option 2 explicitly forbids editing the task spec (steps 1-2 of that procedure) — rewrite so each option routes explicitly to its own Step 5 label: option 1 → full Depth:spec incl. the spec edit; option 2 → its deletion steps only; option 3 → no Step 5 procedure, mirroring non-convergence's explicit skip-to-5.5. Step 3's root-cause-category list/parenthetical (`:131-136`/`:186-189`) doesn't exempt escalation, which has none; the Always-valid guard (`:431`) omits `escalated`. Guard: four pre-existing classifications, Diagnosis Report form, domain-language discipline byte-identical; no new rollback mechanism. Spec: `.ai-factory/specs/trickster77777/89-task-rescue-escalation-branch-polish.md`. [7m 47s]

## The discipline relies on things it never writes down

Grounded in live incidents this session: after a compact the architect reported its editor dead and the user disproved it by hand, and a round was closed before the editor could speak at all. Each time the skill already held the intent — a coexisting buffer, a live editor, a reconcile step — and left the mechanism that would hold it to be inferred. An inference survives neither a compact nor haste. This direction writes the mechanisms down where the assertions already stand.

### Phase 26 — agent-architect states the mechanisms its own text assumes

`agent-architect/SKILL.md` asserts three things it never mechanizes: that a handoff carries the editor's handle and the buffer's path without ever saying whose handoff does, that a dead editor is "a stale handle discovered at recovery", and — by exposition order alone — that nothing is released before the editor's report. So the buffer's path travels only when the architect happens to think of it, the liveness test was filled in by the architect itself with the one tool that cannot answer it, and a relay and its work-order can leave in one message with the rule intact. The first three tasks run in file order: the pairing roles between two architects are named first, since the state task that follows only gains a role to record once the roles themselves exist, then the architect's own state — the editor's handle, the buffer's path, and any assigned role — is written where a compact cannot reach it, and last the release order becomes a stated invariant. A fourth follows them, one file over: the pairing skill 26.4 produces tells its applying half to apply a work-order the way an editor would, and leaves the check that phrase stands for in a file that half never loads. A fifth cuts that fourth back: the paragraph it shipped carries the check and three powers past it.

- [x] **26.4 — agent-architect: pairing roles for two architects working together** — the skill describes one architect with one editor. When two run as a pair — one reasoning and deciding, the other the hand that applies — nothing tells either its half, so the first keeps addressing work-orders to its own editor and the second has no rule against originating edits of its own. The role is per-agent, per-session data, so it lands in one add-on skill an architect loads only when the user assigns it a role, not in the generic skill: that skill holds both halves — the first's editor becomes research-only and application routes to the paired architect through the user; the second originates no edit, applies only what an arriving work-order pins, and never routes it onward to its own editor. Guard: the generic skill's unpaired-session default (`:25-26`) and the `::` / two-format mechanics stay untouched. Spec: `.ai-factory/specs/trickster77777/95-architect-pairing-roles.md`. [19m 4s]

- [x] **26.1 — agent-architect: the architect's own state survives the compact** — `src/skills/agent-architect/SKILL.md` holds the editor's handle and the buffer's path in prose assuming that context survives: the handoff paragraph never says *whose* handoff carries it, the buffer still calls itself a deferral log though it holds the handle, and the liveness sentences name only outcomes, never the test. Rewrite the handoff paragraph to name the addressee and make a failed delivery the only death signal, record the editor's handle and any assigned pairing role in the buffer and say so in its section, and have the handoff carry the buffer's path alone — the pointer, never a copy of what the buffer holds. Guard: the buffer's path stays exactly `.ai-factory/notes/<NN>-architect-buffer.md` with its present numbering — no slug key, no session-derived id, retired and not reopened. Spec: `.ai-factory/specs/trickster77777/93-architect-state-survives-the-compact.md`. [21m 4s]

- [x] **26.3 — agent-architect: nothing closes a round before the editor's report exists** — `src/skills/agent-architect/SKILL.md` states what happens *after* a report (`:76-79` reconcile the independent reads; `:149-150` draft the work-order only for what survives reconciliation) but never says what may not leave before one — the invariant is inferable from exposition order alone, an inference a compact or a hurry drops. Add one standalone `##` section between `## Relay on the marker…` and `## Review in parallel…`, naming summary, verdict, and apply work-order as the three closing artifacts and why a work-order closes a round as finally as a verdict despite being addressed to the editor. Guard: the architect's own parallel pass still runs through the wait — what defers is the announcement, never the work. Spec: `.ai-factory/specs/trickster77777/92-nothing-closes-a-round-before-the-report.md`. [10m 21s]

- [x] **26.5 — architect-pairing-engine: the applying half checks the work-order before it applies** — `src/skills/architect-pairing-engine/SKILL.md` § "The applying half" tells it to apply what an arriving work-order pins "the way an editor would", and the check that phrase stands for — report back an order that is underspecified, self-contradicting, or would break something it never named; fix what is outright wrong and say so; name every decision it left unpinned — is written only in `src/agents/editor.md:77-83`, an agent definition no architect loads. What survives in context reads as unconditional obedience. Add one paragraph to that section stating the check in the skill's own words, and amend the applying clause of `description:` to match. Guard: the deciding half, the load-only-when-assigned rule, `editor.md`, and the generic skill stay untouched; no pointer to an unloaded file. Spec: `.ai-factory/specs/trickster77777/96-pairing-applying-half-checks-the-order.md`. [16m 27s]

- [x] **26.6 — architect-pairing-engine: cut the applying half's check back to a check** — 26.5 was asked for one thing, that the applying half reviews an arriving work-order instead of applying it blindly, and shipped three more: a licence to correct an outright-wrong order inside the change it already asks for (`:61-64`), a sentence existing only to reconcile that licence with `:52`'s "never to its own judgment" (`:65-67`), and a round-closing clause routing the report back through the user (`:67-68`). The licence contradicts the paragraph above it; the round-closing clause is what left the deciding half's mirror unwritten, the open deferred observation on 26.5. Replace `:58-70` with the check and the report alone. Guard: the `description:` field already carries only the check and stays untouched, as do `## The deciding half`, `editor.md`, and the generic skill. Spec: `.ai-factory/specs/trickster77777/97-applying-half-check-cut-back.md`. [6m 16s]

- [x] **26.7 — the paired-loop skills are missing from CLAUDE.md's two enumerations, and one engine misdescribes its own load** — `CLAUDE.md:74` (the active set) omits `architect-editor-engine`, and `:189` (what is ours, never overwritten by a sync) omits both it and `architect-pairing-engine`, though all three are symlinked under `active/skills/` — a re-sync reading `:189` as authority would have no record that either engine is ours. Separately `src/skills/architect-editor-engine/SKILL.md:17` says it is loaded "the architect via its own `loads:` frontmatter edge", reading `loads:` as a loader when it only declares the graph; the load is the instruction in the architect's own body. Add the two names to each list and correct that clause. Guard: the engine still names no caller — the graph stays one-way; no other line of either file changes. Spec: `.ai-factory/specs/trickster77777/98-claude-md-enumerations-and-engine-self-description.md`. [6m 30s]

- [x] **26.8 — architect-pairing-engine: the applying half applies through its own editor** — the half acts on shared artifacts with its own hands (body and `description:` alike), overriding editor-is-the-hand — never asked for: it originates no edit, but what arrives is a decision it composes into an `APPLY-EDIT` for its own editor. Delete the own-hands clause, the overriding paragraph and the simile carrying the old model; name the editor as the hand in `description:`. Alongside, in `agent-architect`: `loads: architect-pairing-engine` has no load instruction in the body — add one where the role is recorded; and only the editor is named as a work-order's addressee, though the deciding half's goes to the paired architect. New rule: a hand-relayed work-order ships as one code block. Guard: the check paragraph 26.6 settled survives word-for-word bar one pinned substitution. Spec: `.ai-factory/specs/trickster77777/99-pairing-wiring-left-half-finished.md`. [21m 13s]

- [x] **26.9 — agent-architect: the single-editor assumption outlives 26.8, and the applying half never states how it starts** — 26.8 gave `:185-187` a paired-architect clause and left the same assumption in two neighbours: `:179-180` closes a round on *the editor's* report, and `## Verify the editor's report by fact` (`:209-215`) fires only when the editor reports done on an `APPLY-EDIT` round — a trigger the deciding half's research-only editor never meets, so its duty to check the paired architect's work by fact reads as not applying. Reword all three to name either reporter. Separately, the applying half never states how it starts, where the deciding half has a whole paragraph: composing an arriving decision into an `APPLY-EDIT` is authoring, so the generic trigger holds and an arriving order needs no `::`. Guard: `:185-187`, both `description:` fields and the deciding half stay untouched. Spec: `.ai-factory/specs/trickster77777/100-single-editor-assumption.md`. [13m 23s]

- [x] **26.10 — agent-architect: sweep the round-closing section's single-editor assumption instead of patching it a third time** — 26.8 widened one sentence, 26.9 two more and one heading, and each pass left the pattern one instance short; 26.9 left `:177`'s heading claiming a round closes on *the editor's* report two lines above the sentence denying it. Two remain: that heading, and `:194`'s "the editor's independence", which names one of the two second readers the section now covers. Widen both to name whoever reported, then record in the spec why every other mention stays — spawn, handle, liveness, buffer and relay mechanics are identical for both halves, and `:22-27` and `:155-161` are overridden by declaration in `architect-pairing-engine` — so a fourth pass has nothing left to find. Guard: `:180`, `:188`, `:213` already carry their paired-architect clause and stay word-for-word. Spec: `.ai-factory/specs/trickster77777/101-round-closing-sweep.md`. [10m 48s]

## The layer every session loads describes two doc modes and assigns neither

`src/global/CLAUDE.md` § "Grounding claims" is the only rule surface loaded into every session of every project. It names two doc modes and never says which one the project's `docs/` are, so an agent mid-task falls to the description default and rules a doc running ahead of its code defective — the argument recurs across projects and is re-litigated by hand each time. This direction states the model the pipeline already implements.

### Phase 27 — the global CLAUDE.md states the direction change moves in

The behavior is real and already carried by the skills, but it lives only in skill bodies and in `docs/sakshi-harness/skill-cycle.md`, neither of which is loaded in an ordinary session of another project. The always-loaded layer is silent on it. No task spec for this phase — the contract line carries the whole change.

- [x] **27.1 — the global CLAUDE.md never says the project's docs are the ТЗ** — § "Grounding claims" states two doc modes and assigns `docs/` to neither, so an agent falls to "Descriptions drift; code wins" and rules a doc running ahead of its code defective. Rewrite `:5` to name `docs/` as the governing spec; add two paragraphs fixing the direction docs → roadmap → code and separating it from the order surfaces appear in; drop `:9`'s duplicated duality; extend `:13` with sequential top-to-bottom task execution. No rename, no new term, no other file.

## The task tier is two-tier; the phase tier above it is not

A task carries a contract line plus a task spec. A phase carries a preamble and nothing else, so the preamble absorbs what a second level would hold and grows until nobody reads it. The pair `roadmap-decompose` / `roadmap-decompose-skeleton` already shows what a second pass over an existing tier looks like; the phase tier never got one.

### Phase 28 — a second pass over drafted phases gives each one a note and a short preamble

`roadmap-outline` drafts phases and stops. Nothing deepens them afterwards, the tier has no note, no preamble budget and no pointer format, and `roadmap-outline` actively forbids a phase-level tag. Outline itself stays as it is — its restraint at the strategic tier is deliberate and the deepening belongs in a separate pass.

- [x] **28.1 — `roadmap-outline-deep`: a second pass that gives a drafted phase a note and a contract-line preamble** — the phase tier has no note, no preamble budget and no pointer format, so preambles absorb note-sized prose. Add `src/skills/roadmap-outline-deep/`, a second pass over existing phases only, `loads: roadmap-engine note`, shaped on `roadmap-decompose-skeleton`; it holds the ~200–500 preamble budget and the inline `Phase note:` pointer itself, being the format's only caller. Symlink into `active/skills/`; add to both `CLAUDE.md` enumerations; `skill-cycle.md` already carries it. `roadmap-prune` gains one capture, symmetric with the `Spec:` one, and widens its single-token assertions and Step 8 summary with it. A phase already carrying `Phase note:` is rewritten in place, never given a second note. Modify no other skill — not `note`, `roadmap-engine`, `roadmap-outline` or `task-rescue`. Spec: `.ai-factory/specs/trickster77777/102-roadmap-outline-deep.md`. [1h 2m 22s]

- [x] **28.2 — the phase header's two pointers get their readers: `roadmap-decompose` and `task-rescue`** — 28.1 defines the `Phase note:` token and nothing reads it. In `roadmap-decompose/SKILL.md` hook (a) Granularity (`:26`), add the phase-preamble read, once per phase before its first task is written, in every mode: where the preamble names `Governing spec:` documents or a `Phase note:`, read those files in full — unconditional, never suspicion-gated. In `task-rescue/SKILL.md`, extend the read at `:61-63`, its title, the Step 3 judgment, the copy rule (`:58`, `:143`, `:356`) and the rule at `:541` from `Governing spec:` alone to both pointers, the same unconditional shape, keeping the existing exit: no phase, or neither named, proceed as today. A pointer whose file is absent is reported, never skipped. Runs after 28.1, which defines the token. Modify no other skill; no new `loads:` edge. Spec: `.ai-factory/specs/trickster77777/104-phase-note-readers.md`. [25m 42s]

## Nothing checks how a task lands on the code that exists

`command-pin-gaps` reads a task and asks where the implementing agent would have to invent. It asks that of the artifact's own text alone, so the answer is only ever as good as the reader's improvisation: nothing walks the transformation the task claims, from the governing spec through the task into the code, and nothing in the family owns the point where new code meets old. `roadmap-decompose` never reads code, and `roadmap-decompose-skeleton` reads it only to judge testability.

### Phase 29 — command-pin-gaps walks the task's transformation and reports where it fails to join

The question the command asks does not change; what changes is that it stops being asked into the air. The command gains no new authority: it sees holes and names them, and the fixes are applied afterwards by an agent, once the findings have been discussed and a decision made.

- [x] **29.1 — command-pin-gaps: join the task to the code it lands in** — nothing reads code at decomposition time, so a task is written away from its code. Rewrite `src/commands/command-pin-gaps.md` to read the task, its spec and that code to the leaf, then walk what the orchestrator will do: how the planner plans against this code and how the reviewer judges the result. A finding is anywhere the run, holding this task alone, would invent; the pass also weighs whether it can be implemented at all. Blast-radius joins value and meaning as a third class; a contradiction that resolves is closed in place, a fundamental conflict or unpickable code goes up as a blocker. It names which of `roadmap-decompose`, `roadmap-decompose-skeleton`, `aif-docs` or `test-philosophy` owns a hole and wields none. It never checks the phase header's pointers. `allowed-tools` and `loads` unchanged; no path outside this repo. Spec: `.ai-factory/specs/trickster77777/103-pin-gaps-blast-radius-class.md`. [22m 59s]

## A failed attempt disappears with the rollback that repairs it

Every artifact that records how a task failed — the plan, the reviews, the sidecar's own count of the time spent — is deleted by the repair that follows it, and the diagnosis of that failure is only ever printed to chat. A task later redone or dropped takes the rest with it. A month of the same failure leaves nothing to point at.

### Phase 30 — task-rescue leaves a durable record of every run

The rescue already writes the diagnosis; it only throws it away. The record belongs in the skills repo, one folder for every project, under version control, so a month of runs can be read together.

- [x] **30.1 — task-rescue writes a durable report of every run it finishes** — the Diagnosis Report is mandatory at `:162` and printed to chat only, so once Step 5 deletes the sidecar the failed attempt leaves no trace, and a task later redone or dropped takes its history with it. Give `src/skills/task-rescue/SKILL.md` a `loads: note` edge; snapshot project, date and the sidecar's `step` and `elapsed` at Step 1, before anything is deleted. One report per run, written once through `note` at the end, when the repair is done and the task is ready for a new run: the four facts, the diagnosis, and what the repair did. Destination is the skills repo, resolved at write time by `git -C ~/.claude/skills/task-rescue rev-parse --show-toplevel`, under `.ai-factory/rescue-reports/<project>/`; never stored, never cached. The grant gains `Bash(mkdir *)`, which `note` needs. Spec: `.ai-factory/specs/trickster77777/105-rescue-report-persisted.md`. [18m 28s]

## A spent handoff looks exactly like a live one

A handoff is one-shot: it is mined from a session that no longer exists and its facts start ageing the moment it is written. Nothing on the file records whether it has been used, so a spent one keeps being cited as confirmation long after it stopped being true, and anyone may edit one that is already spent.

### Phase 31 — the handoff says, on itself, whether it has been used

Nothing in the family reads a handoff as a step, and nothing needs to. The instruction travels on the artifact, addressed to whoever opens it.

- [x] **31.1 — a handoff says on itself that it is a spent, temporary buffer** — a handoff is a temporary memory buffer for carrying context between sessions, but its `description:` calls it a durable note and nothing on the file says whether it has been used. In `src/commands/command-handoff.md`: the `description:` names the entity and its lifetime and drops the word `durable`; the emitted template gains one line between the `# Handoff` title and `## 1. Frame` — an unprocessed mark and the sentence naming the reader as the one who flips it, carried by both emitted shapes; and the body gains the rule that a processed handoff is never edited, that only an unprocessed one is edited, and that a spent previous part means a new handoff rather than an extended one. No skill reads the mark, nothing scans it, `roadmap-prune` keeps its hands off `handoffs/`. Spec: `.ai-factory/specs/trickster77777/106-handoff-processed-mark.md`. [18m 7s]

## A budget stated in characters is measured in bytes

Two budgets in this family are stated in characters and neither says what a character is counted with. The machine sets no locale, so every shell tool reaches for bytes by default, and a rule written correctly is checked incorrectly every time.

### Phase 32 — the budget carries its own unit and method

The numbers are right and do not change. What is missing is one sentence at the budget's own home saying how the count is taken, so nobody invents a command for it a ninth time.

- [x] **32.1 — the character budget names how a character is counted** — `roadmap-engine`'s contract-line budget (`Target ~600 characters (range 400–1000)`) and `roadmap-outline-deep`'s `~200–500 character` preamble budget are both stated in characters and neither names a method; this machine sets no locale, so `wc -m` and `awk`'s `length` both return bytes and read a 991-character line as 995. In `roadmap-engine`, beside the contract-line budget, state that every budget in this family counts Unicode code points with the trailing newline excluded, and pin the one command that yields them here. In `roadmap-outline-deep`, the preamble budget names the engine as that method's home rather than restating it — it already loads the engine. No new skill, no new grant, no verification step gains a count, and neither budget's numbers change. Spec: `.ai-factory/specs/trickster77777/107-character-budget-names-its-method.md`. [10m 16s]

## The durable surfaces name a buffer that is spent on first read

A handoff is `note` under a lens — the same distiller aimed at another genre and another folder — and `docs/sakshi-harness/skill-graph.md` already says so. Notes obey that without a rule: nothing in the family references a note file, and nothing ever tried to. The handoff is the one genre that acquired a top-level skill name, and once the name existed the skills wrote it into surfaces that outlive the artifact — a format, a permission, a deletion-safety rationale, and a protocol's definition of its own writer.

### Phase 33 — the skills stop naming the handoff

Reconciliation, not a new rule: the governing doc leads and four skills trail it. The act of handing off is untouched — `roadmap-prune`'s parked gate still tells the user to make one, and `agent-architect` still carries its buffer path across a compact. What goes is the reference to the artifact from a surface that outlives it.

- [x] **33.1 — four durable surfaces stop naming the handoff** — `docs/sakshi-harness/skill-graph.md` already holds that a handoff is `note` under a lens, yet four skills reference the artifact from surfaces that outlive it: `roadmap-engine`'s roadmap-format block lists `source handoff/spec links` in the direction preamble, `roadmap-outline` permits handoff links in that same prose, `roadmap-prune`'s `Phase note:` deletion-safety rationale rests on that permission, and `orchestrator-artifacts` defines the status-marker writer as the session opened from a parked prune's handoff. Drop the handoff from all four, keeping prune's rationale at full force on task-spec links alone. The act of handing off is untouched: prune's parked gate and `agent-architect`'s compact recovery stay, and `roadmap-test-coverage`'s unrelated `$HANDOFF_LIST` is out of scope. Spec: `.ai-factory/specs/trickster77777/108-skills-stop-naming-the-handoff.md`. [8m 41s]

## A task spec takes the shape of whoever writes it

`roadmap-engine` says where a task spec lives and never what it holds; `note` supplies the genre of a standalone note. So the shape comes from the author's current habit — and an architect who has spent a session writing apply work-orders supplies the work-order's shape: guards against what nobody was going to do, and counts that restate the instruction they check. The orchestrator then spends its rounds reviewing the checklist instead of the change.

### Phase 34 — a spec holds behaviour, and no work-order teaches it to count

Three edits, one home each. The shape goes to `roadmap-engine`, where the two-tier artifact is already defined; `command-pin-gaps` takes it as its base, because its three hole classes already are it. The third removes the register at its source: the apply work-order is required to carry a block of counts, and an architect who writes those all session writes them into a spec next. A sweep finds the four-part enumeration in one line of one file, no skill prescribing a task spec's sections, and self-verify prescribed in exactly four files, so nothing else in the family has to move.

- [x] **34.1 — `roadmap-engine` states what a task spec holds** — § "The two-tier artifact" gives the task spec its path, its numbering, its `Spec:` tag, and says it "holds the full implementation detail" without ever saying what that detail is; `note` supplies only its standalone-note default, so the shape comes from whoever is writing. Give that section one paragraph naming the three parts and nothing else: what is true now, read from code with exact values; what must be true after, in the code's own terms; what breaks on contact, enumerated rather than hedged. Bring the contract-line bullet's `current-state / target / guards / verify` enumeration into line with it. Specs already on disk are not rewritten and no skill gains a template. Spec: `.ai-factory/specs/trickster77777/109-roadmap-engine-states-what-a-task-spec-holds.md`. [12m 52s]
- [x] **34.2 — `command-pin-gaps` closes holes and writes no checks** — the command's three hole classes are already those three parts, but the command never says so, and its mandate "Close all of it now" carries no stopping rule, so a pass adds verification bullets that are not among its own finding classes. Name the three parts as the shape it repairs toward, one class per part, and state that it pins values and enumerates breakage and never authors a check: a check that can only fail where the instruction was ignored is a loud failure by `test-philosophy`'s discriminator. The value-hole repair also stops citing `file:line` into the spec — `docs/reference-by-name.md` forbids a position address there — and names its source instead; the scan-mode report line is untouched. No new hole class, and no restatement of the shape whose home is the engine it already loads. Spec: `.ai-factory/specs/trickster77777/110-pin-gaps-closes-holes-and-writes-no-checks.md`. [15m 25s]
- [x] **34.3 — no work-order is required to carry a check list** — `agent-architect` names four things an apply work-order must carry and the third is "the commands the editor runs to self-verify before reporting"; `architect-pairing-engine` repeats that component in both of its enumerations, and `editor.md` tells the editor to run them. Beside it, § "Verify the report by fact" already has the architect run its own greps against the real files after every round. Across phases 31–34 that block caught no defect in any landing while seven of its own pinned expectations were wrong — a count over the whole tree, a count contradicting a guardrail in the same order, a count that cannot return its number by construction. Drop the component from all three files; the editor keeps reading the diff back in apply mode, and `architect-editor-engine`'s "self-verifies" is unchanged. Spec: `.ai-factory/specs/trickster77777/112-no-work-order-is-required-to-carry-a-check-list.md`. [20m 5s]
- [x] **34.4 — `work-order` names one thing in the registry** — `docs/reserved-words.md` binds each concept to one name and each name to one concept — "a reserved word is never repurposed for another concept" — and then spends `work-order` on two: the task spec, called "the implementation-tier work-order", and the thing the architect drafts for its editor. They are different artifacts with different readers and different lifetimes, and the conflation is what licensed a task spec to be written in a work-order's register. Drop the phrase from the task spec entry, which already names the artifact fully without it; the paired-loop entry keeps the word. The file's opening line forecloses updating its meanings, and this removes a collision its own rule already forbids rather than changing one. No other entry moves, no term is added or retired. Spec: `.ai-factory/specs/trickster77777/113-work-order-names-one-thing-in-the-registry.md`. [13m 13s]

## The rule that belongs to every project is written down in one of them

`docs/reference-by-name.md` is ratified here and reachable only from here. Every other project loads the global CLAUDE.md and nothing else of ours, so a project that meets the naming defect writes its own local rule for it — one already exists in `tradeoxy_core`, and none of it is about `tradeoxy_core`.

### Phase 35 — the always-loaded layer says how a reference addresses

One paragraph, on the one surface every session of every project already loads. The deep home stays where it is and is not linked from there; the single local rule that grew in its absence belongs to the repository that wrote it, and this task tells that repository rather than reaching into it.

- [x] **35.1 — the global CLAUDE.md says a reference addresses by name** — § "Grounding claims" says how to follow a reference down to the leaf and never how one is written, so a project meeting the naming defect states the rule locally or not at all: `tradeoxy_core`'s `RULES.md` states it — cite `ARCHITECTURE.md` by rule name, never by line number — and no other repository here says anything about it. Insert one pinned paragraph into `src/global/CLAUDE.md` § "Grounding claims", between the chain-to-the-leaf paragraph and the one opening "The opening task statement", then write one handoff into `tradeoxy_core`'s own `.ai-factory/handoffs/` saying the rule is now always-loaded and its local copy is a duplicate for that repository to drop. The deep home `docs/reference-by-name.md` is not linked — the file loads in projects that do not have it. Spec: `.ai-factory/specs/trickster77777/111-the-global-claude-md-says-a-reference-addresses-by-name.md`. [18m 43s]

- [x] **35.2 — the always-loaded layer says a claim about a deleted artifact goes with it** — § "Grounding claims"' "Held context decays" sentence said a held file ages back into a description and must be re-read fresh before acting, but stopped one step short: a claim keeps its attribution — a filename attached, reading as grounded — even after the file itself is gone, and gone content cannot be checked, only dropped. Added a twelve-word clause onto that existing sentence in `src/global/CLAUDE.md` § "Grounding claims" — no new sentence, no new paragraph, since the surface is loaded on every run of every project and the whole point of the form is finishing a rule already being read. Names no plan, no rescue, no repository, and no artifact type — the file governs every project, most with none of our machinery.

## The pair is described by what each half does, and nowhere by what they share

The architect's skill defines the buffer as a private file the editor is never told about, and the engine both halves load at birth holds only the channel's two message formats. So the pair has an envelope and no memory: everything the pair has settled lives in one half's private file, and every work-order restates it from scratch. That is why orders grew until they pinned characters instead of meaning — the hand had no context to pin against, so the head sent letters.

### Phase 36 — the pair shares one working memory

The buffer's definition moves from `agent-architect` into the engine both halves load at birth: two zones, settled shared and live private, re-read on change, drained into the skill it should reach rather than restated in the buffer. Phase note: [the buffer is not yet shared memory](.ai-factory/specs/trickster77777/114-buffer-not-yet-shared-memory.md)

- [x] **36.1 — the buffer's definition moves into the engine both halves load** — `agent-architect` states the buffer's closed-to-the-editor claim twice — at its definition and again as a parenthetical — and both go. `docs/paired-loop.md` already calls `architect-editor-engine` the buffer's home, but the engine still scopes itself to the two message formats, and so does `editor.md`'s appositive. Give the engine the definition — its path and numbering named explicitly, the two zones and what each holds, which the editor holds and why the live zone stays private, re-reading on change, the drain rule. `agent-architect` points at the engine, not defining it; both callers' appositives are corrected in the same pass, `editor.md` included. Spec: `.ai-factory/specs/trickster77777/116-buffer-definition-moves-into-the-engine.md`. [10m 11s]
- [x] **36.2 — the buffer's creation is conditional, and it moves to the architect's own start** — `agent-architect`'s spawn-moment paragraph creates the buffer only as a side effect of writing the editor's handle into it, "creating the buffer at its existing path if it does not exist yet," and the section's opening line tells the architect it works alone before that, with nothing said about holding a buffer; nothing distinguishes continuing from a snapshot naming a buffer from starting with no pointer. State the rule as conditional: a snapshot naming a buffer means the architect works in it; no pointer means the architect creates one first, before any editor exists. The engine also becomes resident as part of that same start, ahead of creating the buffer, since after 36.1 the buffer's path lives there. Recording the editor's handle stays where it is — a write into a buffer that exists by then. Spec: `.ai-factory/specs/trickster77777/117-buffer-exists-from-the-architects-start.md`. [12m 11s]
- [x] **36.3 — the rule that no architect reads another's buffer gets a home** — `architect-pairing-engine` loads only "when the user has assigned it one of these two roles for the session; never in an unpaired session," but `docs/paired-loop.md` states the isolation rule unconditionally — "Two architects are two heads. No architect reads another's buffer, and an editor reads only its own architect's" — and this repository runs that exact situation with no pairing declared right now, so the rule would be unreachable exactly where it applies. Move its destination to `architect-editor-engine`, the engine both halves load unconditionally at birth and the same engine 36.1 makes the buffer's home: state both of the rule's clauses beside that definition, after 36.1 lands. No other artifact states the rule today, and `architect-pairing-engine` is no longer this task's target. Spec: `.ai-factory/specs/trickster77777/118-buffer-isolation-rule-gets-a-home.md`. [8m 20s]
- [x] **36.4 — the editor learns the buffer's place at spawn** — `agent-architect`'s spawn moment writes the editor's handle into the buffer but never tells the editor where the buffer is, and, after Task 36.1 lands, `editor.md` names the buffer but is given no path to it — naming is not locating. Give each half the other's address at spawn: `agent-architect` widens "its content *is* the spawn prompt" so the spawn prompt carries the buffer's path alongside the channel-message, at spawn only, in the same act that already writes the editor's handle; `editor.md` gains the account of receiving it, alongside loading `architect-editor-engine`. No new file, no new `loads:` edge. Spec: `.ai-factory/specs/trickster77777/122-editor-learns-the-buffers-place-at-spawn.md`. [7m 48s]
- [x] **36.5 — the memory snapshot is written on request** — `agent-architect` names exactly one occasion for a memory snapshot, "Before a compact, the handoff continuing this same architect across it," and nothing else in the file names a second. Add that second occasion: the architect writes a memory snapshot whenever the user asks for one, mid-session, not only ahead of a compact — its own capability, reached by a request rather than a command, no command invoked and no template consulted, carrying only the volatile residue and never an inventory of the session, each one superseding the last by name. § "On every invocation" points at that same occasion, not a second one, but identifies what recovers the architect by the occasion — "the pre-compact handoff that recorded your buffer's path" — rather than by the artifact, reworded here too since an on-request snapshot recovers the architect just as well. The digest clause — "is your own recovery note and is never sent to the editor" — is untouched. Spec: `.ai-factory/specs/trickster77777/123-the-memory-snapshot-is-written-on-request.md`. [12m 59s]
- [x] **36.6 — the architect writes to the memory as it learns, and names the change to the hand** — `agent-architect` names what it records in its buffer but gives only two of the three an occasion, narrow — the editor's handle at spawn, a pairing role when the user assigns one — and nothing tells the architect to write any of the settled content `docs/paired-loop.md` now has the engine hold, nor to announce a change. State the occasion the governing spec gives — write at the moment of learning, not at the end of the stretch of work — the form an entry takes — what will hold again, never the episode that revealed it — and the announce-obligation beside them: the architect names the change to the hand when the settled memory moves. No zone named, no list added, no restatement of the drain rule or the re-read rule 36.1 moves into the engine. Lands after 36.1. Spec: `.ai-factory/specs/trickster77777/124-the-architect-writes-to-the-memory-as-it-learns.md`. [14m 29s]

### Phase 37 — the channel carries meaning, and the hand that touched writes

Rests on 36: an order names what a sentence must say and where it goes, never composing it blind, and whoever touched a surface writes its grounded account. Two claims in `agent-architect` go with it: authoring a prompt in exactly one case, and its silence on handoffs and rescue reports. Phase note: [the channel is not yet meaning, and the hand is not yet the writer](.ai-factory/specs/trickster77777/115-channel-not-yet-meaning-and-hands.md)

- [ ] **37.1 — the architect's outgoing communication is misdescribed as a closed list, twice over** — `agent-architect` states, in its section heading ("author a prompt in exactly one case"), again in the body, and a third time in the sentence declaring "Two channel-message formats, nothing else," that everything the architect ever sends is one of two forms; false on both counts, since the architect also authors `REPORT-ONLY` rounds delegating its own legwork, and — per `docs/paired-loop.md` § "What crosses the channel" — the two forms govern only what opens a round, not ordinary communication that keeps the hand current. Name both authored cases in the heading and the body; rescope the "nothing else" sentence to what it actually governs. The marker's own rule is untouched — a marker on the user's message still forwards it, an unmarked user message is still never forwarded — and what is added is narrow: delegating the architect's own work needs no marker. Spec: `.ai-factory/specs/trickster77777/119-architect-authors-more-than-one-prompt.md`.
- [ ] **37.2 — an order names meaning and place, not the characters** — `agent-architect` instructs an apply work-order to "pin every value, path, and exact string it needs" — the textual source of the character-pinning register `docs/paired-loop.md` argues against, not a habit. State that an order names what a sentence must say and where it goes, the text written by whoever has the file open, because a writer looking at the passage does not restate what already stands beside it and composing a replacement blind is how that happens. A value stays pinned — a path, a number, a literal, an anchor a match is asserted against are the thing itself, not a description of it; what stops is composing prose for a file the order's author has not got open. Write it so a reader cannot take it as "pin nothing." The anchoring mechanism — a match asserted against a unique string, never a position — does not change. Spec: `.ai-factory/specs/trickster77777/120-order-names-meaning-and-place.md`.

## Three files each carry their own account of how a number is chosen

### Phase 38 — one numbering rule, not three

`note` already owns the mechanism every numbered artifact should share; `roadmap-test-coverage`'s own Layer 4 and the dormant `aif-plan` each kept an independent copy instead of calling it, and each copy is blind to its own limit rather than declaring one and stopping loudly at it. `note`'s scan gains a numeric, unbounded-width comparison with a declared bound; `roadmap-test-coverage`'s copy is brought into line where it stands, its parallel-agent structure untouched; `aif-plan` stays unused and unrepaired, recorded as a dormant carrier of the same defect. The phase closes once the family has one account of how a number is chosen, in fact and not only in one file.

- [ ] **38.1 — the numbering scan sees past ninety-nine** — `note`'s `[0-9][0-9]-*.md` scan, restated at several sites, matches exactly two leading digits and compares matches lexicographically; past 99 it sees a maximum that is not the maximum. Verified against `.ai-factory/specs/trickster77777/`: 19 files match, the visible maximum is 99, `note` would propose 100, and `100-single-editor-assumption.md` already exists. Widen the scan to a leading run of digits of any length for what it reads, compare numerically rather than lexicographically, write every new name — including the empty-directory default, now `0001` — with exactly four zero-padded digits, and at a destination already holding `9999` write nothing and report the bound is reached, naming the destination, instead of proposing a collision. `roadmap-test-coverage` and dormant `aif-plan` carry the identical defect and are untouched by this task. Spec: `.ai-factory/specs/trickster77777/125-note-numbering-sees-past-ninety-nine.md`.
- [ ] **38.2 — `roadmap-test-coverage`'s own numbering scan joins the family rule** — Layer 4 mints its own note numbers with `find .ai-factory/specs -name "[0-9][0-9]-*.md" | sort | tail -1`, never loading `note`, so it carries the same two-digit blindness independently, plus two defects of its own: lexicographic sort still returns a two-digit name once three-digit names exist and are visible to it, and the scan recurses the whole `.ai-factory/specs/` tree while `note` numbers per-directory, so two named roadmaps would share one space today. Widen the pattern for what it reads — its own "extract highest two-digit prefix" widens the same way, its "start at `01`" default becomes `0001` — switch to a numeric comparison, scope the scan to one directory, write every new name with the same four-digit bound `note` gets, and at a destination already holding `9999` write nothing and report the bound before any parallel agent is spawned. The parallel agents still write from one pre-computed number handed to each; nothing here serialises them. `note` gains no ability to reserve a block and stays generic. Spec: `.ai-factory/specs/trickster77777/126-test-coverage-numbering-joins-the-family-rule.md`.

---STOP---
