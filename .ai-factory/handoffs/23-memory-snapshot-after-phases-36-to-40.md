# Handoff — memory snapshot after phases 36 through 40

You are the same architect, in the same work, interrupted by context and nothing else. This supersedes handoff 21. Read this one; leave 19, 20 and 21 alone.

No account of the session follows. The work is committed and the reasoning is in your buffer; this carries only what neither holds.

## Read first

`.ai-factory/notes/07-architect-buffer.md` — yours, both zones, and read it whole before acting. Its Settled zone is the only place the session's judgment lives. Its opening section tells you what the skills now carry and what they do not.

`docs/paired-loop.md` is the governing spec for the pair and it moved four times today. `docs/test-coverage-pass.md` is new.

## What the buffer does not hold

**The skills you are loaded from are the rewritten ones.** Phases 36–38 landed while the session ran — the buffer's definition is in the engine both halves load, the path reaches the editor at spawn, a snapshot is written on request, the architect names it when the memory moves. You have the mechanics. The session before you did not, and worked from the buffer instead.

**The editor is alive and has lost its history twice.** Handle is in the Live zone. Both times nothing announced it and nothing went wrong: it reads files rather than recalling them, and its reports carry the raw output that catches a wrong order. Ask it to read, never to recall.

**It caught more of the architect's errors than the architect caught of its own.** Chief among them: a boundary drawn in an order fenced off the very place a defect lived, six times. None reached execution, and not through vigilance — every one of those orders also demanded the excluded ground be reported. A guard without that demand is how a wrong boundary becomes a silent defect.

## Where the work stands

Phases 36, 37 and 38 are closed: ten tasks, no rescue, every one converging on its first implementation attempt. Phases 39 and 40 are open and every task in both has been through `command-pin-gaps`. Everything is committed; the working tree holds nothing but this file.

`roadmap-prune` is due and will refuse — deferred observations from 36–38 remain unpinned because the tasks answering them have not run. That is the gate working, not a blockage to clear.

## What must not be resolved by inference

**Where the question about excess belongs.** `command-pin-gaps` asks whether a task has anywhere to land, and asks it well. Nothing anywhere asks whether a task does *more than its defect requires* — which was the most productive question of phase 40, four findings out of seven. The chain has no subtractive step at all: decomposition's atomicity gate can only split a task, never shrink one, and the gap pass by construction finds absence and never surplus. The user has decided not to place it yet. Do not place it.

## The habit that cost the most today

A rule with a findable surface displaces the judgment it was meant to serve. Position addresses, counts, completeness claims — each is something you can search for, so it feels like work, and each generated rounds that changed no instruction. The rules that paid were the ones you cannot grep: does this have somewhere to land, is it wider than its defect, whose word is this, who owns this number.

The buffer's method section holds the survivors; four entries were cut today for failing the test *what breaks here if I do not do this*. Apply that test before adding another.

## On the mark

This file carries no processed mark. `command-handoff` still emits one; the user retired it on evidence and a task to remove it sits in the buffer's deferrals. Do not mark this, do not expect a mark, and read no meaning into the marks on older files.
