# Handoff — transplanting this architect across a compact

**Processed:** `[ ]` — whoever reads this marks it; a marked handoff is spent.

You are the same architect, in the same session, interrupted by context and nothing else. This is not a handover to anyone: it carries no summary of the work, because the work is on disk and the reasoning behind it is in your own buffer.

## Read this first, and it is nearly all of it

`.ai-factory/notes/07-architect-buffer.md` — yours, both zones. The Live zone is where the work stands, the editor's state and the open deferrals; the Settled zone is the discipline, the rulings in the user's own words, the method learned the hard way, and the register of where the skills lag it. Read the whole file before acting on anything below.

The governing account of how you and the editor work is `docs/paired-loop.md`. Read it if the buffer's Settled zone leaves you unsure why something is the way it is — it is the spec, the buffer is the practice.

## What the buffer does not hold

**The editor is alive, and it is not blank.** It has read `docs/paired-loop.md` as instructions addressed to itself and holds the buffer's Settled zone as its own working context. It does not read the Live zone, deliberately: a hand that already holds your conclusion returns an echo where you wanted a second reading.

**It has silently lost early history.** Well over 400k tokens have passed through it this session, past any window. Its accumulated context is partial and there is no signal when a piece goes. Never treat a round it handled hours ago as something it still remembers — the file it produced survives, its memory of producing it may not. Ask it to read, not to recall.

**Pin meaning and place, never characters.** This is the single behaviour most likely to slip back, because the skill's own text still instructs the opposite. Composing replacement strings blind produced three doubled sentences in one session; a writer with the file open produces none.

## The next action

`roadmap-decompose` over Phases 36 and 37 in `.ai-factory/roadmaps/trickster77777.md`.

Everything it needs is on disk and none of it was in the lost context: the two phase notes the preambles point at, `docs/paired-loop.md` as the governing spec, and the four artifacts they name — `agent-architect`, `editor.md`, `architect-editor-engine`, `architect-pairing-engine`. That is why the decomposition was deliberately left until after the compact rather than rushed before it.

Before it, optionally, `roadmap-prune`: the roadmap holds 35 closed tasks and folding them into Features leaves the two open phases on a clean map.

## What will go wrong if you are careless

The buffer's standing method rule names the three shapes an expectation goes wrong in, and they cost a round each, seven times in one session. Read it rather than rediscovering them.

And the failure this whole stretch of work exists to remove: a task that carries instructions for reviewing itself. If a spec you write has more checking in it than change, the checking is the defect.
