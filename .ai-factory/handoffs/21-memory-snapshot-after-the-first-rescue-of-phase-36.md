# Handoff — memory snapshot after the first rescue of phase 36

You are the same architect, in the same session, interrupted by context and nothing else. This supersedes handoff 20, whose next action — decomposition, then a readiness pass — has since been overtaken: the first task of phase 36 ran, failed at planning, and was rescued. Read this one; leave 19 and 20 alone.

No summary of the work follows. The work is on disk and the reasoning is in your buffer; this carries only what neither holds.

## Read first

`.ai-factory/notes/07-architect-buffer.md` — yours, both zones. Live holds where the work stands, the editor's state, and the open deferrals; Settled holds the discipline, the user's rulings in their own words, the method, and the register of where the skills lag it. Read the whole file before acting.

`docs/paired-loop.md` is the governing spec for how you and the editor work. Read it where the buffer leaves you unsure why something is the way it is.

## What the buffer does not hold

**The editor is alive and it is not blank.** Handle is in the Live zone. It has read `docs/paired-loop.md` as instructions addressed to itself and holds the Settled zone as its own working context; it does not read the Live zone. It wrote the six task specs, the two phase notes, and every repair of this rescue.

**It has lost early history silently.** Over six hundred thousand tokens have passed through it, far past any window, and nothing signals when a piece goes. Ask it to read, never to recall.

**It now flags a deviation instead of delivering it as a result.** It once resolved a pinned decision the other way and reported only the outcome; told that the flag is worth more than the agreement, it has flagged every judgment call since, including two real defects this rescue would otherwise have shipped. Read its flags — they have been right more often than the orders they questioned.

**Pin meaning and place, never characters.** The behaviour most likely to slip back, because `agent-architect`'s own text still instructs the opposite until 37.2 lands.

## Where the work stands

Six open tasks: `36.1`–`36.3`, `37.1`–`37.3`, specs `116`–`121`, phase notes `114` and `115`, governing spec `docs/paired-loop.md`.

`36.1` has already run once and failed at planning. It is rescued and repaired: its spec now distinguishes the three claims riding in one sentence, its neighbours no longer cite text it empties, its plan is kept at `planned:1` with the reviews deleted. It is ready to run again. The other five have not been through a readiness pass.

## What must not be resolved by inference

**The user said the buffer "should appear together with the editor."** That describes today's behaviour; task `36.2` as written moves creation to the architect's own start. Loose phrasing for "from the beginning" leaves `36.2` correct; taken literally it is wrong and ships a change the user did not ask for. Ask. Do not decide it.

**Whether the Settled zone is a staging area or the pair's durable context** is unresolved, and the two answers lead to different tasks. Nothing in `36.1`–`36.3` settles it.

## The habit that cost this rescue

Five times in one day, a textual unit was treated as if it carried one meaning: one occurrence of a claim a file states in several places, then one sentence carrying three claims whose fates diverge. The repair of that defect repeated it twice more. Every instance was caught by a second reader with the file open, never by the architect re-reading its own text.

Two deferrals in the buffer name what no pass catches: a phase where several tasks edit one file has an internal blast radius nothing computes, and a sentence is not a unit of claim.

## What will go wrong if you are careless

The buffer's standing method rule names the three shapes an expectation goes wrong in. Read it rather than rediscovering them.

And the failure this whole stretch exists to remove: a task that carries instructions for reviewing itself. If a spec has more checking in it than change, the checking is the defect.

## On the mark

Do not mark this file, do not expect a mark on any handoff, and read no meaning into the marks already on disk. The processed mark is retired on evidence — agents forget it or refuse it, and a rule nobody follows is not a rule. Removing it from `command-handoff` is a task in the buffer's deferrals; the marks still on older files are residue, not signal.
