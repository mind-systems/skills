# Architect buffer — 07

One memory, no regions. The architect is its only writer; the editor reads it whole, as its own working context from the head it is the hand of, and never writes to it. It re-reads when the architect names that the memory moved. The governing account of this file is [paired-loop](../../docs/paired-loop.md).

## Read the open tasks before trusting the skills

The six open tasks all edit the skills this pair runs on, so the loaded text and the intended text differ on exactly the points the pair is working. The contract lines are short; read them first, then the skill. This is not a general caution — it is true of this roadmap right now and stops being true when the six land.

## Editor

- **`TaskStop` on the editor is terminal, not a round-cancel.** Learned by doing it: stopping a mid-flight round kills the conversation for good — the next `SendMessage` returns "was stopped by the user and won't be resumed," and the harness then forbids spawning a replacement without the user asking. There is no way to cancel a round and keep the hand. If a round is going the wrong way, let it finish and discard the result, or send a correcting round; the cost of a wasted round is far below the cost of a dead editor. The killed round left no partial writes, which is luck, not a guarantee.
- Handle: `acc89e764a95dcebe`, agent type `editor`. Three hands have run this session; two are dead — one killed by `TaskStop`, one lost to a network fault mid-round, which left its work on disk complete. Do not send to `ad6a88c15b45ee4f5` or `ad2970bdb7f18a347`.
- It holds this whole file as its own working context and re-reads it when told it moved. It writes to it never.
- Its history has now ended three times without a signal — the last time mid-way through the pin rounds, its context a quarter of what it had been — and each time it worked correctly because it reads the buffer and the files rather than recalling. Ask it to read, never to recall. Its reports carry raw output, which is how it caught its own suffix-matching bug before writing a single marker.

## Pairing role

None assigned; `architect-pairing-engine` not loaded. Seven buffers sit in `.ai-factory/notes/`; six belong to other heads and are never opened from here.

## Where the work stands

**The roadmap is being executed, not written.** Five tasks landed today by the orchestrator, one failed and was rescued and then landed: `56.1`, `56.2`, `57.2`, `57.3`, `58.1`, `58.2`. Six remain open — `58.4`, `59.1`, `59.2`, `60.1`, `61.1`, `61.2` — all above the stop, all of them editing the skills this pair itself runs on.

**That last fact is the one to hold.** Until those six land, the skills loaded in a session say the old thing. A fresh head reading `agent-architect`, `architect-editor-engine`, `command-pin-gaps`, `roadmap-engine`, `roadmap-decompose-skeleton` or `command-handoff` is reading text six open tasks are about to change. Read the six contract lines before trusting any of those files on the points they touch.

What landed, so the skills are already different from what an older memory holds: the census is out of `command-pin-gaps`' blast-radius clause, replaced by rule + literal sweep + invariant; `roadmap-engine`'s root reads *pinned* rather than *enumerated*; `polymorphism-philosophy` exists as a skill with a symlink in `active/`; `roadmap-decompose-skeleton`'s Lens 1 fires on the polymorphism unit's event instead of judging testability; and the memory snapshot's paragraph in `agent-architect` now demands the stretch's reasoning and scopes supersession to the next action.

Docs moved too, by our own hand and ahead of the tasks: `docs/what-a-task-carries.md` is new and is phase 61's governing spec; `docs/paired-loop.md` gained what a break destroys, who writes the snapshot and why, supersession scoped, the series as a record, and the two genres — and lost the buffer's zone split entirely; `docs/sakshi-harness/skill-cycle.md` gained the skeleton pass's second axis; `docs/philosophy/context-grove.md` went from twenty-five lines to nine.

Nothing is uncommitted. The working tree is clean as of `9731d7b Docs update`.

## Open deferrals

- **A quoted phrase is not a usable sweep anchor in this family, and the failure is silent.** Our prose wraps at a fixed column — `src/skills/agent-architect/SKILL.md` runs to 99 code points a line — so a "distinctive phrase" of several words routinely straddles a line break in the source. A plain single-line `grep` for it then matches **nothing at all** and reports clean. Met twice in one pin-gaps pass, in two different specs, and in one of them it hid a second live occurrence of the phrase inside the very file being edited. By our own discriminator this is Class C: a check that can no longer fail. The repair is mechanical — anchor on a substring short enough to survive the wrap, and re-run the command before writing its result — but the trap is in the material, not in anyone's care, so it will recur. *Trigger:* it is very likely sitting unnoticed in the blast-radius section of every spec no pass has re-run; worth a sweep of its own when someone has the appetite.

- **The family requires nothing about the shape a distinction takes, and is itself built entirely on that shape.** Measured, not assumed, and stated as the rule rather than the tally, because the tally was wrong once already — the first scan missed `.ai-factory/ARCHITECTURE.md` and reported one occurrence where there are three: **every appearance of the OOP vocabulary in this corpus describes the architecture of our own skill system, and none of it is a requirement on the code the system plans.** `skill-graph.md` names separation of mechanism and policy as microkernel architecture and the philosophy→engine hand-off as Strategy; `ARCHITECTURE.md` § "Composition: mechanism vs policy" says the same twice. Adding a fourth such sentence tomorrow would not disturb that claim, which is why it is the claim worth holding — `docs/counts-go-stale.md` applied to our own writing. Meanwhile eight skills are built on injected hooks: `note` takes destination, template and verbosity from its caller instead of branching on who called it; `roadmap-engine`'s maintenance flow takes granularity, the per-entry gate, routing and extra actions the same way. The registry holds 41 words and none of them is about whether a kind is carried by a type or by a field. So the agent has been following the written discipline faithfully, and the written discipline never asked — which makes the repair **additive**, and predicts that the requirement will be honoured as faithfully as everything already in the corpus. The engine's content is settled in handoff 28: the question *to add a third kind, how many places must change*; the trigger *the arrival of the second invariant*, an event and not a threshold; the exemption *the site that constructs is never itself a finding*; and classical names cited rather than re-derived. One lens, two selectors — time (what a diff just landed) and space (a named region, kinds enumerated and each asked the same question) — which is what lets it run with no task in front of it. *Trigger:* the user opens it; his own candidate for the first consumer is `roadmap-decompose-skeleton`, whose first lens already hunts shared type surfaces without ever naming what it is hunting.
- **The reference graph has no resolver, and that — not the address form — is what keeps failing.** The user's own diagnosis: we copied the book's table of contents and dropped the mechanism that makes it work — nobody hand-writes a page number, typesetting derives it fresh each edition. `docs/reference-by-name.md` already states the missing piece in its first paragraph — "Nothing checks such a reference, and nothing reports it when it rots" — and we read it as an explanation. A name is not immune either: it survives edits *around* the target and dies silently on an edit *to* it. What a name has that a number lacks is redundancy with its target, and that same property is what makes an address machine-checkable: a quoted heading or a link path can be grepped for, `§ 3` and `:41` cannot. The nine `Phase note:` pointers were checked this way in one line. So the honest repair is a check over the graph — silent surface, owes a test by our own discriminator — and the address form falls out as whatever the check can take. Renaming a target still breaks its citations; a resolver makes that loud at the moment it happens, which is the whole gain. *Trigger:* the user rules whether phase 42 keeps its subject (which address form) or is re-aimed at what checks one; the fork was put and not yet answered.
- **A phase has an internal blast radius and no pass counts it.** Where several tasks of one phase edit the same files, the first to land invalidates the citations the others were grounded in. `command-pin-gaps` reads a task against the code; nothing reads a task against its siblings' pending edits. Found by hand on Phase 36 after the same defect surfaced five times: a spec's grounded section cited a heading, a sentence and a span that an earlier task of its own phase removes. Belongs beside 34.2 as a `command-pin-gaps` concern.
- **A sentence is not a unit of claim.** `agent-architect:235-237` is one sentence carrying three claims whose fates diverge — two close the buffer to the editor and go, one names the architect as its only writer and survives with a new reason. A spec that disposes of "the sentence" disposes of three different things in one word, and the rescue order, the spec and the plan each read it differently. The same error one level up is naming one occurrence of a claim a file states in several places. Both are the same habit: treating a textual unit as if it carried one meaning.
- **No recovery path for the buffer itself — ruled and now built on disk.** A compact before any snapshot is written still orphans a buffer, and a fresh architect cannot tell `07` from `01`–`06`; what the ruling settled is that this is a cost paid once rather than a wrong state, since no pointer means a new architect creating a new buffer. 36.2 carries it, and 36.2 has landed — kept here only because this session's loaded text predates it.
- **The processed mark comes back out of `command-handoff`.** Task 31.1 put it in this morning — the mark line in both emitted shapes, the Template-hook exemption that carries it verbatim, and the `## Holding a handoff` body rule built on it. The user has since retired the idea on evidence: agents do not flip it. Removing it is a task against the same file, and the reasoning that put it there is in spec 106 for whoever writes the reversal. *Trigger:* the user says to write it.
- **Does a guard invite a report of what it excludes?** A boundary is drawn from what the drawer can see, so a wrong one is definitional rather than careless — four times in one session an order's guardrail fenced off the very place a defect lived. None reached execution, and not through vigilance: every one of those orders also required the excluded ground to be reported, so a wrong boundary cost a round instead of a defect. Task specs write their blast radius in the forbidding mood — *do not touch X* — with no matching invitation, and the implementer's channel back is a plan review rather than a hand that argues. Whether that asymmetry is what most orchestrator failures were made of is measurable, not arguable. **The sample has now run: ten tasks, no rescue, every one converging at implementation.** That answers less than it looks. A guard that fenced off something wrong and was silently obeyed produces no rescue — it produces a defect in the landed files. The convergence count is the loud instrument and this is a silent failure; reading the landed text is the only measurement that reaches it. *Trigger:* a reading of what phases 36–38 actually put into the skills, against what their specs forbade.
- **The orphan check has run once, and came back clean.** All four paired-loop artifacts were read whole against what phases 36 through 40 did — every heading against the places that cite it by quote, every added or rescoped mechanism at its landing site, and a sweep for four pre-40 phrasings. No sentence still states a removed mechanism. Read it for exactly what it is: a pass that asked about **staleness**, which is the orphan question. It says nothing about the guard question below, which asks whether a boundary fenced off the right ground — that one is still unmeasured, and a clean staleness pass is not evidence for it.
- **`aif-plan` carries the numbering defect and has no task.** The same two-digit prose `note` holds, verbatim, at three sites; the skill is stored but not symlinked into `active/`, so nothing loads it and nothing collides today. The user has parked it deliberately — not used, not deleted — and will say what to do once the live carriers are settled. *Trigger:* the user returns to it. Until then it is a dormant carrier, named in spec 125's blast radius so an activation does not reintroduce what phase 38 closes elsewhere.
- **`roadmap-test-coverage` writes where the roadmap in play does not point.** It loads `roadmap-engine` and resolves the named roadmap for reading, then names the flat `.ai-factory/specs/<NN>-<slug>.md` at every one of seven destinations and states its own guard against a literal `ROADMAP_TESTS.md`, which is the default roadmap's name alone. The engine reaches the skill's reading and never its writing. Deliberately left unowned: prune is rare and touches the whole project, and this skill runs immediately before one, so the misplacement ages out rather than compounding. *Trigger:* a second named roadmap in one project, or the user says.
- **A review dimension no existing reviewer covers — run once, and it re-aimed itself.** The pass asked whether a spec's prohibitions were wrong and silently obeyed. Over ten landed tasks: none were. What it did find, through its secondary check, was the one real defect in the phase — 36.5 left a sentence stale in a section its spec never enumerated, and 36.6 closed it only because it happened to be editing the same paragraph for its own reason. So the subject is not fences but **what a phase's removals orphan that no spec names**; the fence question is answered and the orphan question is the one with evidence behind it. Two implementers also repaired outside their scope unasked, which is why the silent-obedience fear is smaller than it looked. **The user has ruled against building it.** Reviews of executed phases stay as they are — varying, shallow, unfixed — while the rewritten skills are watched in use; a fixed lens now would measure the lens instead of the change. And the pass's one finding turned out to be no gap at all: the reviewer had already recorded that staleness as a deferred observation naming 36.6 as its owner, and 36.6 closed it because the observation pointed there. The existing channel — reviewer records, prune's gate refuses to fold until it is pinned — covers the class. *Closed, not deferred. Reopen only if the watching turns up something this channel misses.*
- **`command-handoff` delegates composition to a hand that cannot hold the material.** Its Step 2 sends the writing to `note`, and `note` mines the session — the one thing a subagent does not have. Worse in the case actually met: a handoff had to carry a hundred and twenty-six entries that were in neither author's context, gathered by a file sweep, so one artifact ended up written by two authors. The skill has no shape for a handoff whose content comes from somewhere other than the conversation. *Trigger:* the user says, or a second handoff needs the same split.
- **A name can be narrower than its content without being wrong, and nothing catches that.** 39.1 fixes the case where research dissolves the area a name was chosen for. The other case slipped through the same assembly unnoticed: a spec named for timeframe validation grew to cover branch selection, positional binding, an ordering reversal and gap grouping — the name covers about a third of it and is not false, so no check fires. Caught only when someone asked. *Trigger:* the user says.
- **`$HANDOFF_LIST` in `roadmap-test-coverage`.** The word carries a second concept there — items handed to `roadmap-decompose`, nothing to do with `.ai-factory/handoffs/`. Not a registry violation: `handoff` is absent from `docs/reserved-words.md`, and that document declares itself final. Task 34.4 set the precedent for removing exactly this collision without breaking that finality. *Trigger:* the user rules on whether the registry admits the genre.
- **One finding still owed to `aif-docs`.** The two handoff genres — continuing the same architect across a compact against handing to a different agent or repository — are named by no document, so neither can become a task until the spec states it. The other finding, the editor's own context ending short of death, is no longer owed: the user has ruled the recovery, and it goes into `docs/paired-loop.md` with the rest of the lifecycle.

## Working discipline

**Standing rule about this section: whenever the user gives feedback on how the architect works, record it here immediately, as settled behaviour — not as an episode.** A ruling that stays in the chat dies with the chat, and the next session repeats the correction. *This debt is assigned: `docs/paired-loop.md` now states the occasion and the form, and 36.6 carries them into `agent-architect`. Erase this rule when 36.6 lands — it is the one rule that cannot go on living only here, since a first architect has no buffer to read it from.*

Keep the chat on substance. Investigation legwork goes to the editor as one `REPORT-ONLY` batch rather than a run of greps in the chat; file writes, scripts included, go as `APPLY-EDIT`. Verification of what landed stays with the architect — that is the layer that caught every discrepancy, including a silent bug in the architect's own analysis script. **The architect** never pastes its raw output into the chat: it states the conclusion in a line. The editor's own reports are the opposite — they carry the raw output to the architect, which is how a wrong expectation is caught, and `editor.md` requires it. A single decisive check is cheaper run directly than relayed.

Until the shared engine holds this file, the editor has no way to notice a change here on its own. The architect names it when this memory moves; an unannounced edit reaches nobody.

**The marker and delegation are different axes.** `::` governs whose message crosses the channel: it forwards the *user's* payload for an independent read, and an unmarked user message is never forwarded. Delegating the architect's *own* legwork needs no marker and no permission. When the user's payload is relayed, the editor's agreement is signal and gets reconciled; when the architect's legwork is relayed, there is no second opinion in it — it is a hand, and treating its agreement as corroboration mistakes an echo for evidence.

**The architect writes handoffs, the buffer and rescue reports itself — because it holds the context, not because a rule forbids the hand.** A handoff is the architect's own context, and having something that does not hold it retell it produces an unpredictable result; the same goes for anything the architect authors from its own reading. This is a practice, not a boundary to enforce: the user has ruled that outside the buffer it does not matter whose hand wrote a thing, and a task naming the class was withdrawn on exactly that ground. Everything else goes to the editor by default.

**Taking a memory snapshot on request is a capability of the architect, not a property of any skill.** The user asks for a snapshot and one is written — no command invoked, no template consulted. It is a snapshot precisely because everything durable already lives outside the conversation: the roadmap holds time, the specs hold decisions, this buffer holds the practice. The snapshot carries only the volatile residue — where the work stands, what the hand knows, what will slip first, and what must not be resolved by inference. Written that way it stays short; written as an inventory of the session it is neither short nor a snapshot. Each new one supersedes the last by name, so a reader never follows a stale next action. *This debt is assigned: 36.5 carries it into `agent-architect`, folded in rather than given a task of its own because the sentence it lands in is already rewritten by two tasks of the same phase. Erase this paragraph when 36.5 lands; do not write a second task for it.*

**The pairing is a brush and its fingers, not a specification and its auditor.** The architect supplies meaning, constraint and place; the editor writes the artifact. Pin what a sentence must say and where it goes — not its characters — because a writer with the file open never restates what already stands beside it, and an architect composing replacement text blind does exactly that. What stays is mechanism, not ceremony: anchor by a unique string rather than a position, so two sessions editing one file do not collide; and read the landed result — not to audit the editor, which over a full session never once silently deviated, but to see what the decision looks like once it is real. Every defect that reading caught was the architect's own.

**Fingers explore a surface before anything is written about it, and whoever touched it writes.** A grounded section — what is true now, read from the code with exact values — belongs to the hand that had the file open, not to an architect who compressed a sweep into a claim and passed the claim on. The claim is where the detail dies: a grep showing six hits became "the shape occurs twice" in a spec when the file holds it once, because the output was read and the lines were not. The architect keeps the decision — what must change and why.

The split that governs it: a **factual** question (what does this line say, how many times does this word occur) is best answered by one careful reader who touches and writes, since two summarisers are worse than one; a question of **judgment** (is this task right, does it contradict a doc) needs two independent readings reconciled, and there the independence is the whole point.

**A delegated round is reconcilable when a second reading exists, not when the user's marker put it there.** The rule as written keys on who asked, and the pair has now run eighteen rounds against it: nine where the hand read independently while the head walked the same ground and withheld its own read. Those returns were not echoes — twice the hand overturned the head outright, and more often it was sharper on a point the head had reached flatly. What a reconcile needs is two readings **produced independently**: existence and independence, both. Origin correlates and does not decide. Contamination is not the binding constraint either: the hand never held the head's in-flight read in the first place — it lived in the conversation, not in this file, which is why the zone erected to guard it prevented nothing and has now been removed. *Phase 48 carries the repair; the defect starts in `docs/paired-loop.md`, not in the skill.*

**A ruling recorded here is a debt against the skill, not a record of one.** It is erased when it reaches the artifact that should hold it. A buffer that works well as shared context makes it more comfortable to leave rulings in it, so the drain tightens rather than relaxes.

## Rulings, in the user's own terms

- **A handoff is `note` under a lens, nothing more.** "Это просто надстройка над ноутом, как дистиллятором смысла! Мы же не пронизываем все скилы ноутами!" Already the governing position in `docs/sakshi-harness/skill-graph.md`; the skills drifted from the doc.
- **Provenance is not reference.** A document written after reading a handoff is fine; citing it is not.
- **A skill is its own documentation.** `docs/` describes the system; a single skill's behaviour belongs in the skill. Behaviour spanning several artifacts — the paired loop — belongs in `docs/`.
- **A task spec holds three things and nothing else** — what is true now, read from code with exact values; what must be true after, in the code's own terms; what breaks on contact, enumerated.
- **A handoff is a snapshot of memory, and the processed mark is retired.** Where you are, what is in flight, what the durable artifacts do not hold — never an inventory of the session. The mark shipped in the morning and failed by evening: agents refuse to flip it or forget, and handoff 19 sat spent and superseded still reading `[ ]`, unmarked by the architect who superseded it. A rule nobody follows is not a rule. Do not mark, do not expect a mark, read no meaning into the marks already on disk.
- **A task is not an instruction for reviewing itself.** The orchestrator has a department for that. Guards against what nobody would do, and counts that restate the instruction, belong to neither tier.
- **The buffer's life is keyed to the handoff, and the editor is told which buffer is ours.** "Если скилл архитектора спавнится вместе с хэндофом, снапшотом памяти — это значит, буфер уже есть и он прописан в хэндофе. Если в хэндофе нет ссылки на буфер — это новый спавн архитектора и он обязан себе создать сначала новый буфер. Потом создать редактора и записать его имя или айди в буфер. Во время спавна редактору должны сказать — какой буфер наш. Если редактор компактнул свой чат, ему надо снова скормить последний хэндоф-снапшот памяти архитектора." Four consequences: a handoff naming a buffer means adopt, never create; no pointer means create first, before the editor exists; the editor learns the buffer's path at spawn; and the handoff is the editor's recovery carrier too, not the architect's alone.
- **Keeping the hand in the loop is a basic need, not something that must find a slot in a format.** "Я не хочу архитектора ограничивать какими то конкретными сообщениями… но свободное общение и держание редактора в курсе происходящего — какая то базовая потребность что ли." The two forms govern what opens a round; they were never an inventory of everything the head may say. Read as exhaustive, they manufactured three problems in one phase — a path that needed a sentence widened to travel, a recovery judged unimplementable, an announcement with nowhere to go. *A debt: `docs/paired-loop.md` states the round/upkeep split, 37.1 carries it into the skill.*
- **A phase is a legitimate routing target while no open task exists.** "Я знаю, что скилл требует указывать в каком конкретно таске это разруливается, но хочу сейчас другое поведение — достаточно указать фазу и чтобы прун прошёл. Суть этого прохода перед пруном — чтоб не потерялись находки при удалении старых артефактов. Мы самим своим существованием гарантируем, что эти таски не канут в лету." The marker reads `[routed → Phase N]`; decomposition of that phase is where the routed finding lands in a spec. *A debt against `orchestrator-artifacts` § "Status-marker grammar", which admits only an open task's spec as the target.*
- **The user's own invocation practice, stated plainly: the skeleton is always called for a whole phase, `command-pin-gaps` always for a single task.** Not a preference to be re-derived from the skills' targeting text, which permits wider scopes in both. It settles phase 43's open question by practice rather than by argument, and it fixes where a shared lens would sit: the skeleton sees a phase's whole set of kinds before any of it lands, the gap pass sees one task's change against code that already exists.
- **A philosophy unit needs no thin lens over it for the user's own use: he supplies the route himself.** "Я могу сформировать эту часть инпута сам, если вызову философию тестов и укажу область в коде и скажу что вызывает у меня подозрения, агент легко справится." So an engine invoked directly with a named region is already usable, and inventing a routing skill on top of it buys nothing — the operator is the route. This retires the last remaining piece of invention in handoff 28 (the "+1" sweeper skill).
- **Phase 57 is scoped to the philosophy unit and the skeleton, and `command-pin-gaps` stays out of it.** "В пингапс я бы эту философию пока не интегрировал, не знаю почему, но не хочу. Хочу только скилл ооп философии и интеграцию в скелет с заменой его описания 'поиска общих поверхностей' которая — и есть полиморфизм." A ruling given without a stated reason is still a ruling; do not re-propose the pin-gaps bead, and do not reconstruct a reason for him. **SRP is dropped from the unit** — "этот принцип слишком абстрактный". It never was in handoff 28 as a proposal; what that file carries is the spaghetti framing, never the principle by name. The drop is also consistent with the unit's own design: its whole value is that it has a *moment* — the arrival of a second invariant — rather than a virtue, and a principle that is true always fires nowhere.
- **A single field instance is not a phase, and a working skill is not repaired because a gap was found in it once.** "Одноразовый случай и мы пытаемся его как то обработать? Эти пропуски и дыры встречаются всегда и повсеместно, мы не можем их все закрыть. Скилл работает замечательно." Said while deleting phase 45 outright rather than decomposing it. The standing consequence for the architect: a finding in a field report is evidence that something happened once, never on its own a case for a permanent rule in a skill — and the cost of the rule is paid on every future run, while the case was paid once. Ask what channel already covers the class before proposing a phase for it; here the reviewer's deferred observations plus the prune gate had already caught and closed an instance of this very class in this repository.
- **A task is complete and self-sufficient on its own; the phase note is the architect's, not the orchestrator's.** "Таск для оркестратора — всё что нужно. Таск обязан быть полным и самодостаточным. Даже доку я не хотел чтоб он читал, но это ладно. Фазовый ноут — для архитектора, а не оркестратора." So the planner's silence about `Phase note:` is the design, not a gap — note `144-` records it as a divergence and is wrong to. Reading a `Governing spec:` is tolerated rather than wanted. What this does **not** mean, corrected by the user on the prompt's own text: a spec naming a phase note is not thereby incomplete. The planner walks named edges to the leaf — "a note referencing another note, a note naming a doc", never stopping one hop short — so a reference from a spec is followed. What the planner never reads is the pointer on the **phase header**, because its walk does not go there. And nothing follows from it for how a spec is written — the architect turned this into a round of prose-tidying across specs and it was waste: whichever of the two a fact is attributed to, the planner reads both and learns the same thing. A searchable pattern is not a defect. And the stronger consequence, from the orchestrator's own prompts read today: the **implementer never reads the spec at all** — it is handed the plan file and nothing else — so a spec's precision reaches the planner and survives only if the planner carries it into the plan. Sentence-level guards are written for a translator, not for the hand that edits.
- **The editor is the architect's hand, not a resource to ask permission for.** "Редактор — твоя рука, не жди моего приказа трогать его! Пользуйся им! Умер — делай нового всегда!" So: never pause work to ask whether to spawn or re-spawn one; never treat a dead hand as a reason to stop, and never as a reason to start editing shared artifacts by your own hand instead. A death is reported in passing, not as a question, and the next channel-message is the new spawn, carrying the buffer's path. This overrides a tool's own advice to wait for an explicit request — the request is standing. What a fresh hand costs is context, not permission: it holds none of the accumulated round history, so the first orders carry more ground than usual and every value it must not invent is pinned in them.
- **A handoff is thick by default; thin is something the user asks for.** "Думаю по дефолту он всегда должен быть толстым, но иногда я могу попросить тонкий. Или по смыслу — передать список ссылок для поднятия контекста, вполне валидный тонкий хэндофф." Two consequences: thickness is never a judgment call made at write time to look finished, and a thin one is a legitimate *form* — a pointer list for raising context — not a failed thick one. This is the axis that crosses the genre split (the architect's own memory snapshot vs. the project handoff `command-handoff` writes); both genres have both weights.
- **The buffer's seed template is its own file, read once at birth.** "Мне кажется это отдельный файл должен быть… Этот шаблон нужен только раз — когда рождается архитектор, дальше после компакта я вызываю тот же скилл, но буфер то уже есть." It is the skill-graph's own cost rule applied to the pair: every loaded line is a recurring context cost, and a one-time need must not be paid for every session. The architect's earlier proposal — put the template in `architect-editor-engine` beside the memory's own definition — is overruled on that ground; the engine is loaded by both halves every session, and the editor never creates a buffer.
- **A document never cites the plan layer.** `.ai-factory/` is the perishable seam — the roadmap and its artifacts are pruned as they close — so a `docs/` file naming a task number, a phase number or an `.ai-factory/` path points at what will not be there. The global CLAUDE.md already forbids this of a code comment; nothing states it for a document, and a governing document is the worse place for it, since it outlives everything it would be citing. Evidence from a run still earns its place in a doc: tell what happened, never where the record of it is filed.
- **A task explains what an artifact must hold, never where to type it.** "Мы должны объяснять 'в таком-то классе нужны методы такого плана', не надо объяснять на какой строке что написать." The register, not just the addressing: name the artifact and what must be true of it. A line range is the visible form of the same error, and a heading substituted for a line range is only half the repair.

## The discriminator that decides a check

`test-philosophy`, applied one tier up. **Silent failure → check it. Loud failure → do not.** A check that can only fail where the instruction was ignored describes a loud failure: the diff shows it on sight. Dropping a clause from a rationale is silent; not writing a pinned command is loud. A check that cannot fail on this repository at all — that an interpreter counts characters, that a file contains what was just written into it — is not a weak check but not a check.

## Standing method rule

**The counts rule has left this buffer: it now lives in `docs/counts-go-stale.md`.** Contract / census / measurement, the one-sentence test, and the standing prohibition on reconciling two counts are all there, written by the user on 2026-09-22. That doc records the reason this drain matters — the same distinction was derived independently in three architects' private buffers and never converged, because no architect reads another's. Do not re-derive it here; cite the doc.

What stays, because the doc does not carry it: `§ N` addresses by position, which is a defect of the same family as `file:line` but about space, not quantity — phase 42 carries it, and `docs/reference-by-name.md` governs it.

**An always-loaded prohibition loses to a loaded procedure's instruction at the moment of writing, and this is not a habit to be disciplined out.** The global CLAUDE.md forbids a position address in any durable artifact; `roadmap-outline-deep`'s verbosity directive asks for "a `file:line` where a claim needs one"; and every phase note in this folder carries them, the newest written minutes after the pair had discussed the prohibition out loud. The hand was not careless and does not need reminding — it executed the procedure it was running. This is `docs/instruction-in-data.md` read in the other direction: what lives in the step executes, and what lives only in the always-loaded layer does not beat it. The prediction is checkable rather than arguable: repair the directive and the instances stop appearing, with no appeal to vigilance anywhere. Phase 42 owns the repair surface; do not treat a fresh instance as evidence of an undisciplined hand.

**In this repository the plan and the thing planned are the same material, so the boundary between them is invisible to the hand.** A decompose round drifted into applying its own target edit — the hand began rewriting `agent-architect`'s paragraph while writing the spec that describes rewriting it — caught it itself, reverted, and reported. Elsewhere the boundary is obvious: a spec is prose and the product is code. Here both are English in markdown, and nothing in the material announces which one is being written. So an order for a planning round says what it produces **and what it must not touch even though the spec describes touching it**, and a verification of a planning round checks that the target files are still clean, not only that the specs exist.

**A rule read wider than it governs manufactures work that looks like real work.** "Two channel-message formats, nothing else" governs what opens a round. Read as an account of everything the head may ever say, it generated three vehicle problems in one phase, each of which felt like a genuine finding, cost a round, and in one case removed a mechanism from the governing spec. The tell is that the difficulty is always about *permission to say something obvious*, never about the thing itself. When a task keeps stalling on where something is allowed to travel, suspect the rule's scope before designing the vehicle.

**A term belongs to the file that defines it, and a spec borrowing one inherits its meaning.** A spec about `editor.md` used that file's own phrase — "architect framing", defined there as *no pre-loaded findings, checklist or verdict* — to mean *the architect chose the topic*, two sentences from the definition. Read literally it licensed what the engine forbids, and it made the task larger than the defect. The registry's one-word-one-meaning rule does not reach a target file's local vocabulary, and this is the same failure without it: before reusing a word the target already uses, read what the target means by it, or say the thing in your own words.

The other direction is the one that catches you unawares: **a phase's own shorthand leaks toward the artifact.** Working through a phase you invent a compact name for the thing under discussion — it earns its place in the chat and in the orders, and then a spec writes it down as though the target file knew it. The target had a construction of its own and never needed a noun. The tell is that the term reads fluently to everyone who sat through the phase and appears nowhere in the file being changed.

**A removal has a blast radius and nothing computes it.** Everything named below is one shape: what leaned on the removed part is not reported when the part goes. Three instances in one day. A clause replaced in a contract line orphaned two references at once, because the clause held the only naming of what both reached for. A task narrowed dropped a repair its wider version owned — the reason for that repair had not gone away, only the half of the task that happened to carry it — and left a sibling spec pointing at reasoning that no longer existed. And an unread deferral says the same thing one tier up: a sentence is not a unit of claim. An addition does it too, in the other direction: naming a second thing where one was named breaks every reference that resolved by being the only candidate — a pronoun in the next sentence, a definite article three lines down. Neither end is caught by reading the change; both are caught only by reading the neighbourhood whole, after.

**A clean pass is a property of the questions asked, not of the task.** A walk over one task came back with nothing, and the defect was sitting in its landing text the whole time — the order had asked about scope and about geography and never about vocabulary. Reporting "nothing found" as though it described the task is the error; what it describes is the order. When a pass returns clean, the useful next thought is which question would have found something, not that there was nothing to find.

**A rule's home is decided by when it must be readable, never by what it is about.** 36.3 put "no architect reads another's buffer" into the skill that governs two architects working together — matching the rule to its subject. That skill loads only where the user has assigned a pairing role, and the rule holds wherever a second architect exists at all, pairing or none; this repository is in that state now, with three buffers on disk and no role declared. The rule would have been absent exactly where it applies. Before choosing a home, ask what must be resident at the moment the rule has to be obeyed — subject matter is the wrong axis and reads as the right one.

**A rescue's own repair is a specification change, and it can plant the next failure.** The first rescue of 36.1 corrected a rule that sorted buffer entries by category, replacing it with a test that sorts by what an entry is. The correction was right about the sorting and said nothing about where the rule would live — so the specification decided a test, granted the engine two characterisations, forbade the architect to restate anything, and left the pointer aiming at a rule no change list writes. The next run died on exactly that, twice over, and the second rescue removed the test the first one added. Repairing under the pressure of a failed run is when a new claim is least likely to be housed: before adding one, name the file that will hold it.

**Lifting a constraint means stating what it was protecting.** "Do not trim to stay under the ceiling" removed a number and left no criterion, and the line came back at 1511 code points doing the spec's job — obeyed exactly, and wrong. The number was never the point; the two-tier split was. The same shape appears in a guardrail drawn around a section instead of around a claim: a boundary named without its purpose is followed to somewhere neither of us wanted. Name the purpose and the hand steers by it; name only the edge and it optimizes for whatever is left.

**An edit to a governing spec reaches that phase's note in the same round.** The note states what diverges *now*, measured against the doc; move the doc and the note's measurements are taken from a line that no longer exists. The failure is not staleness but inversion — a note that said something was undocumented on both sides reports the opposite once one side documents it. Two skills read the note in full and unconditionally: decomposition before a phase's first task, and rescue when judging a failure against its second baseline. A rescue reading an inverted note diagnoses against a false baseline and prescribes the wrong repair, which is the most expensive mistake in the cycle. When scoping a round that touches a doc, the phase note is inside the boundary, never outside it.

## Measurements worth keeping

- **Where the pipeline fails:** across three months of run notifications, 91 of 94 failures happen before any code is written — 73 at plan review, 18 at review, 3 at implement. The run-event log seeded from that history is `orchestrator/metrics/runs.jsonl`.
- Rescue reports 02 and 03 diagnose **over**-specification; 04 and 05 diagnose **under**-verification and prescribe more of what 02/03 identified as the disease. Two architects reached the same wrong prescription independently — convergence here is a shared blind spot, not confirmation.
- **Seven tasks landed in a row with no rescue** after the specs were cut back to behaviour, against five rescues over the four tasks before. The sharpest point: a task that had burned six planning rounds and two rescues passed first time once its spec pinned a paragraph's text instead of describing its scope. The run does not separate the cut check lists from the three-part shape — one intervention through several channels.
- `tradeoxy_core`: 31 open tasks, **13** whose spec carries the counting register, 18 clean. In its eight most recent specs — Guards 17%, Tests 13%, Acceptance 5%. The cut is Guards + Acceptance, about 22%. **`## Tests` stays** — tests are a `test-philosophy` deliverable, not review instructions, and an unwary cleanup takes them first.
- **Ten tasks, no rescue, every one converging on its first implementation attempt** — phases 36–38, about 105 minutes of orchestrator time. The task that had twice failed at planning landed in ten minutes once a decision its spec made but housed nowhere was cut out.
- **Twelve tasks in `tradeoxy_core`, twelve first-try implementations, zero rescues** — and every round that was spent was spent at planning, none at implementation. The rounds track the **breadth of the spec's surface, not the difficulty of the change**: the longest, three plan-review rounds, was the task whose spec covered five surfaces plus a new file and a new decorator.
- **The cost was neither research nor execution.** Research ran in parallel fans — about 75 minutes of agent time inside 18 of the operator's; execution was an hour and a half. The expensive part was serial: keeping the plan layer true while the surfaces under it moved three times, fifteen stale points found in the last re-grounding pass alone. Every one of those rounds existed because something true had become false under an artifact nobody had reopened.
- **The clause that earned its keep:** research agents required to produce each case as a behaviour under a condition, with the arrangement and the doubles named. Twelve implementations, twelve first-try passes.
- **What looked load-bearing and was not:** a careful two-tier resolution of the stack and test command, whose resolved values were then used by nothing — every spec named its own suite path. And a printed handoff list that never ran and was never missed. The thing that actually carried was a demand nobody had listed: that research take a census of the repository rather than trust the spec — that is what dissolved a whole area before it became a task.

## The drift register — the deferred observations, disposed

The prune gate parked on 166 unpinned deferred observations across phases 20–40 (169 by the handoff's own tool, which misses a marker on the last line of a wrapped entry; task 25.1's three are already pinned). They collapse roughly three to one into distinct findings. This is their disposition in final form, written before the review files that carry them are pruned, so that nothing is lost with them. Each row: task key · target · gist. Every occurrence of a finding across that task's review files is pinned with the same marker.

**Dismissed — landed by phase 40, verified on the files, not on the plan:**
36.2 recovered-handle divergence → 40.2 · 36.5 "supersedes by name" referent → 40.7 · 36.6 buffer heading and opening clause → 40.1 · 37.1 `editor.md` relayed asymmetry → 40.3 · 37.1 echo rule absent from the skill → 40.5 · 37.1 formless notice under the ambiguity fallback → 40.4 · 37.1 closed-list claim in three more sites → 40.6 · 26.8–26.10 contract line reads `[ ]` at review time → self-resolving · 26.9 round-closing heading names the editor alone → 40.1/40.6 · 26.9/26.10 "the marker does" overreach → 40.6.

**Dismissed — moot, or informational by the reviewer's own words:**
26.1 meta.json fallback never recovers the buffer's path → a cost the user ruled acceptable · 26.3 echo rationale stated twice in one file, three occurrences, one of them unreadable to the handoff's tool → reviewer: defensible reinforcement · 40.6 spec 133's "directly after" geography → a spec of a `[x]` task, pruned with it · 20.2 `$TEST_CMD` resolves empty here → reviewer: not a defect · 25.2 dropped redundant sentence → reviewer: no behavioural loss · 28.1 `note` folder-style over two genres → informational · 34.4 the one plan without a subheading → harmless.

**Routed — every remaining finding now has a phase.** The review files carry `[routed → Phase N]` on every occurrence, and the phases hold the findings: 41, 42, 43, 47 and 49 took the ten that already fell under their subject, plus `<session-id>`'s derivation (49), a blocker's destination (43) and `agent-architect` as a phase-note reader (47); phases 50–55 were outlined for the rest, each with a note written from the entries' own text — `144` the docs catching up, `145` task-rescue and the protocol, `146` test-coverage's leftovers, `147` orphaned sentences, `148` the registry (gated), `149` the pairing engine (gated). Two more were dismissed on inspection: the `[x]` contract line's guard, pruned with its line, and spec 82's `$TEST_CMD` overstatement, which the live skill does not carry. The corrected scan over all 156 files reads zero; the gate will pass.

*Drain: the dismissed rows above stay — after prune, the reasons live nowhere else. Everything routed has reached its artifact and is gone from here.*

## Buffer ownership

`.ai-factory/notes/` in this repository holds only architect buffers, one per session; `05-` and `06-` belong to the other architect and are never edited from here.
