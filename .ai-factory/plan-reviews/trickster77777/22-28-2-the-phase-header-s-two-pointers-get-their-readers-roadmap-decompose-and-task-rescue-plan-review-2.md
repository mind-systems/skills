## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/22-28-2-the-phase-header-s-two-pointers-get-their-readers-roadmap-decompose-and-task-rescue.md`
**Task spec:** `.ai-factory/specs/trickster77777/104-phase-note-readers.md`
**Files targeted:** 2 (`src/skills/roadmap-decompose/SKILL.md`, `src/skills/task-rescue/SKILL.md`)
**Risk Level:** 🟢 Low — every issue raised in plan-review 1 is closed, every anchor in both target files and in `roadmap-engine` re-verifies exactly, the `task-rescue` sweep is complete with `:227` deliberately excluded and justified. One sentence in the last `task-rescue` step directs an edit outside the task's declared file boundary and has to be reworded before implementation.

### Context Gates

- **Architecture** (`.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy", § "Skill anatomy") — PASS with one WARN. Both edited skills are policy skills gaining a read instruction; the pointer format itself stays in `roadmap-outline-deep` (`:104-108` the `Phase note:` bullet, `:114-115` the header order), its only holder, and the readers match the literal token rather than loading a skill — so "no `loads:` edge; reading a file named on a phase header is a read, not a skill invocation" is right under the ≥2-callers extraction rule. WARN on § "Skill anatomy" (`SKILL.md` ≤ 500 lines): `src/skills/task-rescue/SKILL.md` is 569 lines before this task and the plan adds to four sites in it. Pre-existing and outside this cut — carried under Deferred observations, not a blocker.
- **Rules** — WARN. `.ai-factory/RULES.md` is absent and `.ai-factory/skill-context/aif-review/SKILL.md` is absent (no `skill-context/` directory at all). Nothing project-specific to check beyond the root and global CLAUDE.md, whose relevant rules — protocol tokens byte-exact, one home per fact, docs → roadmap → code — the plan respects.
- **Roadmap** — PASS. `.ai-factory/roadmaps/trickster77777.md:124` carries 28.2 with `Spec: .ai-factory/specs/trickster77777/104-phase-note-readers.md`, matching the plan. Its predecessor is satisfied: `:122` has 28.1 `[x]`, and `Phase note:` exists as a defined token in `src/skills/roadmap-outline-deep/SKILL.md:104-108` with `roadmap-prune`'s capture at `:272-286`, so there is a token to read. The plan's cut matches the spec's Guards — `roadmap-outline-deep`, `note`, `roadmap-engine`, `roadmap-outline`, `roadmap-prune` untouched, no `loads:` edge, no `active/` symlink change. Working tree is clean on `src/`.
- **Governing spec** — Phase 28's header (`:118-120`) carries no `Governing spec:` line and no `Phase note:` pointer, so the governing surface is `docs/sakshi-harness/skill-cycle.md`, committed ahead of the code per docs → roadmap → code. `:17` makes the ТЗ mandatory reading for "`roadmap-decompose` при атомизации … rescue при разборе"; `:21` extends it to the note: "Note читают дальше все, кто читает `Governing spec:`: `roadmap-decompose`, `task-rescue`, планнер и ревьюер orchestrator." The plan conforms for the two in-repo readers and correctly leaves the orchestrator's planner and reviewer to the sibling repo. `:21`'s separation of the two pointers (the ТЗ speaks in the present tense as if the behavior existed; the note names the divergence) is carried into every step's wording, kept apart and never merged.

**Anchors re-verified against the files, all exact.** `task-rescue/SKILL.md` — `:58` the step title "**Read the phase's governing spec.**", `:60` "the phase header and its intro lines", `:61`/`:63` the `Governing spec:` read and its exit, `:63-64` the trailing additive-to-Step-4 sentence, `:143-148` the Step 3 judgment with the report duty on `:147-148` and the root-cause/repair-target distinction on `:145-146`, `:150-151` the depth-choice carry, `:227` the escalation option, `:355-357` depth-spec item 1 with the no-wholesale-copy clause, `:539-543` the `## What NOT to do` bullet with `Governing spec:` on `:541` and "never suspicion-gated" on `:543`. `roadmap-decompose/SKILL.md` — hook (a) is `:26-44` exactly (`:24` the parent heading, `:45` blank), the numbering/flat-fallback paragraph ends `:38`, "Two parity carry-overs" opens `:40`, hook (d) "Decompose existing" is `:73`. `roadmap-engine/SKILL.md` — `:152-154` the Create/Update branch on `$TARGET_FILE` existence, `:188-189` the create-mode draft applying hook (a) per entry, `:241-242` **Add**, `:248-250` **Rewrite** re-running the create-mode draft cycle over the existing `$TARGET_FILE`.

**Plan-review 1's five issues are all closed.** The enumeration now names Rewrite/Add/hook (d) and explicitly excludes first-run create with its ground-truth reason; the unit is stated mode-independently, matching the contract line's "in every mode". Step 3's consequence is decided in the plan (report clause + routing effect) instead of left to the implementer. The absent-file finding has both destinations named in `task-rescue`. The hook (a) range is corrected to `:26-44`. The byte-exactness claim is re-cited to the protocol-token rule with `:104-108` scoped to `Phase note:` alone.

**The `task-rescue` sweep is complete.** A case-insensitive scan for `governing|phase note|preamble|phase header|phase's` over the whole file returns `:58`, `:60`, `:61`, `:63`, `:143`, `:146`, `:147`, `:227`, `:356`, `:541`, `:543` — every one is inside a plan step except `:227`, which the plan excludes with its reasoning stated. There is no step-title index or overview list elsewhere in the file that repeats `:58`'s bolded title, so widening it lands nowhere else. `Phase note:` currently appears 0 times in both target files, so the plan's "at least 1 / at least 2" counts are pure additions and cannot be satisfied by accident.

### Issues

**1. The `:227` step directs an edit outside the task's declared file boundary — plan step "Leave `:227` alone, deliberately".** *Blocking, one sentence.*

The step's `Files:` line names `src/skills/task-rescue/SKILL.md`, and its whole point is that nothing is edited there. It then says: "Record this in the task spec's reasoning so a later sweep of this file has nothing left to find (26.9/26.10 each landed one instance short); make no edit here."

Read literally, "the task spec" is `.ai-factory/specs/trickster77777/104-phase-note-readers.md` — the ratified upstream contract this run executes against. An implementer that follows it appends a paragraph to that file, and three things go wrong at once: the spec's own § "Files & types" pins exactly two edit targets and neither is a spec file; the change runs docs → roadmap → code backwards, with the implementer amending the artifact its work is judged against (amending a task spec is `task-rescue`'s job at spec depth, not the implementer's); and the plan's own negative check — `git diff HEAD --stat -- src/ active/ docs/ CLAUDE.md` — scopes to `src/ active/ docs/ CLAUDE.md`, so an edit under `.ai-factory/` passes verification silently.

The reasoning the sentence wants recorded is already recorded — in this plan step, which is where a run's reasoning belongs. Reword to state that plainly ("this reasoning is recorded here in the plan; this step produces no edit to any file") and the step becomes what its title says it is.

### Positive Notes

- **The unit problem is solved at the source and stated mode-independently.** Hook (a) is the shape the engine applies to *every* entry (`roadmap-engine:189`, `:242`), so an unqualified "read the preamble" placed there becomes a per-entry re-read. The plan makes the paragraph state its own unit — once per phase, at that phase's first drafted entry, in whatever mode the draft runs, never re-run for later entries — as its own step with its own dependency rather than a parenthetical.
- **The exclusion of first-run create mode is correct and now grounded twice.** `roadmap-engine:152-154` enters create mode only when `$TARGET_FILE` does not exist, and `roadmap-decompose:35-38` forbids inventing a phase header, so no preamble can exist there; the flat-fallback exit covers it without contradiction. This is a deliberate divergence from the task spec's § "The change" item 1, which names "create mode" among the sites — the plan follows ground truth over the spec's enumeration and says why, and the spec's own § "Verification" asks only for the unit and the "Decompose existing" action, both delivered. The contract line's "in every mode" is honored by the mode-independent phrasing rather than by an enumeration that would have to lie.
- **Every bare line reference resolves uniquely despite the mixed files.** In the site enumeration, `:241-242` and `:248-250` are out of range for the 97-line `roadmap-decompose/SKILL.md` and follow an explicit `roadmap-engine/SKILL.md` citation; `:73` and `:35-38` are in range for the step's own `Files:` target and match its content (hook (d) exists only in `roadmap-decompose`; `roadmap-engine`'s own flat-fallback rule sits at `:119-120`, not `:35-38`). Each site is additionally named in prose, so no reader has to resolve a number to act.
- **Step 3's decided consequence is the right shape and mirrors ratified precedent.** Extending the Diagnosis Report duty at `:147-148` makes the note's participation observable after the fact — the same test the governing-spec read already has to pass — and pinning the routing effect at `:150-151` mirrors the root-cause/repair-target distinction already ratified at `:145-146`, rather than inventing a new classification. The report's "Domain language only" constraint at `:183-185` ("no phase names") does not collide: the new clause names a finding as already-recorded divergence without naming a phase, exactly as the existing governing-spec clause quotes a clause without naming one.
- **`Governing spec:` is protected at both `task-rescue` sites explicitly** — "the second pointer joins it, never replaces it" — and the verification re-checks both, which is the failure mode a widen-in-place edit invites most.
- **The existing exit survives.** `:63`'s "no phase, or neither pointer named → proceed as today" is kept word-for-word in substance, and the load-bearing trailing sentence at `:63-64` (the read is additive to Step 4's own `$TARGET_FILE` resolution) is called out as staying — easy to lose in a rewrite of that paragraph.
- **Verification refuses line-oriented `grep`** and demands a whitespace-normalized read, with the per-site checks done by reading rather than by count — the right instrument, since `:356` and `:541` both straddle line breaks. The negative half is concrete and satisfiable: the working tree is clean on `src/ active/ docs/ CLAUDE.md` today, and the five untouched skills are named.

## Deferred observations

- Affects: a future task on `src/skills/task-rescue/` (outside this task's boundary — the spec's § "Files & types" pins two files and the plan's verification requires the diff to list nothing else) — `src/skills/task-rescue/SKILL.md` is 569 lines against the ≤ 500-line body constraint in `.ai-factory/ARCHITECTURE.md` § "Skill anatomy" and the repo CLAUDE.md § "Key constraints", and this task adds to four sites in it. The remedy is structural — moving the depth procedures or the `## What NOT to do` inventory into `references/` — which a widening edit cannot carry. Every future widening inherits the same tension, so the split is worth its own task.
- Affects: a future task on `roadmap-outline` / `roadmap-engine` (both excluded by this task's Guards) — carried forward from 28.1 and now sharper, because this plan names **Rewrite** as a site where the read fires: `roadmap-engine:248-250` re-drafts an existing `$TARGET_FILE` and "replac[es] its contents on confirmation", and nothing anywhere preserves a `Phase note:` pointer or a `Governing spec:` line across that rewrite. After this task the pointer has readers that depend on it being there; a rewrite that silently drops it leaves those readers seeing a phase with no note and `roadmap-prune:281-286` capturing nothing for a file that still exists. The writer-side preservation rule still has no owner.
- Affects: the `orchestrator/` repo's roadmap (a sibling repository — outside this repo's cut entirely) — `docs/sakshi-harness/skill-cycle.md:17` and `:21` name four readers of the phase header's two pointers: `roadmap-decompose`, `task-rescue`, and the orchestrator's planner and reviewer. This task gives the first two theirs; the orchestrator half is committed doc with no task behind it anywhere in `.ai-factory/roadmaps/trickster77777.md`. Until it lands, a phase note is read at planning and rescue time but not by the two agents that execute the task against the code.
- Affects: a future task on `src/skills/agent-architect/` (not named in this task's contract line or Guards) — `skill-cycle.md:17` makes the ТЗ mandatory reading for "архитектора при проходе", but `src/skills/agent-architect/SKILL.md` mentions neither `Governing spec:` nor the phase header at all. `:21` does not list the architect among the note's readers, so only the governing-spec half is committed doc without a reader here; it is the one in-repo reader the phase's governing surface names that this task leaves unserved.
