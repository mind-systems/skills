## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/21-28-1-roadmap-outline-deep-…md`
**Task spec:** `.ai-factory/specs/trickster77777/102-roadmap-outline-deep.md`
**Files targeted:** 4 (`src/skills/roadmap-outline-deep/SKILL.md`, `active/skills/roadmap-outline-deep`, `CLAUDE.md`, `src/skills/roadmap-prune/SKILL.md`)
**Risk Level:** 🟢 Low — both round-1 issues are closed at the source and every anchor re-verifies; one aggregate count inside the new verification text contradicts the inventory it heads.

### Context Gates

- **Architecture** (`.ai-factory/ARCHITECTURE.md:30-39`, "Composition: mechanism vs policy") — PASS. `roadmap-outline-deep` is a policy skill with content of its own (the ~200–500 preamble budget, the `Phase note:` pointer form, the re-run rule); it loads `roadmap-engine` and `note` instead of inlining them, declares the edge caller-side only, and both engines already carry their reverse-graph markers (`src/skills/note/SKILL.md:20-22`, `src/skills/roadmap-engine/SKILL.md:21-23`). Keeping the pointer format inside its single caller matches the ≥2-callers extraction rule.
- **Rules** — WARN, `.ai-factory/RULES.md` absent; `.ai-factory/skill-context/aif-review/SKILL.md` absent. Nothing project-specific to check against.
- **Roadmap** — PASS. `.ai-factory/roadmaps/trickster77777.md:122` carries 28.1 with `Spec: .ai-factory/specs/trickster77777/102-roadmap-outline-deep.md`; the plan matches the contract line and the spec point for point, including the cut (`note`, `roadmap-engine`, `roadmap-outline`, `task-rescue` untouched — `task-rescue` is 28.2's) and the "already carrying `Phase note:` is rewritten in place" clause.
- **Governing spec** — Phase 28's header (`:118`) carries no `Governing spec:` line, so the governing surface is the already-committed `docs/sakshi-harness/skill-cycle.md`. The plan conforms: the pass runs after `aif-docs`, the note states what diverges now with pointers into `docs/`, an absent document is recorded as a governing-spec hole owned by `aif-docs`, and `docs/` is not written.

**Round-1 issues — both closed.**
1. The `CLAUDE.md` count check is now site-by-site (`:74` and `:189` each read individually), the pre-existing `:33` occurrence is named as such with the "not a third list" carve-out, and the count is raised to ≥3. Re-verified: `grep -n roadmap-outline-deep CLAUDE.md` returns exactly `:33` today.
2. `:272` ("Tags are captured before any ROADMAP.md line is deleted") and Step 6's `:328` ("only after Step 5's tag capture has run") are both in scope for the same edit, with the reason (the phase-note capture leans on the ordering harder, since Step 6 deletes the very surface Step 5 reads) stated where the implementer needs it.

**Anchors re-verified against the files** (all correct): `src/skills/roadmap-decompose-skeleton/SKILL.md:1-14` field order — `name`, `description` (`>-`), `argument-hint` (bracket-quoted), `disable-model-invocation: true`, `allowed-tools: Read Write Edit Glob Grep AskUserQuestion Skill`, `loads:` — the exact shape the plan pins; `src/skills/roadmap-outline/SKILL.md:40-41` (plain markdown links legal in intro/preamble prose) and `:22-24` (the three prohibitions, none reaching an inline link); `src/skills/note/SKILL.md:30-36` (the three caller hooks) and `:52-58` (Step 3 mints only a new numbered file — the re-run rule is genuinely load-bearing); `src/skills/roadmap-engine/SKILL.md:69-75` (Spec destination through `note`'s hook, per-directory numbering); `src/skills/roadmap-prune/SKILL.md:272`, `:304` (item 3), `:306`, `:328`, `:396-399`, `:431`, and its `Bash(rm *)` grant at `:10` — so the token-not-position keying argument is real. `active/skills/` siblings all use `../../src/skills/<name>`; `AGENTS.md` is a symlink to `CLAUDE.md`, so the two enumerations are the complete edit surface and no third file mirrors them. `git status` shows `docs/` clean, so the `git diff HEAD -- docs/` check is satisfiable as written.

### Critical Issues

**1. "Seven sites" contradicts the eight sites the same sentence enumerates (`roadmap-prune` verification).**
The plan's prune verification reads: *"Site by site: `:272` heads Step 5 …; Step 5 item 1 captures the token; Step 5 item 3 deletes the captured note paths; Step 6's `:328` back-reference names the captured tokens, and its emptied-phase sweep names the preamble as the captured surface; `:306` and `:431` each name **both** tokens; `:396-399` names both deleted classes. **Seven sites**, each read and confirmed individually."*
Counting the locations actually named: `:272`, Step 5 item 1, Step 5 item 3, `:328`, the emptied-phase sweep prose, `:306`, `:431`, `:396-399` — **eight**. The number matches no reading of the list (semicolon groups give six; named locations give eight). It is a leftover from the pre-revision list, before round 1 added `:272` and `:328`.
Why it matters here specifically: this is the one paragraph in the plan that forbids an aggregate count for this file (*"check each edit site enumerated below by inventory, not by a single aggregate count … a bare token count passes with the widenings skipped"*) and then closes with an aggregate count that is short by one. An implementer confirming "seven sites read" is done before the eighth, and the sites most likely to be folded away are exactly the two round 1 had to force in — `:272` and the sweep prose, both of which read as riders on neighbouring clauses.
Fix inside this task: state eight, or drop the number and let the inventory stand alone — the enumeration itself is complete and correct.

### Positive Notes

- The re-run rule remains the plan's strongest element, and revision 2 left it intact: derived from ground truth (`note` mints only new numbered files), naming the exact defect it prevents (a superseded note orphaned the moment the pointer is repointed, invisible to a prune that captures only the current pointer), closed on both sides — workflow branch plus a matching `## Critical Rules` prohibition — and verified by reading rather than by count.
- The `CLAUDE.md` fix is done the right way round: it does not merely raise the count, it names the pre-existing `:33` occurrence, explains why a count is the wrong instrument there, and pins `:33` as prose that stays untouched. The plan applies to itself the diagnosis it makes for prune's Step 8.
- The `:272`/`:328` widening is scoped precisely — "change nothing about what either step does; this is wording that now under-describes the step it heads" — which is what keeps a wording fix from turning into a behavior edit in a file holding `Bash(rm *)`.
- The capture keys on the literal `Phase note:` token and never on a link's position, with the reason grounded in both files: `roadmap-outline:40-41` genuinely permits unrelated handoff/task-spec links in the same prose, and `roadmap-prune:10` genuinely holds `Bash(rm *)`.
- The existing prune carve-outs are carried over by reference rather than restated in new words — a phase keeping its header is never emptied, the always-retained last phase header contributes nothing, a preamble with no token contributes nothing — so the new capture inherits them instead of forking a second set of rules.
- The verification method (whitespace-normalized read, `**` and backticks stripped from both sides, never a line-oriented `grep`) is load-bearing, not ceremony: the negative span at `:431` straddles a line break and every quoted sentence carries code-span backticks. Confirmed present exactly twice pre-edit, so the before/after negative check is non-vacuous.

## Deferred observations

- Affects: a future task on `roadmap-outline` / `roadmap-engine` (outside this task's cut — the spec's Guards exclude both) — nothing preserves a `Phase note:` pointer once it exists. `roadmap-engine`'s update mode offers a rewrite that replaces a section's contents on confirmation, and `roadmap-outline` drives that flow over the same phase headers and intros this pass rewrites. A rewrite that drops the pointer silently orphans the note: `roadmap-prune` keys on the literal token and, finding none, captures nothing, so the file outlives its phase in a directory nothing sweeps — the file-with-no-owner defect this task exists to remove, re-entering through a different door. 28.2 gives the token its readers but no writer-side preservation rule.
- Affects: the planning side / `docs/sakshi-harness/skill-graph.md` (this task must not touch `docs/`; its verification requires `git diff HEAD -- docs/` to stay empty) — § "Воронка в один дистиллятор" enumerates `note`'s callers and names only `command-handoff` as a direct one. After 28.1 there is a second direct caller, `roadmap-outline-deep`, which passes its own destination, template and verbosity hooks without going through `roadmap-engine`; the § "Домены — по стволам" claim that domain boundaries derive from the `loads:` graph makes the omission visible on the next derivation. `skill-cycle.md` was updated ahead of this task per docs → roadmap → code; `skill-graph.md` was not, and the fix belongs on that same planning side.
