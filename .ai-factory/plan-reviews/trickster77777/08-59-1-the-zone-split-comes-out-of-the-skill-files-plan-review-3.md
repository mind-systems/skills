# Plan review — 59.1 the zone split comes out of the skill files

## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/08-59-1-the-zone-split-comes-out-of-the-skill-files.md`
**Targets:** `src/skills/architect-editor-engine/SKILL.md` (frontmatter `description:` + § "The architect's buffer"), `src/skills/agent-architect/SKILL.md` (two pointer sentences), `src/agents/editor.md` (opening paragraph)
**Risk Level:** 🟢 Low

Three prose edits, every replacement pinned word for word in spec `164-…md` § "What must be true after". This is the third review of this plan. Review 1's three findings and review 2's single finding are all closed at the root: the falsified count is gone, no plan blockquote carries any emphasis character at all, the arithmetic claims are replaced by the decisions they supported, and the Context enumeration of sweep survivors is replaced by the spec's own category rule — with the verification task now explicitly refusing a list comparison ("Do not expect the run to match any list, this plan's included"). Every pinned text was re-compared against the spec programmatically, every claim about the target files was re-measured against the files, and all three edits were **simulated in full** and the spec's sweep re-run against the simulated result. No finding survives.

### Ground truth verified against the files

- **Every pinned text is a copy of the spec's, not a paraphrase.** All seven blockquotes in the plan normalise to exact substrings of the spec's § "What must be true after" — never of § "What is true now". The frontmatter block matches the spec **line for line** (5 lines including the unchanged `the definition of the` anchor, all 5 byte-identical). The engine's opening paragraph, the head-is-only-writer paragraph, the re-read paragraph, the channel-message paragraph, `agent-architect`'s pointer sentence, and `editor.md`'s replacement passage all match end to end. The two inline-quoted pinned fragments — `agent-architect`'s "it is where your buffer's path and rules are defined, so it is" and the engine's closing sentence without ", settled zone included" — are likewise exact against the "after" section.
- **No emphasis anywhere in the plan's quoted replacements.** A scan for `*` and `_` across every blockquote line of the plan returns nothing. Review 1's finding 2 stays closed, and the Context now carries the reason as a standing rule for the whole task.
- **The "before" text the plan tells the implementer to match is what stands in the files today.** Engine `description:` lines 9–12 carry exactly the clause quoted; the five engine body paragraphs (lines 37, 39, 41, 43, 47) match the spec's "what is true now" byte for byte; the drain-rule paragraph (line 45) is already in its post-edit form and is correctly left untouched; `agent-architect` line 36 and lines 368–371 carry both pointer texts verbatim; `editor.md` lines 21 and 26 carry both "settled zone" uses. Each locate-by-text anchor is unique in its file, so "never by line number" has a determinate referent.
- **The 810-character claim is exact, and the YAML survives.** The current folded `description:` is 804 characters; applying the pinned replacement yields exactly 810 against the 1024 limit. The four replacement lines at the field's 2-space continuation indent measure 79/75/77/76 — inside the 66–79 range the field's existing lines already occupy — so "take the replacement's line breaks as given" is right. No line is more-indented than the block, so none is read as a literal line inside the `>-` scalar.
- **All three edits simulated; the mechanical expectation is met exactly.** Applying the plan's instructions to the three files and re-running `grep -rniE "settled zone|live zone|two zones|the split|zone"` against the results returns **zero hits in all three files**. The rewritten § "The architect's buffer" contains **zero** `*` characters, satisfying the plan's markup criterion. `", settled zone included"` is gone. All three cited headings survive byte-identical.
- **The editor.md rewrite is exactly the two changes the plan names.** A character-level diff of the replaced half against the file's current text yields precisely two opcodes: `whose settled zone you hold` → `which you hold in full`, and `settled zone` → `memory`. The paragraph's first two sentences — the ones the plan says stay word-identical, bold included — are confirmed untouched by the same diff.
- **Both cited headings resolve, and a third is correctly protected.** `## The architect's buffer` is cited by name from § "The mode rule" in the same file (line 33); `## Your buffer is shared; you alone write it` is cited by name twice in `agent-architect` (lines 150, 277); `## Spawn once, message thereafter`, unchanged by this task, is cited five times (lines 23, 197, 278, 285, 347). All stay byte-identical.
- **The three sites are the whole set — verified by meaning, not only by the sweep.** Sweeps for `private`, `architect alone`, `may not read`, `carve`, `withhold`, `part of the buffer` across `src/` and `docs/` turn up no fourth place where the buffer is described as partly closed to the editor. Checked the one file neither prior review named: `src/skills/architect-pairing-engine/SKILL.md` mentions `architect-editor-engine` only as a reverse-graph example and carries no buffer or zone content — not a fourth site. `agent-architect`'s own frontmatter and `editor.md`'s frontmatter carry no zone language, matching the plan's "frontmatter untouched". `agent-architect`'s independence passage (line 309) rests on "where the round is **settled** is what counts, not who reads it" — a property of the message, never of a private buffer region — so the split's removal leaves it standing.
- **The sweep's surviving hits are all genuinely unrelated, and the plan correctly refuses to list them.** Run now, the six non-target survivors are `command-pin-gaps.md:42` (a task's own scope-zone), `aif-docs/references/split-state-b.md:46` (executing a state-file split), `docs/paired-loop.md:67` (§ "Where the split falls" — the factual/judgment split), `docs/skill-description-field.md:13` (the zone of a skill), and two `ui-ux-pro-max` CSVs. Each is a false positive by the spec's category rule. The plan's four named categories are categories, not file names, and each has a real referent in that set.
- **The named in-file ordinary uses have real referents.** `agent-architect` carries "the live task registry" (119), "Leaving both buffers live" (164), and "where the round is settled is what counts" (309) — all three named by the plan, adapted in article and case to fit its own sentence rather than presented as pinned text (the plan's pinned texts are all blockquotes). None matches the sweep pattern, so none can surface as a candidate for over-edit; the rule the plan states first governs the file's other ordinary uses of "live" (lines 96, 167, 171) identically.
- **The governing text says what the plan says it says.** `docs/paired-loop.md` § "What the memory holds, and who holds it" opens with the two surviving rules verbatim and carries the same content list the engine's replacement keeps. Code is being conformed to the doc; the direction is docs → code, and `Docs: no` is correct precisely because the doc already stands ahead of the code.
- **The three symlinks exist** and all point into `src/` (`active/skills/architect-editor-engine`, `active/skills/agent-architect`, `active/agents/editor.md`), so editing `src/` is the whole job exactly as stated. No `loads:` edge changes.
- **The CLAUDE.md staleness claim holds.** `docs/paired-loop.md` carries no zone language today, so the documentation-table row (CLAUDE.md line 40) summarising it as "the buffer's two zones … why the live zone stays private" is already stale before this task runs. Correctly flagged, correctly not acted on.
- **No collision with any pending task.** 59.2 appends at the *end* of the same § "Spawn once, message thereafter" paragraph and its contract line says so ("Touch only the paragraph's end — 59.1 owns its mid-paragraph clause"), which is exactly what the plan's no-rewrap decision protects. 60.1 replaces the dead-editor passage further down that section — a different paragraph. The re-wrap the plan *does* order is in § "Your buffer is shared; you alone write it", which no remaining `[ ]` line (59.2, 60.1, 61.1, 61.2) touches.
- **Settings are right.** `Testing: no` — skill-body prose has no silently-failing surface per `test-philosophy`.

### Verification apparatus

The single verification task now covers every blind spot the prior rounds named. It reads each edited passage back against the spec word for word including the `description:` field (the positive half the sweep cannot substitute for), then looks at markup *deliberately* with the reason stated ("a word-for-word read-back passes markup silently"), then re-runs the sweep and classifies survivors by the rule rather than against any list, then singles out the one item most likely to be left half-done (the closing paragraph's ", settled zone included"), then confirms the three cited headings survived byte-identical. Every one of these checks was executed here against the simulated result and passes.

### Context Gates

- **Architecture** — OK. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is honoured: the buffer's definition stays in the engine (mechanism), the architect keeps a pointer that restates none of it, the editor's copy is the engine-held contract rather than a second home for the rule, and no `loads:` edge changes. Phase 59's note draws the same boundary — the engine keeps what the memory is and who writes it — and the plan stays inside it. The always-loaded layer is respected: the engine's `description:` is rewritten to read as a definition, not as a note about a change.
- **Rules** — WARN (non-blocking). Neither `.ai-factory/RULES.md` nor `.ai-factory/skill-context/aif-review/SKILL.md` exists in this repository, so no project convention file could be applied.
- **Roadmap** — OK. 59.1 is the seam: the first `[ ]` line in `.ai-factory/roadmaps/trickster77777.md` file order (line 47). Linkage walked to the leaf: contract line 59.1 → `Spec:` `164-…md` → phase note `153-the-buffer-is-born-with-a-shape.md` → phase header `Governing spec: docs/paired-loop.md` § "What the memory holds, and who holds it" → the three target spans. Every clause of the contract line — the three files, the two surviving rules, `.ai-factory/notes/07-architect-buffer.md` out of scope — is honoured by the plan and covered by its verification.

### Findings

None.

### Positive Notes

- **Every prior finding is closed at the class, not the instance.** The count is gone rather than corrected, so it cannot go stale again; the emphasis is stripped from the quotes *and* the reason is hoisted into Context as a standing rule for the whole task; both width claims are replaced by the decision they were supporting; and the sweep enumeration is replaced by the category rule the spec itself adopted — with the verification task now instructed that "a survivor this plan never mentioned is an ordinary outcome, not a sign the sweep was run wrong". That last move removes the trap rather than fixing its contents, which is why the same finding cannot return in a fourth round.
- The plan states plainly what the replacement **drops and does not relocate** — the live zone's contents and the echo argument — and names where the one piece that still needs a home (the handle's timing) already lives. That closes the failure mode where a removed rule quietly reappears under a new name, which is exactly what the spec forbids with "No file gains a replacement boundary of any kind".
- Treating the three edits as one deliverable, with the reason stated ("a buffer defined without zones in the engine while the architect skill still points at 'its two zones' leaves the pair reading two contradictory contracts at birth"), makes a half-landed change unable to read as done.
- Both `agent-architect` edits stay pointers, and the plan says why the pointer must not restate the surviving rule — the writer half is already the next sentence of that same section. Mechanism/policy stays where ARCHITECTURE.md puts it.
- The two opposite wrapping instructions are each justified by what they buy rather than by a number: "take the replacement's line breaks as given" in the folded YAML scalar, "a slightly short line here is correct" where 59.2's span must stay disjoint, and "reading the neighbouring lines for it rather than counting to a number" where a re-wrap is genuinely wanted. Leaving wrap points to the implementer while pinning the words is the right division for prose edits.
- Scope boundaries are verified rather than asserted: the live buffer note, the false-positive category, the file's own ordinary uses of "live" and "settled", and the stale CLAUDE.md row are each named with the reason they are not this task's — and the CLAUDE.md row is raised for the architect rather than silently fixed.

## Deferred observations
- Affects: `skills/CLAUDE.md` documentation table (row for `docs/paired-loop.md`) — the row still summarises the doc as "the buffer's two zones and who holds which, why the live zone stays private". It is stale against its own target already today, since `docs/paired-loop.md` carries no zone language, and this task neither creates nor worsens it; after 59.1 lands it will be the last place in the repository outside `.ai-factory/` history where the zone split is still asserted as current. The row belongs to the documentation index, not to any of the three skill files, so correcting it needs a task that owns that file.
- Affects: Phase 59 / `.ai-factory/specs/trickster77777/165-the-buffer-is-seeded-from-a-template-at-founding.md` — the engine's post-edit content sentence ("It holds the working discipline, the rulings …, the method …, and the register of where the skills still lag the practice") no longer names the editor's handle or an assigned pairing role, both of which `agent-architect` still mandates writing into the buffer (line 96 "of which live in the buffer", and § "Your buffer is shared" naming the handle, the pairing role, and the deferral entries). Under the old text they were named as live-zone contents; now a reader who takes the sentence as exhaustive could conclude they do not belong in the buffer at all. The wording is pinned by the spec and lifted verbatim from the governing doc, so it is not this task's to adjust — and 59.2, which seeds the buffer's rubrics from a template and whose phase note draws the boundary as "the template instantiates the memory, the engine keeps what the memory is", is where the buffer's actual contents get stated. The question has a natural home one task away.

PLAN_REVIEW_PASS
