# Plan review — 58.2 the snapshot itself, not only its digest, is never delegated to the editor

## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/06-58-2-the-snapshot-itself-not-only-its-digest-is-never-delegated-to-the-editor.md`
**Target:** `src/skills/agent-architect/SKILL.md` (two sentences in two sections)
**Risk Level:** 🟢 Low

This is the first reading of the plan as repaired by rescue report `09-58-2-an-invariant-that-counted-lines-across-its-own-rewrap.md`. The three deleted readings all faulted the checking apparatus and never the change; each of their subjects was re-checked here against the files.

### What the rescue repaired, re-checked against the plan

- **No count is asserted anywhere.** Both sweep tasks state the rule and say in their own words that the count is not the test; the first one classifies hits by a question ("does this file quote the digest passage as ground truth for a standing claim about the present?") and routes an unclassified hit to stop-and-report rather than to an edit.
- **No numbered line is predicted.** The two line hints (72, 270) are both correct as the file stands, and each is immediately disclaimed — "locate it by matching that text, never by the number", "again, match on the text". Neither is a state the edits change before it is used: task 1 uses 72 before any edit, task 2 matches on text.
- **No column figure anywhere.** Both wrap instructions read "match the width the surrounding paragraph already uses". Measured, the digest paragraph's body lines run 63–77 columns and the channel paragraph's 68–76, so that instruction has a determinate referent and needs no number.
- **The positive half of verification exists and is named as such.** The comparison task precedes both sweeps in dependency order, states the comparison correctly for a hard-wrapped destination ("words and order only, newlines collapsed to spaces"), and says plainly why the sweeps cannot substitute for it — "they prove survival and scope, both of which a paraphrase of either appended clause would also satisfy".
- **The markup hazard the second deleted reading found is now covered.** The comparison task carries a dedicated entry for it: `REPORT-ONLY` and `APPLY-EDIT` stay backticked and `never` stays bold, with the reason spelled out — the word-and-order comparison is blind to markup by construction and the spec's blockquote renders both tokens bare. Checked: the spec's pinned block does drop the backticks and does keep `**never**`, so the hazard is real and the check is the right shape ("the spec is the authority on the words, the file on the markup").

### Ground truth re-verified against the files

- The file is 377 lines. Both target sentences are byte-identical to what the plan quotes, newlines collapsed: the digest sentence ends line 72 in § "Spawn once, message thereafter" (§ header line 30), and the channel-contents sentence runs lines 270–274 in § "Relay on the marker; author the apply work-order and your own legwork" (§ header line 175), ending the paragraph on `an analysis target.` — a 19-column last line, exactly as the plan states.
- Both replacement texts in the plan are character-identical to the spec's two blockquotes in `160-…md` § "What must be true after" (checked programmatically, newlines collapsed). Typography matches the destination: ASCII apostrophes, U+2014 em dashes, ASCII double quotes — the same forms the file already uses.
- The cross-reference style is the file's own: `(see "Nothing closes a round before the report on it exists")` stands at lines 265–266 and `(see "Spawn once, message thereafter")` at line 269, both in the same paragraph the second edit lands in. The heading `## Spawn once, message thereafter` exists, so the new reference resolves by name, not by position.
- The appended clause introduces a second `(see "Spawn once, message thereafter")` about four lines below the one already at line 269. That is expected, not a defect to reconcile: the wording is pinned by the spec and the plan has no authority to trim it, and the two references sit in different sentences doing different work — one names where the buffer's path is handed over, the other where the snapshot prohibition lives.
- Sweep 1 run now returns exactly six paths — the target, the roadmap, spec `160-`, phase note `152-`, this task's plan, and sibling plan `05-`. Every one falls under a class the plan names, with nothing left over; `.ai-factory/handoffs/` returns nothing, confirming spec `160-`'s own correction of the earlier `26-`/`29-` claim.
- Sweep 2 run now returns one line, 250 — the "the text is written by the editor, who has the file open" sentence the plan names as expected. After the edits it will also return the two new clauses (`editor to compose`, `composing that file … to the editor`), depending on where the re-wrap puts the break; the plan predicts no output and asks for each returned line to be read against the rule, so a new hit costs nothing. Both new clauses are the exclusion itself and satisfy the rule trivially.
- `docs/paired-loop.md` § "How the memory begins, and how it survives" carries the ground both clauses lean on, in the paragraph beginning "The snapshot is written by the head, and a hand cannot write it" — the channel principle applied with the conversation as the surface and the head as what held it. The direction is right: the governing spec states it, the skill body now conforms.
- The file's own scoping of "shared artifacts" as "roadmap, specs, code, docs" (intro, line 25) leaves the snapshot merely absent rather than excluded — exactly as spec `160-` describes — so the second edit is genuinely load-bearing and not a restatement.
- Blast radius outside the target holds. `src/skills/architect-editor-engine/SKILL.md` defines the two format tokens at the level of what each *is* and explicitly leaves "the full authoring and execution mechanics of each format … with the callers", so it states nothing this edit contradicts; `src/agents/editor.md` and `src/commands/command-handoff.md` contain no occurrence of "snapshot" at all. One file is genuinely one edit — `active/skills/agent-architect` is a directory symlink into `src/skills/agent-architect`, so no second surface to keep in sync.
- The frontmatter `description:` makes no claim about who composes the snapshot ("never touches shared artifacts itself"), and `allowed-tools` already carries `Write Edit`, so the architect writing its own snapshot needs no frontmatter change. The plan is right to leave it alone.
- Growth is ~+415 characters, ≈ +7 lines, taking the file to ~384 — far inside the ≤ 500-line body constraint.
- The working tree is clean apart from the plan artifacts, so the edits start from precisely the text the plan quotes; nothing survived from the deleted rounds.
- `Testing: no` is right — skill-body prose has no silently-failing surface, per `test-philosophy`. `Docs: no` is right — the governing claim already stands in `docs/paired-loop.md`, so the doc is ahead of the code, which is its job.

Two provenance labels in the plan are looser than the files, and nothing turns on either, because both sentences they name are placed under an unconditional "stays untouched" in the same breath: `58.4`'s spec (`162-…md`) pins only the two-occasions sentence, not "The on-request one is the architect's own capability…", which belongs to no sibling; and the "the text is written by the editor, who has the file open" sentence sits mid-paragraph rather than opening it (the paragraph begins earlier, at "user has confirmed the edits…"). In both cases the sentence is identified by its own quoted words, which is what the instruction actually keys on.

### Context Gates

- **Architecture** — OK. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is not crossed: no new skill, no `loads:` edge, no mechanism moved between engine and policy. The prohibition stays where the policy lives (`agent-architect`) and not in `architect-editor-engine`, which holds the formats and disclaims their authoring mechanics — the correct side of that boundary.
- **Rules** — WARN (non-blocking). `.ai-factory/RULES.md` does not exist in this repository, and neither does `.ai-factory/skill-context/aif-review/SKILL.md`, so no project convention file could be applied.
- **Roadmap** — OK. The task is the first `[ ]` line of Phase 58 in `.ai-factory/roadmaps/trickster77777.md` — the seam. Linkage walked to the leaf: contract line 58.2 → `Spec:` `160-…md` → phase note `152-…md` → `Governing spec: docs/paired-loop.md` → the two target sentences. The contract line's "Touch only these two sentences" is honoured by both edit tasks and enforced by the neighbour entry in the comparison task and by the scope task.

### Findings

None. The plan is implementable as written: both replacement texts are pinned upstream and copied rather than re-derived, every anchor is addressed by its own words, the re-wrap consequence is named once and then relied on by the scope check ("read the diff as sentences, not lines"), and verification now has a positive half, a markup half, a neighbour half, and two rule checks that assert no number.

### Positive Notes

- The comparison task states its own insufficiency argument in the other direction from the sweeps — "a paraphrase of either appended clause would also satisfy" them — which is the reasoning that was missing when the first deleted reading ran. The order of dependencies matches: arrival first, then survival and scope.
- The markup entry is the sharpest line in the plan. It names the exact reason the most obedient reading of "copy it" destroys something ("the spec is the authority on the words, the file on the markup") rather than just listing the tokens, so an implementer who meets a case the entry did not enumerate still knows which way to rule.
- Task 2's wrap instruction anticipates the one shape of damage its own edit invites — a 19-column last line inviting the whole clause onto it — and resolves it against the paragraph's own width rather than against a figure.
- The first sweep's "do not 'fix' the grep pattern, and do not edit any matching artifact to make the output tidier" closes the failure mode where an apparatus check starts editing the artifacts it is meant to read.

## Deferred observations
- Affects: Phase 58 / `.ai-factory/specs/trickster77777/160-the-snapshot-itself-is-never-delegated.md` — Spec `160-`'s rule reaches one more sentence in the target file than its own sweep can find, and no task in the phase owns it. § "Your buffer is shared; you alone write it" closes with "It is the one file you edit directly: you are its only writer" — an exhaustive claim about which files the architect writes with its own hands, made when the buffer was the only such file. Once this task lands, the snapshot is a second: the clause it pins says "composing that file is forbidden to the editor outright", and `docs/paired-loop.md` puts it plainer still — "The snapshot is written by the head, and a hand cannot write it." The sweep pattern (`written by the editor|editor.*compose|compose.*editor|has the file open`) matches none of that sentence's words, and the plan's read-check asks only whether a sentence *affirmatively licenses* delegating the snapshot, which this one does not — it implies the snapshot is not edited directly, which is a tension rather than a license, so no churn is expected inside this task. The fix lies outside 58.2's pinned span: the contract line says "Touch only these two sentences", and 58.2 is right to follow it. Phase 59's task 59.1 already has that same section open for the zone-split removal and is the natural home if the phase decides the sentence should be scoped.

PLAN_REVIEW_PASS
