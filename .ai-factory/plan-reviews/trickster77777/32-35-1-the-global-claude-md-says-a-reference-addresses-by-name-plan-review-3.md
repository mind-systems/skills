# Plan Review — 35.1 the global CLAUDE.md says a reference addresses by name (round 3)

## Code Review Summary

**Files Reviewed:** plan (1) + target (1): `src/global/CLAUDE.md`; plus the task spec `.ai-factory/specs/trickster77777/111-the-global-claude-md-says-a-reference-addresses-by-name.md`, the 35.1 contract line with `### Phase 35` and its preamble in `.ai-factory/roadmaps/trickster77777.md`, `.ai-factory/ARCHITECTURE.md`, this repo's `CLAUDE.md`, the grove root `../CLAUDE.md`, `docs/reference-by-name.md`, `docs/always-loaded-discipline.md`, `docs/reserved-words.md`, `src/commands/command-handoff.md`, the plan's own sidecar, rounds 1 and 2 of this review, the sibling plan `30-34-3-…` for folder form; and in `tradeoxy_core`: `.ai-factory/RULES.md`, `.ai-factory/handoffs/` (43, 44, 45) and its working tree
**Risk Level:** 🟢 Low — round 2's single finding is closed at the root, and every factual claim the plan makes re-verified true against the files today. The edit was applied to a scratch copy and lands as a single two-line hunk. No findings.

### Context Gates

- **Architecture** — PASS. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is untouched: no skill added or re-tiered, no `loads:` edge, no engine gains policy, no lens inlines mechanism. The edit lands on the always-loaded surface, not the skill graph. Delivery re-verified: `active/CLAUDE.md → ../src/global/CLAUDE.md` and `~/.claude/CLAUDE.md → …/skills/active/CLAUDE.md`, so editing the source is editing what every session loads, and the tracked symlink's blob is a path, not content — which is why the plan's closing `git diff HEAD --stat -- src/ active/ docs/ CLAUDE.md` resolves to one file. `upstream/ai-factory/` has no `global` counterpart, so `scripts/sync-upstream.sh` cannot clobber the target. The plan's "No symlink work" is right.
- **Rules** — WARN (non-blocking), carried unchanged. This repo has no `.ai-factory/RULES.md` and no `.ai-factory/skill-context/aif-review/SKILL.md` — confirmed absent, nothing to check on either surface. The governing conventions live in `CLAUDE.md`, the grove root `../CLAUDE.md`, `.ai-factory/ARCHITECTURE.md` and `docs/`, all read below.
- **Roadmap** — PASS with one WARN, carried unchanged from rounds 1–2. The 35.1 line is the first `- [ ]` in `.ai-factory/roadmaps/trickster77777.md` (line 197) — the seam — and the plan's `# Plan:` heading matches its title. The `Spec:` tag resolves to `.ai-factory/specs/trickster77777/111-…`, which exists and was read in full; the plan's Context, insertion point, guard and verification all trace to it. WARN: `### Phase 35` names no `Governing spec:` and no phase note, so the chain runs contract line → task spec → `src/global/CLAUDE.md`; `docs/reference-by-name.md` is the ratified home in fact but is named by neither the phase header nor the spec's Files list. Not this plan's defect, and the chain terminates at the file the change is made in.
- **Vocabulary** — PASS. No protocol token moves: the `Spec:` tag, the PASS signals and `## Deferred observations` sit outside every edited span. The handoff mark line the plan pins is byte-identical to `src/commands/command-handoff.md` (lines 33 and 93) and to files 44/45 in the destination folder — modulo the `[ ]`/`[x]` marker, which is the point, and which the plan now both pins and checks. `work-order` in the pinned paragraph carries its post-34.4 single registry meaning (the paired-loop apply artifact, "thrown away when applied"), set against "a doc, a spec, or a roadmap line"; `src/global/CLAUDE.md` § "Planning workflow" already carries the word in that sense, so the term arrives in a file where it is already resident and collides with nothing.

### Findings

None.

### Verified against ground truth

- **Round 2's finding is closed at the root, not patched.** The fourth checkbox was retitled "Read the written handoff back, and confirm `tradeoxy_core` is otherwise untouched" and now carries the read-back in the same equality register the paragraph check uses: filename numbered `46` and unpadded with nothing else holding that number, line 1 in the `# Handoff — <slug>` form, the mark line character-for-character equal to the pinned string **with `[ ]` and not `[x]`**, and the prose naming both halves the spec requires while reproducing the paragraph nowhere. The specific trap round 2 named — 44 and 45 both carry `[x]` because both are spent, and the marker rides along with the form — is now caught by a check, and the write step warns about it in advance with the reason attached ("a handoff that lands pre-marked announces itself as already read, which is the one semantic the line exists to carry. Take the shape from them and the marker from here"). Both halves, the pin and the check.
- **The pinned string is byte-identical to the spec's.** Extracted from the plan's fence and dedented by its two-space list indent: **556 characters, equal character-for-character** to the paragraph under the spec's § "The change", em dashes included (U+2014 is the only non-ASCII codepoint in the line), the backticks around `file:line` and the bold `**name**` intact.
- **The edit lands clean; applied to a scratch copy.** § "Grounding claims" holds seven paragraphs; the fourth ends `…("per the spec…") — never invent.` and the fifth begins `The opening task statement is the first artifact`, which occurs **exactly once** in the file. Inserting the spec's string between them produces a single diff hunk — `12a13,14`, one added line plus its blank line, no other hunk — an eight-paragraph section with the new paragraph at index 4 and the two named neighbours in place, the string occurring exactly once, and `reference-by-name` at 0 both before (`grep -c` returns 0 on the live file today) and after.
- **The paragraph is a faithful compression of the ratified home, not a new rule.** `docs/reference-by-name.md` § "The check" carries the same four claims — the `file:line`-as-defect-report, the repair belonging in the target, position addresses living in a throwaway work-order, and the surfaces that outlive their numbering. The pinned paragraph reorders the closing triple ("a doc, a spec, or a roadmap line" against the doc's "a spec, a roadmap line, or a document") and adds nothing. It is the always-loaded register of an already-ratified rule.
- **The handoff destination matches, freshly enumerated.** `tradeoxy_core/.ai-factory/handoffs/` runs to `45-how-a-task-is-built-and-the-defect-that-got-into-ours.md`, so **46** is the next free number, unpadded like its neighbours. Files 44 and 45 open `# Handoff — <slug>`, blank line, mark line; 43 has no mark line, so the plan is right to take the form from 44/45 rather than the folder as a whole.
- **The `RULES.md` claims are exact.** The heading is `## Citing \`ARCHITECTURE.md\` — by rule name, never by line number` at line 31, and all four position addresses the plan names are present and verbatim: `package.json:13-14` (line 21), `src/indicators/runtime/indicator-protocol.ts:58-79`, `src/replay/__tests__/replay-indicator-replayer.spec.ts:288` and `:298` (line 25). Quoting these literally in the handoff names the defective citations as the exhibits they are — it is not the handoff addressing anything by position, and the plan says exactly that.
- **The pre-existing dirt is exactly the eight entries the plan documents.** `git -C tradeoxy_core status --short` returns six modified files (`notes/123-`, `127-`, `128-architect-buffer.md`; `specs/98-`, `99-`, `108-…`) and two untracked handoffs (44, 45). `git -C … diff HEAD --stat -- .ai-factory/RULES.md` is already empty, so the plan's byte-identity check is a real invariant to hold rather than a repair. Stating the spec's literal "shows the new handoff and nothing else" as unsatisfiable and verifying by delta instead is the right call, and it is annotated as a deviation where it is discovered rather than silently swapped.
- **The closing diff scope resolves.** This repo's working tree holds only four untracked artifacts under `.ai-factory/` (the plan, its sidecar, rounds 1 and 2 of this review) and nothing tracked is modified, so `git diff HEAD --stat -- src/ active/ docs/ CLAUDE.md` will list `src/global/CLAUDE.md` alone once the edit lands, exactly as the verify step asserts.
- **Step order matches the spec.** The handoff write depends on the paragraph's verification, so it "reports what is true" — the spec's own sequencing — and the read-back depends on the write. No step can run before what it asserts exists.
- **Blast radius is zero.** Every surface that cites § "Grounding claims" addresses it by name, not position — `docs/always-loaded-discipline.md` (opening paragraph and § body), `docs/reference-by-name.md` § "Where things are normed", `docs/philosophy/context-tree.md`, this repo's `CLAUDE.md` doc table, `src/commands/command-handoff.md`, `src/skills/detangle/SKILL.md` — so inserting a paragraph inside the section breaks no reference anywhere in the grove. That is what makes the one-paragraph shape viable, and it is the rule the paragraph itself states, already holding.
- **`Testing: no` is correct** under `test-philosophy`: one paragraph added to a Markdown instruction file plus one new prose file in another repository — no executable surface, nothing that can fail silently. `Docs: no` is right in the sense the setting carries; the edit *is* the change the task asks for, and no doc holds a copy that must move with it (see the deferred observation for the pointer that does not exist yet).
- **The plan carries no step the orchestrator owns.** No roadmap checkbox flip, no commit — matching the sibling plans in this folder, and matching the spec's "Nothing in that repository is committed."

### Positive Notes

- All three rounds' findings were fixed where they came from rather than worked around, and each fix arrived with its reason attached at the point of use — the block-quote defect closed by changing the presentation *and* upgrading the check from presence to equality; the quote-the-paragraph defect closed by its opposite plus the one-home-per-fact argument; and now the read-back added in the same equality register, with the `[x]`/`[ ]` trap named in the write step so the implementer is warned before it can be sprung and checked after in case it was.
- The `[ ]` instruction is the model of how to carry a divergence from a folder's own precedent: "Take the shape from them and the marker from here." One clause that tells the implementer exactly which part of the exemplar to copy and which part not to, and why.
- "Ground truth read for this plan" states, per claim, what was read and what was found, including every number the later steps depend on. All of it re-verified true, three times over now. This section is what makes the review cheap and is a good addition to this folder's plan form.
- The insertion is pinned three redundant ways — after the paragraph ending "— never invent.", before the one beginning "The opening task statement is the first artifact", with a blank line each side — and the three agree with each other and with the file.
- The plan holds the spec's Guards in both directions: no link to `docs/reference-by-name.md` in the always-loaded file, and no path into a sibling repository from either artifact — including in the handoff, where the pull back toward the deep home is strongest and the plan forecloses it by name with the reason.
- It does not reach into `tradeoxy_core`'s `RULES.md`. Dropping the local duplicate is that repository's own work; the plan states it, verifies it byte-for-byte against HEAD, and writes the `file:line` observation as an observation rather than an instruction.

## Deferred observations

- Affects: Phase 35 / task spec `.ai-factory/specs/trickster77777/111-the-global-claude-md-says-a-reference-addresses-by-name.md` — after this task the naming rule has two homes in this repository and neither knows about the other. `docs/reference-by-name.md` § "Where things are normed" names the global CLAUDE.md § "Grounding claims" only as the home of the one-home-per-fact rule it rests on, so nothing tells a reader arriving at the deep home that the rule's own normative statement is now in the always-loaded layer; `docs/always-loaded-discipline.md` opens by enumerating four things § "Grounding claims" prescribes — docs → roadmap → code, grounding by walking to the leaf, decay, the seam — and after 35.1 the naming rule is a fifth the enumeration omits; and this repo's `CLAUDE.md` doc table gives the "Reference by name" row (line 36) no equivalent of the clause the Context tree row directly below it (line 37) already carries, "the normative rule lives in the global CLAUDE.md § 'Grounding claims'". The spec's Files list pins one edit and the plan correctly does not exceed it, so this belongs to whoever writes the next task on this phase: one clause in `docs/reference-by-name.md`, one in `docs/always-loaded-discipline.md`, and the `CLAUDE.md` row brought into line with its neighbour.

PLAN_REVIEW_PASS
