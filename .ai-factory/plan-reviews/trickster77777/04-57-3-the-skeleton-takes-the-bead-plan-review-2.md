# Plan Review — 57.3 the skeleton takes the bead (iteration 2)

## Plan Review Summary

**Artifacts read:** the plan (iteration 2); plan-review-1; the task spec (`158-…md`); the phase note (`151-…md`); the phase-57 sibling spec (`157-…md`); the contract line in `.ai-factory/roadmaps/trickster77777.md`; the target `src/skills/roadmap-decompose-skeleton/SKILL.md` in full; `src/skills/polymorphism-philosophy/SKILL.md` and its `active/skills/` symlink; `docs/counts-go-stale.md`; `docs/sakshi-harness/skill-cycle.md` and `skill-graph.md`; `CLAUDE.md` § "Dependencies and the skill graph"; `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy".
**Risk Level:** 🟢 Low — all three findings of plan-review-1 are repaired, and every factual claim the new text adds was re-checked against the files rather than taken from the previous round.

### Context Gates

- **Architecture — OK.** One more caller→engine edge of exactly the kind `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" describes: the policy (Lens 1's gate, its targeting, its restraint) stays with the caller; the loaded unit supplies shared content and drives nothing. No boundary crossed, no new file, no central dependency map to keep in sync (there is none — checked: `ARCHITECTURE.md` holds no engine↔caller table).
- **Rules — WARN (optional file absent).** No `.ai-factory/RULES.md` in this repo. The governing convention layer is `CLAUDE.md` § "Dependencies and the skill graph", and the plan honours it: the dependency is declared in the caller's own `loads:` field and nowhere else, and the engine's own reverse-graph marker plus its naming of this caller are already on disk — re-verified in `src/skills/polymorphism-philosophy/SKILL.md`, so nothing is owed on the engine's side.
- **Roadmap — OK.** The plan matches contract line 57.3 clause for clause. The sequencing precondition holds: 57.2 is `[x]`, `src/skills/polymorphism-philosophy/SKILL.md` and its `active/skills/` symlink both exist. The target's own `active/skills/` symlink already exists too, so no deployment step is missing.
- **Governing spec — N/A, correctly.** Phase 57's header names none, and the phase note says so outright; the authority is the phase note plus the task spec. The documentary edge is already closed ahead of the code: `docs/sakshi-harness/skill-cycle.md` already describes this pass by both axes and already names the unit as the second axis's home. `Docs: no` is the right setting; grepped both `docs/` and `ARCHITECTURE.md` for text this diff could falsify and found none.

### Verification of the previous round's findings

All three are repaired, and each repair was re-checked against ground truth rather than accepted on its face:

1. **The sweep's expected result (was critical).** The plan now states four paths before the change and re-phrases the gate against the spec's **rule** — *does this file quote one of the two passages as a standing claim about the present?* — explicitly naming itself and any run artifact of this task as self-referential non-findings, and explicitly saying the count is not the test. Run as written from the repo root, the sweep returns exactly the four paths the plan names; the plan's own match is on line 55 only, inside the fenced grep command, exactly as it claims. After the two content edits the target drops out on both alternatives — the replacement paragraph carries neither the testability phrase nor a `loads:` line ending in `test-philosophy` (it ends in `polymorphism-philosophy`).
2. **The pre-authorised `DEVIATION` (was critical).** Replaced with the correct ruling — no annotation is owed, because the annotation records the file on disk contradicting the plan and here the plan itself orders the edit. The divergence from the spec's "stays accurate" clause stays argued in the plan's own reasoning section, which is where a reviewer reads it.
3. **The unpinned layout of the replacement block (was an issue).** Now a hold of its own, with the destination's convention measured rather than guessed: ≤ ~90 columns, two-space continuation on the restraint bullet, and "verbatim means the words and their order, not the newlines". Both measurements check out against the file — today's Lens 1 block runs 81–90 columns, its restraint bullet is continued at a two-space indent, and the longest line anywhere in the file is 96. The consequence the plan flags (a phrase of the new text may span a line break, so later greps match per line) is real and worth having said.

### Critical Issues

None.

### Issues

None.

### Positive Notes

- **Every claim the plan adds this round is measured, not asserted.** The column range, the file's longest line, the four-path sweep result, the frontmatter field list, the engine's reverse-graph marker — each was checkable, and each checks out. The plan states where it looked, which is what let this review confirm rather than re-derive.
- **The gate is re-phrased from a count to a rule.** The verification step now judges each returned path by whether it quotes a passage as a live claim, tells the implementer not to tidy the pattern or edit a matching artifact, and pre-clears its own review artifacts. That is the same lesson phase 56 is about, applied to the plan's own instruction — the strongest thing in this iteration.
- **The count repair is correctly reasoned and correctly bounded.** The plan detects that its own edit falsifies the lead sentence's "two", runs `docs/counts-go-stale.md`'s one-sentence test (adding a member falsifies it → census → delete), applies the doc's prescription rather than re-measuring, and stops there — honouring the spec's ruling that the section gains no restatement of the third dependency. A sentence its own diff makes false, inside its own file, is in-boundary by the review engine's own test, and the plan says so in those terms.
- **The restraint against restating the loaded unit's content is argued from the file's own precedent.** Lens 2's construction against `test-philosophy` and Critical Rule 2 are cited as the standard; the effect is that Lens 1 leans on nothing about `polymorphism-philosophy` beyond its name, and the question keeps exactly one home. Both citations were checked verbatim against the file.
- **The one thing deliberately not touched is named and argued** — the `description:` frontmatter field — with the reason stated where a reviewer looks for it, instead of left silent.

## Deferred observations

- Affects: `src/skills/polymorphism-philosophy/SKILL.md` (phase 57, spec `157-…md`) — the unit's § "The two entries" routes its consumer to the **time** entry, and that entry is written for a change that has already landed: it takes "what a change just brought", asks "was a seam cut at that arrival or not", and pins "its evidence is the diff". Its only consumer, `roadmap-decompose-skeleton`, runs before any of that exists — over open `- [ ]` roadmap tasks, deciding whether to cut the seam rather than reading whether one was cut, with the task text and the current code as its evidence and no diff anywhere. The question itself transfers intact; the entry's evidence clause does not, and a runtime agent that loads the unit from the new Lens 1 will look for a diff it cannot have. The phase note phrased the consumer's event as "does this *task* bring a second member to a kind", so the divergence is between the note and the engine's written wording, not something this task introduces. It is not fixable here: Lens 1's replacement text is pinned verbatim by the spec, and stating the evidence in the caller would be exactly the restatement the whole design forbids. The repair belongs in the unit's own body — either the time entry's evidence clause widens to a described change as well as a landed one, or the two entries gain a third reading for a planning-time consumer.
- Affects: `.ai-factory/specs/trickster77777/158-the-skeleton-takes-the-bead.md` (phase 57) — after this task the "Load-once / dependencies" section describes two of the three skills the file loads once per chat via the `Skill` tool. The plan correctly deletes the word "two" from the lead sentence, but the colon that sentence ends on still introduces a two-entry list while a third delegation exists, so the enumeration inherits the role the deleted number vacated. By `docs/counts-go-stale.md`'s own taxonomy the two halves are now governed by different rules: the number was a census (adding a member falsifies it → remove), while a bullet list of members reads as a member set — a contract, kept, and wrong when it omits a member. The spec's ruling settled only the first half. The plan is right not to act: the spec rules explicitly that the section "gains no restatement of the third dependency there", and overriding a ratified ruling on a plan's own authority is not this task's to do. The decision belongs where the ruling was made — either that list is deliberately a description of the two engines carrying render and test policy, in which case one sentence there should say so, so the omission reads as intent; or the third delegation belongs in it and the ruling wants revisiting in a later task.

PLAN_REVIEW_PASS
