# Plan Review — 34.2 `command-pin-gaps` closes holes and writes no checks (round 2)

## Code Review Summary

**Files Reviewed:** plan (1) + target (1): `src/commands/command-pin-gaps.md`; plus the task spec `110-…`, the contract line at roadmap line 185 with the `### Phase 34` header and its direction preamble, `src/skills/roadmap-engine/SKILL.md` § "What a task spec holds", `src/skills/test-philosophy/SKILL.md`, `docs/reference-by-name.md`, `docs/sakshi-harness/skill-cycle.md`, `docs/skill-description-field.md`, `docs/sakshi-harness/skill-graph.md`, `.ai-factory/ARCHITECTURE.md`, `src/skills/roadmap-outline-deep/SKILL.md`, and round 1 of this plan review
**Risk Level:** 🟢 Low — three prose edits in one file; both round-1 findings are closed at their root, and every positional, textual and numeric claim re-verified byte-exact against the file rather than against round 1's verification of it.

### Context Gates

- **Architecture** — PASS. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is undisturbed: no skill added or re-tiered, no `loads:` edge changed (the plan forbids one explicitly, twice), no engine gains policy, no lens inlines mechanism. The shape's one home stays `roadmap-engine`, which the command already loads, and the new paragraph points at it by name instead of copying it — the plan names the engine's three elaborations verbatim and forbids paraphrasing them in, which is the precise failure this gate exists to catch. Body cap intact: 45 lines today, 47 after.
- **Rules** — WARN (non-blocking). `.ai-factory/RULES.md` does not exist in this repo; nothing to check against. The governing conventions live in `CLAUDE.md`, `.ai-factory/ARCHITECTURE.md`, `docs/reserved-words.md`, `docs/using-the-language.md` and `docs/reference-by-name.md` — all checked below.
- **Roadmap** — PASS. Line 185 of `.ai-factory/roadmaps/trickster77777.md` is the first `- [ ]` line in the file — the seam — and it is 34.2. The plan's `# Plan:` heading matches the contract line's title verbatim. The `Spec:` tag resolves to `.ai-factory/specs/trickster77777/110-pin-gaps-closes-holes-and-writes-no-checks.md`, which exists and was read in full; the plan covers all five of its obligations (name the shape one class per part, state the register, replace the value-hole citation clause, leave the scan line, add no class). `### Phase 34` names no `Governing spec:`, so the chain runs contract line → task spec → `src/commands/command-pin-gaps.md`, with `roadmap-engine` and `docs/reference-by-name.md` as the two named authorities.
- **Vocabulary** — PASS. Nothing edited is a protocol token: the `Spec:` tag, the scan-mode report format and the `owner: <skill>` token stay byte-identical, and the scan line is pinned out of scope. `task spec`, `loud failure` and the command's own three class names are used at their settled meanings. `work-order` does not appear in either pinned paragraph, so this landing does not deepen the collision 34.4 removes.

### Round-1 findings, re-checked

- **Critical 1 (the pinned bijection contradicted lines 32 and 38) — closed at the root, not patched around.** The pinned text now carries the principle itself: "A class is named by the part its repair supplies, never by the part that went unread", with the worked case spelled out inline ("which is why an existing shape nobody looked at is a meaning hole, the fit it must satisfy being what is undecided"). Checked against the file's own routing: line 32 ends `an existing shape nobody looked at, or work already half-done, is a meaning hole; something that breaks on contact is a blast-radius hole.` and line 38's meaning-hole enumeration ends with the same two members — both now follow from the stated principle rather than colliding with it. The second member, "work already half-done", is not named in the pinned text but is covered by the same principle (its repair supplies *what must be true after*), so no residue. The plan additionally pins the clause as load-bearing and forbids dropping it to shorten the paragraph, which is the right guard for a sentence a later editor would read as filler.
- **Minor 2 (the 120–887 column claim was false for lines 17–18) — fixed and now true as written.** The claim is narrowed to "every body paragraph from line 20 onward", and the exception is named explicitly as the model *not* to follow. Measured: lines 20–45 run 120 (line 22) to 887 (line 38), no line in that stretch is under 120, and lines 17–18 are 81 and 77. The claim now reproduces exactly.

### Verified against ground truth

- **The seam is where the plan says.** Line 34 is the "A task spec that repeats a paragraph from a document…" paragraph and ends byte-exact `unlike the two surfaces it sits between.`; line 35 is blank; line 36 opens `**Value holes:**`. Two paragraphs with a blank line on each side is well-formed markdown, and the trailing blank line before `**Value holes:**` is preserved by construction.
- **The replaced clause is quoted byte-exact.** Line 36 ends `Repair: read the code/proto and pin the **exact** value with a \`file:line\` citation — never invent.` — character for character what the plan replaces, and the value-hole enumeration ahead of it is outside the replaced span.
- **The replacement matches the rule it implements.** `docs/reference-by-name.md` § "The check" states that position addresses "never enter a spec, a roadmap line, or a document", and § "Granularity, not size" gives the unit of naming as "a heading, a bold lead-in, a numbered item, a named clause" — the replacement text names a symbol, a heading and a bold lead-in, so it teaches the rule at the granularity the doc defines it, without citing the doc.
- **The citation form the new paragraph uses is already the file's own.** Line 20 writes `see the engine's "Named roadmaps" section` — the same repo-internal name-citation shape as `` `roadmap-engine`'s "What a task spec holds" paragraph ``, with the same straight double quotes (the file contains no curly quotes at all). The reference resolves: `src/skills/roadmap-engine/SKILL.md` line 46 opens `**What a task spec holds:**` and italicizes the three parts in exactly the wording the pinned text reuses — so the plan's own edit obeys the rule it is installing.
- **The `file:line` occurrence count is right.** Exactly three in the file (lines 26, 36, 44); the Guards' "the two other occurrences stay" is exhaustive, with a stated reason each is exempt, so a `grep file:line` sweep — the most likely way this task goes wrong — is foreclosed.
- **The no-heading claim holds and the bold lead-in is the right instruction.** The file carries no `#` heading at any level, and `docs/reference-by-name.md` line 13 cites exactly that. Two more bold lead-ins strengthen that sentence rather than dating it: it says the three finding classes are cited by name because each opens with a bold lead-in, which stays true.
- **The `test-philosophy` step is inherited, not invented.** `src/skills/test-philosophy/SKILL.md` lines 24–41 hold the signal-immediately / run-on-silently discriminator and the skip rule ("Loud-failure surfaces are already caught"), and the step from "a check that can only fail where the instruction was ignored" to "loud failure" is the contract line's and the task spec's own wording, carried through unchanged. Naming the skill without a `loads:` edge matches line 42's existing treatment and leaves the grep-based reverse graph as it reads today.
- **The "no `docs/*.md` of this repository under `src/`" claim reproduces.** Every `docs/` hit under `src/` (`aif-docs` SKILL.md lines 24/197–200 and its `references/REVIEW-CHECKLISTS.md`, `roadmap-outline-deep`) means the *target project's* docs directory. Stating the naming rule inline rather than linking `docs/reference-by-name.md` is therefore correct for a command that executes inside arbitrary projects.
- **Nothing downstream depends on what changes.** `grep -rn "pin-gaps" --include="*.md"` outside `.ai-factory/` and `active/` returns `docs/skill-description-field.md:13`, `docs/sakshi-harness/skill-cycle.md:37,39,74`, `docs/sakshi-harness/skill-graph.md:49`, `docs/reference-by-name.md:13` and the `CLAUDE.md` index row — all describe the pass's job, none quotes its value-hole repair or claims it writes checks. The Guards' "docs stay correct" holds.
- **Delivery is clean.** `active/commands/command-pin-gaps.md → ../../src/commands/command-pin-gaps.md` is a live symlink, so the edit goes live; `upstream/ai-factory/` ships skills only, so a re-sync cannot clobber a command. The working tree holds only this task's own three untracked artifacts, so the `git diff HEAD --stat` post-condition is a clean signal.
- **The two-item summary in the register paragraph was weighed and cleared.** "it pins values and enumerates breakage" names two of the three repairs line 45 enumerates (`the concrete value, spec clause, or sweep enumeration`), omitting the meaning-hole clause. It is not a claim of exhaustiveness — it is the positive half of a contrast whose subject is the check — both its clauses are true, and the paragraph immediately above it supplies the third repair by name. It is also the contract line's and the task spec's ratified wording; changing it here would be a deviation from ratified text bought for nothing.
- **The stopping rule does not license fabrication.** "with none of them left open the spec is whole and the pass stops" states a sufficient condition, not a necessary one, so it does not pressure a pass to close a genuine product decision that line 38 tells it to raise instead; line 42 ("a hole whose repair belongs elsewhere is reported, in both modes") and line 45's `N closed from source · M blocking · K owned elsewhere` sit directly below and settle the reading.

### Positive Notes

- Round 1's critical finding is closed by naming the underlying principle rather than by softening the sentence that tripped over it — the file now states *why* line 32 routes as it does, which is strictly more than it had before the finding.
- Pinning the full text of both paragraphs, their order, their insertion point, their line-wrapping and the one clause that must not be dropped leaves nothing to re-derive on a change whose entire deliverable is prose.
- The Guards do the harder half: naming the two `file:line` occurrences that must **survive**, with the reason each is exempt — a landing described while reasoning, and a chat report thrown away, which is the exemption `docs/reference-by-name.md` itself grants.
- The negative space is pinned exactly where an implementer would drift: no `loads: test-philosophy`, no heading, no rewrite of line 22, no touching line 42, no citation of a `docs/` path in a file that runs where that path does not exist, no anticipation of siblings 34.3 / 34.4.
- Naming `roadmap-engine`'s paragraph by its bold lead-in — and forbidding paraphrase of its elaborations — holds one-home-per-fact on the exact fact three tasks of this phase circle.

## Deferred observations

- Affects: Phase 35 / `.ai-factory/specs/trickster77777/111-the-global-claude-md-says-a-reference-addresses-by-name.md` — `src/skills/roadmap-outline-deep/SKILL.md`, in the phase note's **Verbosity directive** bullet, still instructs the pass to write "a `file:line` where a claim needs one". A phase note is a durable artifact addressed by the `Phase note:` tag and outlives the numbering it was written against, so it is the same defect class this task repairs in `command-pin-gaps` — `docs/reference-by-name.md` § "The check" names a spec, a roadmap line and a document as the three places a position address never enters. It is correctly outside this task's boundary: the contract line names only `command-pin-gaps`, and no open task owns `roadmap-outline-deep`. 35.1 makes it sharper rather than resolving it — it lands the naming rule in the always-loaded global CLAUDE.md, after which one skill will still explicitly instruct its pass to do what the always-loaded layer forbids, and an explicit instruction beats an ambient rule. The residue while it stands is a half-converted family: `command-pin-gaps` is told to name its source while the pass immediately upstream of it is told to cite a line. `roadmap-prune`'s occurrence needs nothing — it addresses an entry inside a review file for a handoff spent on first read, the throwaway genre that document exempts.

PLAN_REVIEW_PASS
