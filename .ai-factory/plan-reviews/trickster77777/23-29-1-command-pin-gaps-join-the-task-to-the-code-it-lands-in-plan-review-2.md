## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/23-29-1-command-pin-gaps-join-the-task-to-the-code-it-lands-in.md`
**Task spec:** `.ai-factory/specs/trickster77777/103-pin-gaps-blast-radius-class.md`
**Files targeted:** 1 (`src/commands/command-pin-gaps.md`)
**Risk Level:** 🟢 Low — both findings from plan-review-1 are closed with real steps and real checks, every line anchor re-verifies fresh against the target file, and all eighteen of the spec's § Verification checks plus all nine of its § Guards now reach a step. One stray misattribution survives in the Grounding step.

### Context Gates

- **Architecture** (`.ai-factory/ARCHITECTURE.md`) — PASS. `:22` places slash commands under `src/commands/` as a category parallel to `src/skills/`, so an in-place rewrite of a command needs no structural move. § "Composition: mechanism vs policy" is untouched: nothing is extracted, no engine appears, and `loads:` stays at its single existing `roadmap-engine` edge — correct, since naming an owner is a finding, not a delegation, and the plan says so explicitly ("naming an owner needs no load").
- **Rules** — WARN (unchanged from review-1). `.ai-factory/RULES.md` and `.ai-factory/skill-context/` both absent, so there are no project `aif-review` overrides to apply. The binding discipline is the root and global CLAUDE.md: protocol tokens held byte-exact (the plan pins `Repair:`, `owner: <skill>`, `## Blocking decisions`, `N closed from source · M blocking · K owned elsewhere`, `value|meaning →`, `Governing spec:`, `Phase note:` as literals), the direction docs → roadmap → code carried as required body content, and the reserved-words contract — `src/commands/` is product we author, so it binds, and the plan's vocabulary (task, task spec, contract line, leaf, walk, phase, governing spec, owner) conforms.
- **Roadmap** — PASS. `.ai-factory/roadmaps/trickster77777.md:134` carries 29.1 with `Spec: .ai-factory/specs/trickster77777/103-pin-gaps-blast-radius-class.md`; it is the first `[ ]` line, sitting directly above `---STOP---` under Phase 29's header at `:130`. `git status --porcelain` shows only untracked artifacts under `.ai-factory/`, and `git diff HEAD --stat -- src/ active/ docs/ CLAUDE.md` is empty — so the plan's negative diff check is satisfiable exactly as written.
- **Governing spec** — PASS. Phase 29's header names no `Governing spec:` and no `Phase note:`; the governing surface is `docs/sakshi-harness/skill-cycle.md` § "Пины — `command-pin-gaps`" (`:37`, body at `:39` and `:41`), which `CLAUDE.md:33` names the authoritative home of the cycle's order. Every clause of `:39`/`:41` has a step behind it: the walk to the leaf, the two-sided orchestrator emulation with no prompt carried, the finding definition, the three classes with their repairs, the `file:line` pin, the enumeration that is never "возможно, придётся обновить", the resolve-in-place vs. explicit-blocker split, undocumented behavior as a hole with documentation as the foundation, the ordinary document read, the phase-header-pointer exclusion naming `roadmap-outline-deep` and `roadmap-decompose`, the four owners wielded by none, product decisions to blockers, and the three-group report. The plan correctly treats this file as read-never-edited.

### Both plan-review-1 findings are closed

- **The counter-default guard (spec § Guards `:41`) now has a step and two checks.** "State the two ends of a hole" carries it into the file itself — "a task spec that repeats a paragraph from a document instead of linking to it is not a finding" — with the spec's own reason (an agent guesses a file's content from its name; the copy goes stale, and that is proper to a tier that is the perishable seam between documentation and code, pruned as it closes). The step also states *why* it cannot be left implicit, and Verification adds a matched pair: the rule present (≥ 1) **and** no rule telling the reader to collapse or route such a copy (0). The 0-side is what makes this hold — the positive-only check would pass on a file that states the exception in one paragraph and contradicts it in another.
- **The Grounding step no longer supplies the premise while withholding the exception.** It now scopes what carries — "those two facts, and only those two" (docs → roadmap → code; walk to the leaf) — and adds an explicit negative: do not carry the one-home-per-fact rule or the link-never-copy rule into the file, with the reason that at this tier the spec overrides them and the step below states the override.
- **The third end→class mapping is carried.** The class step now writes "something that breaks on contact is a blast-radius hole, stated in the same sentence shape as the other two mappings, since it is the one symptom of the toward-the-code end whose class has no prior usage a reader could lean on", and Verification requires all three mappings stated explicitly. The toward-the-code end lists five symptoms and all five are now classified.

**Every anchor re-verifies exactly.** `src/commands/command-pin-gaps.md` is 26 lines: `:1-12` frontmatter (`description: >-` at `:2`, folded text `:3-8`, `argument-hint: "[path | scan]"` at `:9`, `allowed-tools: Read Edit Grep Glob Bash(ls *) Bash(rg *) Bash(git *) Skill` at `:10`, `loads: roadmap-engine` at `:11`), `:14-15` the engine load, `:17` target resolution, `:19` the premise, `:21` value holes, `:23` meaning holes, `:25` scan mode carrying `value|meaning →`, `:26` the default report `N closed from source · M blocking`. `CLAUDE.md:33` is the Skill-cycle row; `:115` is the `≤ 1024 chars` comment. `skill-cycle.md:37/39/41` are the Пины header and its two paragraphs. The file is tracked and clean, so `git show HEAD:src/commands/command-pin-gaps.md` resolves.

**The reconnaissance holds.** `grep -rn command-pin-gaps docs/ CLAUDE.md src/ active/` returns `docs/skill-description-field.md:13`, `docs/sakshi-harness/skill-graph.md:49`, `CLAUDE.md:33` and `docs/sakshi-harness/skill-cycle.md:37,39,74`. The first two reference the command by name in prose about something else — `skill-description-field.md:13` uses it as an example of action-without-invocation, `skill-graph.md:49` as an example of what gets pinned — and neither quotes the `description:`; `CLAUDE.md:33` is the doc-table row for the cycle. No file under `src/` invokes the command. `active/commands/command-pin-gaps.md` is an existing symlink into `src/commands/`, so an in-place rewrite leaves the active set intact and adds no symlink, exactly as the scope check assumes.

**The tool-grant reasoning is grounded, not stylistic.** `:10` grants the `Grep` tool and `Bash(rg *)` and no bare `grep`, so the plan's "the repair verb is `Grep` or `rg`, never bare `grep`" is fixed by the actual grant. `Read`, `Glob` and `Bash(git *)` cover the walk to the leaf; `Edit` covers the in-place close. Nothing the plan asks for needs `Write`, `Agent` or `AskUserQuestion`.

**Coverage is complete.** All eighteen spec § Verification checks appear in the plan's Verification step, including the ones easiest to lose — `Repair:` exactly 3, `value|meaning →` at 0, `Governing spec:`/`Phase note:` each 0, `orchestrator/` at 0, the `wc -m` measurement, both halves of the emulation plus "no plan and no verdict", the oversized-sweep finding owned by `roadmap-decompose`, and the normalized-read method itself with "No count is evidence until taken this way". All nine § Guards now reach a step: the identity guard and the no-verdict rule (Body step 1), no new authority and the four owners (owner step), no path outside the repo (Verification, `orchestrator/` at 0), no frontmatter growth (Frontmatter step), the repeated-paragraph exception (two-ends step), the comparative calibration (owner step), `Grep`/`rg` over bare `grep` and the single register with the third paragraph in `:21`/`:23`'s shape (class step), and Phase 28 / 28.1 / spec 102 untouched (Verification).

### Critical Issues

None.

### Issues

**1. The Grounding step attributes the one-home-per-fact rule to a section that does not contain it.**
The step reads: "Do **not** carry that section's one-home-per-fact rule, or § 'Documentation style''s 'a fact's second home is always a link, never a copy', into the file". "That section" is § "Grounding claims", which the preceding sentence names. Read fresh, `src/global/CLAUDE.md` § "Grounding claims" spans `:3-17` and states no one-home-per-fact rule anywhere in it — the rule lives at `:31`, under § "Project CLAUDE.md authoring" ("**One home per fact.** Anything stated in two places will drift."). The § "Documentation style" sentence is also inexact: `:27` reads "A fact's second home is always a link **to its first**, never a copy."

The failure: an implementer following the Grounding step reads § "Grounding claims" in full — the plan directs it there — searching for a rule to exclude, finds none, and has to decide whether the instruction is vacuous or whether it is looking at the wrong section. The half of the sentence that carries the actual risk (the link-never-copy rule) is correctly located, and the positive exception is mandated by the "State the two ends" step with its own paired check, so the blast radius is small — but a step whose entire job is to pre-empt a counter-default rule should name that rule's real home, and the plan's own § "Grounding claims" discipline forbids attributing a reference it did not open.

Fix: point at `src/global/CLAUDE.md:31` (§ "Project CLAUDE.md authoring") for the one-home-per-fact rule, and quote `:27` as it reads.

### Positive Notes

- **The exception is fenced on both sides.** Requiring the rule present *and* its inverse absent is the only shape that survives a full-body rewrite of a file whose surrounding reasoning ("follow named references to the leaf") pushes the other way. A presence-only check would pass a file that states the exception once and contradicts it in the meaning-hole paragraph.
- **The Grounding step now scopes what it hands over.** "Those two facts, and only those two, carry into the new body's reasoning, and both are already resident in every session, so the command fetches nothing" — this closes the leak review-1 named without adding a load, and it keeps the produced command free of any fetch it would have to perform at runtime.
- **The `description:` is treated as contract, not summary,** with the reason stated in the step itself — "a capability absent from it is one the invoking agent may never learn exists" — matching `docs/skill-description-field.md`'s account of the always-loaded field, and with the cap ordered measured by `wc -m` rather than estimated. For a folded scalar whose real length is easy to misjudge from its source lines, that is the right instrument; the block measures 433 characters today against a 1024 cap, so the headroom is real.
- **The frontmatter is pinned negatively as well as positively** — the byte-exact capture ordered up front with `git show HEAD:...`, "whatever line numbers they end up on". That is the exact failure a full-body rewrite invites, since every line number the plan is written against shifts.
- **The emulation is fenced three ways.** "It produces no plan and no verdict", "Carry no orchestrator prompt into the file and name none", and separately that no pass/fail verdict displaces the finding list — three distinct routes by which an orchestrator emulation could metastasize into an orchestrator, each closed, each with a check behind it.
- **The walk is given a unit and a stopping rule.** "One behavior the task claims", each ending at a `file:line` landing or becoming a finding, descending into referenced code "only as far as that behavior's landing requires, never past the question". Bounding by the question rather than by depth is the only bound that survives a file whose references fan out.
- **The exclusion is stated as a boundary with an owner, not as an omission** — the pass does not locate the phase, does not read its header's pointers, reports nothing about their presence or absence, and the owning tier is named (`roadmap-outline-deep`, `roadmap-decompose`) while the tokens themselves are forbidden from appearing. Naming the owner is what stops a future editor reading the exclusion as a gap, and 28.1/28.2 just gave those pointers their readers.
- **Blast-radius is specified against its own degenerate output.** "A `Grep`/`rg` sweep whose **enumeration** goes into the task spec — never a sentence saying something may need updating" names the failure directly, and the oversized-sweep escape hatch routes to `roadmap-decompose` with an explicit ban on filing it under `## Blocking decisions`, keeping that heading reserved for genuine product decisions as `skill-cycle.md:41` requires.
- **Authority is denied verb by verb rather than in the abstract** — "does not decompose, does not write documentation, does not author tests, and loads none of the four" — one verb per named owner, so no owner reads as an invitation to act.
- **Verification names the wrong instrument and refuses it.** "Never a line-oriented `grep`", `**` normalized out of both sides before any quoted span is compared, and "No count is evidence until taken this way" — correct for a file whose required spans (`N closed from source · M blocking · K owned elsewhere`, the bolded class names) straddle bold markers and line breaks once a rewrite reflows them.

## Deferred observations

- Affects: `.ai-factory/specs/trickster77777/103-pin-gaps-blast-radius-class.md` / a later task in Phase 29 — the command's default target, kept unchanged at `:17` by both the spec ("its container" is explicitly out of the cut) and the plan, is *all* open `- [ ]` tasks of the roadmap in play above `---STOP---`. Before this task each of those got a text-only interrogation; after it, each gets a walk into the code to the leaf plus a two-sided orchestrator emulation. Neither the spec nor `skill-cycle.md:39` says what the command does when that default fires over a dozen open tasks — walk all of them, walk the first, or ask. The plan cannot address it without contradicting "the container does not change", so it belongs upstream: either the container gains a scale rule, or the doc states that the whole-roadmap default is a scan-mode-only shape.
- Affects: `docs/skill-description-field.md` (outside this task's one-file diff boundary) — `:13` paraphrases the command's zone as "close the places an implementer would guess at". After 29.1 the command also routes what it may not close to a named owner and raises fundamental conflicts as blockers, so "close" narrows the described zone. The passage uses the command as an illustration of action-without-invocation rather than as a specification of its behavior, so nothing breaks; whoever next touches that doc may want the zone stated as close-or-route.
