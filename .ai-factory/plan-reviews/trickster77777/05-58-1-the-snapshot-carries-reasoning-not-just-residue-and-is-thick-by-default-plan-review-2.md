# Plan review — 58.1 the snapshot carries reasoning, not just residue, and is thick by default

## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/05-58-1-the-snapshot-carries-reasoning-not-just-residue-and-is-thick-by-default.md`
**Target:** `src/skills/agent-architect/SKILL.md` (one paragraph, two adjoining sentences)
**Risk Level:** 🟢 Low

### Both findings of plan-review-1 are closed

- **Finding 1** (the enumeration omitted the reasoning clause). The "Copy from the spec; do not paraphrase." bullet now carries the insertion explicitly and in its pinned wording — *"inserts, immediately after the residue list and before the durability clause, the requirement this whole task exists for" — "— and the reasoning that shaped it: why one option was taken over another, which premise proved false and how, what the user's own correction was and in what words"* — checked character-for-character against the spec's first blockquote, and placed at the position the spec puts it. The plan also adds the guard the finding asked for implicitly: the enumeration is declared "a reading aid for the pinned text, **never a substitute change set**", and names the exact truncated rewrite that would satisfy every other entry while losing the task.
- **Finding 2** (verification tested only disappearance, never arrival). A new step, "Compare the edited paragraph against the spec's pinned wording, word for word", precedes the sweep and depends on both edits. It specifies the comparison correctly for a hard-wrapped destination ("words only, ignoring line breaks … newlines collapsed to spaces"), names the two sentences that must match in full including the reasoning clause and the thickness sentence, and adds the third check the spec's own invariant never covers — that the three neighbours are unchanged. It also states why the sweep cannot stand in for it and routes a mismatch differently depending on which half failed. The sweep step was amended in step to say "it is the negative half, and the step above is the positive one; neither alone finishes the task."

### Ground truth re-verified against the files

Every factual claim in the revised plan was checked again, and all of them hold:

- `src/skills/agent-architect/SKILL.md` is 371 lines. The memory-snapshot paragraph is lines 65–81 of § "Spawn once, message thereafter" (§ header at line 30, next header at 169).
- Sentence 1 does begin at the start of line 73, directly after `…is never sent to the editor.` closing line 72, and ends mid-line 76 with `Each new` following on the same line. Sentence 2 ends mid-line 78 with `Of the` following on the same line. The paragraph does open with the two-occasions sentence at line 65.
- The "current" text the plan quotes for both sentences is byte-identical to the file, with newlines collapsed.
- The paragraph's non-terminal lines measure 63–76 columns (lines 73–78 are 71/68/69/69/76/76), so the plan's wrap convention is the paragraph's own; the file's widest body line is 85, so the target band stays well inside it.
- The growth estimate is sound: the two replacements add +477 characters net, ≈ +7 lines, taking the file to ~378 — "under 380", and far inside the ≤ 500-line body constraint.
- Apostrophe and dash typography match between spec and destination (ASCII `'`, U+2014 em dashes), so copying the pinned text introduces no typographic inconsistency.
- The file's other four `snapshot` mentions are at lines 40, 131, 328, 370 — in § "Spawn once…"'s opening, the liveness passage, § "Your buffer is shared…", and § "On every invocation" respectively, exactly as the plan enumerates; none asserts residue-only content or unscoped supersession. `src/skills/architect-editor-engine/SKILL.md`, `src/agents/editor.md` and `src/commands/command-handoff.md` contain no occurrence of "snapshot" at all; the only other repo hits are `roadmap-prune`'s *prune snapshot* and `temporal-tree`'s *roadmap snapshot*, unrelated senses. The blast radius is one paragraph, as stated.
- The frontmatter `description:` makes no claim about snapshot content — correctly left untouched.
- `active/skills/agent-architect` is a symlink into `src/skills/agent-architect`, so one file is genuinely one edit; no second surface to keep in sync.
- The sweep run now returns eight paths: the target, the roadmap, spec `159-`, phase note `152-`, this plan, plan-review-1, `.ai-factory/notes/07-architect-buffer.md`, and `.ai-factory/handoffs/29-…`. The plan's exception list classifies all seven non-target paths by category with nothing left over — plan-review-1 falls under its own "any plan-review or review artifact of this task" clause, which is why the list absorbed the new path without editing. Writing the plan as a rule rather than a tally is what made that work.
- The invariant holds against the pinned replacement: "only" is dropped, "never an inventory of the session" is reworded into "an inventory, not a history", and "stale next action" is continued by a semicolon — and none of the three anchors occurs anywhere else in the target file (lines 73, 76, 78 are the only hits).
- `docs/paired-loop.md` § "How the memory begins, and how it survives" carries both governing claims the change leans on: the break-destroys-the-reasoning paragraph ("What a break destroys is the reasoning: why one course was taken over another, which premise proved false and how it was shown false, which correction came from the user and in what words") together with the durable-artifacts-survive fact in the same paragraph, and "A newer snapshot supersedes the older one's next action, and nothing else." The plan's instruction to take only the operative fact and leave the series-is-a-history argument (the doc's own next paragraph) to the document is exactly right — that argument addresses a reader the skill's executor is not.
- `Testing: no` is right — skill-body prose has no silently-failing surface. `Docs: no` matches the two governing claims already standing in the doc (see the deferred observation below for the one that does not).

### Context Gates

- **Architecture** — OK. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is not crossed: no new skill, no `loads:` edge, no restatement of an always-loaded guarantee. Its rule "where a skill leans on such a guarantee, it names it in one sentence at the point of reliance" is scoped to the always-loaded layer (skill-description-field + global CLAUDE.md); `docs/paired-loop.md` is a governing spec and not part of that layer, so the rule does not fire and the plan's no-citation-aside instruction is consistent with it.
- **Rules** — WARN (non-blocking). `.ai-factory/RULES.md` does not exist in this repository, and neither does `.ai-factory/skill-context/aif-review/SKILL.md`, so no project convention file could be applied. Unchanged from plan-review-1.
- **Roadmap** — OK. The task is the first `[ ]` line of Phase 58 in `.ai-factory/roadmaps/trickster77777.md` — the seam. Linkage is complete and was walked to the leaf: contract line 58.1 → `Spec:` `159-…md` → phase note `152-…md` → `Governing spec: docs/paired-loop.md` → the target paragraph in `src/skills/agent-architect/SKILL.md`. Every level is consistent with the plan, and the contract line's "Touch only these two sentences; 58.2 and 58.4 own the rest" is honoured by both edit tasks and by the neighbour check in verification. Siblings 58.2 and 58.4 own disjoint sentences of the same paragraph (the digest sentence and the two-occasions sentence), so order is free, as spec `159-` states.

### Findings

None. The plan is implementable as written: the deliverable is pinned upstream and the plan says so, the anchors are located by text rather than by position, the re-wrap consequence is named once and relied on, the neighbours that must not move are named by their own end and start text, and verification now has both halves.

### Positive Notes

- The fix for finding 1 went past what the finding asked. Adding the clause to the enumeration would have closed it; the plan instead names the clause *and* demotes the whole enumeration to a reading aid, then spells out the exact failure it is guarding against — "a replacement that satisfies every entry above while dropping the reasoning clause has lost the task's entire purpose" — and shows that two later entries are unreadable without the clause they depend on. That is the difference between patching a symptom and closing the reading that produced it.
- The two verification steps are correctly split by what each can prove, and each says so about the other. The positive check ends on the observation that a truncated rewrite "would also achieve" a clean sweep; the sweep step ends on "neither alone finishes the task". A reader who runs only one knows what they have not established.
- The sweep's exception list is a rule with worked cases — "does this file quote one of the two passages as ground truth for a standing claim about the present?" — plus an explicit stop-and-report for an unclassified hit and an explicit "do not 'fix' the grep pattern". It carries no count, which is why the arrival of plan-review-1 in the sweep output between the two reviews changed nothing about it.
- "Verbatim means the words and their order, never the newlines" is stated once and then leaned on by three later instructions rather than repeated in each. The consequence the plan draws from it — later greps against this passage match per line, never per sentence — is the property that makes both verification steps trustworthy.
- The restraint bullets are the ones that actually prevent damage: forbidding the `docs/paired-loop.md` citation aside that a well-meaning implementer would add to a file that cites no document anywhere today, and pinning the neighbours by their exact text rather than by line number.

## Deferred observations
- Affects: Phase 58 / `docs/paired-loop.md` — The thickness default this task writes into the skill body has no home in the phase's governing spec. `docs/paired-loop.md` § "How the memory begins, and how it survives" carries the break-destroys-the-reasoning claim and the scoped-supersession claim that 58.1's other two changes lean on, but nothing in it distinguishes a thick snapshot from a thin one or names which is the default; the rule's only homes are phase note `152-…md` (recording the user's own ruling verbatim, plus the "pointer list for raising context" form) and the roadmap's contract line. After this task lands, the skill body states a policy its governing spec does not, which is the docs → roadmap → code direction running one level short at the top, and is what a later `aif-docs` verification pass would have to reconcile. Whether `docs/paired-loop.md` gains a thickness paragraph is a phase-level decision about a file 58.1 does not touch, and no sibling in Phase 58 owns `docs/` either — 58.4 only cites the section, and note `152-` closes with "Nothing under `docs/` is written." 58.1 is correct to follow its own spec and keep `Docs: no`.
- Affects: Phase 58 / `.ai-factory/specs/trickster77777/162-a-handoff-phrased-request-can-mean-the-snapshot.md` — Carried forward from plan-review-1 and still live: `src/skills/agent-architect/SKILL.md` contains no `docs/` reference anywhere today (re-checked). 58.1 deliberately leans on `docs/paired-loop.md` by consequence and forbids any citation aside, while sibling 58.4's contract line requires its new routing sentence — in the same paragraph — to "cite `docs/paired-loop.md` § 'How the memory begins, and how it survives'". If both land as specified, one paragraph ends with an explicit doc citation in its opening sentence and a deliberate absence of one in the two that follow, and the file gains its first `docs/` reference by way of the sibling rather than by a decision either spec states. Whether a skill body cites its governing spec is a phase-level call; 58.1 is right to follow its own spec here.

PLAN_REVIEW_PASS
