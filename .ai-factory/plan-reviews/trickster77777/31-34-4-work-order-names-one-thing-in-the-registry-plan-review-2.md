# Plan Review — 34.4 `work-order` names one thing in the registry (round 2)

## Code Review Summary

**Files Reviewed:** plan (1) + target (1): `docs/reserved-words.md`; plus round 1 of this plan review, the task spec `.ai-factory/specs/trickster77777/113-work-order-names-one-thing-in-the-registry.md`, the contract line at roadmap line 187 under the `### Phase 34` header, `.ai-factory/ARCHITECTURE.md`, `CLAUDE.md`, the grove root `../CLAUDE.md`, `src/skills/roadmap-engine/SKILL.md` § "The two-tier artifact", `docs/reference-by-name.md`, `docs/sakshi-harness/skill-cycle.md`, `src/global/CLAUDE.md`, the four paired-loop surfaces (`src/skills/agent-architect/SKILL.md`, `src/skills/architect-pairing-engine/SKILL.md`, `src/skills/architect-editor-engine/SKILL.md`, `src/agents/editor.md`), and the orchestrator's `planner.md` and `implementer.md` prompts
**Risk Level:** 🟢 Low — all three round-1 findings are closed in the register they were raised in, the one surviving checkbox reproduces byte-exact against the file, and every factual claim the plan now carries is true against ground truth. Nothing new was found.

### Context Gates

- **Architecture** — PASS. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is undisturbed: no skill added or re-tiered, no `loads:` edge touched, no engine gains policy, no lens inlines mechanism. The change lands on the language-contract surface `ARCHITECTURE.md:56` names ("the semantic contract every skill body, `description:`, roadmap, and spec is written in; specified in `docs/reserved-words.md`") — the registry's naming, not any skill's mechanism. Delivery is clean and re-verified: `active/` holds `skills/`, `commands/`, `agents/` and `CLAUDE.md` only, never `docs/`, and `upstream/ai-factory/` has no counterpart to `docs/reserved-words.md`, so `scripts/sync-upstream.sh` cannot clobber the edit. The file reaches every session through the grove root's `@skills/docs/reserved-words.md` import at `../CLAUDE.md:14`, so the edited text is live the moment it lands, with nothing to regenerate.
- **Rules** — WARN (non-blocking). `.ai-factory/RULES.md` does not exist in this repo, and neither does `.ai-factory/skill-context/aif-review/SKILL.md`; there is nothing to check on either surface. The governing conventions live in `CLAUDE.md`, `.ai-factory/ARCHITECTURE.md`, `docs/reserved-words.md` and `docs/using-the-language.md` — all read and checked below.
- **Roadmap** — PASS. Line 187 of `.ai-factory/roadmaps/trickster77777.md` is the first `- [ ]` line in the file — the seam — and it is 34.4. The plan's `# Plan:` heading matches the contract line's title verbatim, backticks included. The `Spec:` tag resolves to `.ai-factory/specs/trickster77777/113-work-order-names-one-thing-in-the-registry.md`, which exists and was read in full; the plan implements it and adds nothing beyond it. `### Phase 34` names no `Governing spec:` and no phase note, so the chain runs contract line → task spec → `docs/reserved-words.md` and terminates at the file the change is made in.
- **Vocabulary** — PASS. No protocol token is touched: the `Spec:` tag, `PLAN_REVIEW_PASS`/`REVIEW_PASS` and `## Deferred observations` sit outside the edited span, and the edit removes prose only. `task spec`, `contract line`, `two-tier`, `work-order` and `architect` are used at their registry meanings throughout the plan. The edit is the registry conforming to its own bidirectional rule, so it strengthens the gate rather than testing it.

### Round-1 findings — all closed

**1. The licensing sentence is now in the plan (was Critical).** § Context, paragraph 2, quotes the banner and answers it in the plan's own words: "The file's first line reads `> This document is final and is not subject to update.` — and this edit is licensed anyway, because it is not an update to the registry's meanings, which that line forecloses. It removes a collision the registry's own bidirectional rule already forbids: the document is being made to obey itself. The set of terms it binds is unchanged — no term is added, retired, or given a new meaning."

This is the right repair and it lands in the right place. `orchestrator/orchestrator/prompts/implementer.md` § "Step 0"/"Step 1" was re-read to confirm the reason it mattered: the implementer reads `.ai-factory/ARCHITECTURE.md`, `.ai-factory/RULES.md` (absent here) and **the plan file** — never the roadmap line, never the task spec. The rationale that existed twice upstream now exists once where the implementer will actually meet it, ahead of the banner it must reconcile. The escalation path round 1 predicted is closed off before it opens.

**2. The guard-and-count checkbox is gone (was Minor).** Old checkbox 2 — "Leave the rest of the registry untouched", whose one operation was a diff count that could only fail where checkbox 1 was already ignored — has been removed, and its two real constraints survive where round 1 asked for them: inside checkbox 1, under "Negative space, read while making the edit". The § "Paired loop" `- **architect** —` entry keeping `drafts work-orders`, and the `contract line` / `two-tier` / `governing spec` entries not being rephrased to compensate, are both there. The implementer now reads them while making the edit rather than counting after it. A plan closing the phase that removes the count-block register no longer reproduces that register.

**3. The read-only sweep is now a stated fact (was Minor).** Old checkbox 3 — `Files: (read-only — no edits expected)`, ending in a "report it" instruction `implementer.md` § 2.1 forbids — is gone, its content demoted to § Context: "Blast radius is already established and needs no re-derivation." The plan now carries exactly one checkbox, with one file path and one deliverable, satisfying `planner.md` § "Task Description Requirements" and its rules 2 and 3.

### Verified against ground truth

- **The pinned edit reproduces byte-exact.** The plan's quotation of the current entry is character-for-character the text of `docs/reserved-words.md:25` after the em-dash — compared programmatically, not by eye. Applying the plan's operation to a scratch copy yields a diff of exactly `25c25`, one line, nothing else touched, and the result is `- **task spec** — one task's full specification file under `.ai-factory/specs/`, written through `note`, referenced by the contract line's tag.` — which is the ending the plan pins literally. The bold term, the em-dash and both inline-code spans are unchanged, as the negative space requires.
- **`work-order` drops from two occurrences to one.** Before the edit the file has hits at lines 25 and 95; after it, only line 95 — the § "Paired loop" architect entry, `drafts work-orders`. That is the task's stated goal, reached without touching that line.
- **The blast-radius claim is exactly right, re-derived rather than trusted.** `grep -rln work-order` over `docs/`, `src/`, `CLAUDE.md`, `AGENTS.md`, `README.md`, `.ai-factory/ARCHITECTURE.md` and the grove root `../CLAUDE.md` returns eight files: `docs/reserved-words.md` itself plus the seven the plan names, and no others. Every hit in the seven names the paired-loop artifact; the only place `work-order` sits near `spec` inside those files is `architect-editor-engine/SKILL.md:24` ("a pinned apply work-order: the receiver makes exactly the edits **specified**"), which is the apply artifact, not a task spec. `docs/reference-by-name.md:27` and `src/global/CLAUDE.md:59` do name a work-order and a spec as separate things in one sentence, as the plan says.
- **Nothing is orphaned.** `grep -rn "implementation-tier\|implementation tier"` across this repo, the sibling `orchestrator/` and the grove root returns only the target line plus the planning artifacts that describe it. No surface holds a copy of the phrase.
- **No paraphrase has to move.** `CLAUDE.md:24` and `../CLAUDE.md:10` both summarise the registry as "(phase, task, contract line, task spec, seam, engine, lens, skill description, prune, …)"; neither mentions a work-order or a tier. `.ai-factory/ARCHITECTURE.md:56` names the file, not its entries. `Docs: no` is therefore correct even though the change lands in `docs/` — the edit *is* the doc change the task asks for.
- **The removed phrase carries no fact that loses its home.** This is the one claim the plan asserts rather than proves, and it holds: `src/skills/roadmap-engine/SKILL.md` § "The two-tier artifact" states "the contract line lets the user verify intent … the task spec holds the full implementation detail", and 34.1 added the paragraph naming the three parts directly below it. The tier fact lives at the engine, and the registry's own rule at line 15 — "it indexes names, it never re-homes facts" — is what makes carrying it in the entry wrong in the first place. After the removal `tier` survives in § "Roadmap artifacts" on `phase` ("the strategic tier") and on `two-tier`; "implementation-tier" was a third sense cutting across the `two-tier` entry immediately below it.
- **`Testing: no` is correct** under `test-philosophy`: one prose clause deleted from a markdown glossary, no executable surface, nothing that can fail silently. `Logging: minimal` is vacuous here rather than wrong.
- **The wrap instruction is right.** `docs/reserved-words.md` is one unwrapped line per entry — line 25 is 176 characters — so "do not re-wrap or reflow" forecloses the one plausible drift an implementer used to hard-wrapped skill bodies would introduce.

### Positive Notes

- Round 1's three findings were closed by *removal and relocation*, not by accretion: the plan got shorter, the checkbox count went from three to one, and the only text added is the sentence that was genuinely missing. A revision that answers a review by deleting two checkboxes is the rarer and better shape.
- The negative space is now where it does work — read at the moment of the edit rather than checked after it — and it names the two drifts an agent would actually make: tidying the neighbouring entries to compensate for a removed tier, and reflowing a 176-character line.
- The edit stays pinned three ways that agree with each other and with the file: the current text quoted in full, the operation stated ("remove the final clause together with the semicolon joining it"), and the resulting ending given literally. The landing is checkable against a string, not against a reader's judgment.
- The plan resists the available gold-plating: it does not add a `work-order` entry to the registry, does not touch § "Paired loop", and does not reach into the four skills that use the word — correct against a contract line pinning "no term is added or retired".

## Deferred observations

- Affects: a later phase (this task's contract line pins "no term is added or retired", so it cannot be closed here) — once the phrase is gone, `work-order` names exactly one concept but has no entry of its own in the registry: § "Paired loop" lists `architect` and `editor`, and the word survives only inside the architect's entry, where a reader looking it up will not think to search. Its sibling concepts each got a bullet; this one is defined by implication in a sentence about someone else. Worth a registry entry with its home named — `agent-architect`, `architect-editor-engine` — when a later task is licensed to add one.
- Affects: unknown (plan-file format, not this task) — this is the only plan of the 31 in `.ai-factory/plans/trickster77777/` with no `### <group>` subheading under `## Tasks`, including the one other single-checkbox plan (`02-19-2`). `planner.md`'s format block shows the subheading, so the folder style and the template agree with each other and not with this file. It changes nothing for the implementer, which scans for `- [ ]`, and on a one-task plan the heading would only restate the checkbox subject — so it is not worth a round here, but the template and the one-task case disagree and only the template is written down.

PLAN_REVIEW_PASS
