# Plan Review — 34.3 no work-order is required to carry a check list (round 1)

## Code Review Summary

**Files Reviewed:** plan (1) + targets (4): `src/skills/agent-architect/SKILL.md`, `src/skills/architect-pairing-engine/SKILL.md`, `src/agents/editor.md`, `src/skills/architect-editor-engine/SKILL.md` (read-only); plus the task spec `112-…`, the contract line at roadmap line 186 with the `### Phase 34` header and its direction preamble, `.ai-factory/ARCHITECTURE.md`, `CLAUDE.md`, `docs/reserved-words.md`, `docs/using-the-language.md`, `docs/sakshi-harness/skill-cycle.md`, `docs/reference-by-name.md`, and the round-2 plan review of the sibling task 34.2
**Risk Level:** 🟡 Medium — four prose deletions across three files, every position, quotation and blast-radius claim reproduces byte-exact against the files; but the plan's own closing sweep contradicts the text the plan itself prescribes three tasks earlier, and the one sentence that is rewritten rather than deleted is the only deliverable in the plan left unpinned.

### Context Gates

- **Architecture** — PASS. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is undisturbed: no skill added or re-tiered, no `loads:` edge touched (`agent-architect`'s `loads: architect-editor-engine architect-pairing-engine` is explicitly pinned unchanged), no engine gains policy, no lens inlines mechanism. The change moves in the correct direction for the model: the *policy* skills (`agent-architect`, `architect-pairing-engine`) drop a required component, while the *format engine* (`architect-editor-engine`) keeps its one generic word `self-verifies` — mechanism untouched, policy thinned, which is exactly where the two tiers say each change belongs. Body caps intact: all three edited files shrink.
- **Rules** — WARN (non-blocking). `.ai-factory/RULES.md` does not exist in this repo; nothing to check against. The governing conventions live in `CLAUDE.md`, `.ai-factory/ARCHITECTURE.md`, `docs/reserved-words.md` and `docs/using-the-language.md` — all checked below.
- **Roadmap** — PASS. Line 186 of `.ai-factory/roadmaps/trickster77777.md` is the first `- [ ]` line in the file — the seam — and it is 34.3. The plan's `# Plan:` heading matches the contract line's title verbatim. The `Spec:` tag resolves to `.ai-factory/specs/trickster77777/112-no-work-order-is-required-to-carry-a-check-list.md`, which exists and was read in full; the plan covers all four of its obligations (drop from `agent-architect`'s list, drop from both `architect-pairing-engine` enumerations, restate `editor.md`'s apply-mode verification, leave `architect-editor-engine` alone). `### Phase 34` names no `Governing spec:` and no `Phase note:`, so the chain runs contract line → task spec → the four files, and it terminates at code.
- **Vocabulary** — PASS. No protocol token is touched: `APPLY-EDIT` and `REPORT-ONLY` stay byte-exact and are pinned out of scope, the `Spec:` tag is untouched, and `architect-editor-engine`'s "held byte-exact, like `PLAN_REVIEW_PASS`" sentence is not in any edited span. `work-order`, `editor`, `architect` are used at their registry meanings; the plan does not deepen the `work-order` collision that 34.4 removes.

### Critical Issues

**1. The closing sweep in task 4 contradicts the text task 3 prescribes — the plan carries the exact defect it is removing.**
Task 4 states: sweep `src/` and `docs/` for `self-verif` **and `verify command`**, and "the only remaining hits should be `architect-editor-engine`'s `self-verifies` and `editor.md`'s heading and `description:`". But task 3's own prescribed rewrite of `src/agents/editor.md` keeps the phrase — "running a verify command only where the work-order pins one" — and so does the task spec, which requires the editor to "run a verify command only where the order pins one". Measured against the files as they stand, the post-edit sweep produces:

- `self-verif` → `editor.md:6` (`description:`), `editor.md:71` (heading), `architect-editor-engine:24` (`self-verifies`) — three hits, exactly as the plan predicts;
- `verify command` → one hit, `editor.md`'s newly written body sentence, which is **not** on the plan's allowed list and is not the heading or the `description:`.

An implementer running the plan's own acceptance check reads a legitimate, spec-mandated hit as a failure. The two ways out are both wrong: re-edit `editor.md` to make the sweep return clean, which deletes the clause the task spec requires and silently reverses task 3; or spend a round flagging the contradiction back. This is the third of the three shapes the contract line indicts by name — "a count contradicting a guardrail in the same order, so that it could only pass by breaking one" — reproduced inside the plan that removes them.

Repair: either drop `verify command` from the swept strings and sweep `self-verif` alone (the phase preamble's own claim is "self-verify prescribed in exactly four files", so `self-verif` is the sweep that carries the task's meaning), or keep it and name `editor.md`'s new body sentence as the fourth expected hit, quoted. Do not resolve it by changing the sentence.

**2. The one sentence that is rewritten rather than deleted is left unpinned — "e.g." on the plan's only authored text.**
Task 3 says to "rewrite the apply-mode clause … — e.g. 'read the diff back in apply mode, running a verify command only where the work-order pins one'". Every other edit in this plan pins its result byte-exact, and the sibling plan 34.2 pinned even its inserted paragraphs as unwrapped code blocks. Here the deliverable *is* the wording — the whole task is one sentence — and it is handed over as an example. Two consequences, not one: the resulting text is unverifiable at review (there is no string to check the landing against), and finding 1's sweep keys on a string the plan does not actually fix, so whether the sweep passes depends on wording the implementer chooses. An implementer who writes "run the pinned verify command where one is given" satisfies task 3 and still fails task 4; one who drops the phrase entirely satisfies task 4 and drifts from the task spec's own wording.

Repair: pin the full replacement sentence as a code block, from "Before you report" through "in analysis mode.", so the byte-identical analysis-mode clause and the new apply-mode clause are both fixed and the wrap is decided rather than described.

### Minor Issues

**3. `editor.md`'s stated wrap width is measured off a heading, not off prose.**
Task 3 says "Re-wrap to the file's existing width (~60–75 chars)". The 60 is `editor.md:71`, the section heading `## Self-verify, flag every judgment call, escalate ambiguity` — not a wrapped line at all. The file's body wraps at 67–77 (the target paragraph, lines 73–83, runs 67–75; the paragraph below it, 85–89, runs 73–77). A lower bound of 60 invites a paragraph visibly narrower than its neighbours, which is the defect the instruction exists to prevent. State ~67–77, or moot it by pinning the sentence per finding 2. The other two widths reproduce: `agent-architect` 65–78 across the affected paragraph, `architect-pairing-engine` 69–77 across both.

**4. Task 2's second edit gives an operation and a result that disagree about a comma.**
The instruction is to drop `self-verify command` from line 46; the pinned result is "every pinned value, guardrail and the \"do not commit\" inside it", which also drops the comma after `guardrail`. Read literally the operation yields "guardrail, and the" (or a double space, under a literal `sed`). The pinned result is unambiguous and governs, so this is recoverable — but the sister edit on line 38 states its deletion span as `self-verify commands, ` *including* the trailing comma-space, so the two halves of the same task use two different conventions. Make edit (2) drop `, self-verify command` and the operation matches its own stated result.

### Verified against ground truth

- **Every position claim reproduces.** `agent-architect` line 155 opens `You author your own prompt in exactly one case`, the paragraph runs 155–161, and the self-verify clause is line 159; `## Verify the report by fact` is line 212. `architect-pairing-engine` lines 38 and 46 hold the two enumerations, both in § "The deciding half". `editor.md`'s § "Self-verify…" is line 71 and the sentence spans 73–75.
- **Every quotation is byte-exact**, including the straight quotes around `"do not commit."` in `architect-pairing-engine` (the file carries no curly quotes) and the bold `**"do not commit."**` in `agent-architect`.
- **The "no prose numbers the list" claim holds.** `grep -w "four\|three\|Four\|Three"` returns nothing in any of the three edited files, so deleting a component leaves no stale count anywhere. `agent-architect`'s other eleven `work-order` mentions (lines 5, 35, 91, 165, 183, 187, 190, 208, 218, 236, 244, 246) were each read: none enumerates the components, so line 159 really is the single home in that file.
- **The blast-radius sweep's premise is true today.** `self-verif` across `src/` and `docs/` returns exactly the four hits the phase preamble claims ("self-verify prescribed in exactly four files"), and nothing outside those two trees — `CLAUDE.md`, `AGENTS.md`, `README.md`, `.claude/` all clean. `docs/` describes the paired loop (`skill-cycle.md:29`) without ever enumerating a work-order's components, so **Docs: no** is the right setting.
- **Leaving `architect-editor-engine` alone is correct, not an omission.** Its line 24 says the `APPLY-EDIT` receiver "makes exactly the edits specified, self-verifies, and does not commit" — a generic word for a step that still happens, since the diff read-back survives. Editing it would push policy into the format engine, against `.ai-factory/ARCHITECTURE.md`'s own tiering.
- **`§ "Verify the report by fact"` genuinely stands alone afterwards.** Lines 212–219 require the architect to run its own greps and reads against the real files, confirm cross-references held, and check the reporter's judgment calls "on the file, not on the note" — the check the contract line credits with catching every landing. Nothing in it depends on a work-order having carried commands, so the removal leaves no dangling reference.
- **The residual asymmetry is ratified, not a defect.** After the change `agent-architect` no longer *requires* a verify command while `editor.md` still says what to do "where the work-order pins one" — silence is not prohibition, and the task spec prescribes that exact shape. `architect-pairing-engine`'s applying half is likewise unaffected: it composes an arriving decision into its own `APPLY-EDIT`, and its line 74 ("The deciding half verifies what landed against the files") already rests on the architect's file-check rather than on a command block.
- **Delivery is clean.** `active/skills/agent-architect`, `active/skills/architect-pairing-engine` and `active/agents/editor.md` are live symlinks into `src/`, so all three edits go live with no `active/` work; `upstream/ai-factory/` holds no counterpart for any of the four files (only `aif-architecture` matches the grep), so a re-sync cannot clobber them. The working tree carries only this task's own two untracked plan artifacts.

### Positive Notes

- The negative space is pinned where an implementer would actually drift: no replacement component, no naming the diff read-back in `agent-architect`, `§ "Verify the report by fact"` named as untouchable *with the reason* it must stay, `architect-editor-engine` named as read-only rather than merely unmentioned, and `.ai-factory/` history explicitly excluded so no already-sent work-order is retracted.
- Task 4 is the right shape for a pure-deletion task: a dependency-ordered confirmation step that names the expected residue rather than a new edit. Its content is wrong (finding 1), but a plan that deletes a component from three files and then proves the fourth home does not exist is doing the harder half.
- Quoting each target string in full, in place, with its surrounding clause, makes each deletion a single unambiguous span — for three of the four edits there is nothing left to re-derive.
- The task order is right: the two enumerations that mirror `agent-architect` are cut "in lockstep with the above", so no intermediate state has the pairing engine promising a component the generic skill no longer defines.
