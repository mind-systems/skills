## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/21-28-1-roadmap-outline-deep-…md`
**Task spec:** `.ai-factory/specs/trickster77777/102-roadmap-outline-deep.md`
**Files targeted:** 4 (`src/skills/roadmap-outline-deep/SKILL.md`, `active/skills/roadmap-outline-deep`, `CLAUDE.md`, `src/skills/roadmap-prune/SKILL.md`)
**Risk Level:** 🟡 Medium — the design is sound and every cited anchor verifies; one verification check is vacuous as written.

### Context Gates

- **Architecture** (`.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy") — PASS. The new skill is a lens with content of its own (the ~200–500 preamble budget, the `Phase note:` pointer format), not a pure router; it loads `roadmap-engine` and `note` rather than inlining them, and declares the edge on the caller side only. Both engines already carry their reverse-graph markers (`src/skills/note/SKILL.md:21-22`, `src/skills/roadmap-engine/SKILL.md:21-23`), so the plan's "neither engine needs an edit" holds. The one-caller-therefore-no-extraction argument for keeping the format inside the new skill matches the repo's stated ≥2-callers rule.
- **Rules** (`.ai-factory/RULES.md`) — WARN, file absent; no rule set to check against. `.ai-factory/skill-context/aif-review/SKILL.md` is likewise absent.
- **Roadmap** — PASS. `.ai-factory/roadmaps/trickster77777.md:122` carries 28.1 with `Spec: .ai-factory/specs/trickster77777/102-roadmap-outline-deep.md`; the plan matches the contract line and the spec point by point, including the cut (`note`, `roadmap-engine`, `roadmap-outline`, `task-rescue` untouched; `task-rescue` is 28.2's).
- **Governing spec** — Phase 28's header carries no `Governing spec:` line, so the governing surface is the already-committed `docs/sakshi-harness/skill-cycle.md` § "Углубление phase — `roadmap-outline-deep`". The plan conforms to it exactly: the pass runs after `aif-docs`, the note states what diverges now with pointers into `docs/`, the preamble compresses to ~200–500 characters and closes with a repo-root-relative `Phase note:`, and an absent document is recorded as a governing-spec hole owned by `aif-docs` without writing under `docs/`.

**Anchors verified against the files** (all correct): `src/skills/roadmap-decompose-skeleton/SKILL.md:1-14` frontmatter shape and field order; `src/skills/roadmap-outline/SKILL.md:40-41` (links allowed in preamble prose) and `:25` (the three prohibitions, none of which reaches an inline link); `src/skills/note/SKILL.md:50-58` (Step 3 mints only a new numbered file — no update-in-place path, so the re-run rule is genuinely load-bearing); `src/skills/roadmap-engine/SKILL.md:69-75` (Spec destination through `note`'s hook); `src/skills/aif-docs/SKILL.md:19`, `:26` (present-tense governing-spec genre); `CLAUDE.md:74` and `:189` (the two enumerations); `src/skills/roadmap-prune/SKILL.md:304`, `:306`, `:399`, `:431`. The normalized span `only through the pruned [x] lines' Spec: tags` is present exactly twice pre-edit (`:306`, `:431`), so the plan's before/after negative check is non-vacuous — and it does require the whitespace-normalized read the plan mandates, since at `:431` the span straddles a line break. `active/skills/` siblings all use the `../../src/skills/<name>` form and are tracked in git; `git diff HEAD -- docs/` is currently empty and `docs/sakshi-harness/skill-cycle.md` already carries the skill twice (`:19` prose section, `:67` `## Схема`).

### Critical Issues

**1. The `CLAUDE.md` count check is satisfied by a half-done edit — `roadmap-outline-deep` is already in that file once.**
The plan's verification says: *"`roadmap-outline-deep` in `CLAUDE.md` → ≥2, one per enumeration."* But `CLAUDE.md:33` — the documentation-index row for `docs/sakshi-harness/skill-cycle.md` — already contains `roadmap-outline-deep`, committed with the docs change that preceded this task (`grep -c` on the current file returns **1**, and `git diff HEAD -- CLAUDE.md` is empty). So after inserting the skill into `:74` alone, the count is 2 and the check passes with `:189` skipped — the exact failure mode the plan itself diagnoses one paragraph earlier for prune's Step 8 (*"a bare token count passes with the widenings skipped"*), applied to its own weakest check.
Fix inside this task: state the pre-existing `:33` occurrence, raise the count to ≥3, and check the two enumerations site by site — `:74` ("The active set") and `:189` ("Everything else in `src/skills/` is ours") each carry the name — the same inventory discipline the plan already prescribes for `roadmap-prune`. Note that `:33` is *not* a third list to maintain (it is prose inside a doc-index row, already correct); only the count arithmetic is wrong.

**2. A third "tags"-only sentence survives inside the very step being widened (`src/skills/roadmap-prune/SKILL.md:272`).**
The plan enumerates the sites to widen as `:306`, `:431` and Step 8's `:396-399`. Step 5's own opening line is `:272` — *"Run this after Step 4 and before Step 6. **Tags are captured** before any ROADMAP.md line is deleted."* — and Step 6 opens at `:328` with *"only after Step 5's **tag capture** has run"*. After this task, Step 5 captures two things, and `:272` is the sentence that states the capture-before-deletion ordering for the step as a whole; the phase-note capture depends on that ordering more sharply than the `Spec:` capture does, because the surface it reads (the phase preamble) is deleted by Step 6's emptied-phase sweep. Left as-is, the ordering invariant for the new capture is stated only in the Step 6 pointer-back this plan adds, and a later reconciliation pass reading `:272` sees a step that says it captures tags doing something else — the same 26.10 pattern the plan is otherwise careful to close inside this task.
Fix: widen `:272` (and Step 6's `:328` clause, whose paragraph is already in scope for the pointer-back edit) from "tags" to the captured tokens, in the same edit, and add both to the by-inventory verification list. Do not change what either step does — this is wording that now under-describes the step it heads.

### Positive Notes

- The re-run rule is the strongest part of the plan: it is derived from ground truth (`note` mints only new numbered files), it names the exact defect it prevents (a superseded note orphaned the moment the pointer is repointed, invisible to a prune that captures only the current pointer), and it closes the loop on both sides — the workflow branch *and* a matching `## Critical Rules` prohibition, verified by reading rather than by count.
- The token-vs-position argument for the prune capture is correct and load-bearing: `roadmap-outline:40-41` genuinely permits unrelated handoff/task-spec links in the same prose, and `roadmap-prune` genuinely holds `Bash(rm *)` — a positional key would follow one of those to a deletion.
- The verification method (whitespace-normalized read, `**` and backticks stripped from both sides, never a line-oriented `grep`) is not ceremony here — `:431`'s span straddles a line break and every quoted sentence carries code-span backticks. The plan says so and pairs the normalization set with the spans.
- The `allowed-tools` grant with no `Bash(git *)` is precedent-conformant, not an oversight: `roadmap-decompose-skeleton` states the identical resolution order at `:58` (*argument → "my roadmap" → default*) under the identical grant, and the spec pins the reason.
- Nothing in the repo scans `.ai-factory/specs/` as a directory — every consumer (orchestrator included, per `orchestrator/docs/concepts/context-model.md:20`) resolves by the literal path from a tag — so placing phase notes beside task specs introduces no reader collision, and `note`'s per-directory numbering keeps the counter shared and collision-free as `roadmap-engine:28-30` already requires.

## Deferred observations

- Affects: a future task on `roadmap-outline` / `roadmap-engine` (outside this task's cut — the spec's Guards explicitly exclude both) — nothing preserves a `Phase note:` pointer once it exists. `roadmap-engine`'s update mode offers "Rewrite — major revision … replacing its contents on confirmation" (`src/skills/roadmap-engine/SKILL.md:249-251`), and `roadmap-outline` drives that flow over the same phase headers and intros this pass rewrites. A rewrite that drops the pointer silently orphans the note: `roadmap-prune` keys on the literal token and, finding none, captures nothing, so the file survives its phase inside a directory nothing sweeps — precisely the file-with-no-owner defect this task exists to remove, re-entering through a different door. 28.2 gives the token its readers but no writer-side preservation rule. Worth a contract line of its own once the pointer has proved itself.
- Affects: the planning side / `docs/sakshi-harness/skill-graph.md` (this task must not touch `docs/`; its verification requires `git diff HEAD -- docs/` to stay empty) — the § "Воронка в один дистиллятор" paragraph at `:25` enumerates the callers of `note`: *"`roadmap-outline`, `roadmap-decompose` и `roadmap-decompose-skeleton` рендерят артефакты через `roadmap-engine`, который пишет task-spec через `note`; `command-handoff` зовёт `note` напрямую."* After 28.1 there is a second direct caller of `note` — `roadmap-outline-deep`, which passes its own destination, template and verbosity hooks without going through `roadmap-engine` — and the § "Домены — по стволам" claim that domain boundaries are derived from the `loads:` graph makes the omission visible on the next derivation. `skill-cycle.md` was updated ahead of this task per docs → roadmap → code; `skill-graph.md` was not, and the fix belongs on that same planning side, not here.
