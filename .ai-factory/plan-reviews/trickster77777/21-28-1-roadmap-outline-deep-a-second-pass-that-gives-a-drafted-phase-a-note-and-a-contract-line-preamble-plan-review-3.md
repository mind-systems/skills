## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/21-28-1-roadmap-outline-deep-…md`
**Task spec:** `.ai-factory/specs/trickster77777/102-roadmap-outline-deep.md`
**Files targeted:** 4 (`src/skills/roadmap-outline-deep/SKILL.md`, `active/skills/roadmap-outline-deep`, `CLAUDE.md`, `src/skills/roadmap-prune/SKILL.md`)
**Risk Level:** 🟢 Low — every anchor re-verifies against the files, both prior rounds' issues are closed at the source, and the round-2 aggregate count is gone rather than merely corrected.

### Context Gates

- **Architecture** (`.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy", § "Features") — PASS. `roadmap-outline-deep` is a policy skill with content of its own (the ~200–500 preamble budget, the `Phase note:` pointer form, the re-run rule), loading `roadmap-engine` and `note` rather than inlining them, with the edge declared caller-side only; both engines already carry their reverse-graph markers (`src/skills/note/SKILL.md:21-23`, `src/skills/roadmap-engine/SKILL.md:21-23`), so "neither engine needs an edit" holds. Keeping the pointer format inside its single caller matches the ≥2-callers extraction rule. `## Features` explicitly states that a single skill's own capability accumulates no row, so ARCHITECTURE.md is correctly absent from the file set.
- **Rules** — WARN, `.ai-factory/RULES.md` absent; `.ai-factory/skill-context/aif-review/SKILL.md` absent. Nothing project-specific to check against.
- **Roadmap** — PASS. `.ai-factory/roadmaps/trickster77777.md:122` carries 28.1 with `Spec: .ai-factory/specs/trickster77777/102-roadmap-outline-deep.md`; the plan matches the contract line and the spec point for point, including the cut (`note`, `roadmap-engine`, `roadmap-outline`, `task-rescue` untouched — `task-rescue` is 28.2's) and the rewrite-in-place clause.
- **Governing spec** — Phase 28's header carries no `Governing spec:` line, so the governing surface is the already-committed `docs/sakshi-harness/skill-cycle.md` § "Углубление phase — `roadmap-outline-deep`" (`:19`, plus `:67` in `## Схема`). The plan conforms: the pass runs after `aif-docs`, every drafted phase gets a note stating what diverges now with pointers into `docs/`, the preamble compresses to ~200–500 characters closed by a repo-root-relative `Phase note:` in the same path form `Spec:` uses, and an absent document is recorded as a governing-spec hole owned by `aif-docs` without writing under `docs/`. The doc's remaining readers clause (`roadmap-decompose`, `task-rescue`, the orchestrator's planner and reviewer) is 28.2's and the orchestrator repo's, correctly outside this cut.

**Round-1 and round-2 issues — all three closed.**
1. `CLAUDE.md` is now checked site by site (`:74` "The active set", `:189` "Everything else in `src/skills/` is ours"), the pre-existing `:33` doc-index occurrence is named with the "not a third list" carve-out, and the count is raised to ≥3. Re-verified today: `grep -n roadmap-outline-deep CLAUDE.md` returns exactly `:33`.
2. `:272` ("Tags are captured before any ROADMAP.md line is deleted") and Step 6's `:328` ("only after Step 5's tag capture has run") are both in scope for the same edit, with the reason stated where the implementer needs it — the phase-note capture leans on that ordering harder, because Step 6 deletes the very preamble Step 5 reads.
3. The "Seven sites" aggregate is gone, and replaced by the stronger form: *"no aggregate count stands in for the inventory here, not even a count of the sites themselves."* That removes the failure mode rather than repairing its arithmetic.

**Anchors re-verified against the files** (all correct): `src/skills/roadmap-decompose-skeleton/SKILL.md:1-14` — field order `name`, `description` (`>-`), bracket-quoted `argument-hint`, `disable-model-invocation: true`, `allowed-tools: Read Write Edit Glob Grep AskUserQuestion Skill`, `loads:` — the exact shape the plan pins, and the identical no-`Bash(git *)` grant carried under the identical resolution-order sentence at its `## Targeting`; `src/skills/roadmap-outline/SKILL.md:40-41` (plain markdown links legal in intro/preamble prose) and `:23-26` (the three prohibitions — checkbox bullet, contract line, formal `Spec:` tag — none reaching an inline link); `src/skills/note/SKILL.md:30-36` (the three caller hooks, hooks outranking folder style) and `:50-57` (Step 3 mints only a new numbered file, no update-in-place path — the re-run rule is genuinely load-bearing); `src/skills/roadmap-engine/SKILL.md:69-75` (Spec destination through `note`'s hook, per-directory numbering, `.ai-factory/specs/<slug>/<NN>-<slug>.md`); `src/skills/aif-docs/SKILL.md:19`, `:26` (present-tense governing-spec genre); `src/skills/roadmap-prune/SKILL.md:272`, `:276-279` (item 1's capture and its carve-outs), `:304` (item 3's root join), `:306`, `:328`, `:396-399`, `:431-433`, and the `Bash(rm *)` grant at `:10` — so the token-not-position keying argument is real. Nothing else in prune reads a phase preamble: Step 0.6 scans `plan-reviews/`/`reviews/`, Step 7.5 is report-only and excludes `.ai-factory/`.

**The negative check was executed, not assumed.** Under the plan's own normalization (whitespace collapsed, `**` and backticks stripped from both sides), the span `only through the pruned [x] lines' Spec: tags` occurs exactly **2** times in the pre-edit `src/skills/roadmap-prune/SKILL.md` (`:306` and `:431-432`, where it straddles a line break — a line-oriented `grep` would miss it). Both the file and the plan use ASCII apostrophes throughout (0 typographic apostrophes in either), so the comparison cannot silently fail on the quote character. The replacement wording the plan proposes for `:431` does not re-introduce the span. The check is non-vacuous in both directions.

**Edit surface confirmed complete for the cut.** `AGENTS.md` is a symlink to `CLAUDE.md`, so the two enumerations are the whole in-repo skill-list surface at this level; `README.md`'s "Invoke skills" block is a four-item sample (it omits `roadmap-decompose-skeleton`, `roadmap-prune`, `note`), not an exhaustive list, so it needs no edit; `scripts/sync-upstream.sh` names only reworked-from-upstream skills. `active/skills/` siblings all use the `../../src/skills/<name>` form. `git status` shows `src/`, `active/`, `CLAUDE.md` and `docs/` clean, so both the four-path status check and `git diff HEAD -- docs/` being empty are satisfiable as written.

### Critical Issues

None.

### Positive Notes

- The re-run rule remains the plan's strongest element and survived two revisions intact: derived from ground truth (`note` mints only new numbered files), naming the exact defect it prevents (a superseded note orphaned the moment Step 2 repoints the preamble, invisible to a prune that captures only the current pointer), closed on both sides — the workflow branch and a matching `## Critical Rules` prohibition — and verified by reading rather than by count.
- The verification section now practices what it preaches everywhere it matters: no aggregate count for `CLAUDE.md`, none for `roadmap-prune`, and an explicit statement that not even a count of the sites stands in for the inventory. The two places a count *is* used (the new skill's frontmatter and token presence) are places where a count cannot be satisfied by a half-done edit.
- The `:272`/`:328` widening is scoped as wording, not behavior — "change nothing about what either step does" — which is what keeps a clarity fix from turning into a semantic edit in a file holding `Bash(rm *)`.
- The capture keys on the literal `Phase note:` token and never on a link's position, with both halves of the reason grounded in the files: `roadmap-outline:40-41` genuinely permits unrelated handoff and task-spec links in that same prose, and `roadmap-prune:10` genuinely holds `Bash(rm *)`.
- Prune's existing carve-outs are inherited by reference rather than restated in new words — a phase still holding a `[ ]` or user-kept `[x]` task is never emptied, the always-retained last phase header contributes nothing, a preamble with no token contributes nothing — so the new capture does not fork a second rule set, and the Step 6 retain rule cannot silently delete a note whose header survives.
- The Step 8 widening is the one with a behavioral consequence and the plan identifies it as such: it is the only report that makes prune's `rm` visible, and without the widening it would under-report exactly the deletions this task introduces.
- Placing the phase note beside task specs introduces no reader collision: nothing in the repo scans `.ai-factory/specs/` as a directory — prune resolves by captured path, and `note`'s per-directory numbering keeps the shared counter collision-free as `roadmap-engine:69-75` already requires.

## Deferred observations

- Affects: a future task on `roadmap-outline` / `roadmap-engine` (outside this task's cut — the spec's Guards exclude both) — nothing preserves a `Phase note:` pointer once it exists. `roadmap-engine`'s update mode offers a rewrite that replaces a section's contents on confirmation, and `roadmap-outline` drives that flow over the same phase headers and intros this pass rewrites. A rewrite that drops the pointer silently orphans the note: `roadmap-prune` keys on the literal token and, finding none, captures nothing, so the file outlives its phase in a directory nothing sweeps — the file-with-no-owner defect this task exists to remove, re-entering through a different door. 28.2 gives the token its readers but no writer-side preservation rule.
- Affects: the planning side / `src/global/CLAUDE.md` (out of this task's file boundary — the spec's § "Files & types" pins four paths and the plan's own verification requires `git status --short -uall -- src/ active/ CLAUDE.md` to list nothing else, so touching it here would falsify the task's own check) — the global CLAUDE.md's § "Planning workflow" states the planning chain as *"`/roadmap-outline` → `/roadmap-decompose` (plus `/roadmap-decompose-skeleton` on heavy tasks)"* (`:57`). After 28.1 the chain has a step between the first two — `roadmap-outline-deep`, which `docs/sakshi-harness/skill-cycle.md` places immediately after `aif-docs` and before decomposition — and that line is the always-loaded surface in every session of every project, where `skill-cycle.md` is not loaded at all. This is precisely the gap phase 27 was raised for (27.1: behavior carried by skill bodies and `skill-cycle.md`, with the always-loaded layer silent on it), so it belongs to a task of its own on that same surface, not to this one.
- Affects: the planning side / `docs/sakshi-harness/skill-graph.md` (this task must not touch `docs/`; its verification requires `git diff HEAD -- docs/` to stay empty) — § "Воронка в один дистиллятор" (`:25`) enumerates `note`'s callers and names only `command-handoff` as a direct one: *"`roadmap-outline`, `roadmap-decompose` и `roadmap-decompose-skeleton` рендерят артефакты через `roadmap-engine`, который пишет task-spec через `note`; `command-handoff` зовёт `note` напрямую."* After 28.1 there is a second direct caller — `roadmap-outline-deep`, which passes its own destination, template and verbosity hooks without going through `roadmap-engine` — and § "Домены — по стволам" (`:31`), which derives domain boundaries from the `loads:` graph, makes the omission visible on the next derivation. `skill-cycle.md` was updated ahead of this task per docs → roadmap → code; `skill-graph.md` was not, and the fix belongs on that same planning side.

PLAN_REVIEW_PASS
