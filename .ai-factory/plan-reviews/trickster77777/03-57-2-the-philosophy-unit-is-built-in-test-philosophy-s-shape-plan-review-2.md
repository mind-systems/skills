# Plan review — 57.2 the philosophy unit is built in test-philosophy's shape (round 2)

## Code Review Summary

**Files Reviewed:** 1 plan + 12 context surfaces
**Risk Level:** 🟢 Low

**Read:** the plan; round-1 plan-review of the same task; `.ai-factory/specs/trickster77777/157-…md` (task spec, its pinned block read in full); `.ai-factory/specs/trickster77777/151-…md` (phase note, including the Russian ruling the plan now stands on); `.ai-factory/roadmaps/trickster77777.md` § "Phase 57" (lines 24–25); `CLAUDE.md` (§ "The active set" line 81, § "Everything else in `src/skills/` is ours" line 196, § "SKILL.md frontmatter (required fields)" lines 118–134, § "Dependencies and the skill graph", "Repository Structure"); `AGENTS.md` (symlink target); `src/skills/test-philosophy/SKILL.md`; `.ai-factory/ARCHITECTURE.md` reference from the note; `docs/sakshi-harness/skill-cycle.md` line 33; `docs/sakshi-harness/skill-graph.md` line 31; `src/skills/ui-ux-pro-max/SKILL.md`; `src/skills/roadmap-prune/SKILL.md` and `src/skills/note/SKILL.md` (argument handling); plus three full censuses over `src/skills/*/SKILL.md` and `src/commands/*.md` — `argument-hint`, `user-invocable`, the literal `ARGUMENTS` token, and `allowed-tools` — and `ls -la active/skills/`.

### Context Gates

- **Architecture** — pass. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" admits a skill only on content shared by ≥2 callers; phase note 151 meets that rule head-on and rules the second caller to be the user himself via `user-invocable: true`, the arrangement `test-philosophy` already carries (verified in that file's frontmatter). The plan correctly does not re-argue it.
- **Rules** — WARN. No `.ai-factory/RULES.md` and no `.ai-factory/skill-context/aif-review/SKILL.md` in this repo; the conventions were read from `CLAUDE.md` § "Skill Authoring" instead.
- **Roadmap** — pass. 57.2 is the first `[ ]` line in `.ai-factory/roadmaps/trickster77777.md` (`> Owner: trickster77777@gmail.com`), so the plan is aimed at the seam; the contract line's `Spec:` tag resolves to a file that exists; the artifact path (`plans/trickster77777/`) matches the named-roadmap stem. The phase header names note 151 as `Phase note:`, not `Governing spec:`, consistent with the note's own "No governing document exists for this phase" — there is no ratified spec above this task beyond the note.

**Round-1 findings, re-checked:**
- Finding 2 (the symlink verify could not fail on a dangling link) — **closed**. The step now dereferences: `head -1 active/skills/polymorphism-philosophy/SKILL.md` prints `---`, and the plan states in its own words why the name sweep cannot see that failure. This is the right check and it is placed on the step that can break it.
- Finding 1 (the direct-invocation input contract unpinned, two frontmatter fields riding on it) — **decided, and the decision is defensible; one of the two grounds it is written on is false against the repository.** See below.

### Findings

**1. The census sentence the `argument-hint` omission is argued from does not hold against `src/skills/`.**

Plan § "The input contract, decided", bullet **No `argument-hint`**: "CLAUDE.md § 'SKILL.md frontmatter (required fields)' lists the field, but every skill in `src/skills/` that carries one parses `$ARGUMENTS`; this unit does not".

Measured over all 23 skills in `src/skills/`: 17 carry `argument-hint`, and only 6 of those contain the token `ARGUMENTS` anywhere in the file — `agent-architect`, `aif`, `aif-architecture`, `detangle`, `roadmap-test-coverage`, `temporal-tree`. The other 11 carry the hint and never mention `$ARGUMENTS` at all: `aif-docs`, `aif-plan`, `note`, `observe-logs`, `roadmap-decompose`, `roadmap-decompose-skeleton`, `roadmap-outline`, `roadmap-outline-deep`, `roadmap-prune`, `task-rescue`, `task-rescue-audit`. They take the argument and refer to it in prose or positionally — `roadmap-prune`: "Derive the target repo root from the skill argument"; `note`: "`<slug>` derived from the topic or `$1` argument". So "parses `$ARGUMENTS`" is not the line that separates the field's carriers from its non-carriers; the line the code actually draws is *does an invocation expect a named target at all* — and under that reading the unit's space entry, which takes "a module, a directory, a class", is on the carrier side, not the exempt side. The bullet's second clause is inaccurate in the same way: "`test-philosophy` — the only other user-invocable skill taking no argument" overlooks `ui-ux-pro-max`, which carries no `argument-hint`, no `ARGUMENTS`, and no `user-invocable: false` line (it is simply unsymlinked from `active/`).

Failure scenario: no byte of the deliverable differs — the frontmatter is spec-pinned and the implementer writes it either way. What fails is the decision record, which is this plan's own product. A reader who checks the stated ground — the 57.3 planner, a later `task-rescue`, or the next plan-review — measures the census, finds it false, and a question the plan declares closed ("neither reopens the block") reopens; worse, the corrected census points the other way, so the honest next move from that sentence alone would be to route the field back to spec 157 rather than to leave the block standing.

The repair is inside this plan's boundary and does not touch the decision: state the ground that does hold. The quoted ruling in note 151 is verified verbatim ("если вызову философию тестов и **укажу область в коде и скажу что вызывает у меня подозрения**") and the note does call it "the same arrangement `test-philosophy` already has" — the plan's attribution is exact. What that ruling establishes is that the user supplies the region himself at invocation; it does not distinguish argument text from surrounding prose, and it does not need to, because nothing in the pinned body interpolates `$ARGUMENTS` — a region typed after the slash command and a region named in the next sentence both reach the agent's context identically, so the field's only function here is a UI hint, and its absence breaks no path. That is the accurate and sufficient case for leaving the block unchanged. Write it as that, drop the census clause (or restate it as measured — 6 of 17), and the section closes honestly.

### Positive Notes

The rest of the plan is ground-truth-clean; every claim it makes was re-measured against the files this round and holds.

- **The pinned block is respected as a deliverable, not a sketch.** The plan points at spec 157 § "The change" and forbids re-deriving, extending, or trimming it, and singles out the one omission a well-meaning implementer would most likely "fix" — the single-responsibility clause — with the note's own structural reason (SRP is true at every moment equally, so it has no moment to fire on). That is exactly the guard this task needs.
- **Frontmatter shape verified field-for-field** against `src/skills/test-philosophy/SKILL.md`: `name`, `description` folded with `>-`, `user-invocable: true`, `disable-model-invocation: false`, `allowed-tools: Read`, no `loads:`. The `name:`/directory byte-match requirement is cited to the right section (§ "Key constraints").
- **`allowed-tools: Read` is adequately grounded and correctly fenced.** The repo's own gloss on the field is "pre-approved tools" (`CLAUDE.md` line 125), and its practice matches: every pure-content unit — `test-philosophy`, `roadmap-engine`, `orchestrator-artifacts`, both architect engines — carries `Read` alone while working skills enumerate `Grep`/`Glob`/`Bash`. The plan's reading (the walk belongs to the invoking session, which holds its own tools) sits with that practice, and the platform's other documented reading of the field ("restrict tool access") is precisely what the plan's routing rule covers: a live direct invocation that shows the space entry blocked is a defect against the spec's pinned block and goes back there, not patched in passing. Naming the escape route instead of pre-emptively widening a pinned block is the right call.
- **Both roster spans are quoted byte-exact and the insertion points are right.** Line 81 reads `…aif-docs`, `test-philosophy`, `roadmap-outline`…`; line 196 reads `…note`, `test-philosophy`, `observe-logs`…`. Neither carries an ellipsis, so the plan's reading that both assert completeness is correct, and after-`test-philosophy` is right on both counts — the skill is in the active set and has no upstream counterpart. `grep -c "polymorphism-philosophy" CLAUDE.md` is `0` today and reaches `2` only by matching both roster lines, so the invariant is answerable.
- **Both over-reach guards re-verified true.** The "Repository Structure" skill sample does end in `└── …` and claims nothing, and § "Dependencies and the skill graph" names `test-philosophy` illustratively ("e.g. `roadmap-engine`, `test-philosophy`"). Leaving both byte-identical is what keeps the `= 2` invariant meaningful.
- **The roster blast radius is complete.** Swept independently: every file outside `.ai-factory/` naming `test-philosophy` is `CLAUDE.md`, four `docs/` files, `src/commands/command-pin-gaps.md`, and two skill bodies — none of the non-`CLAUDE.md` mentions is a roster asserting completeness over the skill set, so `CLAUDE.md` really is the only stale-able list at this task. `README.md` holds no skill roster.
- **`AGENTS.md` handled correctly** — a symlink to `CLAUDE.md`, so naming only `CLAUDE.md` in `Files:` is right, and the plan says so rather than leaving it to be discovered.
- **The symlink form is right in every detail.** `../../src/skills/<name>` is what all 22 of our-skill entries in `active/skills/` use (verified by `ls -la`); `ln -sfn` matches; "do not copy the directory" is the correct instruction for a per-item-symlink layer.
- **The known-wrong-for-one-task window is flagged, not silently fixed.** The pinned body names `roadmap-decompose-skeleton` as its caller and carries the reverse-graph grep line before any `loads:` edge exists; `CLAUDE.md` attaches that marker to the first edge's arrival, so the window is real, spec-pinned, and closed by 57.3 — and the plan tells the implementer to leave it.
- **Task order, dependencies, and verify placement are sound**: directory → symlink → rosters, with the spec's first invariant split so each half is answerable at the moment its step runs.
- **`Testing: no` is right on the repo's own rule**, not by default — a missing `SKILL.md` or a `name:`/directory mismatch fails loudly; the one silent surface in the change (a dangling symlink) is now covered by a dereferencing check rather than a test.
- **`Docs: no` matches note 151's "Nothing under `docs/` is written"**, and the documentary edge is genuinely already closed: `docs/sakshi-harness/skill-cycle.md` line 33 already names `polymorphism-philosophy` beside `test-philosophy` on the shape-of-a-distinction axis — the doc held ahead of its code, in the sanctioned direction.

## Deferred observations
- Affects: task 57.3 / `.ai-factory/specs/trickster77777/158-the-skeleton-takes-the-bead.md` — `docs/sakshi-harness/skill-graph.md` line 31 states that the package's trunks number four and names them (`note`, `roadmap-engine`, `test-philosophy`, `orchestrator-artifacts`), while declaring in the same sentence that domain boundaries are *derived* from the `loads:` graph rather than assigned. Task 57.2 creates no `loads:` edge, so the unit is still edgeless when this task closes and nothing there is due now; 57.3 is the task that adds `loads: … polymorphism-philosophy` and so the moment that census becomes answerable differently. Spec 158's blast radius sweeps only Lens 1's testability wording and the `loads:` line of its own target file and never reaches that sentence, so whether a single-caller engine counts as a trunk — and whether the number should be a number at all, per `docs/counts-go-stale.md` — arrives at 57.3 undecided.
- Affects: `.ai-factory/specs/trickster77777/158-the-skeleton-takes-the-bead.md` — its § "Deploys alone" says "Sequenced last of the three tasks in this phase", while phase 57 in the roadmap carries two tasks, 57.2 and 57.3, and no 57.1 line was ever committed. A stale count inside a neighboring task's spec, harmless to this task and outside its file boundary.
