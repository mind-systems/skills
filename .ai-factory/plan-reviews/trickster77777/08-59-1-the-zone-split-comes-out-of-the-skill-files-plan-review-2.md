# Plan review — 59.1 the zone split comes out of the skill files

## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/08-59-1-the-zone-split-comes-out-of-the-skill-files.md`
**Targets:** `src/skills/architect-editor-engine/SKILL.md` (frontmatter `description:` + § "The architect's buffer"), `src/skills/agent-architect/SKILL.md` (two pointer sentences), `src/agents/editor.md` (opening paragraph)
**Risk Level:** 🟢 Low

Three prose edits, every replacement pinned word for word in spec `164-…md` § "What must be true after". This is the second review of this plan; the three findings of review 1 are all closed — the falsified count is gone (replaced by "work the numbered list below to its end"), no blockquote in the plan carries any emphasis markup at all, and both arithmetic claims have been replaced by decisions stated without numbers. Every pinned text was re-compared against the spec programmatically and every claim about the target files was re-measured against the files; all of it holds. One finding remains, and it is in the same family as review 1's third: a list in the plan's Context that the tool it describes does not produce.

### Ground truth re-verified against the files

- **Every pinned text in the plan is a copy of the spec's, not a paraphrase.** Normalised comparison: the engine's frontmatter replacement block (4 lines plus the unchanged `the definition of the` anchor), the engine's opening paragraph, the whole head-is-only-writer paragraph, the re-read paragraph, and the channel-message paragraph all match the spec's "after" text exactly; `agent-architect`'s new pointer sentence and its shortened "path and rules are defined, so it is" fragment match; `editor.md`'s replacement passage matches end to end, closing "…and it is where that memory lives."; the closing two-architects sentence without ", settled zone included" matches. The only difference found anywhere was the plan dropping the spec's `[...]` truncation marker from the channel-message paragraph — correct, since the plan states the remainder stays word-identical.
- **No emphasis markup anywhere in the plan's quoted replacements.** A scan for `*` inside every blockquote line of the plan returns nothing. Review 1's finding 2 is fully closed, and the plan now says in Context why it must be: the paragraph being deleted uses bold for real, so markup surviving into the replacement would read as file text.
- **The "before" text the plan tells the implementer to match is what actually stands in the files.** `description:` lines 9–12 carry exactly the clause quoted; `agent-architect` line 36 is exactly the sentence fragment quoted; lines 368–369 carry the pointer clause; `editor.md` lines 21 and 26 carry both "settled zone" uses. Each locate-by-text anchor is unique in its file.
- **The 810-character claim is exact.** The current folded `description:` is 804 characters; applying the pinned replacement yields 810 against the 1024 limit. The replacement block's line lengths with the 2-space continuation indent are 79/75/77/76 — inside the 66–79 range the field's existing lines already occupy, so "take the line breaks as given" is right and needs no re-wrap.
- **The YAML stays well-formed.** All four replacement lines sit at exactly the block's 2-space indent, so none is read as a more-indented literal line inside the `>-` scalar.
- **The five engine paragraphs the plan addresses match the file byte for byte** (lines 37, 39, 41, 43, 47), and the drain-rule paragraph (line 45) is already in its post-edit form — correctly left untouched. The engine's body prose is genuinely unwrapped single lines; the two hard-wrapped files (`agent-architect` 65–76, `editor.md` 68–76 in the affected paragraphs) are correctly treated the other way.
- **Dropping `zones,` from `agent-architect` line 36 takes it from 70 to 63 columns.** The plan's own instruction — don't re-wrap, "a slightly short line here is correct" — is the right call and is stated without arithmetic this time.
- **The no-rewrap decision still buys what it claims.** 59.2 appends at the *end* of the same § "Spawn once, message thereafter" paragraph and its contract line says so ("Touch only the paragraph's end — 59.1 owns its mid-paragraph clause"). The re-wrap the plan *does* order is in § "Your buffer is shared; you alone write it", which no other pending task (59.2, 60.1, 61.x) touches — checked across every remaining `[ ]` line.
- **Both cited headings resolve and are correctly protected.** `## The architect's buffer` is cited by name from § "The mode rule" in the same file (line 33); `## Your buffer is shared; you alone write it` is cited by name twice in `agent-architect` (lines 150 and 277). Both stay byte-identical under the plan.
- **The three sites are the whole set — verified by meaning, not only by the sweep.** Greps for `private`, `architect alone`, `may not read` across `src/` and `docs/` turn up no fourth place where the buffer is described as partly closed to the editor. `agent-architect`'s independence passage (lines 307–319) rests on what the architect *releases in a message* ("where the round is settled is what counts, not who reads it"), never on a private buffer region, so the split's removal leaves it standing. `editor.md` mentions the buffer only in the paragraph being rewritten.
- **No other file quotes the replaced sentences.** Greps for the distinctive phrases (`load-bearing` in the buffer sense, `returns an echo`, `glancing at private notes`) find no second home inside `src/` — the only match for the content list is `docs/paired-loop.md` itself, which is the source, not a consumer.
- **The markup-verification criterion is satisfiable.** After the edits, § "The architect's buffer" contains no `*` at all — the only emphasis in the section today is on line 39's two zone names, which go out with the paragraph.
- **The governing text says what the plan says it says.** `docs/paired-loop.md` § "What the memory holds, and who holds it" opens with the two surviving rules verbatim and carries the same content list the engine's replacement keeps. Code is being conformed to the doc; the direction is right, and `Docs: no` is correct because the doc already stands ahead of the code.
- **The three symlinks exist** and all point into `src/`, so editing `src/` is the whole job exactly as stated.
- **The CLAUDE.md staleness claim holds.** `docs/paired-loop.md` carries no zone language today (its only sweep hit is § "Where the split falls", which is the factual/judgment split and nothing to do with the buffer), so the documentation-table row summarising it as "the buffer's two zones … why the live zone stays private" is already stale before this task runs. Correctly flagged, correctly not acted on.
- **Settings are right.** `Testing: no` — skill-body prose has no silently-failing surface per `test-philosophy`.

### Verification apparatus

The single verification task is well shaped and now covers the blind spot review 1 named: it reads each edited passage back against the spec word for word, then looks at markup *deliberately* ("a word-for-word read-back passes markup silently"), then re-runs the sweep, then confirms the three cited headings survived byte-identical, and it singles out the one item most likely to be left half-done (the closing paragraph's ", settled zone included"). Its only weak seam is the expectation it hands the implementer for the sweep's output — finding 1.

### Context Gates

- **Architecture** — OK. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is honoured: the buffer's definition stays in the engine (mechanism), the architect keeps a pointer that restates none of it, the editor's copy is the engine-held contract rather than a second home, and no `loads:` edge changes. Phase 59's own note draws the same boundary — the engine keeps "what the memory is and who writes it" — and the plan stays inside it.
- **Rules** — WARN (non-blocking). Neither `.ai-factory/RULES.md` nor `.ai-factory/skill-context/aif-review/SKILL.md` exists in this repository, so no project convention file could be applied.
- **Roadmap** — OK. 59.1 is the seam: the first `[ ]` line in `.ai-factory/roadmaps/trickster77777.md` file order (line 47). Linkage walked to the leaf: contract line 59.1 → `Spec:` `164-…md` → phase note `153-the-buffer-is-born-with-a-shape.md` → `Governing spec: docs/paired-loop.md` § "What the memory holds, and who holds it" → the three target spans. Every clause of the contract line — the three files, the two surviving rules, `.ai-factory/notes/07-architect-buffer.md` out of scope — is honoured by the plan and covered by its verification.

### Findings

1. **The Context section's enumeration of the sweep's surviving hits names two files the sweep does not return.** The plan states the sweep "returns hits outside these three files — … `roadmap-decompose` and `roadmap-prune`'s ordinary 'split' …". Run now, `grep -rniE "settled zone|live zone|two zones|the split|zone" src/ docs/` returns exactly six non-target hits: `command-pin-gaps.md:42`, `aif-docs/references/split-state-b.md:46`, `docs/paired-loop.md:67`, `docs/skill-description-field.md:13`, and two `ui-ux-pro-max` CSVs. `roadmap-decompose` and `roadmap-prune` use "split" only as an ordinary verb ("split into two entries", "the same `roadmaps/`-presence split") — never "the split", never any form of "zone" — so neither matches any alternative in the pattern. The concrete failure: the verification task tells the implementer "the surviving hits are the unrelated uses named in Context", so the check is run against a list with two phantom entries; an implementer who takes the list as the expected output sees two named files missing from their own run and has to rule on whether their grep was wrong (wrong flags, wrong directory, a truncated run) in a task where the sweep is the only mechanical guard that the edits are complete. Worth more than the arithmetic: the spec deliberately *removed* an enumeration of the false positives in favour of the category rule, saying so in § "What breaks on contact" ("This corrects an earlier version of this section, which enumerated the false positives individually instead of relying on the rule's own category — a list that a new file anywhere using either word for its ordinary meaning would make incomplete on the spot"). The plan reinstates exactly the shape the spec retired, and the reinstated list is already wrong. Fix: drop the enumeration and keep the rule the plan already states ("any hit that does not actually describe the buffer is a false positive and stays untouched"), and have the verification task classify what the run returns rather than compare it to a list. The in-file examples that *are* inside the three targets — `agent-architect`'s "a live task registry", "leaving both buffers live", "where the round is settled is what counts" — are worth keeping, since those sit in a file the implementer is editing and naming them prevents an over-edit.

### Positive Notes

- Every one of review 1's findings is closed at the root rather than patched: the count is gone rather than corrected (so it cannot go stale again), the emphasis is stripped from the quotes *and* the reason is stated up front in Context as a standing rule for the whole task, and both width claims are replaced by the decision they were supporting ("a slightly short line here is correct", "take the replacement's line breaks as given"). Each fix removes the class, not the instance.
- The plan states plainly what the replacement **drops and does not relocate** — the live zone's contents and the echo argument — and says where the one piece that still needs a home (the handle's timing) already lives. That closes the failure mode where a removed rule quietly reappears under a new name, which is what the spec forbids with "No file gains a replacement boundary of any kind".
- Treating the three edits as one deliverable, with the reason stated ("a buffer defined without zones in the engine while the architect skill still points at 'its two zones' leaves the pair reading two contradictory contracts at birth"), makes a half-landed change unable to read as done.
- Both `agent-architect` edits stay pointers, and the plan says why the pointer must not restate the surviving rule — the writer half is already the next sentence of that same section. Mechanism/policy stays where ARCHITECTURE.md puts it.
- Scope boundaries are verified rather than asserted: the live buffer note, the false-positive category, and the stale CLAUDE.md row are each named with the reason they are not this task's, and the CLAUDE.md row is raised rather than silently fixed.

## Deferred observations
- Affects: `skills/CLAUDE.md` documentation table (row for `docs/paired-loop.md`) — the row still summarises the doc as "the buffer's two zones and who holds which, why the live zone stays private". It is stale against its own target already today, since `docs/paired-loop.md` carries no zone language, and this task neither creates nor worsens it; after 59.1 lands it will be the last place in the repository outside `.ai-factory/` history where the zone split is still asserted as current. The row belongs to the documentation index, not to any of the three skill files, so correcting it needs a task that owns that file.
- Affects: Phase 59 / `.ai-factory/specs/trickster77777/165-the-buffer-is-seeded-from-a-template-at-founding.md` — the engine's post-edit content sentence ("It holds the working discipline, the rulings …, the method …, and the register of where the skills still lag the practice") no longer names the editor's handle or an assigned pairing role, both of which `agent-architect` mandates writing into the buffer. Under the old text they were named as live-zone contents; now a reader who takes the sentence as exhaustive could conclude they do not belong in the buffer at all. The wording is pinned by the spec and lifted verbatim from the governing doc, so it is not this task's to adjust — and 59.2, which seeds the buffer's rubrics from a template and whose phase note draws the boundary as "the template instantiates the memory, the engine keeps what the memory is", is where the buffer's actual contents get stated. The question has a natural home one task away.
