# Plan review — 57.2 the philosophy unit is built in test-philosophy's shape (round 3)

## Code Review Summary

**Files Reviewed:** 1 plan + 13 context surfaces
**Risk Level:** 🟢 Low

**Read:** the plan; both prior plan-reviews of this task; `.ai-factory/specs/trickster77777/157-…md` (task spec, pinned block read in full); `.ai-factory/specs/trickster77777/151-…md` (phase note, lines 9 and 17 — the SRP exclusion and the Russian ruling); `.ai-factory/specs/trickster77777/158-the-skeleton-takes-the-bead.md` (task 57.3); `.ai-factory/roadmaps/trickster77777.md` § "Phase 57"; `CLAUDE.md` (§ "Repository Structure" tree, § "The active set" line 81, § "Dependencies and the skill graph", § "SKILL.md frontmatter (required fields)", § "Key constraints", § "Everything else in `src/skills/` is ours" line 196); `AGENTS.md`; `README.md`; `src/skills/test-philosophy/SKILL.md` and its directory listing; `src/skills/ui-ux-pro-max/SKILL.md`; `docs/sakshi-harness/skill-cycle.md` line 33; `.gitignore`; `upstream/ai-factory/aif-skill-generator/scripts/validate.sh` read end-to-end; `ls -la active/skills/`; and two fresh censuses over all 23 `src/skills/*/SKILL.md` — `argument-hint` carriers and the literal `ARGUMENTS` token among them.

### Context Gates

- **Architecture** — pass. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" admits a skill only on content shared by ≥2 callers; phase note 151 line 17 meets that rule head-on and rules the second caller to be the user himself via `user-invocable: true`, the arrangement `test-philosophy` already carries (verified in its frontmatter). The plan correctly does not re-argue settled ground.
- **Rules** — WARN. No `.ai-factory/RULES.md` and no `.ai-factory/skill-context/aif-review/SKILL.md` in this repo; the conventions were read from `CLAUDE.md` § "Skill Authoring" instead.
- **Roadmap** — pass. 57.2 is the first `[ ]` line in `.ai-factory/roadmaps/trickster77777.md` (`> Owner: trickster77777@gmail.com`), so the plan aims at the seam; 56.1/56.2 above it are `[x]`. The contract line's `Spec:` tag resolves to a file that exists, and the artifact path (`plans/trickster77777/`) matches the named-roadmap stem. The phase header names note 151 as `Phase note:`, not `Governing spec:` — consistent with the note's own "No governing document exists for this phase", so there is no ratified spec above this task beyond the note.

**Prior findings, re-checked:**

- Round 1, finding 2 (the symlink verify could not fail on a dangling link) — **closed and still closed.** The step dereferences (`head -1 active/skills/polymorphism-philosophy/SKILL.md` prints `---`) and the plan states in its own words why the name sweep cannot see that failure.
- Round 1, finding 1 / round 2, finding 1 (the input contract, and the census the `argument-hint` omission was argued from) — **closed.** The plan's § "The input contract, decided" now states the census as measured and rests the decision on the ground that holds. Re-measured this round, every number in it is exact:
  - 17 of the 23 skills in `src/skills/` carry `argument-hint`. ✓
  - Exactly 6 of those 17 contain the token `ARGUMENTS` anywhere in the file — `agent-architect`, `aif`, `aif-architecture`, `detangle`, `roadmap-test-coverage`, `temporal-tree`. The other 11 carry the hint and never mention it. ✓
  - `test-philosophy` and `ui-ux-pro-max` carry no hint, and `ui-ux-pro-max` is absent from `active/skills/` — "unsymlinked" is right. ✓
  - The ruling quoted from note 151 is verbatim ("укажу область в коде и скажу что вызывает у меня подозрения"), and the note does call it "the same arrangement `test-philosophy` already has". ✓
  - "Nothing in the spec's pinned body interpolates `$ARGUMENTS`" — confirmed by reading the pinned block in full. ✓

  Crucially, the plan concedes the corrected census points the *other* way ("read as *does an invocation expect a named target*, that practice would put this unit on the carrier side") rather than bending it, and then names the narrower checkable fact that actually decides the case. That is the honest form of the repair round 2 asked for, and it leaves the record able to survive being re-measured by the 57.3 planner or a later `task-rescue`. The escape route is named too: a live invocation that shows the space entry blocked is a defect against the spec's pinned block and routes back there rather than being patched in passing.

### Findings

None. Every ground-truth claim in the plan was checked against the files this round and holds.

### Positive Notes

- **The pinned block is treated as the deliverable, not a sketch.** The plan points at spec 157 § "The change", forbids re-deriving/extending/trimming, and singles out the one omission a well-meaning implementer would most likely "fix" — the single-responsibility clause — with the note's own structural reason. Note 151 line 9 states exactly that reason ("SRP is a virtue, true or false at every moment equally, and a principle true always fires nowhere, while the unit's whole value is that it has a *moment* instead"), so the guard is cited accurately, not paraphrased into something weaker.
- **Frontmatter shape verified field-for-field** against `src/skills/test-philosophy/SKILL.md`: `name`, `description` folded with `>-`, `user-invocable: true`, `disable-model-invocation: false`, `allowed-tools: Read`, no `loads:`, no `argument-hint`. The `name:`/directory byte-match is cited to the right section (§ "Key constraints").
- **The omission breaks nothing mechanical — checked against the validator, not just against practice.** `upstream/ai-factory/aif-skill-generator/scripts/validate.sh` was read end to end: it errors on a missing `name` or `description`, a `name`/directory mismatch, a name format violation, and frontmatter over 100 words; it touches `argument-hint` only *if present* (a bracket-quoting check). The pinned frontmatter measures 51 words against `test-philosophy`'s 50, the description is well inside 1024 chars, and the body is far under the 500-line warn. The deliverable passes validation as written, which is the mechanical confirmation behind the plan's "its absence breaks no path".
- **`allowed-tools: Read` is grounded in the body's own claim** ("has no I/O of its own"), and matches the repo's practice: every pure-content unit — `test-philosophy`, `roadmap-engine`, `orchestrator-artifacts`, both architect engines — carries `Read` alone while working skills enumerate `Grep`/`Glob`/`Bash`.
- **"The precedent is a single-file skill" is literally true** — `src/skills/test-philosophy/` contains `SKILL.md` and nothing else, so forbidding `references/`, `scripts/`, and `templates/` mirrors it exactly rather than guessing.
- **Both roster spans are quoted byte-exact and the insertion points are right.** Line 81 reads `…aif-docs`, `test-philosophy`, `roadmap-outline`…`; line 196 reads `…note`, `test-philosophy`, `observe-logs`…`. Neither carries an ellipsis, so reading both as completeness claims is correct, and after-`test-philosophy` is right on both counts — the skill enters the active set and has no upstream counterpart.
- **Both over-reach guards re-verified true.** The "Repository Structure" skill sample does end in `└── …` with an already-partial trailing comment, so it claims nothing; § "Dependencies and the skill graph" names `test-philosophy` illustratively ("e.g. `roadmap-engine`, `test-philosophy`"), not as a roster. Leaving both byte-identical is what keeps the `= 2` invariant meaningful rather than decorative — `grep -c "polymorphism-philosophy" CLAUDE.md` is `0` today, and reaches `2` only by matching both roster lines.
- **`AGENTS.md` handled correctly** — verified a symlink to `CLAUDE.md`, so naming only `CLAUDE.md` in `Files:` is right, and the plan says so rather than leaving it to be discovered. `README.md` holds no skill roster, so `CLAUDE.md` really is the only stale-able list here.
- **The symlink form is right in every detail.** `../../src/skills/<name>` is what all 22 of our-skill entries in `active/skills/` use (`ls -la` confirms), `ln -sfn` matches, and "do not copy the directory" is the correct instruction for a per-item-symlink layer. `.gitignore` ignores only `active/skills/synced/`, so the new symlink is committable as a symlink.
- **The known-wrong-for-one-task window is flagged, not silently fixed.** The pinned body names `roadmap-decompose-skeleton` as its caller and carries the reverse-graph grep line before any `loads:` edge exists; `CLAUDE.md` attaches that marker to the first edge's arrival, so the window is real, spec-pinned, and closed by 57.3 — whose spec (line 30) does set `loads: roadmap-engine test-philosophy polymorphism-philosophy`. The plan telling the implementer to leave the wording alone is correct.
- **Task order, dependencies, and verify placement are sound**: directory → symlink → rosters, with the spec's first invariant split so each half is answerable at the moment its step runs.
- **`Testing: no` is right on the repo's own rule**, not by default — a missing `SKILL.md`, a `name:`/directory mismatch, or malformed frontmatter all fail loudly (the validator errors, or Claude Code never resolves the name); the one silent surface in the change, a dangling symlink, is covered by a dereferencing check rather than by a test.
- **`Docs: no` matches note 151's "Nothing under `docs/` is written"**, and the documentary edge is genuinely already closed: `docs/sakshi-harness/skill-cycle.md` line 33 already names `polymorphism-philosophy` beside `test-philosophy` on the shape-of-a-distinction axis — the doc held ahead of its code, in the sanctioned direction.

## Deferred observations
- Affects: task 57.3 / `.ai-factory/specs/trickster77777/158-the-skeleton-takes-the-bead.md` — `docs/sakshi-harness/skill-graph.md` states that the package's trunks number four and names them (`note`, `roadmap-engine`, `test-philosophy`, `orchestrator-artifacts`), while declaring in the same sentence that domain boundaries are *derived* from the `loads:` graph rather than assigned. Task 57.2 creates no `loads:` edge, so the unit is still edgeless when this task closes and nothing there is due now; 57.3 is the task that adds `loads: … polymorphism-philosophy` and so the moment that census becomes answerable differently. Spec 158's blast radius sweeps only Lens 1's testability wording and the `loads:` line of its own target file and never reaches that sentence, so whether a single-caller engine counts as a trunk — and whether the number should be a number at all, per `docs/counts-go-stale.md` — arrives at 57.3 undecided.
- Affects: `.ai-factory/specs/trickster77777/158-the-skeleton-takes-the-bead.md` — its § "Deploys alone" says "Sequenced last of the three tasks in this phase", while phase 57 in the roadmap carries two tasks, 57.2 and 57.3, and no 57.1 line was ever committed. A stale count inside a neighboring task's spec, harmless to this task and outside its file boundary.

PLAN_REVIEW_PASS
