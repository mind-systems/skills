# Plan Review — 33.1 four durable surfaces stop naming the handoff

## Code Review Summary

**Files Reviewed:** plan (1) + targets (4): `src/skills/roadmap-engine/SKILL.md`, `src/skills/roadmap-outline/SKILL.md`, `src/skills/roadmap-prune/SKILL.md`, `src/skills/orchestrator-artifacts/SKILL.md`
**Risk Level:** 🟢 Low

### Context Gates

- **Architecture** — PASS. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is not disturbed: no skill is added, removed, or re-tiered, no `loads:` edge changes, no engine gains policy and no lens inlines mechanism. The one cross-skill coupling in play — `roadmap-prune` citing `roadmap-outline`'s preamble-links permission by name at the point it relies on it — is exactly the "declare a cross-file invariant at its coupling point" shape ARCHITECTURE.md:41 prescribes, and the plan sequences the two edits (task 3 declared dependent on task 2) so the citation never points at text that no longer exists.
- **Rules** — WARN (non-blocking). `.ai-factory/RULES.md` does not exist in this repo; nothing to check against. The conventions that apply live in `CLAUDE.md`, `.ai-factory/ARCHITECTURE.md`, and `docs/reserved-words.md` / `docs/using-the-language.md`, all checked below.
- **Roadmap** — PASS. `.ai-factory/roadmaps/trickster77777.md:174` is the **first** `[ ]` line in the file — the seam — and it is 33.1, the task this plan targets. The plan's title matches the contract line. The `Spec:` tag resolves to `.ai-factory/specs/trickster77777/108-skills-stop-naming-the-handoff.md`, which exists and was read in full, as were the direction preamble (`## The durable surfaces name a buffer that is spent on first read`) and the `### Phase 33` header prose. Phase 33 names no `Governing spec:` line (the only one in this file sits on Phase 19), so the reference chain runs contract line → task spec → the four target files, plus `docs/sakshi-harness/skill-graph.md`, which the spec cites as the doc that already leads. That citation checks out: `skill-graph.md:27` states a handoff is `note` aimed at another genre and folder, so this is reconciliation of trailing skills to a doc, in the sanctioned docs → roadmap → code direction.
- **Vocabulary** — PASS. Nothing here is a protocol token. The two literals a program scans in this neighbourhood — `## Deferred observations` and the bracketed markers `[fixed]` / `[routed → <path>]` / `[dismissed]` — sit in `orchestrator-artifacts` § 5 and in the bullet list below the edited clause, and the plan explicitly leaves both untouched. `resolution session` is prose, not a token, and its wording is preserved verbatim across the rewrite.

### Verified against ground truth

Every positional and textual claim in the plan was checked against the files themselves, not against the spec's description of them:

- `src/skills/roadmap-engine/SKILL.md:86` is byte-exact `<direction preamble: source handoff/spec links, hard rules, gating for this direction>`, and it is the file's **only** case-insensitive `handoff` hit. The plan's replacement drops exactly the two characters-worth of phrase `handoff/`, leaving the line's other three elements intact.
- `src/skills/roadmap-outline/SKILL.md:40` is `Links to handoffs and task specs are allowed as plain markdown links inside the` and `:41` is ``intro/preamble prose — no formal `Spec:` tag, no invented task specs.`` — the plan quotes both correctly, including which of the two it rewrites. `:40` is the file's only `handoff` hit. The narrowed sentence still reads as one grammatical sentence across the two lines.
- `src/skills/roadmap-prune/SKILL.md:284` is ``preamble: `roadmap-outline` permits unrelated handoff and task-spec links in that``. The plan's three-clause preservation check is checkable against the surrounding lines: `:282–283` carry "Key on the literal `Phase note:` token and on nothing else — never on a link's position in a preamble", `:284–285` carry "links … in that same prose" and ``this skill holds `Bash(rm *)` ``, `:285–286` carry "so a positional key would follow one of those to a deletion". After the narrowing the antecedent of "one of those" is still a plural ("task-spec links"), so the sentence does not become ungrammatical — a real hazard when an enumeration collapses to one member, and it does not fire here.
- The prune count arithmetic is right, and it is the one number in the plan that is easy to get wrong. `grep -in handoff` returns **four lines** in `roadmap-prune` but **five occurrences**: `:61` carries two (`/command-handoff` and "the handoff"), `:63` one (`.ai-factory/handoffs/`), `:284` one, `:447` one. The plan's "5 today → 4 after, the four survivors being two on the `/command-handoff` line, one on the destination line, one on 'never swept'" matches exactly.
- `src/skills/orchestrator-artifacts/SKILL.md` § 6 is at `:65`; `:67` ends `… Written by`, `:68` is `the **resolution session** — the dedicated session the user opens from the parked`, `:69` is `prune's handoff — at the moment it disposes of an observation:`. The plan's line assignment (67 stays, 68–69 rewritten) is correct, and `:69` is the file's only `handoff` hit — so rewriting those two lines is both necessary and sufficient for the → 0 count.
- The plan's line-width claim holds. The new `:68` measures **86** Unicode code points (the plan says ~87 — an approximation, and it uses `~`), against a longest existing line of **96** in that file. No re-wrap is warranted, exactly as the plan and the spec's line-break guard require. The two em-dashes and the `**…**` emphasis are preserved, so the appositive still reads as one.
- The trigger phrasing survives the removal with its meaning intact and stays consistent with the mechanism's own home: `roadmap-prune:59` says "the prune is **parked**, not engineered around", so `orchestrator-artifacts`'s new "when a prune parks" cites prune's own word rather than inventing one. `docs/sakshi-harness/skill-cycle.md:59` describes the same park → `command-handoff` → resolution session sequence and is not contradicted — the clause stops naming the *artifact*, not the event.
- No other surface in the family restates what is being edited. `grep -rn "direction preamble" src/` returns only `roadmap-engine:86`, so the format block is the single home of that line. `grep -rn "resolution session" src/` returns `roadmap-prune:64`, `task-rescue:557`, `task-rescue:686` — all three name the session and cite `orchestrator-artifacts § 6` for the grammar rather than restating the writer's provenance, so none of them dangles after the rewrite. That is 23.1's "cite the vocabulary, don't restate it" still holding.
- The sibling-repo claim is verified rather than assumed. `orchestrator/CLAUDE.md:65` scopes the mirror obligation to "directory layout, artifact naming, PASS signals, sidecar fields, review-section format"; the writer's provenance is none of the five, so no lockstep change is owed. A sweep of the `orchestrator` repo for `resolution session` / `status-marker` finds no independent definition of the writer — only `orchestrator/.ai-factory/handoffs/04-…:32`, that repository's own plan layer, which the spec's guard correctly leaves alone.
- All four targets are per-item symlinks from `active/skills/` into `src/skills/` (verified with `ls -l active/skills/`), so the edits go live without touching `active/`, and all four are ours with no `upstream/ai-factory/` counterpart — a re-sync cannot clobber them. Every file stays far under the 500-line body cap (317 / 75 / 451 / 89), and all four edits only shorten.
- Working tree is clean apart from the plan's own two untracked artifacts under `.ai-factory/`, so the plan's `git diff HEAD --stat -- src/` post-condition will be a clean signal rather than noise.

### Spec coverage

All four change items and every Verification item of the task spec appear in the plan, with the counts and the "an over-deletion cannot pass as a narrowing" residual-content checks carried through intact. The spec guards the plan does not restate verbatim — `agent-architect` untouched, `roadmap-test-coverage`'s `$HANDOFF_LIST` untouched, the plan layer not swept — are enforced transitively by the constraint the plan does state: the diff must list exactly the four named files and nothing else. Nothing in the spec is left reachable-but-open.

### Critical Issues

None.

### Positive Notes

- Task 3 states what must *survive* the edit (three named clauses) rather than only what is removed. That is the right check for a narrowing whose whole risk is losing an argument along with an enumeration item, and it is the one thing a bare `handoff → 4` count could never catch.
- The plan carries the spec's line-break guard down to the individual line and explains it rather than merely asserting it — the char count against the file's existing longest line is what turns "no re-wrap" from a preference into a decided fact the implementer cannot re-litigate.
- Naming both prune sites that must *not* move (`~61–63` and `~447`) with their reason — "those are the act of handing off" — pins the distinction the whole phase rests on at the exact place an implementer running a mechanical `handoff` sweep would over-delete.
- Task 3's declared dependency on task 2 is not bookkeeping: prune's rationale quotes `roadmap-outline` by name, and doing them in this order means the citation is never, at any intermediate state, describing text that has already changed under it.

## Deferred observations

- Affects: `src/skills/roadmap-outline-deep/SKILL.md` — `roadmap-outline-deep:119–121` justifies its `Phase note:` pointer with "This form is already legal under `roadmap-outline`'s own rules — plain markdown links are permitted in intro/preamble prose", generalizing a grant that after this task reads specifically "Links to task specs are allowed as plain markdown links". The justification still holds on the facts — the pointer it emits is a `.ai-factory/specs/<slug>/<NN>-<slug>.md` path, i.e. a task-spec link, squarely inside the narrowed grant — so nothing breaks and no edit is owed by this task, whose boundary is the four named files. Worth a look on whichever future pass touches that skill: the sentence paraphrases the permission more loosely than the permission now reads, and a later narrowing of the grant would strand it.
- Affects: Phase 33 / `.ai-factory/specs/trickster77777/108-skills-stop-naming-the-handoff.md` — the coupling is declared on one side only. `roadmap-prune:284` names `roadmap-outline` as the source of the permission its `Bash(rm *)` safety argument rests on, but `roadmap-outline:40–41` carries no sentence saying that permission is load-bearing for a deletion-safety rationale elsewhere — the both-sides declaration CLAUDE.md § "Dependencies and the skill graph" asks for on invariants grep cannot derive. The spec deliberately closes this off ("The remainder of the sentence is untouched"), so it is correctly out of scope here, and the exposure is mild: prune keys on the literal token, which is the conservative side, so losing the permission entirely would leave prune safe and only its cited rationale stale. Raising it because the spec itself names the failure mode — "Remove the permission and the sentence cites something that no longer exists" — and nothing on the `roadmap-outline` side yet warns a future editor of that.

PLAN_REVIEW_PASS
