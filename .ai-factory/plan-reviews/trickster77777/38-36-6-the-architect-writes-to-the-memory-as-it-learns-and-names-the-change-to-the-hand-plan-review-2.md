## Plan Review Summary

**Plan:** `.ai-factory/plans/trickster77777/38-36-6-the-architect-writes-to-the-memory-as-it-learns-and-names-the-change-to-the-hand.md` (revision after plan-review-1)
**Files targeted:** 1 (`src/skills/agent-architect/SKILL.md`)
**Risk Level:** 🟢 Low

### Context Gates
- **Architecture** — OK. The plan edits the policy skill only; every mechanism it names (path, numbering, zones, the editor's re-read, the drain rule, the isolation rule) is verified present in `architect-editor-engine` § "The architect's buffer" and the plan restates none of it. No new `loads:` edge (frontmatter `loads: architect-editor-engine architect-pairing-engine` pinned byte-identical). Consistent with ARCHITECTURE.md § "Composition: mechanism vs policy".
- **Rules** — WARN (non-blocking): `.ai-factory/RULES.md` is absent; nothing to check.
- **Roadmap** — OK. Contract line 36.6 in `.ai-factory/roadmaps/trickster77777.md` → `Spec:` `specs/trickster77777/124-…` → governing spec `docs/paired-loop.md` § "What the memory holds, and who holds it" (occasion + form sentences) and § "How the memory begins, and how it survives" (announce sentence) — all walked; the plan quotes each verbatim and correctly. The contract line's guardrails (no zone, no list, no restatement of drain / re-read) are all carried into the plan's task. 36.1 (dependency) is `[x]`. § "What crosses the channel" confirms that naming a memory change is "ordinary communication … needs no form" — the plan's refusal to attach a token or carrier to the announcement matches, and leaving the "Two channel-message formats, nothing else" / "exactly one case" sentences to 37.1 (contract line 220, spec 119) is correct.
- **Ground truth** — verified against HEAD `df94aa1`, working tree clean for the target: 282 lines; § "Your buffer is yours alone" is exactly the two paragraphs the plan quotes (lines 252–266); `awk 'length > 77' | wc -l` = 17; `grep -c "memory snapshot"` = 3; `grep -c "REPORT-ONLY\|APPLY-EDIT"` = 8; `settled zone` = 1, `live zone` = 0, `zone` hits on lines 36 / 262 / 263 only, `drain` = 1, `re-read` = 1, `episode` = 0, `stretch of work` = 0, "The handoff continuing you across a compact" = 1. Both grep anchors for the recording list (line 253) and the deferral format (line 260) sit unbroken on single lines. Every baseline the verification step relies on is correct.
- **Review-1 follow-up** — resolved. Item 3 now pins that the inserted paragraph must not carry the substring `re-read` and offers "reading on change" as the allusion; the check (`grep -c "re-read"` → exactly `1`) and the instruction now agree.
- **Carried-in scope** — the "handoff … across a compact" → "memory snapshot" alignment is the deferred observation both 36.5 reviews addressed to 36.6; bounded to one sentence in the section this task already edits; the opening clause "must survive a compact" is deliberately left. Correct.

### Critical Issues
None.

### Issues

1. **Wrong ground-truth claim about vocabulary: the file never uses "head"** — `Tasks › State the occasion…`, closing guardrail sentence.
   The plan says: "'editor' / 'hand' and 'architect' / 'head' as the file already uses them". Checked on the file: `grep -nw "hand"` hits 5 lines (23, 26, 48, 73, 179); `grep -nw "head"` hits **0**. "hand" for the editor is established in the file; "head" for the architect is not — it is the governing spec's word, not the skill's. An implementer taking the sentence at face value may write "the head" into a skill body, introducing a second name for the architect where the registry has one (`architect`), which is exactly the drift `using-the-language` § "What conforms" names. Since the section is second-person ("you"), the architect never needs a third-person name in the inserted paragraph at all. Pin: drop "head" from the guardrail (or state that the paragraph addresses the architect as "you" and names the editor as "the editor" / "the hand", the two forms the file already uses).

2. **"memory snapshot" count check contradicts the guardrail that conditionally permits the phrase in the inserted paragraph** — `Tasks › State the occasion…` guardrail vs. `Tasks › Verify against the file`.
   The guardrail allows "'memory snapshot' only if the artifact is mentioned" in the inserted paragraph; the check pins `grep -c "memory snapshot"` → **HEAD's count plus one** (3 → 4), where the one is the reworked paragraph-1 sentence. An implementer who exercises the permission lands at 5 and fails the check; one who does not passes — the same shape as the review-1 finding. Nothing in the three facts needs the artifact (the recovering snapshot is paragraph 1's and § "Spawn once…"'s concern), so pin it: the inserted paragraph does not mention the memory snapshot, and the check stays at +1. Related fragility in the same step, worth one clause: the multi-word probes (`memory snapshot`, `stretch of work`) are run with line-oriented `grep` on a hard-wrapped file — a wrap between the two words silently yields 0 hits, which makes the "+1" check fail spuriously and the "stretch of work … hits only inside the inserted paragraph" check pass vacuously. Either state that these phrases must sit unbroken on one line, or run those two greps over the unwrapped text (e.g. `tr '\n' ' ' < file | grep -o "memory snapshot" | wc -l`).

### Positive Notes
- Every quoted sentence of the file and of the governing spec matches ground truth character-for-character; the HEAD citation is exact.
- The announce-obligation is stated as the spec states it — the architect's obligation, one act with the write, no form, no carrier — and the plan pre-empts the one implicit edge (no editor yet → nothing to announce; the spawn hands over the path).
- Guardrails are drawn precisely where 36.1–36.5 and 37.1 draw theirs: zone-silence, no new list, no restatement of the four engine facts, no touch on the two sentences 37.1 rescopes, no format token.
- Insertion position keeps "the deferral entries below" resolving downward and "you are its only writer." last; the final step reads the section whole rather than trusting the diff.

## Deferred observations
- Affects: task 37.1 / `.ai-factory/specs/trickster77777/119-architect-authors-more-than-one-prompt.md` — once this task lands, `agent-architect` carries an announce-obligation toward the editor while the same file still says "Two channel-message formats, nothing else" and "You author your own prompt in exactly one case". The plan correctly leaves both to 37.1, which rescopes them to what opens a round per `docs/paired-loop.md` § "What crosses the channel"; until 37.1 lands the file states a closed list it no longer honors. Nothing for 36.6 to do. [fixed]
- Affects: `.ai-factory/specs/trickster77777/124-the-architect-writes-to-the-memory-as-it-learns.md` — the section's opening clause "whatever of your own state must survive a compact" is the second phrase the 36.5 reviews named; the plan deliberately leaves it (the buffer's purpose is survival across a compact whichever snapshot occasion recovers the architect). Defensible; recorded so the choice is visible to the spec's owner rather than silently dropped.
