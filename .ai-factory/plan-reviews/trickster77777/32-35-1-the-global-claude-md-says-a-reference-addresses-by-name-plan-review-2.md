# Plan Review — 35.1 the global CLAUDE.md says a reference addresses by name (round 2)

## Code Review Summary

**Files Reviewed:** plan (1) + target (1): `src/global/CLAUDE.md`; plus the task spec `.ai-factory/specs/trickster77777/111-the-global-claude-md-says-a-reference-addresses-by-name.md`, the 35.1 contract line with `### Phase 35` and its direction preamble in `.ai-factory/roadmaps/trickster77777.md`, `.ai-factory/ARCHITECTURE.md`, this repo's `CLAUDE.md`, the grove root `../CLAUDE.md`, `docs/reference-by-name.md`, `docs/always-loaded-discipline.md`, `docs/philosophy/context-tree.md`, `src/commands/command-handoff.md`, round 1's own review; and in `tradeoxy_core`: `.ai-factory/RULES.md`, `.ai-factory/handoffs/` (43, 44, 45) and its working tree
**Risk Level:** 🟢 Low — both round-1 findings are closed at the root rather than patched, and every factual claim the plan makes re-verified true against the files. The single finding below is a verification step the spec asks for and the plan does not carry; the edit that is this task's deliverable is correct and lands clean.

### Context Gates

- **Architecture** — PASS. `.ai-factory/ARCHITECTURE.md` § "Composition: mechanism vs policy" is untouched: no skill added or re-tiered, no `loads:` edge, no engine gains policy, no lens inlines mechanism. The edit lands on the always-loaded surface, not the skill graph. Delivery re-verified live: `active/CLAUDE.md → ../src/global/CLAUDE.md` and `~/.claude/CLAUDE.md → …/skills/active/CLAUDE.md`, and `git ls-files -s active/CLAUDE.md` reports mode `120000` — a tracked symlink whose blob is the target *path*, so editing `src/global/CLAUDE.md` changes what every session loads without making `active/CLAUDE.md` show up in the diff. That is what makes the plan's closing `git diff HEAD --stat -- src/ active/ docs/ CLAUDE.md` check resolve to one file, and the plan's "No symlink work" is right. `upstream/ai-factory/` has no `global` counterpart, so `scripts/sync-upstream.sh` cannot clobber the target.
- **Rules** — WARN (non-blocking). This repo has no `.ai-factory/RULES.md` and no `.ai-factory/skill-context/aif-review/SKILL.md` — confirmed absent, nothing to check on either surface. The governing conventions live in `CLAUDE.md`, the grove root `../CLAUDE.md`, `.ai-factory/ARCHITECTURE.md` and `docs/`, all read below.
- **Roadmap** — PASS with one WARN, carried unchanged from round 1. The 35.1 line is the first `- [ ]` in `.ai-factory/roadmaps/trickster77777.md` — the seam — and the plan's `# Plan:` heading matches its title. The `Spec:` tag resolves to `.ai-factory/specs/trickster77777/111-…`, which exists and was read in full; the plan's Context, insertion point, guard and verification all trace to it. WARN: `### Phase 35` names no `Governing spec:` and no phase note, so the chain runs contract line → task spec → `src/global/CLAUDE.md`; `docs/reference-by-name.md` is the ratified home in fact but is named by neither the phase header nor the spec's Files list. Not this plan's defect, and the chain terminates at the file the change is made in.
- **Vocabulary** — PASS. No protocol token moves: the `Spec:` tag, the PASS signals and `## Deferred observations` sit outside every edited span. The handoff mark line the plan pins is byte-identical to `src/commands/command-handoff.md` (lines 33 and 93) and to files 44/45 in the destination folder — modulo the `[ ]`/`[x]` marker, which is the point, and see the finding. The pinned paragraph uses `work-order` at its post-34.4 single meaning — the apply artifact "thrown away when applied", set against "a doc, a spec, or a roadmap line" — and `src/global/CLAUDE.md` § "Planning workflow" already carries the word in that sense, so the term arrives in a file where it is already resident.

### Findings

**1. The spec's fourth verification bullet — read the written handoff back — has no step in the plan, and the one thing it would catch is the `[ ]` marker the two neighbouring files carry as `[x]`.**

The spec's Verification lists five checks. Four of them became plan steps, and two were sharpened past the spec (equality instead of presence; delta instead of `status --short`). The fourth did not: *"the handoff exists in `tradeoxy_core`'s `.ai-factory/handoffs/` under the next free number in that folder, carries the mark line unprocessed, and names both the global paragraph and the local rule it makes redundant."*

The plan's fourth checkbox is titled "Confirm `tradeoxy_core` is otherwise untouched" and does only that half: `git status --short` for the delta, `git diff HEAD -- .ai-factory/RULES.md` for byte-identity. Neither reads the new file. The gap is narrow because the write step pins the content well — number 46 in the `Files:` line, the mark line in its own fence, and items 1–3 pin exactly the "names both" requirement — but it is not empty, and the failure it leaves open is the specific one this destination sets up.

Failure scenario: the implementer opens `45-how-a-task-is-built-and-the-defect-that-got-into-ours.md` to copy the folder's form — which the plan's own Ground truth section tells it to do, "Folder form is taken from files 44 and 45" — and both 44 and 45 carry `**Processed:** \`[x]\` …`, because both are spent. The `[x]` comes along with the form. Handoff 46 lands pre-marked, i.e. announcing itself as already read, which is the one semantic the mark line exists to carry; `git status --short` shows it as a single new untracked file and `RULES.md` is untouched, so both of the plan's `tradeoxy_core` checks pass and the defect ships to another repository where nothing in this run will look at it again.

Repair: extend the fourth checkbox to read the new file back, in the same equality register the plan already uses for the paragraph — the mark line character-for-character equal to the pinned string with `[ ]` and not `[x]`, the title in the `# Handoff — <slug>` form, the filename numbered 46, and the prose naming both the global paragraph's opening clause and the `RULES.md` heading. Everything needed is already pinned in the write step; only the read-back is missing.

### Verified against ground truth

- **Round 1 finding 2 is closed at the root, not patched.** The pinned string now appears in a fenced block with the reason stated ("shown fenced here only so this plan does not decorate it") and an explicit negative list — "no block-quote `> ` prefix, no list marker, no indentation, no fence" — naming the exact defect round 1 predicted. Extracted from the plan's fence and dedented by the two spaces of its list container, the string is **556 characters and byte-identical to the spec's paragraph**, em dashes, backticks around `file:line` and bold `**name**` included. The verify step was rewritten from presence to **equality** ("a paragraph that merely contains the string does not pass"), which is what makes a stray `> ` fail rather than pass.
- **Round 1 finding 1 is closed at the root.** The handoff's item 1 now reads "**Address it, do not reproduce it:** name the file, the section and that opening clause and stop there", with the one-home-per-fact reason attached and the observation that the reader's own system prompt already holds the live text. That is the repair round 1 asked for, and naming the opening clause "A reference addresses by **name**, never by position" is itself the name-address the paragraph prescribes — consistent with `docs/reference-by-name.md` § "Granularity, not size", where the unit of naming is "anything that will be depended on … a named clause".
- **The edit lands clean; I applied it to a scratch copy.** § "Grounding claims" holds seven paragraphs; the fourth ends `…("per the spec…") — never invent.` and the fifth begins `The opening task statement is the first artifact`, which occurs exactly once in the file. Inserting the spec's string between them yields a single diff hunk — `12a13,14`, one added line plus its blank line, no other hunk — an eight-paragraph section with the new paragraph at index 4 and the two named neighbours in place, the string occurring exactly once, and `reference-by-name` at 0 before and after (confirmed: `grep -c` returns 0 on the live file today).
- **The paragraph is a faithful compression of the ratified home.** `docs/reference-by-name.md` § "The check" carries the same four claims — the `file:line`-as-defect-report, the repair belonging in the target, position addresses living in a throwaway work-order, and the surfaces that outlive their numbering. The pinned paragraph reorders the last triple ("a doc, a spec, or a roadmap line" against the doc's "a spec, a roadmap line, or a document") and adds nothing; it is the always-loaded register of a rule that is already ratified, not a new rule.
- **The handoff destination matches, freshly enumerated.** `tradeoxy_core/.ai-factory/handoffs/` runs to `45-how-a-task-is-built-and-the-defect-that-got-into-ours.md`, so **46** is the next free number, unpadded like its neighbours. Files 44 and 45 open `# Handoff — <slug>`, blank line, mark line; 43 has no mark line, so the plan is right to take the form from 44/45 rather than the folder as a whole.
- **The `RULES.md` claims are exact.** The heading is `## Citing \`ARCHITECTURE.md\` — by rule name, never by line number` at line 31, and all four position addresses the plan names are present: `package.json:13-14`, `src/indicators/runtime/indicator-protocol.ts:58-79`, `src/replay/__tests__/replay-indicator-replayer.spec.ts:288` and `:298`. Quoting these literally in the handoff names the defective citations as the exhibits they are — it is not the handoff addressing anything by position, and the plan says so.
- **The pre-existing dirt is exactly the eight entries the plan documents.** `git -C tradeoxy_core status --short` returns six modified files (`notes/123-`, `127-`, `128-architect-buffer.md`; `specs/98-`, `99-`, `108-…`) and two untracked handoffs (44, 45). `git diff HEAD -- .ai-factory/RULES.md` is already empty, so the plan's byte-identity check is a real invariant to hold rather than a repair. Stating the spec's literal "shows the new handoff and nothing else" as unsatisfiable and verifying by delta instead is the right call, and it is annotated as a deviation rather than silently swapped.
- **The closing diff scope resolves.** This repo's working tree holds only untracked artifacts under `.ai-factory/` (the plan, its sidecar, round-1's review), so `git diff HEAD --stat -- src/ active/ docs/ CLAUDE.md` will list `src/global/CLAUDE.md` alone once the edit lands.
- **`Testing: no` is correct** under `test-philosophy`: one paragraph added to a Markdown instruction file plus one new prose file in another repository — no executable surface, nothing that can fail silently. `Docs: no` is right in the sense the setting carries; the edit *is* the change the task asks for, and no doc holds a copy that must move with it (see the deferred observation for the pointer that does not exist yet).
- **Blast radius is zero.** Every surface that cites § "Grounding claims" addresses it by name, not position — `docs/always-loaded-discipline.md` (lines 3 and 25), `docs/reference-by-name.md` (line 31), `docs/philosophy/context-tree.md`, this repo's `CLAUDE.md` doc table, `src/commands/command-handoff.md`, `src/skills/detangle/SKILL.md` — so inserting a paragraph inside the section breaks no reference anywhere in the grove. That is what makes the one-paragraph shape viable, and it is the rule the paragraph itself states, already holding.

### Positive Notes

- Both round-1 findings were fixed where they came from rather than worked around. The block-quote defect was closed by changing the presentation *and* upgrading the check from presence to equality — the two halves round 1 named — and the quote-the-paragraph defect by replacing the instruction with its opposite plus the reason, so the next reader of the plan learns why rather than just what.
- The reason is carried with the instruction throughout. "Quoting the paragraph would put a second, unmaintained home for the fact in another repository — the fact this task exists to give one home" is the plan teaching its own rationale at the point of use; an implementer tempted to be helpful and paste the text is stopped by an argument, not a prohibition.
- "Ground truth read for this plan" states, per claim, what was read and what was found, including every number the later steps depend on. All of it re-verified true, twice over now. This section is what makes the review cheap and is a good addition to this folder's plan form.
- The deviation is annotated where it is discovered rather than routed around: the spec's `status --short` check is named unsatisfiable *before* the step that would have run it, with the replacement stated and the eight pre-existing entries enumerated so "untouched" is checkable at all.
- The plan holds the spec's Guards in both directions — no link to `docs/reference-by-name.md` in the always-loaded file, and no path into a sibling repository from either artifact, including in the handoff where the pull back toward the deep home is strongest and the plan forecloses it by name with the reason attached.
- It does not reach into `tradeoxy_core`'s `RULES.md`. Dropping the local duplicate is that repository's own work; the plan states it, verifies it, and writes the `file:line` observation as an observation rather than an instruction.

## Deferred observations

- Affects: Phase 35 / task spec `.ai-factory/specs/trickster77777/111-the-global-claude-md-says-a-reference-addresses-by-name.md` — after this task the naming rule has two homes in this repository and neither knows about the other. `docs/reference-by-name.md` § "Where things are normed" names the global CLAUDE.md § "Grounding claims" only as the home of the one-home-per-fact rule it rests on, so nothing tells a reader arriving at the deep home that the rule's own normative statement is now in the always-loaded layer; `docs/always-loaded-discipline.md` opens by enumerating four things § "Grounding claims" prescribes — docs → roadmap → code, grounding by walking to the leaf, decay, the seam — and after 35.1 the naming rule is a fifth the enumeration omits; and this repo's `CLAUDE.md` doc table gives the "Reference by name" row no equivalent of the clause the Context tree row one paragraph above already carries, "the normative rule lives in the global CLAUDE.md § 'Grounding claims'". The spec's Files list pins one edit and the plan correctly does not exceed it, so this belongs to whoever writes the next task on this phase: one clause in `docs/reference-by-name.md`, one in `docs/always-loaded-discipline.md`, and the `CLAUDE.md` row brought into line with its neighbour.
