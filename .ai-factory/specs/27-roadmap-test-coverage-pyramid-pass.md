# roadmap-test-coverage: pyramid pass — the 8-layer algorithm stays, the ceremony goes

## Current state

`src/skills/roadmap-test-coverage/SKILL.md` (328 lines) is a top carrying a large algorithm — the 8-layer coverage pipeline. By the composition rule the algorithm is legal mass: it has no second consumer, so extraction into an engine is refused (a single-caller engine is negative value). What is not legal mass: ceremony around the layers, restated discriminator content (the silent-failure rule lives in the loaded `test-philosophy`), and narration of steps an executor performs unprompted.

## Change

One pyramid pass — the algorithm survives whole, its packaging slims:

- **Verbatim-protected:** the 8-layer pipeline structure and each layer's contract (agents, hand-offs, the Layer 7 Class A/B classification table, Layer 6/8 handoff-list mechanics per the two-tier handoff decision); Critical Rule 1's precise wording (never write *new* test files; the one allowed write is Layer 7 Class-A patches, assertions intact).
- **Cut:** any inline restatement of the silent-failure discriminator (`test-philosophy` holds it — link/load, never restate), transitional prose between layers, duplicated rationale.
- Two-reader register.

## Guards

- **Behavior-identical** — same layers, same order, same outputs, same handoff list format; protected blocks byte-identical.
- `loads: test-philosophy` and frontmatter unchanged.
- Live baseline before the next phase task: run Layers 1–3 (analysis tier) on a real project pre/post and compare the produced classification.

## Verification

- `diff`: the 8 layers intact and ordered; removed hunks are ceremony/restatement only.
- `grep -n "fails silently\|fails loudly" SKILL.md` → discriminator not restated inline beyond the load pointer.
