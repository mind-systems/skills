# milestone-rescue-audit: pyramid pass — compress around the decisive test, registers verbatim

## Current state

`src/skills/milestone-rescue-audit/SKILL.md` holds a sound analysis pipeline — chain reconstruction → one-sentence root-cause test → discriminators → verdict spectrum — and one hard-won register contract: the chronological prose narrative deliverable ("the narrative is the evidence"). After task 1.6.1 the skill is single-mode (the band-aid hunter, chat-only, writes nothing). Around the pipeline: ceremony and restatement of the artifact protocol the loaded `orchestrator-artifacts` engine already carries.

## Change

One pyramid pass — compression, not redesign:

- **Verbatim-protected:** the one-sentence root-cause test as the decisive instrument; discriminators corroborative-only; "default is NOT band-aid" and the healthy-convergence early-exit, word-for-word; the narrative output register (prose paragraphs per round, no tables in the deliverable); the "What NOT to do" list; the single-mode output contract.
- **Cut:** protocol restatement (layout, sidecar fields, marker grammar — the engine holds them; link, never restate), procedural narration, duplicated rationale.
- Two-reader register throughout.

## Guards

- **Behavior-identical** — analysis pipeline, verdict spectrum, output unchanged; protected lines byte-identical.
- Chat-only, zero file writes — unchanged from what 1.6.1 leaves; frontmatter and `loads:` unchanged beyond 1.6.1's own edits.
- **Runs strictly after 1.6.1** — this pass compresses the single-mode file 1.6.1 leaves behind.
- Live baseline before the next phase task: run the audit cold on a past looped milestone and compare the narrative and verdict to a pre-rewrite run.

## Verification

- `diff`: protected blocks byte-identical; removed hunks are ceremony/restatement only.
- Live baseline: same verdict, same narrative register, same evidence depth on the same input.
