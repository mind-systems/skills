# Phase 54 — three concepts without a registry entry

**"The deciding half" and "the applying half."** `agent-architect/SKILL.md` is now the second skill body to name these two concepts — `architect-pairing-engine` named them first — and `docs/reserved-words.md` § "Paired loop" still registers only *architect* and *editor*. Two skill bodies using an unregistered pair of concepts is precisely the drift the one-word-one-meaning contract exists to catch. The same section is also the natural home for *buffer* as the architect's own private state file: `docs/reserved-words.md:84` currently spends the word on a named roadmap's gloss ("one developer's buffer"), while `agent-architect` has used it for the architect's file since before the task that raised this finding — a pre-existing double use, not created by that task, and out of reach at the time since `docs/` was guarded untouched. Three registry entries are implied by this one thread — "the deciding half," "the applying half," and the *buffer* double use — recorded together because they surfaced together (four occurrences).

**"Second reader."** Task 26.10 introduced this phrase into `agent-architect`, a skill body — the surface where the one-word-one-meaning contract binds — and the registry has no entry for it: § "Paired loop" lists only *architect* and *editor*. The concept is real and is neither of those: it is the role of the independent second pass in a round, fillable by either party, and the phrase is well chosen, harmonizing with the pre-existing "second reading" two lines below it. No defect in the change that introduced it — the text is pinned word-for-word by spec 101 and the implementation was right to land it verbatim — and one occurrence does not yet meet the registry's own "which must recur" bar on its own. Wants a ruling on whether the role earns an entry, not a silent edit (one occurrence).

**"Work-order."** Once the retired phrase it replaced is gone, `work-order` names exactly one concept but has no entry of its own in the registry: § "Paired loop" lists `architect` and `editor`, and the word survives only inside the architect's entry, where a reader looking it up would not think to search. Its sibling concepts each got a bullet of their own; this one is defined by implication in a sentence about someone else. Worth a registry entry with its home named — `agent-architect`, `architect-editor-engine` — when a later task is licensed to add one (two occurrences).

Nothing under `docs/` is written by this phase's own note; the gate above is the reason.
