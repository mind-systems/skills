# Phase 50 — the documentation catches up with what the skills now do

**`docs/sakshi-harness/skill-graph.md`.** § "Воронка в один дистиллятор" (`:25`) enumerates the callers of `note`: "`roadmap-outline`, `roadmap-decompose` и `roadmap-decompose-skeleton` рендерят артефакты через `roadmap-engine`, который пишет task-spec через `note`; `command-handoff` зовёт `note` напрямую" — one direct caller. Phase 28's `roadmap-outline-deep` is a second: it passes its own destination, template and verbosity hooks to `note` without going through `roadmap-engine` at all. § "Домены — по стволам" (`:31`), which derives domain boundaries from the `loads:` graph, makes the omission visible on the next derivation from that graph. `docs/sakshi-harness/skill-cycle.md` was updated ahead of the task that created this gap, per docs → roadmap → code; `skill-graph.md` was not — five occurrences across 28.1's plan-reviews and reviews record the same unclosed line.

**`docs/sakshi-harness/skill-cycle.md`**, four separate gaps on one file. Its `:55` states prune's deletion scope as "вместе с отработавшими артефактами и их task-spec" — with a phase note now a durable artifact addressed by a `Phase note:` pointer the same way a task spec is addressed by `Spec:`, prune deletes phase notes too and the line does not say so (28.1, two occurrences). Its `:39` opens by positioning `command-pin-gaps` as the code-tier half of a pair, symmetric with `roadmap-outline-deep` at the phase tier — "вторая половина сведения с кодом: `roadmap-outline-deep` сводит с кодом phase, `command-pin-gaps` — task" — but `roadmap-outline-deep`, landed by 28.1, carries no instruction anywhere in its own body to read code at the phase tier; the doc states a symmetry only one half of which exists in the skill it describes (29.1, one occurrence). Its `:74`, alongside `docs/skill-description-field.md:13`, paraphrases `command-pin-gaps`'s zone as "close the places an implementer would guess at" (`закрыть места угадывания` in the cycle diagram's own label) — after 29.1 the command also routes what it may not close to a named owner and raises fundamental conflicts as explicit blockers, so "close" now under-describes the zone in both places; neither passage is a specification of the command's behaviour, so nothing breaks on the facts, but a future editor of either doc should state the zone as close-or-route (29.1, four occurrences: two sites × two review rounds). And its `:17`/`:21` name four readers of the phase header's two pointers — `roadmap-decompose`, `task-rescue`, and the orchestrator repo's own planner and reviewer; 28.2 gives the first two their readers in this repository, but the orchestrator half stays committed doc with no task anywhere in `.ai-factory/roadmaps/trickster77777.md` behind it, so a phase note is read at planning and rescue time but not yet by the two agents that execute the task against the code (28.2, five occurrences).

**`docs/philosophy/multiuser-roadmaps.md:47`** describes `.ai-factory/specs/<имя>/` as the home of task specs resolved "всегда … через `Spec:`-тег contract-line". After 28.1 the same directory also holds phase notes, resolved through a `Phase note:` pointer instead, sharing the same per-directory `<NN>` counter as the task specs — the doc names one resolution path for the directory where two now exist (two occurrences).

**Global `CLAUDE.md`**, two separate omissions. § "Planning workflow" states the planning chain as "`/roadmap-outline` → `/roadmap-decompose` (plus `/roadmap-decompose-skeleton` on heavy tasks)" (`:57`) — after 28.1 there is a step between the first two, `roadmap-outline-deep`, which `skill-cycle.md` places immediately after `aif-docs` and before decomposition. This line is the always-loaded surface in every session of every project, where `skill-cycle.md` is not loaded at all — the same class of gap phase 27 was raised for, where behaviour lives in skill bodies and `skill-cycle.md` while the always-loaded layer stays silent on it (one occurrence). Separately, `:189`'s "ours, no upstream counterpart" enumeration — "Everything else in `src/skills/` is ours … sync never touches it: …" — omits `orchestrator-artifacts`: it lives in `src/skills/`, has no counterpart in `upstream/ai-factory/`, and is symlinked into `active/skills/`, the same argument the paired-loop-engines task made for adding its own two engines to this list. After that task the line enumerates 17 of the 18 skills that are ours; `orchestrator-artifacts` is the one still missing (two occurrences).

Nothing under `docs/` is written by this phase's own note; the divergence recorded is between five doc files and the skills they describe.
