# Phase 53 — the skill bodies stop describing retired shapes

**`roadmap-outline-deep`'s own justification for its pointer.** Its own text (`:119–121`) justifies the `Phase note:` pointer with "This form is already legal under `roadmap-outline`'s own rules — plain markdown links are permitted in intro/preamble prose," generalizing a grant that after task 33 reads specifically "Links to task specs are allowed as plain markdown links." The justification still holds on the facts — the pointer it emits is a `.ai-factory/specs/<slug>/<NN>-<slug>.md` path, a task-spec link, squarely inside the narrowed grant — so nothing breaks and 33.1 correctly left it alone (its boundary was four named files). Worth tightening on whichever pass next touches `roadmap-outline-deep`: the sentence paraphrases the permission more loosely than the permission now reads, and a further narrowing of the grant would strand it (two occurrences).

**The same task's coupling, declared on one side only.** `roadmap-prune:284` names `roadmap-outline` as the source of the permission its `Bash(rm *)` safety argument rests on, but `roadmap-outline:40–41` carries no sentence saying that permission is load-bearing for a deletion-safety rationale elsewhere — the both-sides declaration CLAUDE.md § "Dependencies and the skill graph" asks for on invariants grep cannot derive. Task 33.1's spec deliberately closed this off ("The remainder of the sentence is untouched"), and the exposure is mild: prune keys on the literal token, the conservative side, so losing the permission entirely would leave prune safe and only its cited rationale stale. The spec itself names the failure mode — "Remove the permission and the sentence cites something that no longer exists" — and nothing on the `roadmap-outline` side yet warns a future editor of that (two occurrences).

**`roadmap-decompose` and `roadmap-engine` disagree about the shape of a task spec.** `roadmap-decompose`'s hook (d), "Decompose existing" (`SKILL.md:88–89`), still describes a full spec as "(what exists today, the exact change, files/types/methods to touch, guards, how to verify)" — four parts, including both members task 34.1 dropped — in the one mode dedicated to writing a task spec, contradicting the engine it defers to. The file is outside 34.1's contract-line boundary and no task in Phase 34 owns it; it wants a fifth task in that phase or a follow-up. Two upstream surfaces carry the same over-broad claim and would be repaired with it: the phase preamble at `.ai-factory/roadmaps/trickster77777.md:182` ("no skill prescribing a task spec's sections") and the spec's own Blast radius section ("no skill or command restates it"). The exposure while it stands: an agent in "Decompose existing" mode reads both surfaces and writes the verification bullets the phase exists to stop (three occurrences). `roadmap-engine`'s own § "The two-tier artifact" now says a task spec "holds three parts and nothing else" two paragraphs after saying it "follows `note`'s format," whose template hook stays unset and therefore still defaults to the standalone-note skeleton (Key Findings / Details / Open Questions) — the very shape the task spec's own "Current state" section names as part of the original problem. Nothing breaks in practice: `note`'s folder-style layer reads the recent siblings in `.ai-factory/specs/trickster77777/`, which already carry Current state / The change / Blast radius, and the contract line forecloses the template route by name — which is why this was correctly not a finding in 34.1 itself. Raised because the residual tension is what a later reader trips on: the shape is stated in one place while the section skeleton is still inherited from another (three occurrences).

**`editor.md`'s two surviving "relayed" sites.** Its frontmatter `description:` — the always-loaded line read by the spawning side before the body ever loads — still reads "reasons independently over a relayed analysis target," carrying the same `REPORT-ONLY`-equals-relay equation task 40.3 removed from the body. Spec 130 named two body sites and scoped the rest of the file as untouched, so 40.3 correctly left it; it needs a contract line of its own (two occurrences). Its § "The round's unit" still defines the analysis-mode unit as "one relayed message … the relayed message in analysis mode," and § "Analysis mode" still opens "A relayed message — a review, decompose, judge, hazard-hunt —" (kept deliberately by spec 130, quoted there as grounding, not touched). Neither site is 40.3's to fix, and 40.6's sweep of the same register was scoped to `agent-architect` only — `editor.md`'s two sites were never swept. Whoever next sweeps `editor.md` for the remaining "relayed" register should take the frontmatter and these two sites together (two occurrences).

**`architect-editor-engine`'s description omits the ride-alongside bound.** Its `description:` field enumerates what the engine holds — the two formats, the mode rule, the buffer definition with its zones, re-read, drain, isolation rules — and after task 40.4 the buffer section also carries the ride-alongside permission and its bound, which the description does not name. Spec 131 bounded 40.4's edit to two body sites, and 40.2 set the precedent of not touching the description for a widening in this same section, so 40.4 correctly left it. Whether the always-loaded description should name the bound is a question for whoever next revises the engine's description as a whole (one occurrence).

Nothing under `docs/` is written by this phase's own note; the instance class is phase 45's own rule — a removal orphans the prose that described the shape it removed, and none of it fails loudly.
