# Phase 52 — roadmap-test-coverage's remaining observations

**The `$TEST_CMD` primary-source description, checked fresh against the live skill rather than the pruned spec.** Task 20.2's own spec (82) said in its Change bullet 1 that the primary source was "the `## Commands` table `aif` writes into `CLAUDE.md` (the `Tests` row…)" — overstating what `aif` actually mandates: `src/skills/aif/SKILL.md:37` asks for a `## Commands` section "from the detected package manager or Makefile targets," with no table and no `Tests` row specified anywhere. That overstatement lived in the spec's own paraphrase of its upstream, not in the skill task 20.2 shipped. Read fresh today, `src/skills/roadmap-test-coverage/SKILL.md`'s dedicated `$TEST_CMD` resolution rule (landed by 20.2, `:52-58`) reads: "Primary — the test command the project declares in its `CLAUDE.md` `## Commands` section. Typically a table row such as `| Tests | pytest |`, but read the section for the declared test command whatever its shape — a list entry or a bare line counts too." The hedge is already there; the shipped skill does not carry the overstatement its own spec described. With spec 82 pruned alongside its `[x]` task, no live text repeats the inaccuracy — the finding closes on inspection rather than needing a task (four occurrences, all pointing at the spec's description rather than the skill's).

**The note template's spawn-time label.** `roadmap-test-coverage`'s Layer 4 spawns one `Explore` agent per area (`:144`) with a prompt naming `Area: <area name>` (`:152`) and instructing the agent to choose its own slug "not for the Area label above" (`:160`) once research is done. The written note's own H1, `# <Area Name> — Test Plan` (`:179`), and its Layer 8 `(<area name>)` annotation both still fill from the spawn-time `Area:` label rather than the post-research slug the prompt tells the agent to prefer. A note whose slug says what the area turned out to be, under a title that says what it was called before research began, is a small internal contradiction the governing spec's own "name late enough to still be true" reasoning would apply to just as much. Task 39.1's spec pinned it to three template sentences and correctly left this alone; whoever next touches the template should decide whether the title follows the slug (three occurrences).

**The Layer 4 agent type.** The template spawns Layer 4 as an `Explore` agent, which in at least one harness definition is read-only — no `Write`/`Edit`, though `Bash` remains. The template has always asked that agent to write a file, pre-existing and untouched by 39.1; but 39.1 made the agent the sole holder of the pen for that write, which raises the cost if the agent type cannot in fact write — the returned `saved:` path would be the first place the failure surfaces (three occurrences).

Nothing under `docs/` is written by this phase's own note.
