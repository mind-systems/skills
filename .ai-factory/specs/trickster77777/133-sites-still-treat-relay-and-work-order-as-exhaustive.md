# More sites still treat relay-and-work-order as the whole list

## Current state (grounded, read fresh)

`src/skills/agent-architect/SKILL.md` removed the claim that a relay and an apply work-order are the whole of what the architect sends from the places named when the delegated round was named — the heading, the body sentence, and the "nothing else" inventory paragraph. The same claim, in the same closed-list shape, still stands in further places the earlier task never named.

§ "Spawn once, message thereafter" opens: "The first channel-message is the spawn — the first `::` relay or, where none has arrived, the first authored apply work-order — and its content *is* the spawn prompt." Read plainly, the spawn is named as only the relay or the apply work-order; a first-ever channel-message could now also be a self-authored `REPORT-ONLY` round delegating the architect's own legwork, and the sentence does not name that case.

The same section's respawn passage reads: "The respawn is the next channel-message after that report, never eager with authored prose: the user re-phrases a relay as a self-contained spawn prompt, or an apply work-order is resent as-is." Again named as only the relay or the apply work-order, and a delegated round belongs in neither branch as written.

§ "Review in parallel, reconcile before the apply order" closes: "You never decide *when* something goes to the editor; the marker does." The marker decides when a *relay* goes; it has nothing to do with a delegated round, which the architect sends entirely on its own initiative and the marker never touches — read plainly, this sentence claims the marker governs timing for everything, which is no longer true.

37.1 already landed the rule this third site needs, in a different section of the same file. § "Relay on the marker; author the apply work-order and your own legwork" states, in the paragraph naming the delegated case: "That second case needs no marker and no permission: the marker governs whose words cross the channel, not you delegating your own work." The third site's repair does not need to say this again — it needs only to stop claiming the marker decides for everything.

## The change

The spawn and respawn sentences widen in the file's own construction — the case-naming paragraph's own "delegating your own legwork" — never a noun for the round itself, which the file does not have and this task does not invent. The spawn sentence comes to cover a third possibility beside the first `::` relay and the first authored apply work-order: the architect's own `REPORT-ONLY` round, delegating its own legwork on its own initiative, opened with no marker, whichever of the three arrives first. The respawn sentence widens the same way, beside the user re-phrasing a relay and an apply work-order resent as-is: the architect re-authoring its own delegated legwork, the same way an apply work-order is resent.

The third site narrows rather than widens. "You never decide *when* something goes to the editor; the marker does" is corrected to name only what is true of the marker: it decides when a relay goes. Nothing is added beside it, and no pointer to the case-naming paragraph is inserted either — the rule for the delegated case already stands where 37.1 put it, quoted above, and this site's own repair is dropping the overreach, not restating or pointing at what already stands elsewhere.

The marker's own actual scope does not change — it always only governed relay timing; what changes is the sentence that claimed otherwise. Nothing here touches the apply work-order's own timing rule (the user's confirmation, stated elsewhere) — this task only stops the same closed-list claim from resurfacing anywhere else in the file.

## Blast radius

These sites are the ones 37.1's own review found and named as left standing outside its file boundary; no other site in `agent-architect` states the relay-and-work-order pair as exhaustive — checked directly against the current file, not assumed from the earlier finding.

This task's spawn-sentence site sits directly after the paragraph 40.7 edits — the memory-snapshot paragraph stating "Each new snapshot supersedes the last by name" — and its respawn-sentence site sits directly after the fallback-recovery paragraph 40.2 edits. Neither collides: each edit anchors on its own untouched sentence, and neither adjacent pair shares a sentence or a dependency.

`editor.md` and `architect-editor-engine` are not touched: all of them are the architect's own account of its own sending behavior, not a claim the receiving side makes about what arrives.
