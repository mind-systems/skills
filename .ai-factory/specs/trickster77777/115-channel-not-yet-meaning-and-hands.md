# Phase 37 — the channel is not yet meaning, and the hand is not yet the writer

`agent-architect` states "author a prompt in exactly one case" in its section heading, and the body repeats it — false in practice. The same section also states "pin every value, path, and exact string it needs," the textual source of the character-pinning register `docs/paired-loop.md` argues against, not a bare habit. Rescue reports appear nowhere in `agent-architect`; handoffs appear once, narrowly. Neither `agent-architect` nor `src/commands/command-handoff.md` distinguishes a handoff continuing the same architect across a compact from one addressed to a different agent or repository, and `docs/paired-loop.md` does not state that distinction either — no document names it.

See `docs/paired-loop.md`.
