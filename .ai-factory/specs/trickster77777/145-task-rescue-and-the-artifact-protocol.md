# Phase 51 — task-rescue and the artifact protocol close their own gaps

**The sidecar path, stated as behavioural.** `src/skills/task-rescue/SKILL.md` locates the sidecar at the flat `.ai-factory/plans/{seq}-{slug}.json` at two sites (originally `:387`/`:424`, `:407`/`:444` after 30.1 inserted its new Step 1 sub-block) — wrong for any named roadmap, since this very repository's own sidecar lives at `.ai-factory/plans/trickster77777/24-….json`. Task 30.1's new read site correctly routes through `orchestrator-artifacts` § 1's locator, but Step 5's rollback write still writes the `step` field through the flat path — one file now holds two different answers to "where is the sidecar," and for a named-roadmap rescue the write lands at the wrong path while the read finds the right one. This is not a stale description; it is the mechanism itself writing to a location that does not hold what it thinks it holds, and a follow-up task should route both write sites through the same locator the new read site uses (three occurrences).

**`task-rescue`'s file size.** `src/skills/task-rescue/SKILL.md` was 569 lines before 28.2 and is 581 after, against the ≤ 500-line body constraint in `.ai-factory/ARCHITECTURE.md` § "Skill anatomy" and this repo's own CLAUDE.md § "Key constraints." The remedy is structural — moving the depth procedures or the `## What NOT to do` inventory into `references/` — a re-shaping no widening edit can carry; every future widening of this file inherits the same tension and the split is worth its own task rather than another round of "it was already over" (five occurrences across 28.2's plan-reviews and reviews).

**The marker grammar's own two small gaps.** `orchestrator-artifacts` § 6 (`SKILL.md:62-63`) attributes the routed/dismissed markers to "the resolution session — the dedicated session the user opens from the parked prune's handoff" as their only writer, but after 23.1's citation fix `task-rescue` writes those same markers at disposal time — a second, legitimate pin-writer § 6's text does not name (one occurrence). And `task-rescue` Step 5.6 (`SKILL.md:427-432`) offers only a routed branch and a dismissed branch, while § 6 defines three current markers including `[fixed]`; there is no branch for the case where rescue's own spec-plus-plan-plus-code hand-fix directly closes a deferred observation's gap within the same session — the two-branch structure is pre-existing and 23.1 correctly left it alone, but a later pass should decide whether Step 5.6 needs a `[fixed]` disposition of its own (one occurrence).

**The path shorthand.** `task-rescue`'s closed-set table preamble (`:355`) cites the mirror source as `orchestrator/resume.py`, while the grove-root-relative path is `orchestrator/orchestrator/resume.py` — the form the task spec itself uses at its own line 19. Deliberately left byte-identical by 21.1, whose scope was the marker grammar only; a later scoped pass should reconcile the shorthand across both `task-rescue` and `orchestrator-artifacts` § 7 (one occurrence).

**Hand-composing is not explicitly forbidden.** `task-rescue` grants `Write`, and its Step 5.7 states delegation to `note` for the durable rescue report without the explicit ban `command-handoff.md:95` carries for the same kind of delegation ("do not mine, number, slug, `mkdir`, or `Write` yourself"). The step is unambiguous as written — the report is written *through* `note`, whose hooks drive the numbering and the final path — so this is a hardening note for whenever the two callers are next touched together, not a defect in 30.1 (one occurrence).

Nothing under `docs/` is written by this phase's own note.
