# Phase 36 — the buffer is not yet shared memory

`agent-architect` defines the buffer alone and closes it to the editor by name ("The editor is never told about it"), against `docs/paired-loop.md` § "What the memory holds, and who holds it", which has the editor hold it as its own working context. No settled/live split exists anywhere in the four paired-loop artifacts — the buffer's stated content is deferral entries alone. It is created at the editor's spawn, not the architect's own start, so an architect with no editor yet is told to hold nothing, against the governing spec's own position in § "How the memory begins, and how it survives" — "The memory exists before the hands do." That same section now has "Each half holds the other's address," and the skill still has only one direction: the architect records the editor's handle, but nothing tells the editor where the buffer is. `architect-editor-engine` scopes itself to the two message formats and names no buffer, though the governing spec calls it the buffer's home; `architect-pairing-engine` never mentions a buffer at all, so the no-cross-reading rule has nowhere to live. The governing spec now states that "A hand's own history ends without a signal" and that the head's latest memory snapshot recovers it, handed over whenever the head itself rehydrates; the skill still says the opposite of that recovery — the digest is "your own recovery note and is never sent to the editor" — so what was undocumented on both sides has become documented on one and contradicted on the other. `.ai-factory/ARCHITECTURE.md`'s mechanism/policy rule does not block moving the definition into the engine.

See `docs/paired-loop.md`.
