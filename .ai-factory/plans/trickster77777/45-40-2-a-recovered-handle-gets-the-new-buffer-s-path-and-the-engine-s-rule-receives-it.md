# Plan: 40.2 — a recovered handle gets the new buffer's path, and the engine's rule receives it

## Context
`src/skills/agent-architect/SKILL.md` holds two rules written apart: the conditional buffer rule at the top of "Spawn once, message thereafter" (no snapshot pointer → "you are a new architect and create your own buffer first"), and the metadata fallback in the recovery paragraph of the same section (no handle at recovery → read `~/.claude/projects/<project-key>/<session-id>/subagents/agent-<id>.meta.json`, newest by mtime). An architect that reaches the fallback has, by the fallback's own precondition, no buffer pointer — so it creates buffer B — while the editor whose handle it recovers was spawned by an earlier run and still holds and re-reads buffer A. Nothing on either side says what happens next. `src/skills/architect-editor-engine/SKILL.md` § "The architect's buffer" states only "The editor re-reads the settled zone when it changes, not once at birth" — in-place change of the buffer it holds, not being named a different buffer's path outright. This task pins the resolution in both files: the recovered architect still creates its own buffer, names its path to the recovered editor inside the next message as ordinary upkeep (the mechanism `docs/paired-loop.md` § "What crosses the channel" already states), and the engine's re-read sentence widens so the hand adopts a newly named path, replacing what it held.

The governing spec, `docs/paired-loop.md` § "How the memory begins, and how it survives", states the address exchange as symmetric — "Each half holds the other's address. At the moment a hand joins, its own address is recorded in the memory, and the memory's place is given to the hand in turn" — so the recovery path must carry both halves: the recovered handle is written into the new buffer, and the new buffer's path is given to the recovered hand. The handle-write paragraph in `agent-architect` times its write "At the moment you spawn the editor", and a recovery is not a spawn, so without a sentence in the recovery paragraph the handle never reaches the new buffer, the next snapshot carries a buffer with no handle, and the following recovery falls into the same fallback this task closes. Both halves land in the recovery paragraph; the handle-write paragraph stays byte-identical.

Grounded read (fresh): the recovery paragraph is the one opening "Continue in the same conversation if the editor is still alive." and ending "…the id is the filename segment between `agent-` and `.meta.json`." The paragraph after it ("If the send fails, the editor is dead…") is 40.6's territory and is not touched. The spawn paragraph's sentence "in that same act give the editor the buffer's path through the spawn prompt, as defined above, so each half holds the other's address from the spawn on; a later round sent via `SendMessage` never repeats it" stays as written — the recovery case names a *different* path (the architect's own new buffer), never a repeat of the one the spawn gave, so the two sentences do not conflict and the new text must be worded so that this is plain. The rescoped-inventory paragraph in "Relay on the marker; author the apply work-order and your own legwork" ("Keeping the hand current — naming that the shared memory has moved (see "Your buffer is shared; you alone write it"), or handing it the buffer's path at spawn … — is not a round and needs no form of its own") already classes this act as upkeep; the buffer section's "When the memory the hand holds moves, you name the change to the editor in the same act as the write" is the same obligation. Neither is edited; the new text may point at them by heading name. `src/agents/editor.md` names the buffer only as "whose settled zone you hold" and "gives you the buffer's own path — held from birth"; it is untouched per the spec — the widened rule lives in the engine both halves load at birth. `docs/paired-loop.md` is not edited. Current line counts: `agent-architect` 320, `architect-editor-engine` 45 — both well under the 500-line bound.

Assumption: the engine's frontmatter `description:` ("the editor's re-read of the settled zone on change") is left unchanged — the rule keeps its name; only its coverage widens, and the spec bounds the engine edit to the re-read sentence itself ("widened, not restated").

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### The architect names its own buffer to the recovered hand

- [x] **Extend the fallback-recovery paragraph of § "Spawn once, message thereafter" with the resolution and its reasons**
  Files: `src/skills/agent-architect/SKILL.md`
  Append to the paragraph that opens "Continue in the same conversation if the editor is still alive." — after its last sentence ("…the id is the filename segment between `agent-` and `.meta.json`.") and before the next paragraph ("If the send fails, the editor is dead…") — prose that states, in the file's own register (second person, hedge-free, no bullets, no new heading, no new `loads:` edge):
  1. **The rule.** A handle recovered this way came with no buffer pointer of your own, so the conditional rule at the top of this section applies unchanged: you are a new architect and create your own buffer at the path and numbering `architect-editor-engine` defines, exactly as you would with no recovered handle at all. Pin the recovered editor's identity in one framing only, and use it throughout the new text: the editor was spawned by an architect whose buffer did not reach you — it still holds, and re-reads, *that* buffer — and you are a new architect; the re-pointing below is precisely what makes that editor your hand, holding your buffer rather than the one that spawned it. Never write "an earlier run of this architect", "the same architect", or any phrasing that claims lineage between you and the spawning architect — the conditional rule denies it, and the engine's "an editor reads only its own architect's buffer" reads correctly only under the one framing. Then the two halves of one act, per the governing spec's rule that each half holds the other's address: (a) you write the recovered handle into your own new buffer — the same write the spawn moment already makes, now made at recovery too, since the recovery is not a spawn and the handle-write paragraph's timing ("At the moment you spawn the editor") does not otherwise cover it; point at that paragraph by concept ("the handle-write the spawn moment makes"), do not restate its mechanics, and do not edit it — without this write the next snapshot carries a buffer holding no handle and the next recovery falls into this same fallback again; (b) in the same act you name your own buffer's path to that editor, and the naming rides inside the next message you send it, as ordinary upkeep: no form, no permission, no message of its own — the same act the buffer section already binds you to when the memory the hand holds moves (refer to it by its heading, "Your buffer is shared; you alone write it", and to the upkeep classing under "Relay on the marker; author the apply work-order and your own legwork" — by name only, restating neither). Make explicit that the path named is a *different* one — your new buffer's, not the one the spawn gave the editor — so the spawn paragraph's "a later round … never repeats it" stays literally true. Do not describe what the editor does on receiving the path beyond "the hand adopts it, replacing what it held" — the receiving half is the engine's rule (next task); point there rather than restating it.
  2. **The ordering with the probe.** The liveness probe is unchanged: attempting to send the next channel-message to the recorded handle is still the probe, and the naming rides inside that very message — if the send lands, the editor now holds your buffer; if it fails, the paragraph that follows governs, and nothing here changes it. Do not restate or edit the respawn paragraph.
  3. **Why not the alternatives** (both must be named, each with its reason, in one or two sentences apiece): leaving both buffers live — your new one and the editor's old one — is not a resolution but the defect itself, the pair holding two memories with nothing saying so on either end; discarding the recovered editor to spawn a fresh one throws away a live, working hand with accumulated history for no reason tied to this situation — the editor is not dead, only pointed at the wrong memory — and the file already treats losing a working editor as a cost ("Losing the editor is never fatal; losing it silently is the defect"). Re-pointing is the one path that keeps exactly one live memory and keeps the hand already working.
  Guardrails: edit only this paragraph; leave the conditional buffer rule, the spawn paragraph, the snapshot paragraph, the handle-write paragraph, the "If the send fails" paragraph, and every other section byte-identical. Use the reserved vocabulary where the concept is named (buffer, settled zone, handle, editor/hand, snapshot); it need not conform in form. Keep the SKILL.md body ≤ 500 lines. Verify with `grep -n "recovered" src/skills/agent-architect/SKILL.md` (zero hits today) that every hit lies inside the recovery paragraph — between the line opening "Continue in the same conversation" and the line opening "If the send fails" — and with `grep -c "own buffer" src/skills/agent-architect/SKILL.md` that the one pre-existing hit in the conditional rule ("create your own buffer first") is still there, plus only whatever the new text adds; the lineage-phrasing probe is scoped to the recovery paragraph, because "same architect" legitimately occurs today in the snapshot paragraph ("The memory snapshot continuing this same architect has two occasions"), where the lineage is real: `sed -n '/^Continue in the same conversation/,/^If the send fails/p' src/skills/agent-architect/SKILL.md | grep -n "earlier run\|same architect"` must return nothing (the range's closing line is the untouched respawn opener and carries neither phrase).

### The engine's rule receives the naming

- [x] **Widen the re-read sentence in `architect-editor-engine` § "The architect's buffer" to cover being named a different path outright** (depends on the previous task only for consistency of wording — may be done in the same pass)
  Files: `src/skills/architect-editor-engine/SKILL.md`
  Rewrite the standalone sentence "The editor re-reads the settled zone when it changes, not once at birth." so the one sentence (or the one short paragraph it becomes) carries both cases: the settled zone of the buffer the editor holds changing in place, *and* the editor being named a different buffer's path outright by its architect — either way the memory has moved, and the hand's obligation is the same: adopt what it is now told, replacing what it held, holding one memory at a time. Keep it as the same rule widened, not a second rule beside it: no new section, no new heading, no bullet list, no restatement of the two zones or of the buffer's path and numbering, no mention of the metadata fallback or of how the architect came to hold no pointer (that is the caller's policy, not the engine's mechanism). The "not once at birth" contrast must survive. Do not touch the frontmatter `description:`, "The mode rule", the drain-rule paragraph, or the two-architects paragraph; do not add a `loads:` field (the engine has none and lists no callers). `src/agents/editor.md` and `docs/paired-loop.md` are not edited.
  After editing, verify: `grep -n "re-reads" src/skills/architect-editor-engine/SKILL.md` shows the single widened sentence; `grep -rn "re-reads the settled zone" src/ docs/` still resolves (the architect's buffer section refers to "the editor's re-read of the settled zone" by concept, not by quoting the sentence, so no other site needs rewording); `wc -l` on both edited files stays ≤ 500.
