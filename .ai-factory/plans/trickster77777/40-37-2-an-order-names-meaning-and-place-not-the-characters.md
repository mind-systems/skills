# Plan: 37.2 — an order names meaning and place, not the characters

## Context
`src/skills/agent-architect/SKILL.md` § "Relay on the marker; author the apply work-order and your own legwork" tells the architect to send the apply work-order and "pin every value, path, and exact string it needs" — the one passage in the file that sources the character-pinning register `docs/paired-loop.md` § "What crosses the channel" argues against. This task rewrites that instruction so an order names what a sentence must say and where it goes while the text itself is written by the editor who has the file open — and so a value (path, number, literal, anchor) stays pinned exactly as before, with no reader able to take it as "pin nothing."

Ground truth read fresh: the sentence sits in the paragraph opening "You author your own prompt in two cases" (already fixed by 37.1, commit `6a5c02b`), immediately after "Send the apply work-order as an `APPLY-EDIT` channel-message:" — the clause "pin every value, path, and exact string it needs; state the guardrails — what NOT to touch, a collision-safe method where order matters; and an explicit **"do not commit."** Leave the mechanical steps to the editor — it does the obvious unprompted, and over-told steps only drift." No other sentence in `agent-architect` states the register. Neighbouring files (`src/agents/editor.md` "A work-order pins every value, path, and guardrail for a reason"; `architect-pairing-engine` "the same pinned values, guardrails"; `architect-editor-engine` "a pinned apply work-order") speak only of pinned values/paths/guardrails — they already sit on the retained side of the boundary and are out of this task's scope, untouched.

Governing spec for the wording: `docs/paired-loop.md` § "What crosses the channel", first paragraph (an order names meaning and place; the text is written by whoever has the file open; a writer looking at the passage does not restate what stands beside it, and a head composing blind does exactly that) and fifth paragraph (every address is a name, never a position — a heading, a bolded rule, a symbol, a unique string).

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### Rewrite the apply work-order instruction

- [x] **Replace the "pin every value, path, and exact string" clause with the meaning-and-place rule, keeping values pinned**
  Files: `src/skills/agent-architect/SKILL.md`
  In § "Relay on the marker; author the apply work-order and your own legwork", in the paragraph opening "You author your own prompt in two cases", edit only the run beginning at "Send the apply work-order as an `APPLY-EDIT` channel-message:" through "over-told steps only drift." Do not touch the two-cases sentence before it (37.1's landing) or the following paragraph ("The two channel-message formats govern what opens a round…"). The rewritten run must state, in the file's existing second-person prose register (no bullets, no table, wrapped at the file's ~78-column width):
  1. **What an order names.** An order names what a sentence must say and where it goes — the meaning and the place. It does not compose the sentence: the text is written by the editor, who has the file open. Give the reason as non-stylistic, drawn from `docs/paired-loop.md` § "What crosses the channel": a writer looking at the passage does not restate what already stands beside it, and composing a replacement blind — for a file you do not have open — is exactly how that restatement happens.
  2. **What stays pinned (the boundary).** State explicitly that a value is still pinned: a path, a number, a literal that must appear verbatim, an anchor a match is asserted against — these are the thing itself, not a description of it, and remain in the order exactly as before. Word it so that "pin nothing" is not a possible reading — e.g. name the two sides in one sentence: what stops is composing prose for a file you have not got open; what does not stop is pinning the values the edit turns on. Do not drop the word "pin" from the passage — the editor (`src/agents/editor.md` § "Apply mode: apply exactly, add no scope") and `architect-pairing-engine` still read the work-order as pinned values and guardrails, and this file must keep saying so.
  3. **The anchor is unchanged.** Keep the anchoring mechanism as it is: a place is addressed by a name — a heading, a bolded rule, a symbol, a unique string a match is asserted against — never a position. One clause is enough; the deep discipline lives in `docs/paired-loop.md` and is not restated.
  4. **Guardrails and "do not commit" retained verbatim in substance.** Keep the existing guardrails clause (what NOT to touch, a collision-safe method where order matters) and the explicit **"do not commit."**, and keep the closing sentence about leaving mechanical steps to the editor ("it does the obvious unprompted, and over-told steps only drift") — that sentence already argues the same direction and the new text should read as its complement, not a second version of it.
  Constraints: no new section, no new heading, no new `loads:` edge, no change to `description:` frontmatter. Do not cite the roadmap, the phase, the spec, or any `.ai-factory/` path in the skill text. Do not link `docs/paired-loop.md` from the skill unless the section already links it (it currently does not; keep it that way — the rule is stated in the skill's own words). After the edit, `grep -n "exact string" src/skills/agent-architect/SKILL.md` must return nothing, and `grep -n "pin" src/skills/agent-architect/SKILL.md` must still hit the rewritten passage.
