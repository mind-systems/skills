# Plan: 40.5 — a delegated round's report is an echo, not signal, and the skill says so

## Context
`docs/paired-loop.md` § "The user's marker" states two weights for two kinds of answer: a relayed payload's agreement "is signal, reconciled against the architect's own read"; a delegated round's carries "no second opinion in the result — it is a hand, and treating its agreement as corroboration mistakes an echo for evidence." `src/skills/agent-architect/SKILL.md` carries only the first: the reconcile step is stated for relays ("This reconcile step applies to every before-mark relay, not only a review-shaped one") and the delegated case is named — "a `REPORT-ONLY` round on your own initiative, delegating your own legwork" — with nothing said about how its report is weighed once it returns. This task adds the echo rule as its own sentence beside the paragraph that names the delegated case, and edits nothing else.

Grounded read (fresh, at commit `415ecb4`): the delegated case is named once, in § "Relay on the marker; author the apply work-order and your own legwork", in the paragraph opening "You author your own prompt in two cases: the **apply work-order**, once the user has confirmed the edits, and a `REPORT-ONLY` round on your own initiative, delegating your own legwork — a message you compose yourself, carrying no relayed user payload and asking for no edit, opening with the literal `REPORT-ONLY` token like every message of that format." The next sentence — "That second case needs no marker and no permission: the marker governs whose words cross the channel, not you delegating your own work." — is the last sentence about the delegated case; the paragraph then turns to the apply work-order ("Send the apply work-order as an `APPLY-EDIT` channel-message: …") and stays there to its end. The word "echo" occurs twice in the file today, both about a relay's second reading: "the only way its agreement is real signal rather than manufactured echo" (the enrichment paragraph, same section) and "its agreement can no longer be told from an echo" (§ "Nothing closes a round before the report on it exists"). The reconcile sentence at issue sits in the before-mark paragraph earlier in the same section, and — being hard-wrapped — spans two physical lines: `the user both your own read and a summary of the editor's. This reconcile` / `step applies to every before-mark relay, not only a review-shaped one. You`; a single-line grep for the whole sentence returns 0 on the unedited file and must return 0 after a correct edit too. The file is 361 lines and hard-wrapped at roughly 78 columns, so the two anchor sentences share one physical line — `the channel, not you delegating your own work. Send the apply work-order as` — and the insertion point is mid-line, not between lines; the paragraph already carries ragged short lines ("enrich the payload before sending only", "guardrails — what NOT to touch, a collision-safe method"), so leaving a short line behind needs no reflow of what follows. The file never coins a noun for the delegated round — it says "delegating your own legwork" / "your own delegated legwork" every time — and the spec pins that construction for the new sentence too.

Assumptions: `docs/paired-loop.md`, `src/agents/editor.md`, and `src/skills/architect-editor-engine/SKILL.md` are not edited (per the spec's blast radius: the weighing is the architect's reading discipline, and the governing spec already states the rule). The frontmatter `description:` of `agent-architect` is left unchanged — the spec bounds the edit to one paragraph of the body. The paragraph beneath ("The two channel-message formats govern what opens a round …", the rescoped inventory), the marker-flagging paragraph ("When the editor flags back a scope question …"), and § "Verify the report by fact" are not touched — the last of these is about checking a report's claims, not weighing its agreement, and the spec names the site as the paragraph 37.1 authored.

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### The delegated case gets its weighing rule

- [x] **Add the echo rule as its own sentence(s) right after "That second case needs no marker and no permission: the marker governs whose words cross the channel, not you delegating your own work."**
  Files: `src/skills/agent-architect/SKILL.md`
  In § "Relay on the marker; author the apply work-order and your own legwork", inside the paragraph opening "You author your own prompt in two cases:", immediately after the sentence ending "not you delegating your own work." and before "Send the apply work-order as an `APPLY-EDIT` channel-message:", insert one or two sentences — the skill's own register (second person, declarative, no bullets, no heading) — stating the governing spec's rule as a fact of its own:
  1. **What the report is.** A report on your own delegated legwork carries no second opinion: the editor did your own asking, not the user's, so what comes back is a hand's answer to your own question — there is no independent reading in it to reconcile against.
  2. **What its agreement is not.** Reading its agreement as corroboration mistakes an echo for evidence — the same way the governing spec puts it; it is not signal the way a relay's agreement is.
  Line shape: the two anchor sentences share the physical line `the channel, not you delegating your own work. Send the apply work-order as`; rewrite that one line so its head ("the channel, not you delegating your own work.") is kept, the new sentence(s) follow it hard-wrapped to the file's column width, and "Send the apply work-order as" is left as its own short line — so every line beneath it stays untouched and the paragraph is not reflowed. Byte-exactness in the guardrails below is at the sentence level, not the line level: this one line is the only line the edit rewrites. When hard-wrapping the new text, keep each grep-checked phrase — "second opinion" and "echo" — whole on one physical line; a wrap that splits "second" from "opinion" makes the Verify checks below misreport a correct edit as a miss. Do not rejoin any existing wrapped line to make a check pass — in particular never rejoin the two lines carrying the reconcile sentence.
  Word the delegated case in the file's own construction — "your own delegated legwork" / "delegating your own legwork" — never a noun for the round the file does not have (no "delegated round", "delegation round", "self-round", or any capitalised label). The sentence stands on its own and does not point at, quote, or qualify the reconcile step: no "unlike a relay's, which is reconciled …" clause that would fold the two rules into one; the contrast with a relay is at most the phrase that a relay's agreement is signal and this is not.
  Guardrails: the reconcile sentence "This reconcile step applies to every before-mark relay, not only a review-shaped one." stays byte-exact, as does the whole before-mark paragraph it lives in; every other sentence of the "You author your own prompt in two cases" paragraph stays byte-exact — the insertion is additive between two existing sentences; the paragraph stays one paragraph, no split; the two existing "echo" sentences (enrichment paragraph, § "Nothing closes a round before the report on it exists") are not reworded; § "Verify the report by fact" is not touched — verification by fact is a different discipline from weighing agreement; no new `loads:` edge, no change to the frontmatter; `docs/paired-loop.md`, `src/agents/editor.md`, `src/skills/architect-editor-engine/SKILL.md` are not edited. Keep the file under 500 lines (361 today). Do not commit.
  Verify: `grep -c "step applies to every before-mark relay, not only a review-shaped one" src/skills/agent-architect/SKILL.md` is 1 and `grep -c "a summary of the editor's. This reconcile$" src/skills/agent-architect/SKILL.md` is 1 (the sentence still spans its two original lines), and `git diff src/skills/agent-architect/SKILL.md` contains no hunk whose lines include "This reconcile" or "step applies to every before-mark relay" — the reconcile sentence's byte-exactness is a guardrail on the diff, never a single-line grep; `grep -n "echo" src/skills/agent-architect/SKILL.md` returns exactly three hits — the two pre-existing ones plus one new hit lying after the line containing "not you delegating your own work" and before the line that begins "Send the apply work-order as" (that line now exists on its own, and everything from `an \`APPLY-EDIT\` channel-message:` onward is unchanged); `grep -n "second opinion" src/skills/agent-architect/SKILL.md` returns one hit in that same span; `grep -in "delegated round\|delegation round" src/skills/agent-architect/SKILL.md` returns nothing; `git diff src/skills/agent-architect/SKILL.md` shows exactly one deleted line, and its text is `the channel, not you delegating your own work. Send the apply work-order as` — the concatenation of the two anchor fragments — with the added lines all lying in the span above; no other line is deleted; `git diff --stat` shows only that file; `git status --short` lists only that file; `wc -l src/skills/agent-architect/SKILL.md` ≤ 500.
