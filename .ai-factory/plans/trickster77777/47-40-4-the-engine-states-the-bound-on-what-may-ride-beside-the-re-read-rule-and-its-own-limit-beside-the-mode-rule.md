# Plan: 40.4 — the engine states the bound on what may ride beside the re-read rule, and its own limit beside the mode rule

## Context
`src/skills/architect-editor-engine/SKILL.md` is the one file both halves of the pair load at birth, in every project, and today it carries neither half of the paragraph `docs/paired-loop.md` § "What crosses the channel" states about keeping the hand current: not the permission (upkeep "rides inside whatever message the head is already sending, never as one of its own … which is also why the rule for an unrecognised opening never reaches it") and not the bound (the relay's protection is untouched — "what that protection guards is the second reading itself — and a fact about where the shared memory sits carries no reading of it at all"). A reader of the engine alone therefore has "Ambiguity resolves to `REPORT-ONLY`" with nothing that says upkeep never reaches that fallback, and nothing that stops the permission — once learned from `agent-architect` — from reading as license to append anything to a relayed payload. This task puts the fact and its bound in § "The architect's buffer", beside the re-read rule 40.2 already widened, and puts the mode rule's own limit in § "The mode rule" as a pointer — one home per fact, the argument staying in the governing spec.

Grounded read (fresh, at commit `32db19f`): 40.2 has landed — the re-read paragraph in § "The architect's buffer" now reads "The editor re-reads the settled zone when it changes, not once at birth — and the same rule covers being named a different buffer's path outright by its architect: either way the memory has moved, and the hand adopts what it is now told, replacing what it held, holding one memory at a time." § "The mode rule" is one paragraph: "A receiver keys its mode strictly off the token that opens the message — never off content, never off a referenced skill's own default. Ambiguity resolves to `REPORT-ONLY`. The full authoring and execution mechanics of each format stay with the callers; this engine holds the two formats, the rule for telling them apart, and the buffer's definition below — when-to-use policy stays with the caller." The file is 45 lines; the words "finding", "verdict", and "payload" do not occur in it today; "conclusion" occurs once, in the live-zone sentence ("a hand that already holds the head's conclusion returns an echo"). `agent-architect` already names the same act on its own side ("riding inside the next message you send it, as ordinary upkeep — no form, no permission, no message of its own" in the recovery paragraph; "Keeping the hand current … is not a round and needs no form of its own" under "Relay on the marker; author the apply work-order and your own legwork") and holds the enrichment ban — none of that is restated or edited here. `src/agents/editor.md` keys off the opening token and states the no-framing sentence ("no findings, no checklist, no verdict for you to confirm"); it is not edited.

Assumptions: the frontmatter `description:` is left unchanged — the spec bounds the edit to two body sentences inside two existing sections and names no change to the skill description; the engine's list of what it "holds" in § "The mode rule" ("the two formats, the rule for telling them apart, and the buffer's definition below") also stays as written, since the new statement lives inside the buffer section and is covered by "the buffer's definition below". The concept is named in the engine's own words — "keeping the hand current" as the governing spec has it, or "upkeep" as `agent-architect` has it — but never as a capitalised token or a noun that reads as a third format.

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### The buffer section states what may ride, and the bound

- [x] **Add the ride-alongside statement and its bound to § "The architect's buffer", immediately after the widened re-read paragraph**
  Files: `src/skills/architect-editor-engine/SKILL.md`
  Directly after the paragraph that opens "The editor re-reads the settled zone when it changes, not once at birth" and before the drain-rule paragraph ("A ruling recorded in the buffer is a debt against the skill…"), add one short paragraph — two or three sentences, in the engine's own register (declarative, third person, no bullets, no heading, no new `loads:` edge) — carrying both halves of the governing spec's paragraph as a stated limit, not an argument:
  1. **What may ride.** A channel-message the architect is already sending may carry, alongside its payload, what keeps the hand current: a fact about where the shared memory sits — that the settled zone has moved, or the buffer's path itself — or what the hand needs in order to work at all. This opens no message of its own and takes no form of its own; it rides inside a message that already opens with its token.
  2. **The bound, in the same breath.** It is never a reading of the payload: no finding, no conclusion, no verdict about what the message carries — what crosses for an independent reading still reaches the hand undisturbed, and a fact about where the memory sits carries no reading of it.
  Guardrails: do not restate the buffer's path and numbering, the two zones, or the re-read rule's own wording — the new paragraph sits beside them and refers to "the settled zone" / "the buffer's path" by name only; do not restate the architect-side enrichment mechanics (the `::` marker, before-mark/after-mark, the explicit enrich instruction) — the engine states the limit, `agent-architect` keeps the mechanism; do not mention the metadata fallback, the spawn moment, or recovery — those are the caller's occasions, not the engine's rule; do not name a third format or coin a token-like noun for upkeep; do not give a reason why the bound exists beyond the one clause above ("carries no reading of it") — the argument stays in `docs/paired-loop.md` § "What crosses the channel". Leave the paragraphs before and after byte-identical. Keep the file well under 500 lines (45 today).
  Verify: `grep -n "verdict" src/skills/architect-editor-engine/SKILL.md` returns exactly one hit and it lies between the line opening "The editor re-reads the settled zone" and the line opening "A ruling recorded in the buffer"; `grep -c "re-reads the settled zone" src/skills/architect-editor-engine/SKILL.md` is still 1 (the widened sentence untouched); `grep -n "^## " src/skills/architect-editor-engine/SKILL.md` still lists exactly the three headings "The two channel-message formats", "The mode rule", "The architect's buffer".

### The mode rule keeps only its own limit

- [x] **Add the one-sentence limit to § "The mode rule", right after "Ambiguity resolves to `REPORT-ONLY`."** (depends on the previous task — the sentence points at what that task placed in the buffer section)
  Files: `src/skills/architect-editor-engine/SKILL.md`
  Inside the existing § "The mode rule" paragraph, immediately after the sentence "Ambiguity resolves to `REPORT-ONLY`." and before "The full authoring and execution mechanics of each format stay with the callers…", insert one sentence stating the rule's own limit: the fallback never reaches what keeps the hand current — what rides alongside a channel-message, as § "The architect's buffer" states, opens no message of its own, so there is no opening for this rule to classify; it travels inside a message that already carries its own token. Word it as a pointer to the buffer section by heading name, not as a second copy: the sentence names neither the content that may ride nor the bound on it — only that upkeep opens nothing, hence the ambiguity rule has nothing to resolve.
  Guardrails: "Ambiguity resolves to `REPORT-ONLY`." stays byte-exact, as do the sentences before and after the insertion; the paragraph stays one paragraph — no new heading, no new section, no list; the tokens `REPORT-ONLY` and `APPLY-EDIT` are not redefined and no third token appears; `src/agents/editor.md`, `src/skills/agent-architect/SKILL.md`, and `docs/paired-loop.md` are not edited.
  Verify: `grep -c "Ambiguity resolves to \`REPORT-ONLY\`" src/skills/architect-editor-engine/SKILL.md` is 1; `grep -n "The architect's buffer" src/skills/architect-editor-engine/SKILL.md` shows the heading plus the new pointer inside § "The mode rule"; `git status --short` shows only `src/skills/architect-editor-engine/SKILL.md` modified; `wc -l src/skills/architect-editor-engine/SKILL.md` ≤ 500.
