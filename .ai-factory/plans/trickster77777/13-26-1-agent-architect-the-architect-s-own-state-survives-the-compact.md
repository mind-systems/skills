# Plan: agent-architect: the architect's own state survives the compact

## Context
`src/skills/agent-architect/SKILL.md` keeps two pieces of the architect's own state — the editor's handle and the buffer's path — in prose that assumes the context holding them survives a compact: the handoff paragraph never names its addressee, the buffer section still describes itself as a pure deferral log although the handle now belongs there, and the liveness sentences name only the outcomes ("still alive" / "dead") without naming the test, a gap a live session filled in with `ListAgents` and got wrong. This task rewrites two regions of that one file so the mechanism is written down — the handoff paragraph (`:47-58`) and the "Your buffer is yours alone" section (`:161-169`) — plus a one-clause correction at `:183` that this same diff falsifies.

**Ground truth checked fresh.** The file on disk matches the spec's quoted `:47-58` and `:161-169` byte-for-byte, so both anchors are valid as written. The meta.json fallback shape the spec pins is real: `~/.claude/projects/-Users-max-projects-sakshi-orchestrator/<session-id>/subagents/agent-<id>.meta.json` exists and contains `{"agentType":"editor","description":…,"toolUseId":…,"spawnDepth":1}` — `agentType` names the editor's agent type exactly as the spec states. The `Agent` tool schema in the build used to check this exposes **no** `name:` parameter, which is precisely why the spec calls it a convenience "wherever the running build exposes it" and never the carrier.

**Three interpretive calls, made here so the implementer does not guess:**
1. *The digest sentence stays.* The roadmap line's "carry the buffer's path alone — the pointer, never a copy of what the buffer holds" scopes "alone" to **buffer-held state** (handle, pairing role): the handoff points at the buffer instead of copying what is in it. The existing "a digest of what it has accumulated … your own recovery note, never sent to the editor" is not buffer-held state and no clause of the spec retires it, so it carries forward. The spec's "Recorded state now spans three things" enumerates the three items whose home is the buffer; only the buffer's own path travels.
2. *Writing the handle at spawn creates the buffer if it does not exist yet.* Today the buffer is written lazily on the first deferral; recording the handle "at that same moment" as the spawn makes the spawn a second creation trigger. This is a consequence of the rule the spec already pins, at the same path and numbering — not new machinery.
3. *`:183` is corrected inside this diff, not deferred.* `:183` ("the pre-compact handoff that recorded your editor's handle") is falsified by this change: the handoff records the buffer's path, and the handle lives in the buffer. It sits in `## On every invocation` — the recovery entry point a post-compact architect reads first — so leaving it stale would reintroduce at `:183` the exact defect being removed at `:47-58`. The spec's `Files & types` bound reads "no restructuring beyond them"; correcting one noun phrase this diff itself falsifies is not restructuring, stays in the same file, and leaves every verification assertion (`git diff --stat` = one file, `git status --porcelain` = one entry) passing unchanged.

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### The handoff paragraph

- [x] **Rewrite the handoff-and-liveness paragraph (`:47-58`)**
  Files: `src/skills/agent-architect/SKILL.md`
  Replace the whole paragraph that currently begins "Before a compact, your handoff records the editor's handle…" and ends "…losing it silently is the defect." It sits between the `architect-editor-engine` load paragraph (`:42-45`) and the `## Relay on the marker…` heading (`:60`); anchor on that opening sentence, not on the line numbers. The rewrite states these four things **in the spec's order** — prose in the file's existing register (second person, present tense, no bullet list unless the paragraph genuinely reads better as one; keep it a paragraph):

  1. **Addressee (Defect A).** The handoff that carries the buffer's path is specifically the one addressed to *this same architect* continuing across a compact — no other handoff has any reason to mention the buffer or the handle, because nothing outside this skill ever learns about either. Say this as the qualifier on the sentence, not as a separate caveat afterwards, so the paragraph cannot be read as "every handoff carries it". Keep the existing digest clause in substance: the handoff also records a digest of what the editor has accumulated, which is the architect's own recovery note and is never sent to the editor. Of the recorded state, only the buffer's **path** travels — the pointer, never a copy of the handle or the role, which live in the buffer.
  2. **Recording at spawn, and at role assignment (Prevention + Defect B).** At the moment the editor is spawned — the existing one-spawn moment described in "Spawn once, message thereafter", which this task does not move or duplicate — write the editor's handle into the buffer, creating the buffer at its existing path if it does not exist yet. Where the running build exposes `Agent`'s `name:` parameter, spawn with it so the editor can later be addressed by name; phrase this conditionally and explicitly as an addressing convenience layered on the recorded handle, never as the carrier and never as a required step — the parameter is absent from some builds and nothing contracts the name's behaviour beyond the run. The buffer, not the handoff, is where the handle and any assigned pairing role live — and the role gets a **recording moment of its own**, on the same sentence: it is written into the buffer at the moment the user assigns it (`architect-pairing-engine`'s deciding or applying half), which may be mid-session and need not coincide with the spawn. Naming the home without the moment is the same outcome-without-mechanism shape this phase exists to correct; both items need a *when*, not just a *where*.
  3. **The liveness test (Defect C, The rule).** At recovery, the only liveness test is the next channel-message itself: attempting to send it to the recorded handle *is* the probe, and its outcome — not any prior lookup — is the signal. State explicitly that `ListAgents` is ruled out as a death signal: its subagent rows come from the live task registry of what is an active running task right now, a completed round drops out of it on the order of a minute, so absence from that listing is never evidence of death — a compact does not touch the editor at all, it only erases the address from the architect's own context. Introduce **no** third channel-message kind as a probe; `REPORT-ONLY` and `APPLY-EDIT` stay the only two.
     The fallback's trigger is the **recovery-time state, not the recording history**: it covers a handle you do not hold at recovery — never recorded, *or* recorded into a buffer whose path did not reach you (an auto-compact that fired before any handoff was written, or a handoff addressed elsewhere). Write the trigger in that two-branch form. The file already admits this state exists — `:182-183` says "if one exists, the pre-compact handoff" — and the spec's guard forbids a directory scan to locate the buffer, so without the second branch the architect has no stated route and re-spawns over a live editor, the very defect the paragraph's closing sentence names. This satisfies the spec's rule *a fortiori*: a never-recorded handle is one you do not hold. It adds no machinery and reopens nothing the guards retired — in particular it is still **not** a licence to scan for the buffer; the route recovers the *handle*, not the buffer path.
     Then name the fallback in full: read `~/.claude/projects/<project-key>/<session-id>/subagents/agent-<id>.meta.json`, where `<project-key>` is the working directory's path with separators replaced by hyphens, `<session-id>` is the running session's own id, `"agentType"` inside the file names the editor's agent type, newest first by file mtime, and the id is the filename segment between `agent-` and `.meta.json`. Write the full `~/.claude/projects/<project-key>/<session-id>/` root, not the bare `subagents/agent-<id>.meta.json` form. Never recover a handle from a listing, for the identical reason `ListAgents` is ruled out above.
  4. **The "dead" consequence, unchanged in substance.** Carry forward, byte-identical in meaning: report to the user **before anything is sent onward**; an undelivered payload is never auto-replayed into a fresh spawn, because the user phrased it for a warm context; the respawn is the next channel-message after that report, never eager with authored prose — the user re-phrases a relay as a self-contained spawn prompt, or an apply work-order is resent as-is; a respawned editor resumes through the same two channels, self-contained per round; losing the editor is never fatal, losing it silently is the defect. "Dead" is now defined by the failed delivery alone, so drop "a stale handle discovered at recovery, or a failed send" as a two-branch definition and let the failed send stand as the single signal.

  Guards on this task: do not touch the frontmatter — `allowed-tools` already grants `Read Grep Glob Bash`, which covers the meta.json fallback and the buffer write, and `loads:` already names both engines. Do not add `ListAgents` to `allowed-tools`; it is named only to be ruled out. Do not change "Spawn once, message thereafter" (`:30-45`), the `::` mechanics, the two-format statement, or any section other than the two regions plus the `:183` clause named in the next task. One spawn per session stays exactly as it is: this task changes only what happens at the spawn moment that already exists, never when or how often a spawn happens.

- [x] **Correct the falsified carrier at `:183`** (depends on Rewrite the handoff-and-liveness paragraph (`:47-58`))
  Files: `src/skills/agent-architect/SKILL.md`
  `## On every invocation` (`:180-183`, shifted by the rewrite above — anchor on the heading) closes with "…and, if one exists, the pre-compact handoff that recorded your editor's handle." After this diff the handoff records the buffer's path and the handle lives in the buffer, so that noun phrase names the wrong carrier at the recovery entry point. Correct that one clause to name the buffer's path as what the handoff recorded.
  Scope is one noun phrase and nothing else: the "if one exists" hedge stays (it is what makes the second fallback branch in the task above necessary), the rest of the sentence and the section stay byte-identical, and no mechanism from the rewritten paragraph is restated here — one home per fact, so this line points at the carrier, it does not re-explain it. Do not extend the section, do not add a sentence about the liveness test or the meta.json fallback.

### The buffer section

- [x] **Open the buffer section to the state it actually holds** (depends on Rewrite the handoff-and-liveness paragraph (`:47-58`))
  Files: `src/skills/agent-architect/SKILL.md`
  The `## Your buffer is yours alone` section (`:161-169` before the first edit — the earlier rewrite shifts these numbers, so anchor on the heading text). Keep the heading as-is; no rename is called for. Rewrite so the section:
  - **Opens by stating the buffer's real scope**: it holds whatever of the architect's own state must survive a compact — not deferral entries alone. Name all three items explicitly: the editor's handle, any pairing role the user has assigned for the session, and the deferral entries.
  - **States that the handoff continuing this architect across a compact carries the buffer's path alone**, mirroring the rewritten paragraph above without restating its mechanism: one home per fact, so point rather than copy. Do not re-explain the liveness test, the recording moments, the `name:` convenience, or the meta.json fallback here.
  - **Keeps the deferral-entry shape unchanged and primary**: each entry names *what*, *why deferred*, and the *trigger* that resolves it; delete an entry once it's done. Deferral entries remain the file's primary content.
  - **Keeps the closing sentence's substance**: it lives at `.ai-factory/notes/<NN>-architect-buffer.md`, numbered like the other temporary notes so several architects can coexist without colliding; the editor is never told about it and no work-order references it — nothing is broken if it happens to see the file; it is the one file you edit directly, because it isn't a shared artifact.

  Guard, from the spec and restated as a hard boundary: the path stays **exactly** `.ai-factory/notes/<NN>-architect-buffer.md` with its present numbering. No slug key, no session-derived id, no fallback directory scan to locate the buffer, no echo-the-path step. A prior attempt at this task built exactly that machinery for a problem this reduced fix does not have; all of it is retired by the spec and must not be reintroduced or offered as an option.

### Verification

- [x] **Verify the three regions and the diff boundary** (depends on Open the buffer section to the state it actually holds, Correct the falsified carrier at `:183`)
  Files: none
  Run the spec's own verification from the repo root `/Users/max/projects/sakshi/skills`:
  - `grep -n "name:" src/skills/agent-architect/SKILL.md` → at least one hit inside the rewritten paragraph (the frontmatter `name:` at `:2` will also match; confirm the paragraph hit exists separately).
  - `grep -n "ListAgents" src/skills/agent-architect/SKILL.md` → at least one hit, in a sentence that explicitly rules it out as the death signal.
  - `grep -n "subagents/agent-" src/skills/agent-architect/SKILL.md` → present, with the full `~/.claude/projects/<project-key>/<session-id>/` root stated, not the bare form.
  - `grep -n "<NN>-architect-buffer.md" src/skills/agent-architect/SKILL.md` → the path and numbering unchanged from today's form.
  - `git diff --stat` → exactly one file changed, `src/skills/agent-architect/SKILL.md`.
  - `git status --porcelain -- ':!.ai-factory'` → exactly one entry, ` M src/skills/agent-architect/SKILL.md` (this pathspec returns empty on the pre-implementation tree, so that entry is the whole delta). Any second entry under this pathspec is out of scope and must be reverted; never revert or delete anything under `.ai-factory/`, which is where the plan, sidecar, plan-review and review live.

  Then a manual read of the three regions:
  - The paragraph states, in order, the addressee of the handoff that carries the buffer's path, the at-spawn recording via `name:` plus the buffer write **and** the role's own recording moment at assignment, the send-based liveness test with `ListAgents` explicitly ruled out, the fallback triggered on the recovery-time state in its two-branch form ("never recorded, or recorded in a buffer whose path did not reach you") with the full meta.json root, and the unchanged "dead" consequence.
  - The buffer section opens by acknowledging the handle and any assigned pairing role, states that the handoff carries the buffer's path alone, keeps the deferral-entry shape, and keeps the closing sentence's substance.
  - `## On every invocation` names the buffer's path — not the editor's handle — as what the pre-compact handoff recorded, with the "if one exists" hedge intact and the rest of the section unchanged.

  Confirm untouched, per the spec's guards: `src/skills/note/SKILL.md`, `src/commands/command-handoff.md`, `src/skills/roadmap-engine/SKILL.md`, `src/agents/editor.md`, `src/skills/architect-editor-engine/SKILL.md`, `src/skills/architect-pairing-engine/SKILL.md`, `docs/`, and `CLAUDE.md` — this knowledge lives entirely in the architect's own skill and nowhere else.
