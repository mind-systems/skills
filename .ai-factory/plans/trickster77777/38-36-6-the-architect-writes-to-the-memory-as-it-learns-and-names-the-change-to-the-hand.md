# Plan: 36.6 — the architect writes to the memory as it learns, and names the change to the hand

## Context
`src/skills/agent-architect/SKILL.md` § "Your buffer is yours alone" names what the architect records but gives an occasion only to the editor's handle (at spawn) and a pairing role (when assigned) — nothing tells the architect when to write the settled content the engine now has the pair hold, what shape such an entry takes, or that the hand must be told when the memory moves. This task adds those three facts, as one prose paragraph in that section, from `docs/paired-loop.md` § "What the memory holds, and who holds it" and § "How the memory begins, and how it survives". One file changes.

Ground truth read fresh (post-36.5, commit `df94aa1`; file is 282 lines, hard-wrapped at ≤ 77 columns):
- § "Your buffer is yours alone" has two paragraphs. Paragraph 1 opens "Keep one buffer file for whatever of your own state must survive a compact: the editor's handle, any pairing role the user has assigned for the session, and the deferral entries below." and continues "The handoff continuing you across a compact carries this buffer's path alone: of the state recorded there, the pointer, never a copy of the handle or role it holds — see "Spawn once, message thereafter" for the rest of what is recorded, when, and the liveness test at recovery; this section does not restate any of that." Paragraph 2 opens "Each deferral entry names *what*, *why deferred*, and the *trigger* that resolves it; delete an entry once it's done — deferral entries remain the buffer's primary content." then points at the engine for path, numbering, zones, the editor's re-read, and the drain rule ("this section points there and restates none of them"), and closes "It is the one file you edit directly: you are its only writer."
- `src/skills/architect-editor-engine/SKILL.md` § "The architect's buffer" already holds the two zones and what each holds, "The editor re-reads the settled zone when it changes, not once at birth.", the drain rule, and the isolation rule. None of that is restated here.
- The governing spec states the occasion ("The head writes to the memory at the moment it learns something a later beginning would otherwise pay for again, not at the end of the stretch of work that produced it — the stretch of work is exactly what does not survive, and a conclusion left in the conversation dies with it, so the next beginning repeats the correction that made it."), the form ("An entry is written as what will hold again — a ruling of the user's in the user's own words, a mistake as the pattern behind it and the reason that pattern holds — never as the episode that revealed either."), and the announce-obligation ("And the head names the moment that memory moves: an unannounced change reaches no one, however plainly the discipline says the hand re-reads on change.").
- § "Relay on the marker; author a prompt in exactly one case" still says "Two channel-message formats, nothing else" and "You author your own prompt in exactly one case". Task 37.1 rescopes both to what *opens a round*, leaving ordinary communication that keeps the hand current outside them. This task does **not** touch those sentences and does **not** attach a format token, a new message kind, or a carrier description to the announcement — it states the obligation only.

Assumptions pinned from the spec (not invented):
- The addition lands in § "Your buffer is yours alone" — the section naming what the architect records — and nowhere else. No edit to the engine, `src/agents/editor.md`, `docs/paired-loop.md`, or the spawn-moment paragraph in § "Spawn once, message thereafter".
- The addition is a form, not a list: it enumerates nothing new to record. The recording-list sentence and the deferral-entry-format sentence stay as they are — neither cut nor narrowed.
- No zone is named in the new text: it speaks of the memory / the buffer, never "settled zone" or "live zone". The contract line's own phrase "when the settled memory moves" is the ceiling — "memory", not "zone".
- Writing to the memory and telling the hand it moved are one act, stated together — the announce-obligation is not a second paragraph.
- Wording is the implementer's; the facts are the governing spec's. Passing the spec's sentences through is acceptable (prior tasks in this phase did so) with one exception pinned in the task below — the announce sentence carries `re-read`, which the inserted paragraph must not; inventing facts beyond the spec is not acceptable.

Also in scope, carried in from the 36.5 review's deferred observation addressed to 36.6: paragraph 1 still introduces the recovering artifact by the compact occasion alone ("The handoff continuing you across a compact carries this buffer's path alone"), while § "Spawn once, message thereafter" and § "On every invocation" now name it "memory snapshot" with two occasions. This is a name alignment of an existing sentence to the artifact's name the file already uses, not a new fact — bounded to that one sentence.

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### State the occasion, the form, and the announce-obligation

- [x] **Insert one paragraph into § "Your buffer is yours alone" between its two existing paragraphs**
  Files: `src/skills/agent-architect/SKILL.md`
  Add a new prose paragraph after paragraph 1 (ending "this section does not restate any of that.") and before paragraph 2 (opening "Each deferral entry names"), so that paragraph 1's "the deferral entries below" still points downward at paragraph 2. The paragraph carries these facts, in this order, wording the implementer's own:
  1. **Occasion.** You write to the buffer at the moment you learn something a later beginning would otherwise pay for again — not at the end of the stretch of work that produced it. Reason: the stretch of work is what does not survive; a conclusion left in the conversation dies with it, and the next beginning repeats the correction that made it. This is the occasion for every entry beyond the two the spawn-moment paragraph already times (the handle, a pairing role) — say so briefly or leave it implicit, but do not re-time those two here.
  2. **Form.** An entry is written as what will hold again — a ruling of the user's in the user's own words, a mistake as the pattern behind it and the reason that pattern holds — never as the episode that revealed either.
  3. **Announce-obligation, in the same breath.** When the memory the hand holds moves, you name the change to the editor in the same act as the write — the memory kept current and the hand told it moved are one act, not two; an unannounced change reaches no one, whatever the hand's own discipline says about reading on change. State it as the architect's obligation toward the hand; do not describe the editor's re-read (engine's) — and the inserted paragraph must not carry the substring `re-read` at all, so the governing spec's own announce sentence ("however plainly the discipline says the hand re-reads on change") is **not** passed through verbatim here; allude to the hand's discipline as "reading on change" or leave the allusion out — do not say which message carries it, and do not open it with `REPORT-ONLY` or `APPLY-EDIT` or invent a third token. Before an editor exists there is no hand to tell — do not add a pre-spawn announcement; the spawn already hands over the buffer's path (§ "Spawn once, message thereafter").
  Guardrails: no "settled zone" / "live zone" / "zone"; no new list of things to record; no restatement of the drain rule, the re-read rule, the path, or the numbering (all four are the engine's — the pointer sentence in paragraph 2 already says so and stays verbatim); no edit to "Two channel-message formats, nothing else" or "You author your own prompt in exactly one case" (37.1's). One paragraph, no bullets, no sub-heading, in the section's existing second-person register ("you"), hard-wrapped at ≤ 77 columns like the rest of the file. Skill bodies are bound by the reserved vocabulary: the paragraph addresses the architect as "you" (the section's register — the file never names the architect "the head", and the paragraph must not introduce that name) and names the editor as "the editor" or "the hand", the two forms the file already uses; "buffer" and "memory" as the file and the governing spec use them. The inserted paragraph does not mention the memory snapshot — none of the three facts needs the artifact, and paragraph 1 already owns it — so the phrase "memory snapshot" appears only in the paragraph-1 rework below.

### Name the recovering artifact as the file names it elsewhere

- [x] **Align paragraph 1's "The handoff continuing you across a compact" sentence to the artifact's name** (independent of the paragraph insertion; same section)
  Files: `src/skills/agent-architect/SKILL.md`
  Rework only the sentence "The handoff continuing you across a compact carries this buffer's path alone: …" so its subject is the memory snapshot continuing you — the artifact § "Spawn once, message thereafter" defines with two occasions (before a compact, on the user's request) — rather than the compact occasion alone. Keep the rest of the sentence intact: "carries this buffer's path alone: of the state recorded there, the pointer, never a copy of the handle or role it holds — see "Spawn once, message thereafter" for the rest of what is recorded, when, and the liveness test at recovery; this section does not restate any of that." Do not re-list the two occasions here — the pointer to § "Spawn once, message thereafter" already covers them. Leave the section's opening clause "whatever of your own state must survive a compact" untouched.

### Verify against the file

- [x] **Confirm the invariants on the file, not on the diff** (depends on both edits)
  Files: `src/skills/agent-architect/SKILL.md`
  Run and read the results:
  - `grep -n "zone" src/skills/agent-architect/SKILL.md` → the same three hits as at HEAD (the spawn paragraph's "zones, and rules" and the two lines of the paragraph-2 pointer, shifted only by the inserted lines) — no hit inside the inserted paragraph. This single-word probe is wrap-safe and subsumes "settled zone" / "live zone".
  - `grep -c "drain" src/skills/agent-architect/SKILL.md` → exactly `1`, and `grep -c "re-read" …` → exactly `1` — both the existing pointer sentence, neither restated; the inserted paragraph is pinned above to carry neither substring, so a second hit on either is a defect in the paragraph, not in the check.
  - `grep -c "REPORT-ONLY\|APPLY-EDIT" src/skills/agent-architect/SKILL.md` → unchanged from `git show HEAD:src/skills/agent-architect/SKILL.md | grep -c "REPORT-ONLY\|APPLY-EDIT"` — the announcement carries no format token.
  - `grep -n "Two channel-message formats, nothing else\|exactly one case" src/skills/agent-architect/SKILL.md` → the same hits as at HEAD, untouched.
  - `grep -n "Each deferral entry names \*what\*, \*why deferred\*, and the \*trigger\*" …` → exactly one hit; `grep -n "the editor's handle, any pairing role the user has assigned for the session" …` → exactly one hit — the recording list and the deferral format survive verbatim.
  - Multi-word probes run over the unwrapped text, since the file is hard-wrapped and a line-oriented grep silently misses a phrase broken across a wrap: `tr '\n' ' ' < src/skills/agent-architect/SKILL.md | grep -o "The handoff continuing you across a compact" | wc -l` → `0`; `tr '\n' ' ' < … | grep -o "memory snapshot" | wc -l` → `4` (HEAD's 3 plus the reworked paragraph-1 sentence; the inserted paragraph contributes none); `tr '\n' ' ' < … | grep -o "stretch of work" | wc -l` → at least `1` (HEAD has `0`), and `grep -n "stretch" …` → hits only on lines inside the inserted paragraph (HEAD has none; "unit of work" on lines 5/8/10/20 is a different phrase and is not probed).
  - `grep -n "episode" …` → one hit, inside the inserted paragraph; `grep -nw "head" …` → `0` (the architect stays "you", the editor "the editor" / "the hand").
  - `git diff --stat` → only `src/skills/agent-architect/SKILL.md`; frontmatter (`name`, `description`, `loads:`) byte-identical (`git diff -U0 | grep -c "^[-+]\(name\|description\|loads\):"` → `0`); `wc -l` ≤ 500; `awk 'length > 77' src/skills/agent-architect/SKILL.md | wc -l` → not greater than at HEAD (17).
  - Read § "Your buffer is yours alone" once in full and confirm: three paragraphs, the inserted one between the original two, "the deferral entries below" still resolving downward, the closing "you are its only writer." still last.
  Do not commit.
