# Plan: 1.8.3 — roadmap-prune: pyramid pass

## Context
Compress `src/skills/roadmap-prune/SKILL.md` (currently 330 lines, post-1.8.1/1.8.2) to the "instructions only, no rationale prose" mandate the skill already declares — cutting procedure narration and restated `orchestrator-artifacts` protocol while landing every named contract byte-identical. A compression, not a redesign: same files deleted, same anchors written, same gate refusals, same commit.

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### Phase 1: Compress roadmap-prune to a lens over its procedure

- [x] **Task 1: Pin the verbatim-protected inventory + cut list**
  Files: (read-only) `src/skills/roadmap-prune/SKILL.md`, `.ai-factory/specs/28-roadmap-prune-pyramid-pass.md`, `src/skills/orchestrator-artifacts/SKILL.md`
  Do not edit yet — preparatory reading with **no diff**; Task 1 and Task 2 run in one implement session, and the checklist is re-derivable from SKILL.md + spec 28 + the engine. An empty diff here is expected, not a stale/no-op implementation. Read the current skill, the spec, and `orchestrator-artifacts`, and record as an in-session checklist for Task 2 — the contract Task 2 must not break. Two lists:
  - **Verbatim-protected (must land byte-identical — this is 1.8.1's and 1.8.2's landed wording; do not reintroduce the removed keep-recent slice heuristic or the audit hint). Closure rule (per spec 28): this list is criterion-backed, not exhaustive — *any* sentence stating a contract (capture, sweep, anchor, gate, commit, classification decision, edge path) is protected verbatim whether or not an item below names it; a contract-bearing sentence discovered mid-rewrite joins the protected set on the spot:**
    (1) **Step 0 deferred-observations gate** — the whole gate logic byte-identical: the read-and-refuse-only stance; the unpinned-entry test (pinned = ≥1 bracketed status marker per the engine, cited never redefined); **1.8.1's exact three-part refusal/resolution message** (park not engineer-around → user runs `/command-handoff` carrying every unpinned observation + gate context into `.ai-factory/handoffs/` → a dedicated resolution session fixes / routes into an **open** task's spec / dismisses, setting pins per `orchestrator-artifacts` § 6 `[fixed]` / `[routed → <path>]` / `[dismissed]` → re-run when every entry is pinned, gate passing is the resolution's proof never manufactured); the "make no edits, no sweep, no partial prune" line; the Step 0.6 pre-standardization-marker capture (the marker-phrase list) feeding Step 8; the `ROADMAP_TESTS.md` parity sentence (shared `plan-reviews/`/`reviews/` scan, `test-runs/` not scanned);
    (2) **Step 4.1 pre-prune snapshot** — `git rev-parse --short HEAD` captured before touching any files, the "last commit where ROADMAP.md still contains all pruned tasks" meaning, and its drop-history append;
    (3) **Step 4.2 `## Features` anchor** — the append rule (existing feature → append hash space-separated; new feature → new row), the drop-history comma-separated snapshot append, the table format + section-header/domain/operator-naming/7-char-hash rules and the example table;
    (4) **Step 5 scoped `Spec:`-tag sweep** — capture the `Spec:` tag of every **pruned** `[x]` line only (user-kept `[x]` line's tag not captured; **no `Spec:` tag → skip, never synthesize a path** verbatim); `rm -rf` the four artifact dirs (`plans/`, `plan-reviews/`, `reviews/`, `patches/`) directly under `<target repo root>/.ai-factory/`; `rm -f` each captured spec path (repo-root-relative, join onto target repo root); the "no spec directory is ever scanned or swept" invariant; target-repo-root anchoring rule; the `ROADMAP_TESTS.md` `test-runs/` addition;
    (5) **Step 6 emptied-phase / emptied-direction sweep** — delete a phase's `### Phase N` header + intro prose when its last task is deleted; delete a `## <Direction name>` section + preamble when its last phase is removed; **never renumber** (numbering is historic, gaps normal); the retain-last-phase-header anchor rule and its coexistence clauses;
    (6) **Commit policy** — `## Commit (on request only, never automatic)`: run ends with changes in the working tree, no commit; on any wording of "commit" → `git add -A` scoped to target repo root, exactly one commit, message exactly `Roadmap prune`, no body/prefix/co-author, never ask about the message;
    (7) any **What NOT to do** bullet that states a contract an executor would otherwise get wrong (plain `rm` not `git rm`; do not scan/sweep a spec directory; do not touch `handoffs/`; do not prune `[ ]`; do not invent hashes; do not resolve artifacts per task);
    (8) **Step 2.1/2.2 classification-and-grouping logic** — this is behavior determining *what anchors get written at all*, sitting amid the file's densest rationale prose, so it is a protected contract, not a cut site: the "could you write an e2e test for this that didn't exist before?" discriminator; the New feature / Extended feature / Internal-only three-outcome table and its signals/actions; the rules "never copy a phase or section header as a feature name," "a refactor that enables a future feature is still internal," "prefer extending when uncertain," and "**internal-only items never get a named row**" (only `Roadmap drop history` under **Internal**); the 2–5-word operator-perspective naming rule and the domain-grouping/section-header rules (item (3) covers the Step 2.2 table *format*; this covers the *decision* of which row a task becomes). The surrounding rationale glosses (e.g. "the Features table is the answer to 'what does this system know how to do?'") are cuttable — but the discriminator, table, and rules above land byte-identical;
    (9) **Step 3 hash-selection rule** and **Step 7 verify contract** — contract-bearing, cut-narration-only: Step 3's "pick the commit where the **last task of the feature** was marked `[x]`" decision and its `git log` recipes stay (trim only the GPS-coordinate rationale glosses); Step 7's verify checks (feature-row coverage, `git cat-file -t <hash>` resolvability, no orphaned `[x]`, coherent Milestones section) stay as operative instructions;
    (10) **Step 8 echo contract** — the report end of Step 0.6's capture-before-sweep coupling, so it is protected, not verify-step boilerplate to trim: the `possible unharvested margins` heading, one entry per source file, "Report-only, never gates," "Do not re-scan `plan-reviews/`/`reviews/` here" (Step 5 already deleted them; echo only what Step 0 captured), and "If Step 0 captured nothing, omit the heading" all land byte-identical. Item (1) protects the Step 0.6 capture; this protects the Step 8 echo it feeds — the two are one coupling and must stay mirrored;
    (11) **Step 2 preamble first-ever-prune contract + Step 4.2 create path** — the Step 2 opener's edge-case contract ("If ARCHITECTURE.md does not exist yet (first-ever prune), skip gracefully and proceed with fresh grouping" — together with the read-`## Features`-first instruction it rides on) and Step 4.2's "Find **or create** a `## Features` section" create path land byte-identical. Protected despite the narration-flavored lead: this repo itself has no `## Features` section, so the first-ever-prune path is the live one, and Task 3's baseline is blind to it (the anchor-row half of the comparison is degenerate here) — the itemized protection is the only guard.
  - **Cut candidates (narration / restated protocol):**
    - **Restated `orchestrator-artifacts` protocol** — any inline restatement of the `## Deferred observations` section format, the status-marker grammar, or the **pinned** definition. Load and reference the engine (via the Skill tool), never restate its content (`.ai-factory/ARCHITECTURE.md` → "Composition: mechanism vs policy").
    - **Rationale / narration prose** — the opening blurb ("Cleans up a roadmap by collapsing… GPS-accurate…"), the "GPS coordinate" glosses, the Step 3 "that commit is the moment…" explanations, the "Reading the history later" tail if it is pure rationale — trim any sentence that explains *why* rather than instructs *what*, where the step sequence already carries the flow. Keep any sentence stating a contract.
    - **Steps an executor performs unprompted** — verify-step boilerplate and connective narration that restates an already-instructed action.
  - Frontmatter (`name`, `description`, `argument-hint`, `allowed-tools`, `loads: orchestrator-artifacts`) is **unchanged** — the spec pins frontmatter and `loads:` unchanged.

- [x] **Task 2: Compress the file** (depends on Task 1)
  Files: `src/skills/roadmap-prune/SKILL.md`
  Rewrite for compression, not redesign — the step sequence (Step 0 gate → Steps 1–8 → Commit → What NOT to do) and every capture/sweep/anchor/gate/commit decision stay identical; only narration and restated protocol shrink. Requirements:
  - **Land every verbatim-protected block from Task 1 byte-identical.** Do not paraphrase, re-order clauses within, or "improve" a protected block — copy it through untouched. This especially covers 1.8.1's three-part refusal message, the scoped-capture "no tag → skip, never synthesize" wording, the four-dir `rm -rf` set, the `## Features` table format + example, the never-renumber sweep clauses, and the exact `Roadmap prune` commit policy.
  - **Cut the restated protocol:** the deferred-observations gate loads `orchestrator-artifacts` and references its grammar / **pinned** definition — never restate the section format or marker grammar inline beyond the load pointer.
  - **Cut narration and rationale prose:** remove the opening blurb, GPS-coordinate glosses, and inter-step connective narration that re-explains a step's purpose without stating a contract. This is the mandate the skill already declares ("Do not add rationale or explanation prose… instructions only").
  - **Two-reader register throughout:** rules stated as intent for the executing orchestrator; contracts (refusal message, table format, commit message, sweep set) pinned exact for the editor.
  - **Frontmatter unchanged:** `loads: orchestrator-artifacts` and all other frontmatter fields byte-identical.
  - Write in English.

- [x] **Task 3: Verify against guards** (depends on Task 2)
  Files: (read-only) `src/skills/roadmap-prune/SKILL.md`
  Confirm each spec guard before declaring done:
  - **Behavior-identical:** `git diff src/skills/roadmap-prune/SKILL.md` — removed hunks are narration / restated-protocol only; report any change to a capture, sweep, anchor, gate, or commit contract as a failure. Same files deleted, same anchors written, same gate refusals, same commit.
  - **Protected blocks byte-identical:** spot-check the decisive lines — 1.8.1's refusal chain (`/command-handoff`… → dedicated resolution session → `[fixed]` / `[routed → <path>]` / `[dismissed]` → gate passing is the resolution's proof never manufactured); "no `Spec:` tag… skip it, never synthesize a path"; the four-dir `rm -rf` set `plans/`, `plan-reviews/`, `reviews/`, `patches/`; "Do not use `git rm`"; the `## Features` example table; "never renumber"; the commit message exactly `Roadmap prune`.
  - **1.8.2 landed rule intact:** default prunes all `[x]` tasks, only an explicit user-named line is retained; the removed keep-recent slice heuristic and audit hint are **not** reintroduced.
  - **Protocol not restated inline:** the deferred-observations section format and status-marker grammar are referenced from `orchestrator-artifacts`, not re-defined in the body beyond the load pointer.
  - **Frontmatter intact:** `loads: orchestrator-artifacts`, `allowed-tools`, `argument-hint`, `description`, `name` all unchanged.
  - **Line count:** report actual; meaningful narration shrinkage expected, but the step contracts are legal mass — do not pad or clamp to a line number.
  - **Live baseline — run it here, in this milestone (required; do not defer).** The direction preamble requires it "before the next rewrite starts" — 1.9.1 is the next `[ ]` task in Phase 1. Run it now rather than punting to that boundary because it is safe to run early (a read-only dry run, no `rm`) and running now strictly over-satisfies the spec guard "Live baseline before the next phase task." It is a read-only dry run (**list, don't delete**), safe in-session:
    - Against the **current** SKILL.md, walk Steps 1/5 by hand over this repo's own `[x]` tasks: enumerate the pruned `[x]` lines, capture each line's `Spec:` tag (applying "no tag → skip, never synthesize"), and produce the would-sweep file set — the four artifact dirs (`plans/`, `plan-reviews/`, `reviews/`, `patches/`) plus the captured spec paths. **Do not run any `rm`.** Also record the would-write anchor rows (Step 2.1 classification → Step 4.2 `## Features` rows).
    - Repeat the same walk against the **rewritten** SKILL.md.
    - Require the two would-sweep file sets **identical**; report any divergence as a behavior change and a failure.
    - **Anchor-rows caveat:** this repo has no `## Features` section yet (never pruned), so the anchor-row half is degenerate — treat the **capture/sweep file-set** comparison as the load-bearing check and say so; do not fabricate feature rows to force a comparison.
