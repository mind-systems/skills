# Plan: command-handoff: one path — always transfer the session's meaning-tree

## Context
Collapse `command-handoff` from a four-combo (destination × register) user-facing mode surface to a single path: every handoff persists to `.ai-factory/handoffs/` via `note` and always carries the full leaf→branch→trunk meaning-tree (grid-vs-prose shape is the agent's inference, never a user knob). One contract, two files — `note`'s verbosity hook is broadened so a caller directive can override the findings-focus/not-process rule (Rule 2), which today blocks thread transfer.

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Tasks

### Phase 1: Engine — broaden note's verbosity hook (strictly additive)

- [x] **Task 1: Let a caller verbosity directive override note's Rule 2, and let the template hook carry a free-form body**
  Files: `src/skills/note/SKILL.md`
  Broaden the **Verbosity directive** hook (`:33`) so a set caller directive replaces **both** Rule 1 (concision) **and** Rule 2 (findings-focus / not-process) for that run; unset must stay byte-identical to today (concise + findings-focus). Edit the three prose spots that assert Rule 2 as always-on — leaving any one standing recreates the hook-vs-text contradiction the spec warns about:
    1. Hook description `:33` — *"all other Important Rules still apply"* → name the still-always-on set explicitly as **file paths and English only** (Rule 2 dropped from it).
    2. `:62` clause A — *"Findings-focus, file paths, and English always apply to both cases"* → *"File paths and English always apply to both cases."*
    3. `:62` clause B — *"…replaces concision for this run — the other Important Rules are unaffected"* → a set directive replaces **both** Rule 1 and Rule 2 for that run.
  Post-change the always-on set is **Rules 3 (file paths) + 4 (English) only** — Rules 1 & 2 both become overridable-by-directive.
  Also reword the **Template hook** (`:32`) so it states it tolerates a **free-form (non-skeleton) body directive** the caller passes verbatim (grid or prose) — do not force a section skeleton. Do NOT reconceive `note`: standalone/default behavior, the default template block, Rule text 1–5, and Steps 1–4 mechanics stay untouched.
  Verify (per spec): with the directive unset, default note and every roadmap-family caller is byte-identical to today; `grep -n "always apply\|other Important Rules" src/skills/note/SKILL.md` shows no clause naming findings-focus as always-on (only file-paths/English remain). Re-run `grep -rn "^loads:.*\bnote\b" src/skills/*/SKILL.md src/commands/*.md` — callers are `roadmap-engine` and `command-handoff`; the change must leave every existing caller behavior identical.

### Phase 2: Caller — collapse command-handoff to a single path

- [x] **Task 2: Remove the user-facing mode dispatch — frontmatter and both axes** (depends on Task 1)
  Files: `src/commands/command-handoff.md`
  Strip every user-selectable mode surface:
    - **Frontmatter `argument-hint` (`:8`)** → empty: `argument-hint: ""` (drop the `[note] [narrative]` mode tokens).
    - **Frontmatter `description:` (`:5-7`)** — rewrite its last sentence to state **unconditional** persistence to `.ai-factory/handoffs/`; drop the argument-token list (`"note"` / `"ноут"` / `"давай ноут"` / `"с ноутом"`). No mode dispatch may survive anywhere, including the user-facing description.
    - **Destination axis** — delete the opener that declares two orthogonal axes (`:13`), the Destination-axis block (`:15-17`), and the chat-destination branches at `:25` and `:129`. Handoff always persists to a file; there is no chat-only mode. (`:121` is the per-destination/per-shape **population** block, not a pure chat branch — Task 3 collapses it in full; do not partially delete it here.) Keep the minimal paste-back pointer to chat (`:148-164`) — it is a pointer, not a destination. **Do NOT delete `:151-153`**: those are the register-conditional bullets *inside* the kept pointer (frame / next-step / work-unit-count), not a chat-destination branch — **reword them shape-neutral** (e.g. *"grid shape: section 1 of the handoff / prose shape: the opening framing line"*; *"grid shape: section 4 / prose shape: the closing next-step sentence"*; the work-unit count applies to the grid shape). Leave no stale "skeleton register / narrative register" mode wording inside the pointer.
    - **Register axis** — delete the user-cue Register-axis selection block (`:19-23`) and the `:25` "How Steps 1–2 apply" dispatch. The executing agent still chooses body **shape** (enumerated 11-section grid for a wide, many-work-unit session vs. flowing prose for a reasoning journey) inferred from session shape — but never a user argument.
  Keep both the 11-section skeleton grid **and** the prose form available as agent-selectable shapes; delete only the user-facing dispatch between them. **Reword every surviving block heading that still carries a register/destination label — shape-neutral, keep the block content:** `:53` **"Skeleton register."** → **"Grid shape."**; `:117` **"Narrative register."** → **"Prose shape."**; `:131` **"Note destination — skeleton register:"** → a single-path delegation heading with no destination/register label (both shapes delegate to `note`; Task 3 supplies the hooks). This is the same grep-enumerated exhaustive-sites discipline as Task 1's Rule-2 fix — `:53`/`:117` are the two prior-review-3 sites, `:131` a third the review rounds never reached. After the edits, `grep -niE "register|note mode|chat mode|destination axis|note destination" src/commands/command-handoff.md` must return nothing — no dead mode label survives outside a deleted block. Frontmatter `loads: note` and `allowed-tools` stay unchanged (numbering moves to `note`, so the hand-write tooling becomes merely unused, not newly granted).

- [x] **Task 3: Single-path composition and output — delegate all numbering/placement to note for both shapes** (depends on Task 2)
  Files: `src/commands/command-handoff.md`
  **First close the Step-1 gap Task 2 opens:** with the `:25` "How Steps 1–2 apply" dispatch deleted, Step 1 ("Mine the session", `:29-44`) is left as an unconditional agent instruction while both shapes now delegate mining to `note` (its own Step 1 "Analyze Context"). Do not leave it standing as an agent action — that is the double-mining the milestone-55 refactor removed. Reframe Step 1's extraction list as **`note`'s mining lens** handed via the template hook (blank 11-section placeholders for the grid shape; folded into the free-form body directive for the prose shape), not something `command-handoff` does itself. The agent supplies the lens; `note` performs the mining and population.
  Then rewrite Steps 2–3 to one path:
    - **State the fixed invariant up front** (Step 2 opener): every handoff is verbose and transfers the full **tree of meanings** the session built (leaf→branch→trunk perception the successor would otherwise re-derive), rehydrating the next agent to the whole session's understanding **minus the dross** — irrelevant tool-calls and dead-end file reads are stripped, only the load-bearing tree is carried. Do **not** encode any literal token target (the "~200k" is metaphor).
    - Reword the Step-2 opener *"Compose in the register selected above"* (`:51`) → *"Compose in the shape inferred from the session's type."*
    - **Collapse `:121`'s three per-destination/per-shape population branches in full** to the single delegated-population statement — apply the same exhaustive-sites discipline as the engine's Rule-2 fix. This includes deleting the narrative-shape **direct-write** clause *"**Narrative + note destination:** the agent composes and populates the narrative itself here too — it `Write`s the result directly in Step 3, not via `note`."* — a second, distinct restatement of the milestone-60 drift separate from the `:140-146` block. Leave no clause asserting any shape `Write`s directly / bypasses `note`; both shapes populate via `note`.
    - **Delete the direct-`Write` path and its hand-rolled `<NN>` numbering (`:140-146`)** — the milestone-60 drift. Both shapes now go through `note`.
    - Supply `note` the hooks (Step 3): **Destination directory** = `.ai-factory/handoffs/`; **Template** = the chosen shape — the blank 11-section skeleton for the grid, or a **free-form body directive** through the same template hook for the prose shape (uses `note`'s tolerance from Task 1 — do not force a skeleton); **Verbosity/fidelity directive** = "verbose; carry the full meaning-tree and its causal thread; strip irrelevant tool-calls and dead-end reads" (this exercises note's new Rule-2 override); **Slug** = semantic, derived from the session subject, never the literal word `handoff`.
    - `note`'s own Step-4 report stays **not surfaced** — once `note` completes, emit only the minimal paste-back pointer.
  Preserve spec 14's **boundary-neutral framing** — introduce no compact claim and no boundary taxonomy (`grep -ni "compact" src/commands/command-handoff.md` must stay clean).
  Verify: `grep -n "<NN>\|highest existing prefix\|zero-padded" src/commands/command-handoff.md` returns nothing (numbering delegated to `note`). Scoped check of this task's own sites — after this task the `destination axis` / `note destination` tokens and both direct-write sites (`:121` narrative + `:140-146`) are gone; the whole-file completeness grep (#8) is deferred to Task 4 because `register`/`chat mode`/`note mode` still legitimately match at `:119`/`:123`, which Task 4 clears.

- [x] **Task 4: Replace the proportionality guard with a tree-completeness check; re-anchor its every reference** (depends on Task 3)
  Files: `src/commands/command-handoff.md`
  Remove the proportionality guard as a governing directive at **all** its sites — the proportionality prose at `:55` and `:119`, and the proportionality clauses of the self-check gate (`:123`). Also delete the **mode-dispatch tail of `:123`** — *"In **chat mode** this gate applies to the chat output. In **note mode** this gate applies to the content written to the note file…"* — the collapsed gate applies to the persisted note-file content, and only the paste-back pointer is exempt (no "chat mode / note mode" wording may survive). Replace the self-check gate with a **tree-completeness** check: *could a fresh agent, from this handoff alone, hold the same perception tree — leaf→branch→trunk — the session ended with, without the irrelevant exploration?* Re-anchor the paste-back pointer's exemption clause at `:155` (*"exempt from the proportionality gate"*) **and** the exemption clause inside the `:123` self-check to the **tree-completeness** gate — the minimal chat pointer is exempt from carrying the full tree (that lives in the persisted file). Leave **no orphaned quotation** of the removed proportionality text.
  Verify: `grep -ni "proportional" src/commands/command-handoff.md` returns nothing; the self-check tests tree-completeness, not proportionality; the paste-back pointer's exemption is stated against the tree-completeness self-check. **Whole-change final gate** — as the last task, run the spec's completeness grep (#8) verbatim over the fully-edited file: `grep -niE "register|note mode|chat mode|destination axis|note destination|Write.*(direct|itself)" src/commands/command-handoff.md` returns nothing (all shape headings and pointer bullets reworded grid/prose; both direct-write sites collapsed; `:119`/`:123` mode wording now cleared here). This grep — not a reading pass — is the gate this milestone's whole history shows catches what enumeration misses.
