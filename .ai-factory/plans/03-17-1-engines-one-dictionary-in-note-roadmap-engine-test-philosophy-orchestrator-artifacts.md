# Plan: Engines: one dictionary in note, roadmap-engine, test-philosophy, orchestrator-artifacts

## Context

Bring the four engine skills into naming conformance with the reserved-words registry — retire the `spec note` and `milestone` synonyms, swap the real example email for a fictional one. Deepest-first: these trunks define the words their callers inherit in 17.2–17.4. Zero behavior change.

## Settings
- Testing: no
- Logging: minimal
- Docs: no

## Assumptions and spec corrections

Every occurrence in spec 62's inventory was re-grepped this session and still sits at its stated line. The inventory was **not complete** when this plan was drafted, and the corrections below govern — the implementer works from these, not from the spec's line lists alone. **Update on disk:** spec 62 has since been amended to fold all five corrections in — the seven-site `spec note` list with the 16–17 wrap, the eight bare artifact-`note` sites, `roadmap line`→`contract line` at both sites, the post-16.1 fence restated to zero, the no-change expectation for `test-philosophy`, the multiline `rg -U -in` check forms, and the static behavior gate. Plan and spec now agree; the assumptions below remain as the derivation record, not as open divergences.

1. **Spec 62's `spec note` inventory misses one occurrence.** It lists six in `roadmap-engine` (5, 28, 69, 103, 105, 209); there is a **seventh at lines 16–17**, where the synonym wraps across a line break ("the contract line, the spec / note, and the roadmap file format") — the opening paragraph, the first prose a caller reads after the description field. A single-line `grep -in 'spec note'` cannot see it and returns zero with it still in place. **The general lesson, applied to every check in Task 6 *and* to every line citation in the task bodies: a guard over a line-wrapped file must express the newline (`\s+`), never a literal space, and must carry `-i` unless a capital is impossible; a quoted phrase in a task body must be checked for a wrap before it is quoted as one line.** Two greps in an earlier revision violated the first half and certified clean over live occurrences; a citation in another violated the second half and quoted a two-line phrase as one, which would have failed an exact-match edit. Both halves are now swept: every quoted phrase in Tasks 1–4 was re-read against the live file this session, and each wrap — `roadmap-engine` 16–17, 32–33, and 74–75, `orchestrator-artifacts` 73–74 — is marked as one. Every other citation is single-line, verified, including the two the earlier revision got wrong in opposite directions (`orchestrator-artifacts:31` is *not* wrapped despite an earlier claim; 73–74 *is* despite an earlier claim it wasn't).
   **Two kinds of wrap, and only one of them needs a two-line edit.** In 32–33, 74–75 and 73–74 the *phrase* wraps but the *substitution* lands wholly on one line, so each bullet names the single line to edit and the line to leave alone. **16–17 is the only site where the substitution itself spans the break** — `spec` ends 16, `note` opens 17 — so it is the only one given a verbatim two-line target (Task 1). Citing a wrap is not enough; each bullet must say which line the edit falls on.

2. **Bare `note` for the artifact is in scope; the spec's silence is not an exemption.** After the `spec note` substitutions, `roadmap-engine` still calls the same artifact plain "the note" / "a note" / "notes" at lines 33, 36, 43, 47, 75, 212, 213, 214 — leaving the file reading "write its **task spec**" (209) beside "the **note** holds the full implementation detail" (43): one concept, two names, one file, which is the exact drift the contract forbids. Two sites are the sharpest:
   - **Line 36** ("The note follows `note`'s format") — artifact noun and skill name in one sentence, indistinguishable.
   - **Lines 212–214** sit five lines below Task 1's target at 209, inside the finalize paragraph a caller reads *while actually finalizing entries*. Rewriting 209 alone leaves that paragraph saying "write its **task spec** … need neither a **note** nor a tag swap … the confirmed set gets **notes**."
   Decision: **rewrite all eight to `task spec`** (Task 2). Bare `note` is not accepted as post-establishment shorthand, because this file's audience is the skills that inherit its vocabulary in 17.2–17.4.

3. **The `milestone-rescue` guard is already moot.** Spec § Guards fences off `orchestrator-artifacts` lines 7 and 44 as "Phase 16's". Task 16.1 has since landed `[x]` — those already read `task-rescue` / `task-rescue-audit`, and a re-grep of `milestone` in that file returns **only lines 49 and 73**, both in scope. Consequence: spec § Verification's third check ("→ only the skill names … until Phase 16 lands") is stale; after this task it must return **zero**.

4. **`test-philosophy` is expected to land no edit.** The spec lists lines 14, 25, 41 as casing targets, but its own operative rule — "capitalized only at a sentence start or in a heading", attributive hyphens legal — exonerates all three: 14 and 25 are title-case headings, 41's `Loud-failure` opens a sentence attributively. Spec § Verification agrees, asking for zero occurrences *mid-prose*. Task 5 applies the **rule**, not the line list; per the direction preamble, "no change" is a legal per-file outcome. Do not manufacture an edit to satisfy an inventory.

5. **`roadmap line` → `contract line` is in scope, on the same doctrine as Assumption 2.** `roadmap-engine:105` and `orchestrator-artifacts:31` say "the roadmap line" where the registry concept is **contract line**. Spec 62 lists neither, and an earlier revision of this plan deferred them on the ground that "a certification pass does not widen itself" — while Assumption 2 widened past the same spec for the same class of defect. Both cannot govern one plan, and the distinguishing reasons all fail: `contract line` is a registry concept the callers inherit in 17.2–17.4 exactly as `task spec` is; task 13.1 already set the precedent (`src/global/CLAUDE.md` uses `contract line` at 7, 28, 53, 55 and `roadmap line` nowhere); and both sites sit inside files these tasks already open.
   `roadmap-engine:105` is the sharper of the two — it sits **inside the section headed "Rules for writing a contract line"**, three bullets below one that names the concept correctly, so the file teaches the term and mis-names it in the same list, on the tier this engine exists to define. That is Assumption 2's "one concept, two names, one file" verbatim. Decision: **fold both into Tasks 1 and 4.** Two words, zero behavior risk.
   The boundary that still holds: `roadmap-prune` also carries the variant (155, 231, 276), and that file belongs to 17.3 — out of scope here, carried below. In-file is in scope; reaching into another task's file is not.

## Tasks

### Phase 1: roadmap-engine — the deepest trunk

- [x] **Task 1: Retire `spec note` → `task spec` in roadmap-engine**
  Files: `src/skills/roadmap-engine/SKILL.md`
  **Seven** occurrences: lines 5, **16–17**, 28, 69, 103, 105, 209. Six are single-line substitutions of `spec note` → `task spec`. **16–17 is the one site that cannot be** — `spec` ends line 16 and `note` opens line 17, so the pair straddles the break and no single-line edit reaches it. Pinned verbatim, and it happens to be length-preserving (`the spec`→`the task`, `note,`→`spec,`), so no reflow of the surrounding prose is needed:
  - line 16 → `This is the shared explanation of the roadmap artifacts — the contract line, the task`
  - line 17 → `spec, and the roadmap file format — not any decomposition philosophy. The calling`
  Do not attempt an exact-match edit on `the spec note` here — that string exists on neither line, and a looser one-line attempt yields the dangling "the contract line, the spec task spec". Line 5 sits in the frontmatter `description:` — that is the skill description field, the always-loaded surface the contract binds hardest, so it is in scope, not an exception. Line 69 is plural (`spec notes`) → `task specs`. **Line 105 carries two synonyms in one bullet** — fix both: "lives in the spec note, not the roadmap line" → "lives in the task spec, not the contract line" (Assumption 5; the bullet sits inside the section headed "Rules for writing a contract line").
  Do **not** touch: the `` Spec: `` tag (structural, stays legacy), the `.ai-factory/specs/` paths, the skill name `note` in "written via note", or the `` `<note pending>` `` placeholder at lines 190 and 210 — that literal is emitted and later re-scanned by this same skill, so it is a fixed-form string on the protocol axis, not vocabulary. This task is find-and-replace over the word `spec`, which puts `` Spec: `<note pending>`. `` at highest risk; Task 6 guards both counts.

- [x] **Task 2: Retire bare artifact-`note` → `task spec` in roadmap-engine** (depends on Task 1)
  Files: `src/skills/roadmap-engine/SKILL.md`
  Eight sites where a bare `note`/`notes` names the artifact (Assumption 2), with the rewrite pinned for each:
  - **32–33 (wrapped; edit falls on 33 alone)** — the sentence's leading `the` ends line 32, `note and tag machinery…` opens line 33. Substitute on line 33 only: `note and tag machinery` → `task-spec and tag machinery` (attributive hyphen is legal English). Line 32 is untouched.
  - **36** → "The task spec follows `note`'s format …" — the backticked `` `note` `` in the same sentence is the skill name and stays.
  - **43** → "the task spec holds the full implementation detail".
  - **47** → "the task spec is the implementation".
  - **74–75 (wrapped)** — line 74 ends `so readers resolving through the tag`, line 75 is `reach the note.`; an exact-match edit on the full phrase finds nothing. The substitution falls entirely on line 75: `reach the note.` → `reach the task spec.` Line 74 is untouched — it carries the named-roadmap path template `` `.ai-factory/specs/<slug>/<NN>-<slug>.md` `` that Task 6's byte-identity gate protects.
  - **212** → "need neither a task spec nor a tag swap".
  - **213** → "receive no task spec".
  - **214** → "only the confirmed set gets task specs" (plural — cannot be the skill name).
  Rewrite only where the noun means the **artifact**. Every backticked `` `note` `` is the skill name and stays byte-identical, as do `loads: note` (line 11), "written via note" in the description (line 5), and both `` `<note pending>` `` placeholders (190, 210).
  **Substitute in place — do not reflow the surrounding prose. The file's line count and line numbering must be identical pre/post.** Unlike Task 1's length-neutral swaps (`spec note` and `task spec` are both 9 chars), `note` → `task spec` adds 5 characters at every site: line 43 goes 85→90 and line 213 84→89, against a prose band that currently tops out around 88 in those regions. Line 43 becomes the widest prose line in its section, and the temptation to re-wrap the paragraph is exactly the trap — a reflow shifts every subsequent line number, and Task 3 cites 104 and 57–58, Task 4 is stated per-line, and Task 6's fixed-form guards cite 74, 190 and 210, all by number. Slight over-wrap is the correct trade for a naming-only pass: reflow churn would bury the small diff Task 6's guards exist to read.

- [x] **Task 3: Retire `milestone` → `task` and swap the example email in roadmap-engine** (depends on Task 2)
  Files: `src/skills/roadmap-engine/SKILL.md`
  Line 104: "make two milestones" → "make two tasks" — roadmap-engine's unit is the task-tier entry. Lines 57–58: `kg.wmservice@gmail.com` → `john.doe@example.com` and the derived slug `kg-wmservice` → `john-doe`, per handoff 18. The slug example must stay arithmetically true to the derivation rule stated in the same sentence (local-part lowercased, every non-alphanumeric run collapsed to one hyphen): `john.doe` → `john-doe` holds.

### Phase 2: orchestrator-artifacts

- [x] **Task 4: Conform orchestrator-artifacts — synonyms and slug examples** (depends on Task 3)
  Files: `src/skills/orchestrator-artifacts/SKILL.md`
  - Line 49: "commits a milestone's artifacts together with the milestone" → a task's artifacts together with the task (the orchestrator's processed unit is the task, never the phase).
  - Line 68: "an open task's spec note" → the registry name is `task spec`; a literal substitution yields the clumsy "an open task's task spec", so reword to "the task spec of an open task". Naming is what the contract binds, phrasing is ordinary English — pick the sentence that reads.
  - **Lines 73–74 (wrapped):** "that milestone's" → "that task's". The phrase "that milestone's review files" spans the break — line 73 ends at `that milestone's`, line 74 opens `review files` — so an exact-match edit on the full phrase finds nothing. The substitution itself falls entirely on line 73.
  - Lines 28–29: `roadmaps/kg-wmservice.md` → `roadmaps/john-doe.md`, `plans/kg-wmservice/…` → `plans/john-doe/…`.
  - Line 31: "recoverable from a roadmap line" → "recoverable from a contract line" (Assumption 5). The string sits entirely on line 31 — line 30 ends at `is not`; there is no wrap here, despite an earlier revision of this plan asserting one.
  **Substitute in place — do not re-wrap the 67–68 bullet; line numbering identical pre/post** (same constraint as Task 2, for the same reason). The line-68 bullet is the plan's only deliberate reword rather than a swap: "an open task's spec note" (24 ch) → "the task spec of an open task" (29 ch) takes line 68 from 81 to 86 characters. It stays under line 67's 89, so the band is not violated and the temptation to tidy is weak — but line 68 is the wrapped continuation of the bullet opened at 67, and re-wrapping the pair would shift the 73–74 wrap the next bullet cites by number.
  Leave byte-identical: the `## Deferred observations` heading (lines 5, 53) and the `- Affects: …` entry line — a joint protocol the orchestrator's `reviewer.md` emits (verified at `orchestrator/orchestrator/prompts/reviewer.md:108`) and this skill scans; a one-sided rewrite silently breaks the scan (handoff 21). The emitter has **already landed** `task-spec path` (orchestrator commit `8f34644`, `reviewer.md:108` re-read this session); `orchestrator-artifacts:55` is now the lagging half of the pair. It still stays byte-identical *in this task*: 17.5 owns that one field (spec 73, guard "after 17.1, same file") — the surviving `spec-note` is 17.5's remaining work, not a frozen pair, and nothing the scan depends on (the `- Affects: ` prefix, the heading) involves the placeholder text. Re-verify at implementation time — this cross-repo fact has moved underneath the plan once already. Also leave the `[fixed]`/`[routed → <path>]`/`[dismissed]` marker literals, `PASS signal` prose, `named roadmap`, and the generic "field"/"fields" at lines 5, 41, 58.

### Phase 3: Certification of the clean engines

- [x] **Task 5: Certify `note` and audit `test-philosophy` against the casing rule** (depends on Task 4)
  Files: `src/skills/note/SKILL.md`, `src/skills/test-philosophy/SKILL.md`
  `note`: verify zero synonym tokens and land **no edit** — its inclusion is a certification. Its own genre vocabulary ("research notes / task specs / handoffs") is already correct.
  `test-philosophy`: judge lines 14, 25, 41 by the rule in Assumption 4, each in its grammatical position. Expected outcome is no change; if an occurrence genuinely sits mid-prose as a bare noun, normalize only that one. Leave "field" at line 35 (a data-field) and the `Class A`/`Class B` labels.

### Phase 4: Verification

- [x] **Task 6: Run the conformance greps, the literal guards, and the behavior baseline** (depends on Task 5)
  Files: none (verification only)

  **Synonyms — every pattern multiline-tolerant (`\s+`, never a literal space) and case-insensitive, per Assumption 1:**
  - `rg -U -in 'spec\s+notes?' src/skills/{note,roadmap-engine,test-philosophy,orchestrator-artifacts}/SKILL.md` → zero. (Note this correctly does **not** match the hyphenated `spec-note path` still standing at `orchestrator-artifacts:55` — hyphen ≠ `\s` — that field is 17.5's, not this task's; the check and that hold-out do not fight.)
  - `rg -U -in 'milestones?' src/skills/{note,roadmap-engine,test-philosophy,orchestrator-artifacts}/SKILL.md` → **zero** (Assumption 3 — the skill-name exemption no longer applies).
  - `rg -U -in 'roadmap\s+lines?' src/skills/{roadmap-engine,orchestrator-artifacts}/SKILL.md` → **zero** (Assumption 5). Scoped to these two files only: `roadmap-prune` legitimately still carries the variant until 17.3.
  - `grep -in 'kg.wmservice\|kg-wmservice' src/skills/{roadmap-engine,orchestrator-artifacts}/SKILL.md` → zero.
  - **Read the new value back, not just the old one's absence.** The absence grep alone passes if the implementer deletes the example outright, or writes a slug that violates the derivation rule stated in the same sentence of the target file — and the slug is derived, not substituted, so it is the one edit here whose correct value has to be computed. Assert all four strings positively:
    - `grep -n 'john\.doe@example\.com' src/skills/roadmap-engine/SKILL.md` → line 57
    - `grep -n 'john-doe' src/skills/roadmap-engine/SKILL.md` → line 58
    - `grep -n 'john-doe' src/skills/orchestrator-artifacts/SKILL.md` → lines 28, 29

  **Bare artifact-`note` — enumerate the legal residue, do not target zero.** `note` is a skill name that must survive, so a zero-expecting grep over the bare word cannot close this class:
  - `rg -U -in 'the\s+note\b' src/skills/roadmap-engine/SKILL.md` → zero. (The `\s+` and the `-i` are both load-bearing: the literal-space, case-sensitive form returns only 43/47/75 and is blind to the wrap at 32–33 and the sentence-initial capital at 36 — the very site Task 2 calls the worst of the set.)
  - `rg -U -in '\bnotes?\b' src/skills/roadmap-engine/SKILL.md` → every remaining hit must be one of the four legal classes, and **no hit may be the artifact noun**:
    1. the backticked skill name `` `note` `` / `` `note`'s `` (lines 36 ×2, 37, 39, 70, 73);
    2. the `loads: note` frontmatter edge (line 11);
    3. "written via note" in the `description:` field (line 5);
    4. the `` `<note pending>` `` placeholder (lines 190, 210).
    Nine lines currently carry legal residue; line numbers shift with the edits, so verify by class, not by number.

  **Certification outcomes — the two no-edit files, asserted rather than assumed:**
  - `git diff --stat src/skills/note/SKILL.md src/skills/test-philosophy/SKILL.md` → **empty**. Task 5's certifications are byte-no-change outcomes. The synonym greps above do scope over these two files but return zero there, so they cannot catch a casing-only edit; the `Silent-Failure|Loud-failure` count below is the primary guard against an implementer reflexively lowercasing spec 62's inventory (14, 25, 41), and this diff assertion is the byte-level backstop that catches any other stray edit. All three positions are individually verified as exonerated (heading, heading, sentence-initial attributive), so Task 5's mid-prose escape hatch is a dead branch on the current file — if it ever fires anyway, the edit must be justified against Assumption 4's rule in the run report and this assertion re-scoped to that one file; the hatch and this check must not silently disagree.
  - `rg -n 'Silent-Failure|Loud-failure' src/skills/test-philosophy/SKILL.md` → exactly **3** hits, at 14 (H1 heading), 25 (H2 heading), 41 (sentence-initial attributive) — i.e. zero mid-prose, which is what spec § Verification item 4 actually asks.

  **Fixed-form literals — the surfaces a `spec`-wide replace damages most easily:**
  - `grep -c '<note pending>' src/skills/roadmap-engine/SKILL.md` → **2** (lines 190, 210).
  - `grep -c 'Spec: `' src/skills/roadmap-engine/SKILL.md` → **7**, unchanged pre/post.
  - `grep -c '## Deferred observations' src/skills/orchestrator-artifacts/SKILL.md` → **2**, unchanged pre/post.
  - Frontmatter, stated positively so it cannot be read as freezing the whole block: **`description:` is the one frontmatter line that changes** (`roadmap-engine:5`, Task 1 — the skill description field is in scope precisely because it is the surface the contract binds hardest). `name:`, `loads:`, and `allowed-tools` are byte-identical in all four engines, as are both reverse-graph marker sentences (`roadmap-engine` 21–23, `orchestrator-artifacts` 17–19) — `git diff` those → zero hits. This matches spec 62 § Files & types, which names only `name` / `loads:` / `allowed-tools` as untouched.
  - **Line count and numbering identical pre/post** for **both** edited files (`git diff --stat` shows equal insertions and deletions for `roadmap-engine` *and* `orchestrator-artifacts`, no net line change): the numeric citations throughout Tasks 3, 4 and 6 depend on it, per the no-reflow constraints on Tasks 2 and 4.

  **Behavior baseline.** A live `roadmap-decompose` render is *not* mechanically runnable here: `roadmap-engine` drives `AskUserQuestion` prompts (lines 164–182, 196–206) with no one to answer in a headless run, and a real render writes stray roadmap and spec files into `.ai-factory/`. Use the static equivalence check as the primary gate — diff pre/post and require byte-identity of: the `## Roadmap File Format` block, the `Spec:` tag literal at line 34, the `` `<note pending>` `` placeholder at 190/210, and the spec-path templates — the literals, not their lines: `` `.ai-factory/specs/<NN>-<slug>.md` `` wherever it appears (28, 34, 93–95, 211; line 28's prose is itself a Task 1 target, the path string inside it must survive) and the named-roadmap form `` `.ai-factory/specs/<slug>/<NN>-<slug>.md` `` at line 74 — the line directly above Task 2's 74–75 wrap target, the likeliest collateral of a wrap-confused edit. If a live render is attempted anyway, point it at a scratch target file outside `.ai-factory/` and discard the output; it confirms, it does not gate. A rename that changes output is a bug, not a conformance.

## Deferred observations carried forward

**Read this first: every entry below is about a file outside this task's boundary, and this repo is being actively conformed while the plan is reviewed.** Four entries in earlier revisions went stale between writing and review — `docs/philosophy/multiuser-roadmaps.md` was fixed mid-session, spec 72 gained the substitution it was asked for, spec 62 was amended, and `skills/CLAUDE.md` turned out already conformant. Each was true when written and false when read. So: **re-grep each claim before acting on it, and treat "already closed" as the expected outcome, not a surprise.** An entry that resolves to nothing is the system working, not a defect in the entry. None of these blocks 17.1.

- **This task is the last live surface of the example-email swap.** Re-verified at the moment of writing: `kg.wmservice@gmail.com` / `kg-wmservice` survives repo-wide in exactly two files — `roadmap-engine:57–58` and `orchestrator-artifacts:28–29` — both closed by Task 3 and Task 4. `docs/philosophy/multiuser-roadmaps.md` is **already clean** (it reads `john.doe@example.com` at 15 and 24, `plans/john-doe/…` at 47, and contains no `Spec note`); an earlier revision of this plan carried it as an open gap, which was true when first grepped this session and false by the time the plan was reviewed — the file was fixed underneath us. Nothing further is needed: after 17.1 lands, the address survives only in archival and planning artifacts — spec 62's own inventory lines (**`:14`, `:25`, `:52`** — re-swept; the amendment to spec 62 shifted these down from the `:12`/`:22`/`:47` an earlier revision cited), `ROADMAP.md:27` (the 17.1 contract line itself, historical once `[x]`), handoff 18, and this task's plan/plan-review files — all records of the swap, exempt as the archival record. **Do not seed a follow-up docs task from this.** An earlier revision attached a caveat that this doc's fix sat uncommitted in the working tree; **that is no longer true — the file is committed and clean** (`git status --porcelain` returns nothing for it), so the claim rests on history, not on an unsaved buffer. Caveat dropped.
- **Spec 72 over-includes `roadmap-prune:155` and must be narrowed to 231/276 before 17.3 runs.** Spec 72 (line 9) already names the `roadmap line(s)` → `contract line(s)` substitution for `roadmap-prune` — that half needs nothing from anyone — but it lists **three** sites where only two are the concept. Line 155 reads "the commit before which the prune deleted roadmap lines; equivalently `<prune-commit>^`": those are lines removed from the roadmap **file** during a prune, describing a git snapshot boundary, not the contract-line tier. Substituting "contract lines" there would make the sentence state something it does not mean, corrupting the description of the drop-history ledger. **17.3 must edit 231 and 276 and leave 155 alone.** This is 17.3's file and 17.3's spec, outside this task's boundary (Assumption 5) — recorded so the carve-out is not lost. Do not dismiss this entry on the ground that spec 72 already names the substitution; that is its already-satisfied half, and the carve-out is the live one.
- The `- Affects:` placeholder's `spec-note path` now survives on **one side only**: the emitter half already landed `task-spec path` (orchestrator task 17.1, commit `8f34644`, `reviewer.md:108`), so `orchestrator-artifacts:55` is the last `spec-note` occurrence anywhere in the pair — the lagging, unblocked half. It stays byte-identical in *this* task because **task 17.5 owns that field** (`.ai-factory/specs/73-affects-placeholder-task-spec-path.md`, guard "after 17.1, same file") — one field on one line in this repo, no cross-repo negotiation left. This record is what explains the hit to a later hyphen-tolerant grep: do not read it as a double-frozen pair, and do not defer 17.5 on "needs a paired change" — the pairing is already half-executed and was order-free by design.
- **Spec 62's Phase-16 carve-out — closed on disk; entry retained only as a record.** An earlier revision raised that § Guards and § Verification item 3 still fenced `orchestrator-artifacts` 7/44 as "Phase 16's", moot since 16.1 landed, and warned a later `task-rescue` reading spec 62 alone would re-derive the stale carve-out and judge the pass under-delivered. **Spec 62 has since been amended:** line 38 now reads "Skill names were Phase 16's, and it has landed… (16.1 `[x]`)" and line 53's check now expects **zero**. Nothing to do. Assumption 3 remains the operative statement for running the task and is unaffected. (This entry has now been dropped once and restored once across revisions; keeping it visible-but-closed is what stops that cycle — a deferred observation is disposed of by being answered, not by being omitted.)
- **`skills/CLAUDE.md` — checked, already conformant; closed, no task needed.** An earlier revision raised it as a scope gap carrying `spec note` at 108/166 and "strategic milestones" at 164, and proposed a 17.6 or an amendment to 17.4's sweep. Re-grepped: **zero hits** across all 197 lines — those regions already read "in **task specs** are contract text" (108), "`/roadmap-outline` (strategic **phases**)" (164), "New **task specs** land in…" (166). The file was conformed in an earlier commit and has no working-tree diff. **Do not seed a task from this.** The underlying scope question is real in principle — [using-the-language](../../docs/using-the-language.md) § "What conforms" binds "skill bodies, skill descriptions, and the docs that specify the system", while Phase 17's header scopes itself to the skill *bodies*, so a project CLAUDE.md falls between the two — but it has no live surface. Worth keeping for the contrast it draws: the `milestone` occurrences in `docs/reserved-words.md:5`, `docs/using-the-language.md:23` and `docs/skill-description-field.md:25` are **not** in this class — each quotes the retired word as an example of input the agent maps, or as the subject of the retirement doctrine itself. Legitimate use; leave them.
- **The tail divergence on that same line is a settled decision, not drift to reconcile.** The emitter writes `— <one-paragraph observation>`, the skill documents `— <observation>`. 17.5's contract line has already ruled: "Tails stay per-side (`<observation>` here — format description; `<one-paragraph observation>` there — length instruction): recorded difference, not drift." An earlier revision of this plan recommended reconciling the whole entry line at once, which would contradict that ruling in a file 17.5 is about to touch. **Defer to 17.5; do not equalize the tails.**
