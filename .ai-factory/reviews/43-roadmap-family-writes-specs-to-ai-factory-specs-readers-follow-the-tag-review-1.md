# Code Review — Roadmap family writes specs to `.ai-factory/specs/`; readers follow the tag

**Scope reviewed:** `git diff HEAD` across `CLAUDE.md`, `docs/workflow.md`, `src/global/CLAUDE.md`, `src/skills/milestone-rescue/SKILL.md`, `src/skills/milestone-rescue/docs/overview.md`, `src/skills/roadmap-engine/SKILL.md`, `src/skills/roadmap-outline/docs/overview.md`, `src/skills/roadmap-test-coverage/SKILL.md`. This is an instruction/documentation refactor (no executable application code), so correctness = internal consistency, completeness of the rename, and absence of contradictions the implementing/reading agent would trip over.

## Verification performed

- **Writers retargeted (Tasks 1–3).** `roadmap-engine` now points the two-tier artifact, the `<NN>` numbering scan, the format-block examples, and the Create-mode finalize tag at `.ai-factory/specs/`, and adds the note-destination-hook pass (`pass destination .ai-factory/specs/ via note's destination hook`). `roadmap-test-coverage` Layer 4 `mkdir`/`find`, all three agent-prompt path strings, and the Layer 6/8 pointers all moved to `specs/`. `roadmap-outline/docs/overview.md` examples moved to `specs/`. Consistent and complete.
- **Readers de-hardcoded (Tasks 4–5).** All five literal spec-directory references in `milestone-rescue/SKILL.md` (intro pair, Step 1 filter, Option 3, Step 5 guard, Depth:spec step 1) and the one in `docs/overview.md` now resolve through the `Spec:` tag; the four-directory artifact whitelist (`plans/`, `plan-reviews/`, `reviews/`, `patches/`) is preserved and its semantics are intact. `command-pin-gaps` was correctly left unchanged — it already resolves via "its `Spec:` note file" with no directory literal. `temporal-tree` was correctly left unchanged — grep confirms it holds no spec/note directory reference. `roadmap-decompose`/`-skeleton` confirmed to carry no directory literal (tag-resolved), so correctly untouched.
- **Docs genre split (Task 6).** Repo `CLAUDE.md`, `src/global/CLAUDE.md` (both routing + planning-chain lines), and `docs/workflow.md` (in Russian, matching the doc's language) each state the split once. Lazy-migration reality ("older notes still resolve via the `Spec:` tag") is preserved in every doc.
- **Global constraints honored.** No project data touched (no `git mv`, no existing tag rewritten). `note`'s default destination is unchanged — the remaining `.ai-factory/notes/` references in `src/skills/note/SKILL.md` are the intended defaults (out of scope, note 53). `roadmap-prune`'s `notes/` guard is correctly untouched (out of scope, note 56). No `upstream/` edits. Standalone `/note` remains byte-identical.
- **No missed/contradictory references.** A repo-wide grep for `notes/` leaves only: `note`'s hook defaults (intended), `roadmap-prune`'s legacy guard (out of scope), and the three deliberate legacy-mention doc lines. No dangling or contradictory directory literal remains in any writer or reader.

## Non-blocking observations (no action required for this milestone)

- **Cross-directory numbering.** New `specs/` numbering restarts at `01` independently of `notes/` (per-directory scan, as designed), so a project mid-migration can hold e.g. `notes/54-…` and `specs/01-…` simultaneously. This is intended and harmless because every `Spec:` tag carries the full path; resolution never depends on the number alone. Called out only so it is not mistaken for a collision bug later.
- **Prune lag is expected.** After this change `roadmap-engine` writes to `specs/` while `roadmap-prune` still only references `notes/`. Pruned tasks' spec files in `specs/` are neither deleted nor mis-deleted (prune's guard preserves spec files as before) — the deletion behavior is explicitly deferred to note 56. No regression.

REVIEW_PASS
