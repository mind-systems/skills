## Review — 40.2: a recovered handle gets the new buffer's path, and the engine's rule receives it

**Changes reviewed:** `git diff HEAD` — `src/skills/agent-architect/SKILL.md` (+40 lines, one hunk, no deletions: three new paragraphs inserted between the recovery paragraph ending "…between `agent-` and `.meta.json`." and the respawn paragraph opening "If the send fails, the editor is dead") and `src/skills/architect-editor-engine/SKILL.md` (one sentence widened in § "The architect's buffer"). Both files read in full; `src/agents/editor.md` and `docs/paired-loop.md` confirmed untouched. Plan and spec 129 read against the result.

**Plan probes, run against the working tree:** `grep -n "recovered"` → four hits, all inside the `Continue in the same conversation` … `If the send fails` range; `grep -c "own buffer"` → the pre-existing conditional-rule hit plus the new text's; scoped lineage probe (`sed -n '/^Continue…/,/^If the send fails/p' | grep "earlier run\|same architect"`) → no hits; `grep -n "re-reads"` in the engine → the single widened sentence; line counts 360 / 45, under the 500 bound. Every guardrailed paragraph (conditional rule, spawn, snapshot, handle-write, respawn) is byte-identical — the diff has no `-` lines in `agent-architect`.

**What landed correctly:** the conditional rule is applied unchanged (new architect, own buffer at the engine's path and numbering); the recovered editor's identity is held in the one framing the plan pinned ("spawned by an architect whose buffer did not reach you … the re-pointing below is precisely what makes that editor your hand"); both halves of the address exchange are present — the recovered handle is written into the new buffer, pointed at the spawn-moment handle-write by concept, and the new buffer's path is named to the editor riding inside the next message as upkeep, with both sibling sections cited by their exact heading text; the "different path, not a repeat" point is made so the spawn paragraph's "a later round … never repeats it" stays true; the probe is stated as unchanged; both alternatives are named with their reasons and "Losing the editor is never fatal; losing it silently is the defect" is quoted exactly. The engine sentence widens the one rule without a second rule beside it, keeps the "not once at birth" contrast, and leaks no policy (no fallback, no pointer-absence) into the mechanism. Frontmatter untouched.

### Findings

1. **A position address that now resolves to the wrong paragraph** — `src/skills/agent-architect/SKILL.md`, the new probe paragraph, closing sentence: "if it fails, the paragraph that follows governs."
   The plan wrote this phrase assuming the new text would be one paragraph appended to the recovery paragraph, so that "the paragraph that follows" was the respawn paragraph. The implementation inserted three paragraphs, and the probe paragraph is the middle one: the paragraph that literally follows it is the alternatives paragraph ("Leaving both buffers live…"), which governs nothing about a failed send. As placed, the sentence points a reader at the wrong rule, and it is a position address in a file whose own governing discipline (global CLAUDE.md § "Grounding claims", `docs/reference-by-name.md`) forbids exactly that — the target has a name it could have used.
   **Fix (either):** replace the phrase with a by-name pointer to the respawn rule — e.g. "if it fails, the editor is dead and the rule below for a dead editor governs" or quote its opener ("If the send fails, the editor is dead") — or move the alternatives paragraph above the probe paragraph so the probe paragraph sits directly before the respawn paragraph, and still prefer a name over "that follows".

2. **"the governing spec" has no referent inside the skill** — `src/skills/agent-architect/SKILL.md`, the new rule paragraph: "per the governing spec's rule that each half holds the other's address".
   `grep -n "governing spec\|paired-loop"` over the three paired-loop files returns only this new line: `agent-architect` never names `docs/paired-loop.md`, and the skill is loaded globally into projects where our `docs/` does not exist at all — the same fact 40.4's spec leans on for the engine. A reader of the skill alone cannot resolve which spec is meant. The fact itself is already stated in this file, one section earlier, in the spawn paragraph: "so each half holds the other's address from the spawn on" — the file's own sentence is the right anchor, and it is the rule this act extends to the recovery moment.
   **Fix:** drop "per the governing spec's rule" and anchor to the file's own statement — e.g. "the same address exchange the spawn makes — each half holding the other's address — now made at recovery" — without adding a path or link to `docs/paired-loop.md` (the skill has no such link today, and a skill body citing a repo-local doc would break in every other project).

### Observations (non-blocking)

- Spec 129 bounds the `agent-architect` edit to "the fallback-recovery paragraph … and only that paragraph", and the plan said "append to the paragraph"; the result is three paragraphs inserted at that site. The bound is about the site — no other paragraph was touched — and the volume the spec demands (rule, probe ordering, two alternatives with reasons) justifies the split; recorded only so the spec's wording and the file's shape are seen to agree.
- "since a recovery is not a spawn and its timing does not otherwise cover it" — "its" reads as the spawn moment's timing or the handle-write's; both readings are true, so no change required.

## Deferred observations

- Affects: `docs/paired-loop.md` § "What the memory holds, and who holds it" / the final `aif-docs` verification pass — the engine's re-read sentence now covers being named a different buffer's path outright; the governing spec's own sentence at the same place ("The editor re-reads the settled zone when it changes, not once at birth: a buffer earns its keep by being written to during a session") covers only in-place change. The meaning is carried elsewhere in the doc (§ "How the memory begins": "the head names the moment that memory moves"), so no contradiction, but docs → code direction says the doc's sentence should widen at its next revision; spec 129 keeps the doc unedited.
- Affects: task 40.3 / `src/agents/editor.md` — "gives you the buffer's own path — held from birth" describes the spawn moment only; after this task the path the hand holds can be replaced by a naming inside a later message. Not false, since the engine governs and is loaded at birth; 40.3 edits the paragraph directly below and can bring the clause level in passing.
- Affects: governing spec `docs/paired-loop.md` § "How the memory begins, and how it survives" / phase 40 follow-up — the conditional rule closes "Either way the buffer exists before any editor does", and the doc says "creates its own buffer first, and only then takes on a hand"; on the recovery path pinned here an editor exists before the new buffer does, and what stays true is that the buffer exists before the architect *addresses* a hand. Spec 129 keeps the conditional rule byte-identical; candidate wording for the doc's next revision.
