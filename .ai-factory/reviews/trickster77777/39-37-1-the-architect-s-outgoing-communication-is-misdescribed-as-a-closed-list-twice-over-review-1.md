## Review — 37.1 — the architect's outgoing communication is misdescribed as a closed list, twice over

**Files changed:** 1 — `src/skills/agent-architect/SKILL.md` (the plan, plan-reviews, and plan sidecar are pipeline artifacts, not reviewed). No other file in `src/`, `docs/`, or `active/` touched; frontmatter byte-identical (`name`, `description`, `loads:`, `allowed-tools`, and the rest — 0 changed lines).

### What landed, read against the file (not the diff)

Four edits, all inside the two sections the plan named:

1. **Heading renamed** — `## Relay on the marker; author the apply work-order and your own legwork` (72 columns, under the unwrappable-line bound). It names both authored cases; "exactly one case" is gone from the heading, and no closed count replaces it.
2. **In-file quotation moved with the heading** — § "Spawn once, message thereafter" now reads `— not the enrichment "Relay on the marker; author the apply work-order and your own legwork" forecloses —`; the quoting sentence's frame (`not the enrichment "` … `" forecloses`) survives once each, the paragraph re-wrapped and otherwise unchanged. The heading string occurs exactly twice in the file: the heading and this quotation. No dangling reference.
3. **Body sentence widened** — "You author your own prompt in two cases: the **apply work-order**, once the user has confirmed the edits, and a `REPORT-ONLY` round on your own initiative, delegating your own legwork — a message you compose yourself, carrying no relayed user payload and asking for no edit, opening with the literal `REPORT-ONLY` token like every message of that format. That second case needs no marker and no permission: the marker governs whose words cross the channel, not you delegating your own work." Both cases are named; the second is exactly as narrow as spec 119 § "The change" states — no weighing rule, no reconcile change, no list of delegable work. The next sentence now opens "Send the apply work-order as an `APPLY-EDIT` channel-message:" — noun in place of the stranded pronoun — and the stretch from "pin every value, path, and exact string it needs" through "over-told steps only drift." is byte-identical (verified as one unwrapped phrase, count 1), so 37.2's landing anchor is intact.
4. **Inventory paragraph rescoped** — "The two channel-message formats govern what opens a round — a unit of work sent out and reported back on (see "Nothing closes a round before the report on it exists") — not everything you may ever send the editor. Keeping the hand current — naming that the shared memory has moved (see "Your buffer is yours alone"), or handing it the buffer's path at spawn (see "Spawn once, message thereafter") — is not a round and needs no form of its own. A `REPORT-ONLY` message carries either the before-mark payload, worked in parallel and enriched only with named context, or your own delegated legwork; the `APPLY-EDIT` channel carries the apply work-order alone, and it **never** carries your own analysis of an analysis target." "nothing else" is gone; the round is leaned on, not redefined (the definition in § "Nothing closes a round…" survives verbatim); the two acts of keeping the hand current are pointed at by section name, not restated; no token, carrier, or "channel-message" is attached to ordinary communication; the `REPORT-ONLY` description agrees with the widened body sentence; the `APPLY-EDIT` clause and its bold `**never**` are kept. All three pointed-at headings exist in the file.

### Invariants verified on the file

- `exactly one case` → 0 (HEAD 3); `nothing else` → 0 (HEAD 1); `Send it as` → 0 (HEAD 1).
- Eight `## ` headings; only the second differs from HEAD.
- Distinct `UPPER-UPPER` tokens → exactly `APPLY-EDIT`, `REPORT-ONLY` — no third format. `REPORT-ONLY` 7 (HEAD 5: one in the body sentence, one in the rescoped paragraph), `APPLY-EDIT` 4 (HEAD 4).
- Marker's own rule untouched, each as one unwrapped phrase, count 1: "No marker anywhere in the message means the whole message is conversation aimed at you, not the editor — it is **never** forwarded."; "you never append the marker to a message the user did not mark, and an unmarked answer is yours to hold, not to forward."; "enriched only with named context"; "This reconcile step applies to every before-mark relay"; "carries your own analysis of an analysis target".
- Out-of-scope sentence untouched: "the first `::` relay or, where none has arrived, the first authored apply work-order" → 1.
- Register: whole-word `head` → 0 (architect stays "you"; editor stays "the editor"/"the hand"). "shared memory" is the governing spec's own phrase (`docs/paired-loop.md` § "What crosses the channel") and the engine's "one working memory" names the same thing — no synonym drift against the reserved vocabulary; "round", "before-mark payload", "channel-message", "apply work-order" are used as the file already uses them.
- 308 lines (≤ 500); lines over 77 columns → 16 (HEAD 17; bound not exceeded).
- Consistency with the engine: `architect-editor-engine` defines `REPORT-ONLY` as a research relay — read/run, reason independently, report, write no files — and the mode rule keys off the opening token. The authored delegation round is that format used on the architect's initiative and is stated to open with the literal token; no engine change is needed, and none was made.
- Consistency with § "Nothing closes a round before the report on it exists": an authored `REPORT-ONLY` round opens a round like any channel-message and is subject to the same hold-the-announcement rule; nothing in the new text exempts it.

### Findings

None. The change does what the contract line and spec 119 § "The change" ask — both authored cases named in heading and body, the pronoun disambiguated, the inventory rescoped to what opens a round — and nothing outside that boundary moved.

Non-blocking note: line "an `APPLY-EDIT` channel-message:" is a 32-column line mid-paragraph, an artifact of keeping the 37.2 stretch byte-identical line-for-line rather than re-wrapping through it. Cosmetic; a reader of the rendered skill sees one paragraph. 37.2 re-wraps that stretch anyway when it edits "pin every value…".

## Deferred observations

- Affects: `src/skills/agent-architect/SKILL.md` § "Review in parallel, reconcile before the apply order" — its closing sentence, "You never decide *when* something goes to the editor; the marker does.", is the same closed claim this task removed from three sites, in a fourth place neither the contract line nor spec 119 § "Blast radius" names. In its section's context "something" is the review target — the user's material — and a reader can reconcile it with the new second authored case (the marker governs whose *words* cross; the architect's own legwork is not the user's words). Read plainly, though, it contradicts "a `REPORT-ONLY` round on your own initiative". The same enumeration-of-two appears in § "Spawn once, message thereafter" ("the first `::` relay or, where none has arrived, the first authored apply work-order" — a first channel-message could now also be an authored delegation round) and in the respawn sentence ("the user re-phrases a relay as a self-contained spawn prompt, or an apply work-order is resent as-is"). All three are outside this task's stated sites; they belong on one contract line that sweeps the file for the relay/work-order pairing wherever it still reads as exhaustive.
- Affects: `src/skills/architect-editor-engine/SKILL.md` § "The two channel-message formats" and § "The mode rule"; `src/agents/editor.md` — the rescoped paragraph now says keeping the hand current "needs no form of its own", per the governing spec, while the engine says every channel-message the architect sends opens with its token and the editor resolves a message with no recognizable token to `REPORT-ONLY`. A formless memory-moved notice therefore lands on the editor as a research round by the receiving side's own rule. This task is right not to name a carrier — the governing spec forbids one — but the receiving side has no account yet of how ordinary communication is told from a round. Already recorded in plan-review-1; restated here because the sentence that exposes it is now in the file.
- Affects: `src/agents/editor.md` § "Analysis mode: reason independently" — the editor still describes `REPORT-ONLY` as the architect forwarding the user's own payload with "no architect framing". A delegated-legwork round is authored by the architect and carries its framing by definition. The two halves now describe one format asymmetrically; spec 119 clears the engine but is silent on `editor.md`. Carried forward from plan-review-1.

REVIEW_PASS
