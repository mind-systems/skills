# Review — 59.1 the zone split comes out of the skill files

**Targets:** `src/skills/architect-editor-engine/SKILL.md` (frontmatter `description:` + § "The architect's buffer"), `src/skills/agent-architect/SKILL.md` (two pointer sentences), `src/agents/editor.md` (opening paragraph)
**Diff:** 3 files, 17 insertions, 17 deletions — every changed line inside a span the plan names.

The whole content of this task is word-for-word conformance to text the spec pins, so the review is built the same way: each replaced passage was compared against `.ai-factory/specs/trickster77777/164-…md` § "What must be true after" programmatically with whitespace normalised, each retained passage against `HEAD`, and every scope claim re-measured against the files rather than read off the plan. All three files were read end to end. Nothing needs changing.

**Every pinned replacement is a copy, not a paraphrase.** The spec's "after" section yields seven quoted blocks; six are whole paragraphs and match the landed files exactly — the engine's frontmatter clause, its opening paragraph with the added "and of the rule that governs who may write to it and who may only read", the head-is-only-writer paragraph, the re-read paragraph, the untouched drain-rule paragraph, and the closing two-architects paragraph. The seventh (the channel-message paragraph) is the spec's own truncated quote ending `[...]`; its landed prefix matches character for character and the remainder — from "This opens no message of its own…" to "…carries no reading of it." — is byte-identical to `HEAD`, which is what the plan required. `agent-architect`'s shortened fragment ("it is where your buffer's path and rules are defined, so it is") and its rewritten pointer sentence both match the spec, as does `editor.md`'s replacement passage end to end, closing "…and it is where that memory lives."

**The split is gone, and gone rather than renamed.** `grep -rniE "settled zone|live zone|two zones" src/ docs/` now returns nothing at all; a bare `zone` search across the three target files also returns nothing. More to the point than the sweep — since the spec's real requirement is that no file gains a replacement boundary — the three files were searched for the vocabulary a renamed split would use: `private`, `architect alone`, `may not read`, `outsider`, `withheld`. Two hits, both false: the engine's pinned "not as an outsider glancing at private notes", which is the analogy *denying* the editor is an outsider, and `agent-architect`'s "this skill is the architect alone", which says this file documents only the architect half. The live zone's contents and the echo argument that justified them are dropped outright and appear nowhere under a new name. The closing paragraph's ", settled zone included" — the item the plan singled out as easiest to leave half-done — is gone, and the sentence ends "never another architect's."

**The two surviving rules are stated where the buffer is defined and where it is referenced.** The engine's body carries both in its own voice ("The head is the memory's only writer. The hand reads it in full … and never writes to it."), and the always-loaded `description:` field now carries them too, so the field states the contract rather than a superseded split. `agent-architect` keeps a pointer that names the reader half and restates none of the engine's content, with the writer half standing in the local sentence that follows it ("It is the one file you edit directly: you are its only writer.") — which is the architect's own obligation, not a second home for the engine's rule. `editor.md` now holds the buffer "in full", with no carve-out and no qualifier added anywhere on what it may read.

**The frontmatter is well-formed YAML, and the field fits.** All four replacement lines sit at exactly the block's 2-space continuation indent — a single distinct indent across the whole `>-` scalar, so no line is promoted to a more-indented literal and the folded value stays one paragraph. The folded field is 810 characters against the 1024-char limit, exactly the figure the plan predicted, and the replacement's line lengths (79/75/77/76) sit inside the 66–79 range the field's existing lines already occupy — the pinned line breaks were taken as given, as instructed, with no re-wrap. `name:`, `user-invocable:`, `disable-model-invocation:` and `allowed-tools:` are byte-identical to `HEAD`.

**No markup leaked into the replacements.** § "The architect's buffer" now contains zero asterisks — the two bolded zone names went out with the paragraph and nothing took their place. `editor.md`'s opening paragraph retains only its pre-existing "**editor**" in the first sentence. This was review 1's named blind spot for the word-for-word read-back, and it is clean.

**Every by-name reference still resolves.** The headings of both edited skills are byte-identical to `HEAD` (compared as whole heading sets, not spot-checked): `## The architect's buffer`, cited by name from § "The mode rule" in the same file; `## Your buffer is shared; you alone write it`, cited by name twice in `agent-architect`; `## Spawn once, message thereafter`, cited five times. No reference anywhere addresses these spans by position, and none of the seven citations breaks.

**Wrapping follows each file's own convention.** The engine's rewritten paragraphs are single unwrapped lines, matching its settled body style. `agent-architect`'s two edits are hard-wrapped inside the file's own range — the rewritten pointer paragraph at 72–74 columns, and the spawn sentence left at 62 after `zones,` was deleted, deliberately not re-wrapped so the paragraph's other lines keep their positions. That restraint is what keeps this task's span disjoint from 59.2, which appends at the same paragraph's end. `editor.md`'s rewritten passage sits at 67–76, its file's own range. No trailing whitespace was introduced in any of the three, no CRLF, and all three keep a final newline.

**Scope holds on both sides.** `git diff HEAD -- src/` is exactly the spans the plan names and nothing else: `agent-architect`'s frontmatter, its memory-snapshot paragraphs (58.1's, 58.2's and 58.4's landed work), its handle-write paragraph and its independence passage are untouched, as are `editor.md`'s frontmatter and all five sections below the opening paragraph, and the engine's §§ "The two channel-message formats" and "The mode rule". `.ai-factory/notes/07-architect-buffer.md` — the live buffer, explicitly out of scope and still carrying its own `# Live` / `# Settled` headings — is untouched; `.ai-factory/notes/` is clean in `git status`. The three ordinary uses of the vocabulary inside `agent-architect` that the plan flagged against over-editing ("a live task registry", "leaving both buffers live", "where the round is settled is what counts") all stand unchanged.

**Nothing downstream is left quoting what was removed.** Searched repository-wide (excluding `.ai-factory/` history and `upstream/`) for the distinctive phrases of the deleted paragraph — `load-bearing` in the buffer sense, `returns an echo`, `glancing at private notes`. The only match for the content list outside the engine is `docs/paired-loop.md` itself, which is the source the engine was conformed to, not a consumer. Every other `load-bearing` hit is an unrelated ordinary use. No skill or command file quotes a replaced sentence as ground truth.

**The removed argument's work is still done elsewhere.** The live zone existed to keep the editor's second reading uncontaminated, and that reason left with it, so the obvious hazard to check is whether anything still protects that independence. It does, and not via the buffer: `agent-architect` § "Nothing closes a round before the report on it exists" rests the rule on what the architect *releases in a message* ("once your read has been released in any form, its agreement can no longer be told from an echo"), never on a private buffer region. The complementary guard is the same file's write-discipline, which admits only entries "written as what will hold again — a ruling …, a mistake as the pattern behind it … never as the episode that revealed either", so a forming diagnosis is not something the architect is told to put in the buffer in the first place. The split's removal leaves both standing.

**Deployment and the governing direction.** All three targets are reached through symlinks that already exist and resolve (`active/skills/architect-editor-engine`, `active/skills/agent-architect`, `active/agents/editor.md`, all into `src/`), so editing `src/` deployed the change with no second copy and no symlink work. The engine is 47 lines and `agent-architect` 388, both inside the ≤ 500-line body limit. No `loads:` edge changed, no mechanism moved between engine and policy: the buffer's definition stays in the engine, the architect keeps a pointer, the editor holds the engine-held contract rather than a second home — ARCHITECTURE § "Composition: mechanism vs policy" is not crossed. `docs/paired-loop.md` § "What the memory holds, and who holds it" already stated both surviving rules before this task ran, so the code was conformed to the doc and the docs → roadmap → code direction is intact.

## Deferred observations

- Affects: `skills/CLAUDE.md` documentation table (row for `docs/paired-loop.md`) — the row still summarises the doc as "the buffer's two zones and who holds which, why the live zone stays private". Re-measured after the edits: this row is now the only place in the repository outside `.ai-factory/` history where the zone split is asserted as current. It was already stale against its own target before this task ran, since `docs/paired-loop.md` carries no zone language, so 59.1 neither created nor worsened it — but it is the last carrier, and it belongs to the documentation index rather than to any of the three skill files, so correcting it needs a task that owns that file. Raised in the plan and left untouched, correctly.
- Affects: Phase 59 / `.ai-factory/specs/trickster77777/165-the-buffer-is-seeded-from-a-template-at-founding.md` — the engine's post-edit content sentence ("It holds the working discipline, the rulings …, the method …, and the register of where the skills still lag the practice") names neither the editor's handle nor an assigned pairing role, both of which `agent-architect` mandates writing into the buffer and both of which the deleted text named as live-zone contents. A reader taking the sentence as exhaustive could conclude they no longer belong there; nothing in the landed files actually says that, and `agent-architect` § "Your buffer is shared; you alone write it" still names both in its own opening sentence, so there is no contradiction to repair here. The wording is pinned by the spec and lifted from the governing doc, so it was not this task's to adjust, and 59.2 — which seeds the buffer's rubrics from a template — is where the buffer's actual contents get stated. Carried forward from both plan reviews unchanged; recorded here because it is the one question this task's landing leaves open one task away.

REVIEW_PASS
