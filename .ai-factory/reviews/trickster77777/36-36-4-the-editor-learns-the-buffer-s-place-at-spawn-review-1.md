## Review — 36.4 — the editor learns the buffer's place at spawn

**Diff:** `src/skills/agent-architect/SKILL.md` (+12 −2), `src/agents/editor.md` (+5 −1); nothing else under `src/` changed. Both files read in full; `active/agents/editor.md` and `active/skills/agent-architect` are symlinks into `src/`, so the change is live without a second copy. `agent-architect` is 271 lines (≤ 500); no `loads:` line or `description:` changed; `architect-editor-engine`, `architect-pairing-engine`, `docs/paired-loop.md` untouched, as the plan pins.

### Substance — landed

- **Architect side, widened spawn sentence** (§ "Spawn once, message thereafter"): the spawn prompt is now the first channel-message "joined at the spawn and only there by the buffer's own path: the pointer, never a copy of what the buffer holds, traveling alongside the channel-message rather than inside the before-mark payload, and carrying no reading, no finding, no conclusion … while the format token still literally opens the message". This covers every constraint the spec sets: at spawn only, alongside not inside, no reading (enrichment ban not reopened), token still opens (engine's mode rule intact), pointer not copy. "there is no spawn before one exists" is preserved.
- **Architect side, the handle-writing act** now says "in that same act give the editor the buffer's path through the spawn prompt … so each half holds the other's address from the spawn on; a later round sent via `SendMessage` never repeats it." — the symmetric obligation the governing spec states ("the memory's place is given to the hand in turn"). The respawn paragraph is unedited; since a respawn is an `Agent` spawn on a channel-message, it inherits the widened definition, and "an apply work-order is resent as-is" stays true — the order itself is untouched, the path only sits beside it.
- **Editor side**: "Alongside that first channel-message, the spawn prompt gives you the buffer's own path — held from birth, never part of the message itself, since the format token alone still opens it and decides the mode — and it is where that settled zone lives." Stated as something received, not derived; no recovery account, no re-read restatement, no literal path form — all correctly left to the engine / later tasks. The mode-rule paragraph below it is not contradicted.
- `grep -n "spawn prompt"` names the path on both sides; the two accounts describe one event. No task/phase/plan-layer citation in either skill text.

### Findings

1. **Minor — change-history phrasing in the skill body.** `src/skills/agent-architect/SKILL.md`, the handle-writing paragraph: "give the editor the buffer's path through the spawn prompt, **as widened above**". "Widened" narrates the edit that produced the sentence, not present behavior — the global docs rule is present tense, never change history, and skill bodies are shipped product read on every rehydration. Replace with a present-tense pointer, e.g. "as defined above" / "as stated above", or drop the aside entirely (the sentence already says "through the spawn prompt").

2. **Minor — cross-reference form is inconsistent with the file's own convention.** Same file, widened spawn sentence: `not the enrichment § "Relay on the marker" forecloses`. The heading is "Relay on the marker; author a prompt in exactly one case", and every other in-file cross-reference uses `see "<full heading>"` (lines citing "Spawn once, message thereafter"). A truncated heading still resolves as a name-prefix, so this is not a broken reference — but the `§` glyph and the shortened form are new to the file. Prefer the existing form: `not the enrichment "Relay on the marker; author a prompt in exactly one case" forecloses` (or `see "…"`), so a reader grepping the exact heading finds it.

Neither finding changes behavior; both are one-phrase edits. No security or runtime concerns — these are prose instructions, no executable surface changed.
