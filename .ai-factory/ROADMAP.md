# Skills Roadmap

> Generic AI Factory skills — reusable slash-command packages for Claude Code.

## Milestones

- [x] **milestone-rescue: update JSON sidecar after artifact cleanup** — After deleting plan-review or review files, the orchestrator's `plans/{slug}.json` sidecar retains a stale `step` value referencing the deleted files. Fix: at the end of Step 5 (Apply and clean up), read the sidecar, determine the correct `step` from what remains on disk (plan-reviews deleted → `"planned"`; reviews deleted, plan-reviews pass → `"plan_reviewed"`), write only the `step` key back — keep `planner`/`implementer`/`elapsed` untouched. Spec: `.ai-factory/notes/08-milestone-rescue-sidecar-update.md`. [17m 24s]

- [x] **roadmap-decompose: enforce atomicity via deployability test** — `roadmap-decompose/SKILL.md` currently states "one atomic task / one concern / one reason to revert" as a declaration but has no enforcement step. The rule is too abstract to apply consistently — concerns slip in through "while I'm here" additions (e.g. adding a service method inside a component task). Fix: add a mandatory **Atomicity Gate** step that runs after each task description is drafted, before it is saved. The gate asks one question: **"Can the first half be deployed without the second half and still make sense?"** — if yes, split into two tasks. This is the only atomicity criterion; drop the file-count and verb-count heuristics as too noisy. Concretely: in Mode 1 (Step 1.3) after drafting each milestone, and in Mode 2 (Step 2.4) after drafting each new task — pause and apply the gate. If the answer is yes, split and apply the gate again to each half recursively until no half is independently deployable. Add this as **Step 1.3.1** and **Step 2.4.1** in the skill body. The gate question and split instruction should appear verbatim in the skill so the agent always uses the same wording.

- [x] **command-handoff: self-handoff prompt command that survives /compact** — There is no first-class way to produce a rich continuation prompt before `/compact`; the freeform ad-hoc prompt fires inconsistently and the built-in summary drops the domain model, working discipline, and error log. Add a new **slash command** (not a skill) `command-handoff` at `.claude/commands/command-handoff.md` (its dir and the `~/.claude/commands` symlink are already provisioned — author only the file). Default (no args) emits a dense, session-mined handoff prompt to chat (9-section skeleton: frame → two-tier read-first map → current state → next step → working discipline → error log, +optional orientation/domain-spine/hard-rules), English. A `note`/`ноут` trigger also persists a durable copy **inline** — the command writes the same skeleton to `.ai-factory/notes/<NN>-<slug>.md` itself (semantic slug like aif-note, not a hardcoded `handoff`); it does **not** route through aif-note, which would reshape the handoff into its own note template and lose the skeleton. Also update `CLAUDE.md` (Repository Structure + Upstream Sync, registering `.claude/commands/` as ours and never synced), the `README.md` Setup guide (add the commands symlink), and one line in `ARCHITECTURE.md` registering commands as a hosted artifact type. Spec: `.ai-factory/notes/12-task-command-handoff.md`.

- [x] **roadmap-decompose: always emit two-tier output by invoking aif-note** — The skill writes full specs inline in each ROADMAP bullet (Mode 1 Step 1.3, Mode 2 2.4/2.5, "Roadmap File Format") — no contract/note split, no aif-note call. Getting two-tier today means manually chaining `/roadmap-decompose` + `/aif-note`, which fires inconsistently (`/a /b` parses only `/a`; plus, until now, aif-note's model-invocation ban). Fix: after each Atomicity-Gate-passed task, decompose **invokes the aif-note skill itself** to write the per-task spec note, then emits a roadmap **contract line** — the interface, ~600 chars (range 400–1000), naming key files/types/guards and ending with `` Spec: `.ai-factory/notes/<NN>-<slug>.md`. `` Tell aif-note the content is a task spec but **do not constrain or alter its behavior**. Small surgical change — don't restructure the modes. Supersedes note 09's inline-template mechanism (keeps its concept). Spec: `.ai-factory/notes/13-task-decompose-two-tier-via-aif-note.md`. [26m 26s]

- [x] **aif-docs: state-not-process docs (no-motivation rule + `3d`/`3д` mode)** — Two coupled fixes to `aif-docs`, one theme: docs state factual *state*, not *process*. **A (always-on, both modes):** the skill describes motivation/history ("we changed X to Y because…", "previously…") which the user patches every run — add a Core Principle + Step 4 check that flags and removes it. **B (`3d`/`3д` param):** the skill assumes shipped code (Step 1 "Current State" `:93`, Step 2.1 staleness `:409`, Step 4 accuracy verify `:474`) and **refuses to document not-yet-built features**; a `3d`/`3д` token activates a Document-Driven branch — author docs **as if already shipped** (present-tense target behavior, doc = contract), mostly by **explaining what 3D is** so the agent stops arguing, plus branching those three points (current→target, suppress staleness + accuracy-verify). Keeps all formatting/nav/language rules; drops *current-state*, keeps *no-history*. Small surgical change, no second skill. Marker dropped (softer skill suffices); conformance pointer printed not auto-inserted. Also register `aif-docs` as intentionally diverged in `CLAUDE.md` Upstream Sync (else next sync wipes it). Spec: `.ai-factory/notes/14-task-aif-docs-3d-mode.md`. [11m 1s]

- [x] **milestone-rescue: sidecar lifecycle + non-convergence detection** — Two failure-mode fixes. **A (sidecar lifecycle):** Step 5 always keeps `plans/{slug}.json` and rewrites its `step` even when the plan `.md` is deleted (re-plan) — `step: "planned"` then claims a plan exists when it was just deleted and `planner`/`implementer`/`elapsed` describe a discarded run. Make the sidecar track the plan's lifetime: full reset (plan `.md` deleted) → delete the sidecar too (git-native, elapsed-loss accepted); re-implement (plan + passing plan-reviews kept) → update `step` to `"plan_reviewed"` in place. Carve the exception into the "never delete the sidecar" rule; reconcile the `step` table. **B (non-convergence):** Step 2 treats any no-`REVIEW_PASS` state as a defect and Step 4 always proposes a description change, but a milestone can be done-yet-unrecognized — every review non-blocking, deliverables on disk, reviewer just never emits `REVIEW_PASS` and loops on cosmetic nits. Detect that (no PASS + all rounds non-blocking + deliverables present) and recommend **committing the deliverable** instead of editing the milestone; keep the standard flow when any finding is Blocking/Critical or deliverables are absent. Spec: `.ai-factory/notes/15-task-milestone-rescue-full-reset-deletes-sidecar.md`. [18m 38s]

- [x] **roadmap-decompose: ensure aif-note is loaded once, then write spec notes manually** — Commit `0bfa177` (note 13) made decompose invoke `aif-note` via the Skill tool **once per task**, plus wrong cruft ("pass the spec as a subject so aif-note doesn't blend siblings" — aif-note has no such parameter and handles its own context). Per-task invocation degrades on long phases and the plan-reviewer rejected the plan three times over it. Fix — one simple rule: per task, **ensure `aif-note` was invoked at least once in this chat** (if not, invoke it once; if already, do not re-invoke) so its note-writing instructions are in context, **then write the spec note manually with `Write`**. decompose describes **no** note format of its own (the format comes from aif-note's in-context instructions — add nothing beyond what existed before `0bfa177`) and **does not modify aif-note**. Two-tier shape, contract line, `Spec:` tag, char budget, Atomicity Gate unchanged. Cleanest path: revert `0bfa177`, then re-add two-tier with this rule. Spec: `.ai-factory/notes/16-task-decompose-inline-notes-aif-note-format.md`. [9m 49s]

- [x] **command-handoff: scale handoff detail proportionally to session depth** — `src/commands/command-handoff.md` produces fixed-size output regardless of session scale: skeleton sections default to 1–2 lines each, so a 1M-token architecture session and a trivial bugfix yield handoffs of equal thinness. Fix: add a proportional-depth mandate (enumerate every work-unit individually; never collapse a subsystem to summary bullets), two new first-class sections for substantial sessions (cross-cutting contracts/invariants checklist; per-unit map with watch-points), a concrete named error-log requirement, and a richness self-check gate the agent applies to its own draft before emitting. Guard: mandate is proportional not maximal — short sessions stay short; padding a trivial session is its own failure. Spec: `.ai-factory/notes/18-task-command-handoff-scale-depth.md`. [5m 32s]

- [x] **command-handoff: note mode emits short chat pointer, not full re-dump** — In note mode Step 3, the full handoff is currently emitted to chat AND written to file — same bytes twice. Note mode fires before `/compact`, so re-dumping the full body re-bloats the context the user is clearing. Fix in `src/commands/command-handoff.md`: rewrite Step 3 Note-mode block so (1) the file gets the full handoff skeleton unchanged, (2) the chat gets only a short pointer — the note path + 3–5 line orientation (frame + next step) the user can paste into the next session. Chat mode unaffected. Self-check gate applies to the file only; the chat pointer is intentionally minimal regardless of session size. Spec: `.ai-factory/notes/19-task-command-handoff-chat-pointer-not-full-dump.md`. [3m 43s]

- [x] **observe-logs: new skill for Loki debug slices** — The `observability` stack centralizes logs in a local Loki backend queried via LogQL over the HTTP API, but there is no ergonomic way for Claude to pull the common slices — each session re-derives the label schema (`project`/`service_name`/`level`; note `service.name`→`service_name`), the structured-metadata filter for `trace_id`, and the two-step "since last restart" recipe (latest `event.name="service.start"` marker → query after its timestamp). Decided as a **skill, not MCP** (sole consumer is Claude Code; zero running infra; reversible — the script can be MCP-wrapped later). Add a personal-scope skill package `src/skills/observe-logs/`: `SKILL.md` (label schema, endpoint via env `OBS_LOKI_URL` default `http://localhost:3100`, when-to-use, raw LogQL templates, read-only discipline) + `scripts/query-loki.sh` (`since-restart <svc>` / `trace <id>` / `window <range> [--level/--project/--service]`, backend-down guard). Read-only `GET /loki/api/v1/query_range`. Spec: `.ai-factory/notes/20-observe-logs-skill.md`. [13m 8s]

- [x] **observe-logs: full restart feed — unbounded, agent-controllable, cross-service merged** — The debug flow is start→reproduce→read **all** logs since start, interleaved across services, but `query-loki.sh` fails twice: `DEFAULT_LIMIT=200` (`:11`) caps every subcommand (silently truncating `since-restart`), and `format_response()`'s `jq` walks `.data.result[]` per-stream so multi-service output prints as per-service blocks, never time-ordered. Fix in one script + one `SKILL.md` section: (1) pagination helper looping `query_range` `forward` (cursor past last ns, until a short page) so `since-restart` returns **every** line from the `service.start` marker past Loki's per-call `max_entries_limit_per_query` cap (~5000) — no limit on this mode; (2) verify the `service.start` marker (`| event_name="service.start"`) + anchor actually return logs against a real restart (mind_api / mind_mobile), fix if broken; (3) `--limit N` flag for checkpoint-less `window`/`trace`, default 200, agent bumps — 200 applies only here; (4) rewrite the `jq` branch of `format_response()` to flatten all streams' `values[]` into one list carrying `project`/`service_name`/`level`, sort by ns, emit the existing `[ts] [level] [project/service] msg` line (single-stream sort is a no-op → single-service output unchanged). Modes: clean `--service <svc>` vs merged `--project <p>` (omit `--service`). Guards: read-only `GET` only; pagination must terminate (short page / cursor stall); `since-restart`/`trace` label & metadata semantics unchanged otherwise; `--services a,b` out of scope; non-`jq` fallback best-effort; frontmatter `name` unchanged. Spec: `.ai-factory/notes/21-task-observe-logs-cross-service-merge.md`. [16m 0s]

- [x] **milestone-rescue-audit: new skill — outside-view convergence audit** — `milestone-rescue` handles failed milestones, but there is no companion that audits a milestone that *passed* yet looped (2–3 rounds at plan-review/implement-review, or a wall-clock outlier) — where a green check can hide a structural/spec gap the implementation routed around instead of fixing. Add a downstream-only skill `src/skills/milestone-rescue-audit/SKILL.md` (frontmatter `name: milestone-rescue-audit`, `allowed-tools: Read` — read-only, diagnoses never edits). Body is the "Convergence by Understanding or by Attrition" procedure: reconstruct the finding→fix chain → the decisive **one-sentence root-cause test** (the single spec/design sentence that would have prevented all N findings → band-aid accretion, else healthy convergence) → discriminators → verdict on the [independent]—[mixed]—[band-aid] spectrum (default is NOT band-aid) → output a diagnosis + one upstream recommendation **to chat** (no file, no ROADMAP edit). Manual invocation only, mostly right after `milestone-rescue` while artifacts are warm — so a thin `Inputs` block names the artifacts and points to `milestone-rescue` for layout rather than re-documenting it. Register the skill in `CLAUDE.md` "Custom skills — never overwrite from upstream" so the next sync doesn't wipe it. Spec: `.ai-factory/notes/22-milestone-rescue-audit-skill.md`. [6m 40s]

- [x] **command-pin-gaps: new command — close underspecified spots before implementation** — Added `src/commands/command-pin-gaps.md`, a short slash command that scans a plan/note/task for "fantasy holes" (anything the implementing agent would have to guess: TODO/TBD/open forks, unpinned symbols/enum values/error codes/field types, unspecified edge cases, magic numbers, unstated ordering) and closes each one now by reading the exact value from code/proto with a `file:line` citation — never invents; genuine product decisions are escalated as one-line `## Blocking decisions` instead of fabricated. `scan` mode lists holes without editing; default edits the target in place. Registered in `CLAUDE.md` Repository Structure tree.

- [x] **command-commit-roadmap-update: commit-or-amend "Roadmap update" command** — Added `src/commands/command-commit-roadmap-update.md`, a minimal slash command that stages everything and commits as `"Roadmap update"` — but if the current `HEAD` is already an unpushed `"Roadmap update"` (last-commit subject matches and `HEAD` is not an ancestor of `@{u}`), it `--amend --no-edit`s instead of stacking a second one. No push, no message change, no extra output.

- [x] **milestone-rescue: document the closed set of valid sidecar `step` rollback states** — Step 5 of `src/skills/milestone-rescue/SKILL.md` documents only two sidecar outcomes (full reset → delete sidecar; re-implement → `step: "plan_reviewed"`) and its `step` table has both rows writing the same value. It never enumerates the full set the orchestrator's `_validate_sidecar_step()` / `_detect_milestone_step()` (`orchestrator/main.py`) actually accepts — `"planned"`, `"plan_review_failed:N"`, `"plan_reviewed"`, `"implemented"`, `"review_failed:N"` — so a rescue doing any other rollback must infer the value from orchestrator source (we had to guess `"planned"` for a corrected-in-place plan). Doc-only fix, two changes: (A) add a closed-set reference table to Step 5 — each `step` value with the phase it resumes at and the artifact required on disk to validate, mirroring the orchestrator; (B) document the missing "plan corrected in place → re-run plan-review" outcome (keep plan `.md` + sidecar, delete plan-reviews, write `step: "planned"`). Guards: orchestrator is the source of truth (table must not diverge); `"planned"`/`"implemented"` always validate so note the precondition that their earlier-phase artifacts must exist; no Python touched. Spec: `.ai-factory/notes/23-task-milestone-rescue-document-rollback-states.md`. [15m 46s]

- [x] **aif-docs: strip nav headers / See Also and protect from upstream sync** — Core Principle 4 of `src/skills/aif-docs/SKILL.md` mandates prev/next navigation headers and "See Also" footers; both are explicitly banned by global `~/.claude/CLAUDE.md` — the conflict caused incorrect additions to orchestrator docs this session (rolled back). Fix: delete Principle 4 (renumber 5→4, 6→5, 7→6) and strip nav/See Also from 7 more spots in SKILL.md (Step 1.1, Step 2.3 template + nav-order example block, Step 2B 2.3, Step 2C 2.1 audit bullet, Step 2C 2.1.1, Step 2C 2.2 example, Step 4 3D carve-out); remove 6 lines in `references/REVIEW-CHECKLISTS.md` (lines 9–11 checklist items, line 45 readability item, lines 61–62 auto-fix table rows, line 81 sample output). Also update `CLAUDE.md` Upstream Sync: remove `aif-docs` from "Intentionally diverged" list, append it to "Custom skills — never overwrite" (post-removal the divergence is a permanent policy conflict — no upstream diff can be applied). Verify: `grep -n "nav\|See Also\|prev/next" SKILL.md references/REVIEW-CHECKLISTS.md` → zero matches; `grep "aif-docs" CLAUDE.md` → only in "never overwrite" line. Spec: `.ai-factory/notes/24-aif-docs-remove-nav-see-also.md`. [14m 20s]

- [x] **milestone-rescue: refactor — repair by depth, roll back to the repaired state** — `src/skills/milestone-rescue/SKILL.md` is bloated and its only output is a ROADMAP milestone-line edit forbidding plan rewrites — outdated. Refactor (compress, don't gut): after the orchestrator stops, diagnose the root cause, then **repair to the depth the cause reaches** and roll the `{NN}-{slug}.json` sidecar + artifacts back to exactly that state — (1) spec note + ROADMAP contract line → regenerate plan; (2) +plan → `step="planned"`; (3) +code fixed by hand → `step="implemented"`. User picks the depth. Keep downstream propagation and scope-overload-as-flag (→ `roadmap-decompose`); compress the non-convergence / template / sidecar-prose bloat. Guard: never compress the per-variant deleted-file set, the `step` closed-set table mirroring `orchestrator/main.py`, or the artifact-discovery block. Spec: `.ai-factory/notes/25-milestone-rescue-rewrite-rollback.md`. [24m 20s]

- [x] **roadmap-engine: new skill — shared two-tier artifact-output engine** — The roadmap family duplicates output machinery: `roadmap-decompose` owns the two-tier render (contract line + spec note) and roadmap file format, and `roadmap-decompose-skeleton` / `aif-roadmap` would copy it. Extract the *form of external artifacts* into a new skill `src/skills/roadmap-engine/SKILL.md` (`user-invocable: false`, `disable-model-invocation: false`, `allowed-tools: Read Write Edit Glob Grep Skill`). Given a confirmed `{name, full spec}` it: loads `aif-note` once, writes the spec note (`.ai-factory/notes/<NN>-<slug>.md`, scan for `<NN>`), writes the ~600-char contract line ending in the `Spec:` tag, and saves. Owns the canonical roadmap format + contract-line rules (moved from decompose); renders into the **target roadmap file the caller names** (never infers main-vs-test). Holds **no** decomposition philosophy, gate, mode, or interaction — the calling philosophy skill stays in control; load-once. Consumers: decompose, skeleton, aif-roadmap. Register in `CLAUDE.md` "never overwrite from upstream". Spec: `.ai-factory/notes/28-roadmap-engine-skill.md`. [9m 41s]

- [x] **test-engine: new skill — shared silent-failure testing philosophy** — The rule "test only what fails silently, skip what fails loudly" lives inline in `roadmap-test-coverage` (Layer 3 filter + Layer 7 Class A/B) and would be duplicated in `roadmap-decompose-skeleton`'s TDD lens. Extract it into a pure-content skill `src/skills/test-engine/SKILL.md` (`user-invocable: false`, `disable-model-invocation: false`, `allowed-tools: Read`): the core question (immediate signal — compile/exception/DI/4xx–5xx — vs wrong output silently), the loud→skip / silent→test table, and the after-the-fact Class A (API drift → patch) / Class B (silent bug → escalate) corollary. No procedure, no I/O; loaded for the discriminator, caller in control, load-once. Consumers: test-coverage + skeleton. Register in `CLAUDE.md` "never overwrite from upstream". Spec: `.ai-factory/notes/29-test-engine-skill.md`. [4m 59s]

- [x] **roadmap-decompose: extract `roadmap-engine` (artifact format) from the philosophy** — the built `roadmap-engine` is a rigid render *procedure* (input contract, append+save) that invented phantom "append-only" limits and looped the decompose refactor at review. It should be the shared **explanation of the roadmap artifacts**, not an API — applied contextually by the one agent. Fix: rewrite `roadmap-engine/SKILL.md` as decompose's "Roadmap File Format" + two-tier note **verbatim** (drop the procedure/contract framing); trim `roadmap-decompose/SKILL.md` to the philosophy remainder (Atomicity Gate, modes, exploration, `AskUserQuestion`), referencing the engine for format. Guard: no procedure/modes in the engine; philosophy byte-identical; full detail in spec. Spec: `.ai-factory/notes/30-roadmap-decompose-render-via-engine.md`. [15m 43s]

- [x] **roadmap-decompose-skeleton: new skill — skeleton/TDD/concurrency decomposition lens** — `roadmap-decompose` splits on atomic *deliverability*; nothing splits on the *spec-before-code* axis, so heavy tasks (45–80 min) discover their type shape and concurrency invariants at code-review. Add `src/skills/roadmap-decompose-skeleton/SKILL.md` (`disable-model-invocation: true`, `allowed-tools: Read Glob Grep Bash(git *) AskUserQuestion Skill`) in the `aif-roadmap` family, on **open** `[ ]` tasks, rendering all outputs (incl. TDD) into the main `ROADMAP.md`. It copies no body: owns only the three lenses and calls `roadmap-engine` (artifact format) + `test-philosophy` (silent-failure rule) — **no `roadmap-tdd`** middle skill (pure-routing = negative value; its one sentence inlines here); load-once each. Lenses: skeleton (interface/abstract-only, no bodies), TDD (tests-first against the skeleton surface, test-philosophy-filtered), concurrency (no-prod-code contract-task for heavy ≥2-of-{async I/O, buffer, lifecycle}). Order skeleton→TDD→impl; fuse skeleton+red-specs into one contract milestone at 1:1, standalone scaffold when shared 2+. Restraint first-class. Depends on engine + test-philosophy. Spec: `.ai-factory/notes/27-roadmap-decompose-skeleton-skill.md`. [17m 47s]

- [x] **test-philosophy: rename `test-engine` + separate the test philosophy from the algorithm** — `test-engine` is a misnomer: what we pulled out of `roadmap-test-coverage` is a **philosophy** ("test only what fails silently"), not an engine — the test family has no shared algorithm-engine like decompose's; we forced the analogy. Rename the skill `src/skills/test-engine` → `test-philosophy` (dir + frontmatter `name`, H1 heading, body wording) and **flip `user-invocable: false → true`** (a philosophy is usable standalone, unlike the engine); update every reference (`CLAUDE.md`, `roadmap-decompose-skeleton`, ROADMAP). Then refactor `src/skills/roadmap-test-coverage/SKILL.md` to **load `test-philosophy`** for the discriminator (Layer 3 filter + Layer 7 Class A/B) instead of inlining — its 8-layer algorithm stays **verbatim**. This is a **separation of philosophy from algorithm, not a rewrite** (the earlier engine mistake): the philosophy content is unchanged (just renamed), test-coverage's pipeline/agents/hand-off stay byte-identical, only the discriminator becomes loaded. Spec: `.ai-factory/notes/31-roadmap-test-coverage-source-from-test-engine.md`. [8m 43s]

- [x] **aif-roadmap: render milestones via roadmap-engine (two-tier)** — Refactor `src/skills/aif-roadmap/SKILL.md` to call `roadmap-engine` for output — a deliberate **behavior change**: high-level milestones become two-tier (contract line + spec note) instead of inline short descriptions. Keep its sole philosophy — **granularity** (high-level major goals, 5–15 milestones, not tasks; Critical Rule 1). Edits: Step 1.3 + Step 2.4 produce each confirmed milestone as two-tier per `roadmap-engine`'s format (load-once); delete the local "ROADMAP.md Format" section; keep modes, exploration, `AskUserQuestion`; add `Skill` to `allowed-tools`. `CLAUDE.md`: move `aif-roadmap` from "Intentionally diverged" to "Custom skills — never overwrite from upstream" (now depends on a local-only engine). Guard: don't change granularity; don't add gate/lenses. Spec: `.ai-factory/notes/32-aif-roadmap-render-via-engine.md`. [10m 45s]

- [x] **repo: split ours vs upstream + curated active set; rename `aif-note`→`note`, `aif-roadmap`→`roadmap-outline`** — Restructure into three zones: our skills/commands in `src/`; a pristine mirror of `lee-to/ai-factory` in `upstream/ai-factory/` (refreshed by `scripts/sync-upstream.sh`, never hand-edited, so sync is a conflict-free overwrite); and `active/{skills,commands}` — per-item symlinks for only the skills actually used (14 ours + `aif`, `aif-architecture`, `aif-skill-generator`), which `~/.claude` now points at. Rename `aif-note`→`note` and `aif-roadmap`→`roadmap-outline`, patching every consumer (`roadmap-engine`, `roadmap-decompose`, `roadmap-decompose-skeleton`, `command-handoff`). Docs (CLAUDE/README/AGENTS/ARCHITECTURE + composition model) updated to the new layout and the real planning chain (`roadmap-outline`→`roadmap-decompose`→`roadmap-decompose-skeleton`→orchestrator). Done directly in-session, not via orchestrator.

- [x] **milestone-rescue: reframe so the substantive defect is the subject, not orchestrator mechanics** — `src/skills/milestone-rescue/SKILL.md` is built around the pipeline halt (iteration limit, missing `PLAN_REVIEW_PASS`/`REVIEW_PASS`), so after a rescue the agent explains failures in orchestrator terms, not what the spec/contract mis-specified and how the code failed it. Reframe (not rewrite — artifact discovery, depth-routing, rollback, sidecar stay behavior-identical): opening frames the halt as tripwire; Step 2 PASS-signals relabeled as internal routing; Step 3 gains a mandatory **Diagnosis Report** as chronological prose narrative (no tables), in the task's domain language (recurring-finding evidence, zero pipeline vocabulary), printed before the depth menu and ending with a standalone root-cause sentence; non-convergence template leads with substance; Step 5 restates the diagnosis before file bookkeeping; add a "What NOT to do" banning mechanics-as-the-problem. Guard: ≤500 lines; don't touch the sidecar `step` table or git commands; frontmatter unchanged. Spec: `.ai-factory/notes/33-milestone-rescue-reframe-substantive-defect.md`. [6m 55s]

- [x] **milestone-rescue-audit: narrative output register (match milestone-rescue's Diagnosis Report)** — `src/skills/milestone-rescue-audit/SKILL.md`'s analysis pipeline (chain reconstruction → one-sentence root-cause test → discriminators → verdict) is sound, but its output forces a `Round | Finding | Fix` table (Step 1 mandates it, Step 6 emits it as evidence, Step 5 reduces evidence to 2–3 bullets) the user can't read. Reframe output only — analysis behavior-identical: Step 1 table demoted to internal scratch; Step 6 deliverable becomes a **chronological prose narrative** (a paragraph per round, findings woven as quotes, no tables/fragments, length scales with rounds; round counts/severity trends allowed); verdict ends the narrative ("the narrative is the evidence"); band-aid/mixed extras rendered as prose — root-cause block quote, reframe paragraph, prose fix-mapping, plain upstream recommendation, one-line cost note; add a "What NOT to do" banning tabular/fragment output. Guard: one-sentence test decisive, discriminators corroborative-only, "default is NOT band-aid" + healthy-convergence early-exit word-for-word; chat-only (no file writes); ≤500 lines; frontmatter unchanged. Spec: `.ai-factory/notes/34-milestone-rescue-audit-narrative-output.md`. [5m 45s]

- [x] **`loads:` convention: declared, colocated skill dependencies; graph derived by grep** — Skill-to-skill loads exist only as prose inside SKILL.md bodies, invisible to the orchestrator; a central map in CLAUDE.md was rejected as a cache without invalidation. Make the edge syntactic and colocated: top-level frontmatter `loads:` in every depending skill (`roadmap-outline`/`roadmap-decompose` → `roadmap-engine`; `roadmap-decompose-skeleton` → `roadmap-engine test-philosophy`; `roadmap-test-coverage` → `test-philosophy`; `roadmap-engine` → `note`), verified complete by grepping for Skill-tool loads; engines never list callers (reverse graph = `grep -l`). Declare the rescue↔audit shared Diagnosis-Report register at both ends (add the missing sentence in rescue). CLAUDE.md gains a lean "Dependencies and the skill graph" subsection: the convention, forward/reverse derivation, both-ends invariants, editing rules (grep callers before touching an engine; behavior-identical is contract text; output register is behavior; live-run verification) — no static map anywhere. Runs FIRST so every later task executes with the convention already in CLAUDE.md. Spec: `.ai-factory/notes/47-loads-declaration-convention.md`. [8m 0s]

- [x] **roadmap-test-coverage: hand findings to decompose via chat; legalize Class-A test patching** — Layers 6/7 of `src/skills/roadmap-test-coverage/SKILL.md` write plain-text tasks into `$ROADMAP_PATH` ("Task text = refactor description" / "area name + reason") with no contract line or `Spec:` note — underspecified tasks reach the orchestrator; and Critical Rule 1 ("Never write test files") contradicts Layer 7's "patch the failing test". Fix: Layers 6/7 collect refactor + Class B findings into a handoff list instead of writing the roadmap (Layer 6's `## Refactor Required` note append stays); Layer 8 prints the list as task descriptions for `/roadmap-decompose` (never auto-run); Rule 1 reworded — never write *new* test files, the one allowed write is Layer 7's Class-A (API-drift) patches, assertions intact. Keep the Layer 7 classification table. Layers 1–5 untouched. Spec: `.ai-factory/notes/35-test-coverage-two-tier-handoff.md`. [8m 35s]

- [x] **roadmap-outline + roadmap-decompose: remove stale `/aif-plan`, `/aif-implement`, `/decompose` references** — Both skills point at a pipeline that no longer exists in the active set: `aif-plan` is stored but not symlinked, `aif-implement` doesn't exist in the repo, and execution belongs to the orchestrator per `docs/workflow.md`. Fix outline Critical Rules 1 & 5 (granular tasks → `/roadmap-decompose`; implementation → the orchestrator, a separate run), decompose Critical Rule 5 (same rephrase), and decompose Mode 3's pre-rename `/decompose check` → `/roadmap-decompose check`. Grep both files for leftovers; reference cleanup only, no logic changes; never touch `upstream/ai-factory/`. Spec: `.ai-factory/notes/36-remove-stale-pipeline-references.md`. [3m 7s]

- [x] **command-pin-gaps: default target = open roadmap tasks / chat scope, not stale plans** — `src/commands/command-pin-gaps.md` falls back to "the newest `.ai-factory/plans/*.md`", but the command runs *before* the orchestrator, when `plans/` holds only committed plans of previous milestones — it silently scans a stale plan. New priority: `$ARGUMENTS` file(s) → scope under discussion in chat (task/phase/note) → all open `- [ ]` tasks above `---STOP---` in `.ai-factory/ROADMAP.md`, scanning contract lines **and** their `Spec:` notes. Drop the `plans/*.md` fallback; explicit plan targeting via `$ARGUMENTS` still works. Hole taxonomy, `file:line` pinning, `## Blocking decisions`, scan mode, report format — unchanged. Spec: `.ai-factory/notes/37-pin-gaps-default-target.md`. [3m 8s]

- [x] **roadmap-engine: drop caller knowledge; decompose stops inlining the engine's format** — The engine names its callers twice ("Currently loaded by roadmap-decompose…" in description, "(currently `roadmap-decompose`)" in body) — stale (outline/decompose/skeleton all load it) and wrong by design: an engine is caller-agnostic. Remove both; describe only what it holds (two-tier format, load-once). In `src/skills/roadmap-decompose/SKILL.md` Mode 1.3, replace the fenced roadmap-format block (duplicated engine content) with a pointer to the engine's format; keep decompose's own milestone rules (atomicity gate, ordering) — philosophy stays with the caller. No format changes, no caller registry. Spec: `.ai-factory/notes/38-engine-caller-boundary.md`. [5m 41s]

- [x] **workflow.md §7: audit reframed as outside-view check, healthy convergence expected** — `docs/workflow.md` §7 triggers the audit on "видно, что оркестратор обходит проблему" — pre-judging the band-aid verdict, while the skill itself runs on *any* looped/outlier milestone (even passed) with "Default is NOT band-aid" and healthy convergence as the expected outcome. Rewrite the audit bullet (Russian, matching the doc): runs on any looped or wall-clock-outlier milestone; distinguishes convergence-through-understanding from convergence-through-attrition; healthy is expected, band-aid the exception worth naming; chat-only diagnosis + one upstream recommendation. Keep the "сперва rescue, следом audit по тёплым артефактам" ordering sentence; don't touch the rescue bullet or other sections. Spec: `.ai-factory/notes/39-workflow-audit-reframe.md`. [3m 32s]

- [x] **milestone-rescue-audit: pre-approve discovery tools for cold runs** — Frontmatter allows only `Read`, but Inputs promises "If run cold, locate and read them before Step 1" — locating artifacts needs `Glob`/`Grep` and (mirroring rescue's discovery) `git status --short -- .ai-factory/`, so a cold run stumbles on permission prompts at step one. Change `allowed-tools` to `Read Glob Grep Bash(git *)`; frontmatter-only, no body changes; the chat-only/no-writes contract stays enforced by the body. Spec: `.ai-factory/notes/40-rescue-audit-cold-run-tools.md`. [2m 45s]

- [x] **frontmatter tool hygiene: drop `Questions` pseudo-tool; fix aif-docs Bash rule syntax** — `roadmap-outline` and `aif-docs` declare a `Questions` tool that doesn't exist in the harness (the 1/2/3 menu pattern already runs via `AskUserQuestion`, which both declare), and `aif-docs` uses invalid `Bash(mkdir, npx, python)` — comma-separated commands match nothing, so every call prompts. Fix: drop `Questions` from both; in `aif-docs` replace with `Bash(mkdir *) Bash(npx *) Bash(python *)`. Frontmatter-only in both files; dialogs unchanged; aif-docs' >500-line body is out of scope; never touch the upstream mirror. Spec: `.ai-factory/notes/41-frontmatter-tool-hygiene.md`. [3m 8s]

- [x] **milestone-rescue: read the phase's `Governing spec:` before diagnosing** — Live incident (tradeoxy Phase 7): three code-reviews plus the rescue editor — four readers in a row — never opened the governing spec named in the roadmap phase header; the ratified spec tier didn't participate in the rescue at all. Fix `src/skills/milestone-rescue/SKILL.md`: Step 1 — after identifying the slug, locate the milestone's phase in `$TARGET_FILE` and, if its header/intro names `Governing spec:` documents, read them all before Step 2 (unconditional, not suspicion-based); Step 3 — judge recurring findings against it (a "spec gap" may be a ratified-contract violation; Diagnosis Report says whether and quotes the clause); What NOT to do — no semantic diagnosis/blocker/repair without that read. Depth routing, rollback, sidecar table untouched. Spec: `.ai-factory/notes/42-milestone-rescue-governing-spec.md`. [4m 46s]

- [x] **roadmap-engine: absorb the shared roadmap-maintenance flow** — `roadmap-outline` and `roadmap-decompose` duplicate ~70% of their text: Step 0 context, create/update/check modes, dialogs, draft-in-memory → confirm → notes-after-confirmation, progress review, check scan, summaries — pure mechanism living in two philosophy skills. Add a caller-agnostic "Roadmap maintenance flow" section to `src/skills/roadmap-engine/SKILL.md` with explicit hook points supplied by the caller: granularity rules, optional per-entry gate, target-file routing, extra update actions; move mechanism-tier critical rules (read-before-modify, never remove silently, `[x]` stays until prune) into it. No philosophy in the engine (no atomicity gate, no 5–15 rule, "entry" not "task"); no caller names (per note 38); callers NOT touched here (notes 44–45). ≤500 lines. Spec: `.ai-factory/notes/43-roadmap-engine-maintenance-flow.md`. [8m 44s]

- [x] **roadmap-decompose: slim to philosophy-only over the engine's flow** — After note 43, delete the duplicated flow machinery from `src/skills/roadmap-decompose/SKILL.md` and replace with "load `roadmap-engine` once this chat, run its maintenance flow with these hooks". Keeps only philosophy: atomic granularity + two-tier discipline; the Atomicity Gate stated once (registered as the flow's per-entry gate hook); target-file routing (default `ROADMAP.md`, explicit filename wins, test keywords → `ROADMAP_TESTS.md`); the "Decompose existing" update action with its note-handling rule (existing `Spec:` tag → update note in place; legacy inline → new note + tag; offer split on 2+ concerns); philosophy-tier critical rules. Behavior-identical for the user in every mode; ~60–100 lines; presumes notes 36/38 landed — don't reintroduce stale refs or inline format. Spec: `.ai-factory/notes/44-roadmap-decompose-philosophy-slim.md`. [9m 18s]

- [x] **roadmap-outline: slim to philosophy-only over the engine's flow** — After note 43, delete the duplicated flow machinery from `src/skills/roadmap-outline/SKILL.md` and replace with "load `roadmap-engine` once this chat, run its maintenance flow with these hooks". Keeps only the strategic lens: entries are high-level goals (capabilities, not tasks), 5–15 sweet spot, milestones later serve as phase names for decomposition, dependencies-first ordering; coarse two-tier rendering per the engine's format with vision from `DESCRIPTION.md`; the spec note is **optional** at this tier — when the contract line alone carries the milestone-phase, skip the note and the `Spec:` tag rather than inventing content (decompose writes the real notes later); target always `ROADMAP.md`; explicitly registers **no** per-entry gate (no improvised atomicity at the strategic tier). Behavior-identical in every mode; ~40–70 lines; presumes notes 36/41 landed — don't reintroduce `/aif-plan` refs or the `Questions` pseudo-tool. Spec: `.ai-factory/notes/45-roadmap-outline-philosophy-slim.md`. [6m 42s]

- [x] **command-pin-gaps: split holes into value vs meaning; add scope-boundary hole type** — The command's taxonomy is dominated by value holes (enums, codes, strings, versions, types, magic numbers) and its single repair move — "pin the exact value with `file:line`" — can't close semantic holes: tradeoxy Phase 7 failed on an unspecified interaction contract and an undrawn scope boundary, and this command would have found nothing in that spec note. Restructure `src/commands/command-pin-gaps.md`: **value holes** keep exact-value + `file:line` pinning (the forced deep read is deliberate); **meaning holes** (edge-case behavior, open forks, unnamed invariants/interaction contracts, undrawn scope boundaries — new type, unstated ordering) are repaired by writing the missing constraint as a spec clause derived from observed code behavior, citation where a source exists; product decisions still go to `## Blocking decisions`. Scan format names the class; stay ~20 lines; don't touch note 37's target logic. Spec: `.ai-factory/notes/46-pin-gaps-meaning-holes.md`. [5m 0s]

- [x] **aif-docs: documentation index moves to CLAUDE.md; README loses the table** — `src/skills/aif-docs/SKILL.md` mandates a `## Documentation` table in README (Step 2.2 template + rules, Step 1.1 consolidation, State B/C split and audit checks), contradicting the standing convention: no README doc table — the index lives in the project's CLAUDE.md, where it enters every agent run as reachable perception-tree edges. Retarget every create/update/audit touchpoint of the README table to a `## Documentation` section in CLAUDE.md (`| Doc | What it covers |`, README not listed, create CLAUDE.md if absent); README keeps one pointer line to the docs directory; add an audit check flagging a legacy README table. Core Principle 3 reworded. Everything else (landing-page rules, 3D, `--web`, checklists, no-motivation pass) unchanged; don't grow the already-over-500-line body materially; upstream mirror untouched. Spec: `.ai-factory/notes/48-aif-docs-index-in-claude-md.md`. [11m 48s]

- [x] **aif-docs: diet to ≤500 — rare branches to `references/`, drop the aif-evolve block** — `src/skills/aif-docs/SKILL.md` is 574 lines against the repo's ≤500 norm, the only accepted deviation. Restore it with byte-identical behavior, two moves: (1) progressive disclosure — per-topic content guidelines (Step 2.3, ~45 lines) → `references/topic-guides.md`, HTML mechanics (Step 3, ~25) → `references/html-generation.md` (read only on `--web`), consolidation table + sample dialog (Step 1.1, ~35) → `references/consolidation.md` — body keeps rule + when-to-read pointer, matching the existing REVIEW-CHECKLISTS.md pattern; (2) delete Step 0's skill-context/aif-evolve block (~20 lines) — aif-evolve is not in the active set, its files exist in no project. Verbatim AskUserQuestion dialogs are contract text — NOT compressed; Core Principles, 3D, Step 4 untouched. Target ~450 lines. Runs strictly after task 48 (same sections) and preserves its changes; verify by a live State C run. Spec: `.ai-factory/notes/49-aif-docs-diet-references.md`. [11m 25s]

- [x] **aif: fork into `src/` — CLAUDE.md-first bootstrap, cut the marketplace half** — Copy `upstream/ai-factory/aif` (777 lines) to `src/skills/aif`, repoint the `active/` symlink, rework **recognizably** (diff vs upstream shows whole sections deleted, structure/headings kept). Keep: three-mode skeleton, config.yaml machinery (`update-config.mjs` + template, verbatim), language keys resolved from config (ceremony of two mandatory questions goes — ask only when config absent and context insufficient), stack analysis, MCP section, `rules/base.md`, final `/aif-architecture` call. Cut: skills.sh install + both security-scan levels + Python-probe ceremony, skill-context/aif-evolve, localized headings (artifacts are English), DESCRIPTION.md generation (note 52), fat AGENTS.md template → one-line pointer. Add: **CLAUDE.md as the first artifact** in every mode — Purpose/Status/Commands/doc-index from chat context + stack scan; update-not-clobber if it exists. Update repo CLAUDE.md (aif moves to the reconcile-by-diff list). ≤500 lines. Spec: `.ai-factory/notes/50-aif-fork-claude-md-first.md`. [35m 58s]

- [x] **aif-architecture: fork into `src/` — source from CLAUDE.md, pointer lands in CLAUDE.md** — Copy to `src/skills/aif-architecture`, repoint the symlink, rework recognizably. Keep verbatim: Step 1 recommendation flow + decision matrix + CRITICAL `references/architecture.md` read, Step 1.5 Codebase Alignment Check, Step 2 template with both policy branches, `references/` as-is. Change: Step 0 sources context from the project CLAUDE.md + light codebase scan instead of DESCRIPTION.md (note 52); Step 3 → ensure one `## Architecture` pointer line in the project CLAUDE.md (add-if-absent) instead of the DESCRIPTION pointer; Step 4 (AGENTS.md row) deleted; template gains a reserved empty `## Features` section (roadmap-prune's anchor store for temporal-tree). Cut: skill-context block, localized headings. Update repo CLAUDE.md — after this the only upstream original in `active/` is `aif-skill-generator`. Spec: `.ai-factory/notes/51-aif-architecture-fork.md`. [8m 41s]

- [x] **Retire DESCRIPTION.md across our skills** — DESCRIPTION.md duplicates the project CLAUDE.md (stack, purpose, conventions), which every agent already receives unconditionally via the CLI; its one unique tier — vision — lives in the roadmap's `>` line and CLAUDE.md Purpose. Grep-driven edit (`grep -rn DESCRIPTION src/skills/ src/commands/`), files are in flux post-refactors: delete every "Read `.ai-factory/DESCRIPTION.md`" instruction from our skills (expected: roadmap-engine flow, outline, decompose, skeleton, test-coverage Layer 1, aif-docs Step 0) **without** adding "read CLAUDE.md" replacements (it's auto-loaded — instructing it is noise); where DESCRIPTION was one of several sources (vision line, stack inference), drop that branch and keep the rest (user input / package-manager files). ARCHITECTURE.md and RULES.md reads untouched. The files themselves are already removed from all maintained projects (load-bearing facts salvaged into CLAUDE.mds) — this task edits skill bodies only. Orchestrator prompts covered by its own roadmap task. Spec: `.ai-factory/notes/52-retire-description-md.md`. [13m 28s]

- [x] **note: destination and template hooks — one generic distiller, three genres** — `note` is the generic distillation engine (chat → note, part of a chat, a task spec, a handoff), but its template and destination (`.ai-factory/notes/`) are hardcoded — which forced `command-handoff` to hand-roll its own file machinery. Split per the composition model: mechanism stays (mining, `<NN>` numbering per directory, slug, placement, concision, English); **destination directory and section template become caller-supplied hooks**. Unset → today's defaults, byte-identical standalone behavior (lazy migration depends on the default staying `.ai-factory/notes/`). Callers adopt the hooks in their own tasks (engine → `specs/`, handoff → `handoffs/`). No genre content enters `note`; no rename; load-once unchanged. Spec: `.ai-factory/notes/53-note-destination-template-hooks.md`. [4m 51s]

- [x] **Roadmap family writes specs to `.ai-factory/specs/`; readers follow the tag** — `notes/` changed genre under its name: only the roadmap family writes there and the files are task specs (research notes became handoffs). Writers retarget: `roadmap-engine` format + numbering + note-destination hook → `.ai-factory/specs/<NN>-<slug>.md`; `roadmap-test-coverage` Layer 4 paths; any literal `notes/` in the philosophies (grep-driven). Readers de-hardcode: `milestone-rescue` (literal note paths, the artifact filter, the "never touch notes/" guard), `command-pin-gaps`, `temporal-tree` — all resolve specs **through the `Spec:` tag, never a directory**, making the migration invisible to them. **Migration is lazy: no project is touched** — old tags keep literal `notes/` paths and keep working; content moves as roadmaps live. Docs updated once (genre split: `specs/` new, `notes/` legacy-via-tags, `handoffs/`). Depends on note 53. Spec: `.ai-factory/notes/54-roadmap-family-writes-specs.md`. [12m 49s]

- [x] **command-handoff: philosophy over the note engine — the engine distills; lift the right tree** — The command duplicates the `note` engine's whole behavior: its own mine-the-context step, compose choreography, and file mechanics (numbering, slug, mkdir, Write). Distillation is the agent applying *one* instruction — and that instruction is `note`. Two-sided fix: `note` gains a third generic hook, a **verbosity directive** (unset → today's concision rule; caller text replaces it); `command-handoff` note mode delegates to `note`'s own flow — destination `.ai-factory/handoffs/`, template = the **blank** skeleton (placeholders are the mining lens), verbosity = proportionality policy + self-check gate, semantic slug — deleting its own mining/compose/file-mechanics choreography; `note`'s report suppressed, only the minimal pointer emitted. Rejected: pre-filling the template so distillation is a no-op. Chat mode composes directly, no engine; `allowed-tools` covers note's full grant set + `Skill`. Plus the map fix: the read-first map lifts the lazy tree scoped to the *next step*; several independent work-units → several small cross-linked trees, not one merged inventory. Depends on note 53. Spec: `.ai-factory/notes/55-handoff-philosophy-over-note.md`. [12m 47s]

- [x] **roadmap-prune: sweep the artifact dirs; delete completed tasks' specs by tag — no slug matching** — Everything completed work leaves at HEAD is dead weight (content reachable forever via the completion commits and the Step 4.1 snapshot), yet prune forbids deleting specs and plans/reviews are cleaned by hand. The user's manual workflow, encoded — plain `rm`, staging once at commit time; **no `git rm` anywhere**: (1) capture the `Spec:` tag path of every `[x]` line (pruned and kept) **before** roadmap lines are deleted; no tag → skip, never synthesize a path; (2) `rm -rf` the four artifact dirs `plans/`, `plan-reviews/`, `reviews/`, `patches/`; (3) `rm -f` each captured spec path. Anchor on the target repo root (from the argument): the four dirs sit under its `.ai-factory/`, the captured tag paths are used verbatim (already `.ai-factory/`-prefixed — never double it). Open `[ ]` tasks' specs safe by construction — no spec directory is ever scanned. Instructions only, no rationale prose in the skill. No slug derivation, no discovery, no orphan report, no extended verify. **No auto-commit** — user reviews; on request `git add -A` then exactly one commit, message exactly `Roadmap prune` (no body, no prefixes, never ask); Step 4.1 snapshot before any changes. Spec: `.ai-factory/notes/56-prune-deletes-pruned-specs.md`. [40m 58s]

- [x] **Engines carry a reverse-graph marker — the edit-time trigger lives in the engine file** — The skill graph is one-way by design (`loads:` at the caller, no `loaded-by:` anywhere), so the engine file itself is silent about being depended upon — and the "grep callers before touching an engine" rule in CLAUDE.md doesn't fire at edit time (live incident: task-55 respec extended `note` without the check). Fix: a **genre marker without names** in each engine's body, near its load-once statement — one **declarative** sentence: this is a load-once engine, callers depend on its exact behavior, edits must honor their expectations; the reverse graph *is* the inline grep. Never an imperative — the marker loads for every *user* of the engine, and "run grep before editing" would make a using agent eagerly load all callers. Apply to the three engines named in `loads:` fields today — `roadmap-engine`, `test-philosophy`, `note` — and add one line to CLAUDE.md → "Dependencies and the skill graph" making the marker part of the convention (a new engine gets it with its first `loads:` edge). Never a caller list; body prose, not frontmatter. Spec: `.ai-factory/specs/01-engine-reverse-graph-marker.md`. [8m 0s]

- [x] **milestone-rescue: discard the implementer session with the implementation; legalize `plan_reviewed`** — Live incident (task 56): a spec+plan rescue preserved the sidecar's `implementer` key per the "update only `step`" rule; the orchestrator resumed that session (`main.py:296`), whose memory says the (discarded) implementation is done — three no-op implement iterations, three blocked reviews. Fix `src/skills/milestone-rescue/SKILL.md`: (1) at spec+plan depth, delete the `implementer` key alongside `step: "planned"` — the session's memory dies with the implementation it produced; carve the exception into the preserve-all-keys wording and What-NOT-to-do (`planner`/`elapsed` stay everywhere); (2) add the rollback for "plan ratified, implementation absent" (reviews report the product byte-identical to HEAD): keep plan + passing plan-reviews, delete reviews/patches, set `step: "plan_reviewed"` (valid only with a `PLAN_REVIEW_PASS` file on disk), drop `implementer` — orchestrator resumes at implement iter 1 fresh; amend the table note that says `plan_reviewed` is never written. Sidecar table values unchanged; no orchestrator changes. Spec: `.ai-factory/specs/02-milestone-rescue-stale-implementer-session.md`. [11m 9s]

- [x] **observe-logs: remote Loki auth** — `query-loki.sh` has no auth support; all `curl` calls are unauthenticated. Add two env vars: `OBS_LOKI_URL` (already exists, default `http://localhost:3100`) and `OBS_LOKI_AUTH` (new, full `Authorization` header value — `Bearer <token>` for Grafana Service Account, unset for local). For remote access via Grafana, the datasource UID is embedded in `OBS_LOKI_URL` (`.../api/datasources/proxy/uid/<uid>`); the script appends Loki API paths as usual and stays unaware of Grafana internals. Nothing hardcoded in the skill beyond the localhost default for `OBS_LOKI_URL`. Three `curl` call-sites to patch: `check_backend`, `query_range`, `/ready` check. Default (both unset) must be a no-op. Update `SKILL.md` Endpoint section with a two-row table and local/remote examples. Spec: `.ai-factory/notes/26-observe-logs-remote-auth.md`. [9m 25s]

- [x] **orchestrator-artifacts: engine — the orchestrator's file protocol in one durable home** — Three skills read the orchestrator's artifacts and each holds or points at its own layout description: `milestone-rescue` inline, `milestone-rescue-audit`'s Inputs says "see `milestone-rescue`" (a cold audit loads a 440-line repair philosophy for ~15 lines of layout), and the prune gate needs the `## Deferred observations` format; the status-marker grammar meanwhile lives in a spec — and specs die at prune. New engine `src/skills/orchestrator-artifacts/SKILL.md` (`user-invocable: false`, `allowed-tools: Read`, ≤~60 lines, reverse-graph marker): layout (four dirs + `test-runs/`, `<seq>-<slug>` naming, rounds), PASS signals, sidecar fields, committed⇔completed, the `## Deferred observations` section format + reserved status field, the **marker grammar** (moved from spec 03, substance verbatim), and the declared mirrors-the-orchestrator coupling. Rewire `milestone-rescue`: `loads:` edge, drop layout re-descriptions, keep procedure + sidecar `step` table byte-identical (single consumer — stays). Symlink into `active/skills/`, add to CLAUDE.md active set. No procedure/policy in the engine; audit/prune edges land in their own tasks. Spec: `.ai-factory/specs/05-orchestrator-artifacts-engine.md`. [11m 4s]

- [x] **roadmap-prune: deferred-observations gate — no prune while unpinned entries exist** — Reviewers write `## Deferred observations` (entry: `- Affects: <target> — <observation>`) into `plan-reviews/` and `reviews/`; prune's sweep `rm -rf`s exactly those dirs, but resolving observations is **not prune's job** — it is `milestone-rescue-audit`'s (prune mode, spec 04). Add `loads: orchestrator-artifacts` and a **gate at the very top** of `src/skills/roadmap-prune/SKILL.md`, above Step 1: scan both dirs for entries not pinned per the engine's grammar; any found → the skill **stops entirely** with `<file>:<line> — <entry>` per unpinned entry and the instruction to run `milestone-rescue-audit prune` first — no edits, no partial prune. All pinned → normal flow unchanged. Prune never promotes/evaluates/marks — read-and-refuse only; grammar and pinned-definition live in the engine, referenced never redefined. Summary report quotes marker-phrase hits of pre-standardization files — report-only, never gating. Sweep/commit policy untouched. Depends on the engine task. Spec: `.ai-factory/specs/03-prune-harvest-deferred-observations.md`. [9m 36s]

- [x] **milestone-rescue-audit: two modes — `rescue` (band-aid hunt) and `prune` (pin the observations)** — Add a run-mode argument `$1` ∈ `rescue|prune` (absent → `rescue`, historic behavior; update `argument-hint`) and `loads: orchestrator-artifacts` to `src/skills/milestone-rescue-audit/SKILL.md`; Inputs states each mode's inputs directly and **drops the "see `milestone-rescue` for layout" pointer** — layout/naming/rounds/signals come from the engine. **Rescue mode:** deferred observations are non-findings — excluded from the finding→fix chain/counts/severity trend, captured separately (round, `Affects:`, gist; `audit-*`-marked entries skipped as evaluated); an observation naming the gap the chain circles around is band-aid corroboration — corroborative only, never replacing the one-sentence test; evaluated ones get `[audit-corroborated]`/`[audit-dismissed]`. **Prune mode** (when prune's gate refused): for every unpinned entry in both review dirs — evaluate against current code/roadmap, then pin per the engine's grammar: dismissed / promoted (append verbatim + source slug under `## Deferred observations` in the resolvable `Affects:` file, mark `[promoted → <path>]`) / `[unrouted-reported]` + report for the user to route; every sibling occurrence marked; exit = zero unpinned. Write contract: append-only suffixes + promotion appends only; `Edit` joins `allowed-tools`. Pipeline, discriminators, "default is NOT band-aid" unchanged. Depends on the engine task. Spec: `.ai-factory/specs/04-audit-reads-deferred-observations.md`. [15m 59s]

- [x] **Roadmap family: two-level phase grammar — outline emits `### Phase N` headers, decompose/skeleton emit `N.M` tasks** — All three roadmap skills emit identical checkbox bullets; the tradeoxy sessions settled a two-level grammar (phases as headers with prose intros, tasks numbered under them) that the user now re-teaches by hand every session. Five files, one contract: `roadmap-engine` "Roadmap File Format" rewritten to the two-level grammar — globally sequential phase numbers across the file (gaps legal), `N.M` tasks under their phase, `N.M.x` sub-numbering on splits (impl becomes last child, no cascade renumbering), flat unnumbered fallback where no headers exist — plus create-mode loosened to hook (a)'s shape; `roadmap-outline` hook (a) → phase headers + prose intro, never checkboxes, no `Spec:` tag at this tier, check mode dropped; `roadmap-decompose` → `N.M` under the parent phase header, sub-numbering in "Decompose existing" splits; `roadmap-decompose-skeleton` disposition → insertions numbered `N.M.1…k` with impl last; `roadmap-prune` → delete emptied phase headers, never renumber. Engine stays caller-agnostic; `ROADMAP_TESTS.md` and legacy flat roadmaps untouched via the fallback. Spec: `.ai-factory/specs/06-roadmap-family-two-level-grammar.md`. [19m 27s]

- [x] **roadmap-engine: shape-condition the checkbox flows; soften the two-tier opener** — Milestone 52 gated the engine's note/placeholder mandates on the entry shape (draft/finalize/Add), but the checkbox-marking flows still assume every entry is a checkbox'd milestone: update-mode "Review progress" (`SKILL.md:201-203`) proposes marking `[x]` on entries that have no checkbox at the phase tier; Check mode (`:225-264`) scans `- [ ]` entries and is still reachable by any caller's literal `check` argument (`:116`) even when its hook (a) shape carries none; the opener "Each milestone is a two-tier entry" (`:27`) is categorically false for phase entries. Fix, one file: shape-condition Review progress (mark checkbox entries only; phase progress is derived from its `N.M` tasks and report-only), one Check-mode sentence (checkbox entries only — a checkbox-less caller registers no check mode), soften the opener to task-tier scope. Guards: draft/finalize/Add wording, format section, and numbering rules byte-identical; engine stays caller-agnostic. Spec: `.ai-factory/specs/07-engine-shape-condition-checkbox-flows.md`. [5m 6s]

- [x] **roadmap-prune: Step 6 speaks the direction-section grammar** — Step 6 of `src/skills/roadmap-prune/SKILL.md` names the section it prunes literally `## Milestones` (`:218`, `:222`), but new-grammar roadmaps hold tasks under `## <Direction name>` sections with `### Phase N` subheaders — the named block doesn't exist there. Widening, not a regression: generalize the delete/keep wording to the task-holding sections of both shapes (flat `## Milestones` unchanged; direction sections covered), and add the emptied-direction sweep parallel to the existing emptied-phase rule — a direction that loses its last phase loses its `##` header and preamble too, never renumbering anything (gaps are historic; the retain-last-phase-header rule keeps the newest direction alive by construction). Guards: Step 6 only — gate, sweep, commit policy, and the "never copy a section header as a feature name" rule untouched. Spec: `.ai-factory/specs/08-prune-direction-section-vocabulary.md`. [5m 42s]

- [x] **milestone-rescue: disposal-pinning — promote observations at the moment of routing** — `orchestrator-artifacts/SKILL.md:63-64` closes the marker writer set with "written by `milestone-rescue-audit` only", forcing a ceremonial audit-prune run even when a rescue session already routed the observations into roadmap tasks (live case: milestone 52's three observations → specs 07/08 in-session, hand-pinned outside the ratified writer set). Legalize the real invariant — whoever disposes of an observation pins it at the moment of disposal: the engine's §6 sentence becomes a writer-set split (`[promoted → <path>]` — written by the disposal skill that routes the entry into a task, at routing time; `[audit-corroborated]`/`[audit-dismissed]`/`[unrouted-reported]` stay audit's; reviewer never writes markers), and `milestone-rescue` gains a lean Step 5.6 — pin `[promoted → <spec path>]` on every sibling occurrence per the engine's dedup rule when a task/spec lands, plus a What-NOT-to-do clause banning the other three markers. Guards: grammar/pinned/dedup byte-identical; audit body and prune gate untouched; rescue stays ≤500. Spec: `.ai-factory/specs/09-rescue-disposal-pinning.md`. [11m 14s]

- [x] **aif-docs: write the ТЗ — stop refusing docs-ahead-of-code** — The one complaint: called on a not-yet-built feature, aif-docs assumes shipped code (`Step 1: Determine Current State` at `SKILL.md:93`, accuracy checks against a running referent) and argues instead of writing the spec; `3d`/`3д` (note 14) was a bolted-on exception-mode. Delete `3d` wholesale (SKILL.md + references); reframe the one mode to write behavior+invariants present-tense whether code exists or not (accuracy checks conditional on the referent, no lead/lag meta); weight up the doctrine that docs are ТЗ (behavior, protocols, flows, connections), README + relatives the onboarding exception; purge the See-Also/next-steps nav residue for feature-cross-link trees. Fork to a lean ТЗ-only skill out of scope — README genre, state machine, `--web` all stay. Spec: `.ai-factory/specs/10-aif-docs-rewrite-tz.md`. [20m 16s]

- [x] **observe-logs: mandatory env selection via `.env` registry (Grafana-only)** — The stack moved to Grafana; direct Loki is gone, but `query-loki.sh` still defaults to `http://localhost:3100`/no-auth and reaching an env means hand-exporting `OBS_LOKI_URL`/`OBS_LOKI_AUTH` per call. **Remove the localhost default and no-auth path**: every read goes through Grafana's proxy (`.../api/datasources/proxy/uid/<uid>`, `Bearer glsa_…`), only host/token differ per env. Add a pipe-delimited registry in the skill root — `.env.example` committed, `.env` gitignored+chmod-600, user-filled with `stage`/`dev`/`prod` lines `name|grafana-url|auth`. Parse (never source) it; add a **required** top-level `--env <name>` stripped in main dispatch that resolves the two vars; no/unknown env ⇒ hard error listing available names (no fallback). Locate `.env` via `${BASH_SOURCE[0]}` (not `$PWD`) so it works through the `~/.claude/skills` symlink. `project` stays a `--project` label filter, inferred by the agent from the current repo; `/observe-logs <env>` ⇒ recent logs for the current project. Skills-repo change: `query-loki.sh` + new `.env.example` + `.gitignore` (`/src/skills/observe-logs/.env`, keep `.example`) + **lean** SKILL.md Endpoint rewrite (agent-facing, not a manual). Spec: `.ai-factory/specs/11-observe-logs-multi-environment-registry.md`. [12m 21s]

- [x] **aif-docs: ТЗ is an input word, not the skill's identity** — Task 10 branded the skill's identity, description, and Core Principles with the Russian abbreviation **ТЗ** (`SKILL.md:3`, `:13`, `:15` ×2, `:19` ×2), threading a Russian term through an otherwise all-English skill. But the capability it names — write a present-tense governing spec **ahead of code** *or* describe **shipped code's** behavior in one voice — is already implemented label-free (referent-conditional `Step 1` `:70`, Step 2.1 staleness `:318`, Step 4 `:369-374`), so ТЗ carries **no mechanism**, only wrong-language branding. Rename/reframe pass, behavior identical: replace `ТЗ` → **`governing spec`** in all four doctrine spots (exact target strings pinned in the spec), and re-anchor ТЗ **once** as an input *trigger* in the `description`'s "Use when user says…" list (`"напиши ТЗ"`/`"техническое задание"`) — never in identity/principles/body. Guards: do **not** touch the referent-conditional dual-write logic (that's the real feature), `references/` (zero ТЗ there), or the upstream mirror. Spec: `.ai-factory/specs/12-aif-docs-tz-input-not-identity.md`. [4m 13s]

- [x] **milestone-rescue: dismiss disposed observations in-session — not just promote** — spec 09 let rescue pin `[promoted → <path>]` for observations it *routes* into a task, but `[audit-dismissed]` stays audit-only (`orchestrator-artifacts` §6: the evaluative markers "remain `milestone-rescue-audit`'s"; rescue's What-NOT-to-do bans it). So a rescue that evaluates an observation and finds it **moot / already handled in code** — nothing to route — must either ceremonially run `milestone-rescue-audit` just to stamp a dismissal, or hand-write the marker outside its permitted set (live case this session: `Affects: note 37` stub-drift on milestone 23, fix already in the Makefile → three siblings hand-pinned `[audit-dismissed]`). Generalize spec 09's invariant (whoever disposes pins, at the moment of disposal) by one marker: engine §6 moves `[audit-dismissed]` into the disposal-tool writer set beside `[promoted]`; `milestone-rescue` Step 5.6 gains the dismiss path (evaluate-and-moot → pin `[audit-dismissed]` across siblings) and its What-NOT-to-do clause drops `[audit-dismissed]`. `[audit-corroborated]` (root-cause corroboration) and `[unrouted-reported]` (prune sweep) stay audit's; grammar/pinned/dedup byte-identical; audit body + prune gate untouched; keep the `[audit-dismissed]` token (renaming ripples every existing pin). Spec: `.ai-factory/specs/13-rescue-dismiss-disposed-observations.md`. [6m 49s]

- [x] **command-handoff: boundary-neutral framing + first-class narrative register** — the skill over-fits to one shape (post-compact skeleton state-transfer), two independent over-fits. (1) `/compact` is hard-coded as the trigger — description (`command-handoff.md:3-6`: "after /compact… Run this BEFORE /compact… After compact there is nothing left to mine"), the Frame skeleton (`:54-55` "the chat is compacted…"), the example pointer (`:136`) — so a receiving agent narrates a compact that never happened (live this session: skills handoff 07 → agent announced "we had a compact, rehydrating" on a cross-project transfer). Reframe boundary-neutral: state the trigger by the *act* (context transfer to a future agent/session), enumerate no boundary kinds, keep only the "mine while context is still live" truth. (2) Both note/chat **destination** modes force the 11-section skeleton; add a **register** axis orthogonal to destination — skeleton (state-transfer) vs narrative (understanding-transfer: prose carrying the causal thread, refs inline where load-bearing), selected by explicit cue else session shape. Narrative × note: agent composes prose and `Write`s directly to `.ai-factory/handoffs/`, NOT via `note`'s distiller (it flattens the thread). Guards: skeleton register + proportionality/read-map/self-check byte-identical; keep the note/chat axis. Live basis: handoff 07. Spec: `.ai-factory/specs/14-handoff-narrative-register-boundary-neutral.md`. [17m 14s]

---STOP---
